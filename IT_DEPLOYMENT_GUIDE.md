# 📘 راهنمای استقرار و پیکربندی سامانه (ویژه واحد IT)
### VLSE — سامانهٔ ارزیابی و رتبه‌بندی کیفی تأمین‌کنندگان مواد اولیهٔ دارویی (API)

این سند مرجع استقرار سامانه روی سرور داخلی شرکت است. هر دستور و هر عددی که اینجا آمده مستقیماً از کد فعلی مخزن استخراج شده است.

> ⚠️ **پیش از اجرای هر چیز، بخش ۸ (چک‌لیست پیش از استقرار تولیدی) را بخوانید.** سه مورد در پیکربندی پیش‌فرض هست که اگر دست‌نخورده بماند، سامانه روی شبکهٔ شرکت امن نیست.

> 🚀 **نصب سریع:** اگر فقط می‌خواهید سامانه بالا بیاید، کل کار یک دستور است:
> ```bash
> ./deploy/install.sh
> ```
> اسکریپت پیش‌نیازها را می‌سنجد، `.env` را با رمز و کلید تصادفی می‌سازد، تصویر را build و سرویس‌ها را بالا می‌آورد، منتظر سالم شدن سامانه می‌ماند و آدرس ورود را چاپ می‌کند. بقیهٔ این سند برای وقتی است که بخواهید جزئیات را بدانید یا دستی کار کنید.

---

## 🏗️ ۱. معماری

| لایه | فناوری |
| :--- | :--- |
| فرانت‌اند | React 19 + Vite 6 + TypeScript، Tailwind CSS 4، فونت Vazirmatn (رابط فارسی و RTL) |
| بک‌اند | Express 5 (`server.ts`) |
| دسترسی به داده | Prisma ORM 5 |
| پایگاه‌داده | PostgreSQL |

**بیلد تولیدی:** `npm run build` فرانت را با Vite می‌سازد و بک‌اند را با esbuild در یک فایل واحد `dist/server.cjs` بسته‌بندی می‌کند.

### 🔴 نکتهٔ حیاتی: هیچ fallback فایلی وجود ندارد

نسخه‌های قبلی این سند از «سیستم تاب‌آوری خودکار» و بازگشت به `database/vendors.json` صحبت می‌کردند. **چنین چیزی در کد نیست و نباید روی آن حساب کرد.**

- PostgreSQL **تنها منبع حقیقت** است. `requirePrisma()` در `server.ts` اگر `DATABASE_URL` نامعتبر باشد استثنا پرتاب می‌کند.
- `server.ts` هیچ‌جا `database/vendors.json` را نمی‌خواند؛ این فایل فقط ورودی اسکریپت seed است.
- **رفتار واقعی هنگام قطعی دیتابیس:** پروسه بالا می‌آید و پورت را می‌گیرد، ولی **همهٔ مسیرهای داده خطای ۵۰۰** می‌دهند. یعنی «سرویس بالاست» به معنی «سامانه کار می‌کند» نیست (به بخش ۷ مراجعه کنید).
- `localStorage` مرورگر فقط کش نمایش آفلاین است، نه پایگاه‌داده.

---

## 🛠️ ۲. پیش‌نیازهای سرور میزبان

1. **Node.js نسخهٔ ۲۰** — تصاویر Docker پروژه روی `node:20-alpine` هستند. (در `package.json` فیلد `engines` تعریف نشده، پس این الزام توسط ابزار بررسی نمی‌شود؛ رعایتش با شماست.)
2. **PostgreSQL نسخهٔ ۱۵** (پیشنهادی، مطابق `docker-compose.yml`) — حداقل ۱۳. نیازمند مجوز ساخت جدول و ایندکس.

> ℹ️ **نکتهٔ مدیر بسته:** مخزن هم `bun.lock` دارد (روش توسعه) و هم `package-lock.json` (روش بیلد Docker). `Dockerfile` با `npm ci` نصب می‌کند، یعنی دقیقاً نسخه‌های قفل‌شده — همان‌هایی که آزموده شده‌اند — نصب می‌شوند. تصویر تولیدی فقط وابستگی‌های تولیدی را دارد؛ ابزار توسعه (Vite، ESLint، TypeScript) روی سرور نمی‌نشیند.

---

## 🔑 ۳. متغیرهای محیطی

فقط متغیرهای زیر واقعاً توسط کد خوانده می‌شوند:

| متغیر | اجباری؟ | توضیح | اگر ست نشود چه می‌شود |
| :--- | :--- | :--- | :--- |
| `DATABASE_URL` | **بله** | رشتهٔ اتصال PostgreSQL | پروسه بالا می‌آید ولی هر درخواست داده ۵۰۰ می‌دهد |
| `JWT_SECRET` | **بله** | کلید امضای توکن ورود؛ حداقل ۳۲ کاراکتر | با `NODE_ENV=production` سرور **بالا نمی‌آید** و با پیام `[FATAL]` متوقف می‌شود. در محیط توسعه یک کلید تصادفی برای همان اجرا ساخته می‌شود (نشست‌ها با هر ری‌استارت باطل می‌شوند). |
| `NODE_ENV` | **بله در تولید** | باید دقیقاً `production` باشد | سرور به‌جای `dist/`، Vite dev را بالا می‌آورد |
| `VITE_ENABLE_LOCAL_DEMO` | خیر | فقط برای دموی بدون دیتابیس. **در نسخهٔ شرکت ست نکنید.** | دکمهٔ دمو در حالت عادی دیده نمی‌شود — ولی بند هشدار زیر را بخوانید |
| `DISABLE_HMR` | خیر | فقط محیط توسعه | HMR فعال می‌ماند |
| `VERCEL` | خیر | توسط خود Vercel ست می‌شود | بی‌اثر روی سرور داخلی |

> 🟠 **دکمهٔ «ورود آزمایشی» فقط به این متغیر وابسته نیست.** صفحهٔ ورود این دکمه را **خودکار** هم نشان می‌دهد اگر یک تلاش ورود نشان دهد بک‌اند یا دیتابیس در دسترس نیست. کلیک روی آن یک نشست با نقش `admin` می‌سازد که **کاملاً محلی است**: هیچ داده‌ای به سرور نمی‌رود و همه‌چیز فقط در `localStorage` همان مرورگر می‌ماند.
> ریسک عملیاتی این است که در زمان قطعی دیتابیس، کاربر ممکن است این حالت را با سامانهٔ واقعی اشتباه بگیرد و اطلاعاتی وارد کند که **هرگز در پایگاه‌داده ذخیره نمی‌شود**. برای یک سامانهٔ GxP این نکته را به کاربران اطلاع دهید، و قطعی دیتابیس را مطابق بخش ۷ مانیتور کنید تا زودتر از کاربر متوجه شوید.

> ℹ️ **`PORT` خوانده می‌شود** (پیش‌فرض `3000`، گوش‌دادن روی `0.0.0.0`). مقدار نامعتبر نادیده گرفته می‌شود، در لاگ هشدار می‌دهد و به `3000` برمی‌گردد — پس یک اشتباه تایپی سرور را نمی‌خواباند. در `docker-compose.yml` پورت داخلی همان ۳۰۰۰ می‌ماند و تغییر پورت بیرونی با map انجام می‌شود.

نمونهٔ فایل `.env` در ریشهٔ پروژه:

```env
DATABASE_URL="postgresql://pharma_admin:<رمز-قوی-خودتان>@your-db-host:5432/pharma_regulatory_db?schema=public"
JWT_SECRET="<خروجی دستور زیر>"
NODE_ENV=production
```

کلید JWT را تصادفی بسازید — از مقدار نمونه استفاده نکنید:

```bash
node -e "console.log(require('crypto').randomBytes(48).toString('base64'))"
```

---

## 🚀 ۴. استقرار

### روش اول: Docker Compose

```bash
docker compose up -d --build
```

> ⚠️ **قبل از اجرا فایل `.env` را بسازید.** `docker-compose.yml` دیگر هیچ رمزی در خودش ندارد؛ همه از `.env` خوانده می‌شوند و با نحو `${VAR:?پیام}` نوشته شده‌اند، یعنی اگر متغیری تعریف نشده باشد **compose با پیام روشن بالا نمی‌آید** و بی‌صدا با مقدار پیش‌فرض کار نمی‌کند. حداقل این چهار مورد لازم است:
> ```env
> POSTGRES_USER=pharma_admin
> POSTGRES_PASSWORD=<رمز قوی خودتان>
> POSTGRES_DB=pharma_regulatory_db
> JWT_SECRET=<خروجی: openssl rand -base64 48>
> ```
> نمونهٔ کامل در `.env.example` است. این فایل در `.gitignore` قرار دارد و نباید کامیت شود.

> ℹ️ **پورت دیتابیس عمداً روی هاست منتشر نمی‌شود؛** برنامه از شبکهٔ داخلی compose به آن می‌رسد. اگر واقعاً به اتصال بیرونی نیاز دارید، بند `ports` که در فایل کامنت شده را باز کنید و حتماً به `127.0.0.1` محدودش کنید.

> ℹ️ **مهاجرت‌ها هنگام بالا آمدن کانتینر اجرا می‌شوند.** نقطهٔ ورود تصویر (`scripts/docker-entrypoint.sh`) اول `prisma migrate deploy` را می‌زند و بعد سرویس را بالا می‌آورد، پس روی یک سرور تازه هیچ گام دستی لازم نیست. اگر مهاجرت شکست بخورد کانتینر عمداً بالا نمی‌آید: سرویسی که اسکیمایش با کدش نمی‌خواند، بدتر از سرویسی است که اصلاً بالا نیامده.
>
> اجرای دوباره بی‌ضرر است؛ اگر مهاجرت معلقی نباشد پیام «No pending migrations to apply» در لاگ می‌آید.

> ℹ️ سرویس Postgres **healthcheck دارد** (`pg_isready`) و برنامه با `depends_on: condition: service_healthy` منتظرش می‌ماند، پس در اولین اجرا زودتر از آمادگی دیتابیس بالا نمی‌آید. خودِ برنامه هم healthcheck دارد که `/api/health` را می‌زند؛ آن مسیر یک کوئری واقعی به پایگاه‌داده می‌فرستد، پس «کانتینر سالم» یعنی «پایگاه‌داده پاسخ می‌دهد»، نه صرفاً «پروسه بالاست».

> ℹ️ **پورت بیرونی از `.env` خوانده می‌شود:** `APP_PORT` (پیش‌فرض ۸۰۸۰). داخل کانتینر همیشه ۳۰۰۰ است. اگر روی سرور پورت دیگری آزاد است، فقط همین متغیر را عوض کنید.

---

### روش دوم: استقرار دستی

#### گام ۱ — نصب وابستگی‌ها
```bash
bun install        # روش پروژه (bun.lock موجود است)
# یا: npm install
```

#### گام ۲ — ساخت فایل `.env`
مطابق بخش ۳.

#### گام ۳ — ساخت جداول
```bash
npx prisma migrate deploy
```

> ℹ️ اگر `DATABASE_URL` را قبل از `npm run build` (گام ۵) ست کرده باشید، این گام خودکار در بیلد انجام می‌شود و اجرای دستی‌اش لازم نیست. اجرای دوباره بی‌ضرر است.

> ⚠️ **از `prisma db push` استفاده نکنید.** پروژه تاریخچهٔ نسخه‌بندی‌شدهٔ مهاجرت دارد (اکنون **۹ مهاجرت**)؛ `db push` این تاریخچه را دور می‌زند و مهاجرت‌های بعدی را ناهماهنگ می‌کند.

> 🔴 **برای به‌روزرسانی یک نصب موجود، اجرای مهاجرت اختیاری نیست.** سه مهاجرت اخیر داده را جابه‌جا می‌کنند و بدون آن‌ها سامانه رفتار غلط نشان می‌دهد:
> | مهاجرت | چه می‌کند | اگر اجرا نشود |
> | :--- | :--- | :--- |
> | `20260901120000_vendor_partner_columns` | پیوند سورس↔شریک را از داخل متن «اطلاعات تماس» به ستون‌های `manufacturer_id`/`supplier_id` منتقل و متن را تمیز می‌کند | تغییر تأمین‌کنندهٔ یک سورس ذخیره نمی‌شود |
> | `20260901140000_vendor_partner_fk` | همان ستون‌ها را کلید خارجی می‌کند (`ON DELETE SET NULL`) | ارجاع به شریک حذف‌شده در دیتابیس باقی می‌ماند |
> | `20260901150000_vendor_timestamps` | `created_at`/`updated_at` به `vendors` اضافه می‌کند | تشخیص ویرایش هم‌زمان کار نمی‌کند |
>
> هر سه idempotent‌اند و اجرای دوباره‌شان بی‌ضرر است.

#### گام ۴ — تزریق دادهٔ اولیه (اختیاری)
```bash
npx prisma db seed
```

> ⚠️ دستور `npm run seed` **وجود ندارد**؛ seed از بلوک `prisma` در `package.json` اجرا می‌شود.
>
> ℹ️ **این seed فقط کاربران را می‌سازد.** فایل `database/vendors.json` در مخزن خالی است، پس پیام «Seeding completed» با **۰ سورس و ۰ ماده** تمام می‌شود. این رفتار مورد انتظار است، نه خطا. دادهٔ واقعی باید از رابط کاربری وارد شود.

#### گام ۵ — بیلد تولیدی
```bash
npm run build
```

#### گام ۶ — اجرا
```bash
NODE_ENV=production npm run start
```

> 🔴 دو نکتهٔ اجرا:
> - **بدون `NODE_ENV=production`** سرور تلاش می‌کند Vite dev را بالا بیاورد و در محیط تولیدی خطا می‌دهد.
> - مسیر `dist` نسبت به **دایرکتوری جاری** حل می‌شود؛ سرویس را حتماً از ریشهٔ پروژه اجرا کنید.

> 💡 **اجرای پایدار:** فایل `ecosystem.config.cjs` برای `pm2` آماده است:
> ```bash
> pm2 start ecosystem.config.cjs
> pm2 save
> pm2 startup
> ```

---

### به‌روزرسانی یک نصب موجود

ترتیب مهم است — مهاجرت باید **قبل** از بالا آمدن نسخهٔ جدید اجرا شود، وگرنه کد تازه با اسکیمای قدیمی حرف می‌زند.

```bash
# ۱. پشتیبان بگیرید. مدارک پیوست هم داخل همین dump‌اند.
./deploy/backup.sh            # روی نصب Docker؛ خروجی در ./backups
# یا مستقیم:  pg_dump "$DATABASE_URL" > backup-$(date +%F).sql

# ۲. کد جدید را بگیرید و وابستگی‌ها را نصب کنید
git pull && bun install        # یا npm install

# ۳. مهاجرت‌ها
npx prisma migrate deploy

# ۴. بیلد و ری‌استارت
npm run build
pm2 restart pharma-regulatory-app      # یا: docker compose up -d --build
```

> ↩️ **بازگشت:** اگر لازم شد به نسخهٔ قبل برگردید، تنها راه امن بازگرداندن همان dump مرحلهٔ ۱ است. مهاجرت‌های این پروژه اسکریپت بازگشت (`down`) ندارند، و مهاجرت پیوند شریک متنی را که از `contact_info` پاک کرده دوباره نمی‌سازد.

### بررسی خودکار پیش از تحویل

مخزن یک workflow برای GitHub Actions دارد (`.github/workflows/ci.yml`) که روی هر push اجرا می‌شود: بررسی نوع (`tsc`)، کل تست‌ها، بیلد فرانت و باندل سرور. تست‌ها شامل **۲۵ تست API** هستند که برنامهٔ واقعی را روی یک PostgreSQL موقت بالا می‌آورند و گاردهای دسترسی، قواعد کسب‌وکار و مسیرهای نوشتن را از راه HTTP بررسی می‌کنند.

اگر روی سرور خودتان هم می‌خواهید همین را اجرا کنید:

```bash
npm run lint     # tsc --noEmit
npm test         # بدون DATABASE_URL، تست‌های API خودشان skip می‌شوند
npm run build
```

> ℹ️ `npm test` روی ماشینی که PostgreSQL ندارد هم کار می‌کند: تست‌های API به‌جای شکست، skip می‌شوند و ۱۳۱ تست واحد اجرا می‌شود.

---

## 🔐 ۵. کاربران و سطوح دسترسی

### رمزهای پیش‌فرض — دو مسیر متفاوت

| مسیر | چه زمانی اجرا می‌شود | رمز `admin` | بقیهٔ کاربران |
| :--- | :--- | :--- | :--- |
| seed خودکار سرور | اولین بالا آمدن سرور، **فقط اگر جدول `users` خالی باشد** | `123456` | `123` |
| `npx prisma db seed` | دستی، از `database/users.json` | `123` | `123` |

کاربران پیش‌فرض: `admin`، `commercial`، `qa`، `planning`، `finance`. هر پنج حساب با پرچم «تغییر اجباری رمز» ساخته می‌شوند و در اولین ورود به فرم تغییر رمز هدایت می‌شوند.

> 🔒 رمزها با **PBKDF2 + SHA-512، ۲۱۰٬۰۰۰ تکرار** و salt یکتا به ازای هر کاربر ذخیره می‌شوند. هش‌های ساخته‌شده با ضریب کار قدیمی هنگام اولین ورود موفق **خودکار ارتقا** می‌یابند، پس برای بالا بردن این عدد هیچ اقدام دستی لازم نبوده و نیست.

### ماتریس دسترسی

نقش‌های تعریف‌شده در دیتابیس: `admin`, `commercial`, `qa`, `planning`, `finance`, `lab`.
(نقش `lab` هیچ دسترسی‌ای ندارد و هیچ حسابی از آن استفاده نمی‌کند؛ فقط در enum باقی مانده چون حذفش نیازمند مهاجرت است.)

| نقش | دسترسی‌های پیش‌فرض |
| :--- | :--- |
| **`admin`** | همهٔ ۲۱ مجوز |
| **`commercial`** | سه مجوز خواندن + دانلود مدارک SOP + ثبت/ویرایش سورس + مدیریت کامل شرکای تجاری + امتیاز بازرگانی |
| **`qa`** | سه مجوز خواندن + دانلود مدارک SOP + نتایج آزمایشگاهی + **ارزیابی ریسک (FMEA)** + مدیریت کامل مواد + امتیاز QA |
| **`planning`** | سه مجوز خواندن + امتیاز برنامه‌ریزی |
| **`finance`** | سه مجوز خواندن + امتیاز مالی |
| **`lab`** | هیچ |

> 🔴 **خواندن هم مجوز است.** نسخهٔ قبلی این سند می‌گفت «خواندن محدود نیست» — دیگر درست نیست. سه مجوز `vendor.read`، `material.read` و `partner.read` روی **۹ مسیر GET** اعمال می‌شوند، پس می‌توان حسابی ساخت که مثلاً فقط شرکای تجاری را ببیند. هر چهار نقش کاری هر سه را دارند، پس با این تغییر کسی چیزی از دست نداد.

**مجوزها به تفکیک عملیات‌اند، نه ماژول:** `material.create` ≠ `material.edit` ≠ `material.delete` (همین‌طور `partner.*` و `vendor.*`). فهرست کامل ۲۱ مجوز:

`vendor.read`, `vendor.create`, `vendor.edit`, `vendor.delete`, `vendor.analysis`, `vendor.risk`,
`material.read`, `material.create`, `material.edit`, `material.delete`,
`partner.read`, `partner.create`, `partner.edit`, `partner.delete`, `partner.files`,
`score.commercial`, `score.qa`, `score.planning`, `score.finance`, `audit.read`, `users.manage`

> ℹ️ نام‌های قدیمی (`vendor.write`, `material.write`, `partner.write`, `archive.read`) بازنشسته شده‌اند ولی اگر در استثنای ذخیره‌شدهٔ کاربری باقی مانده باشند، هنگام خواندن به معادل امروزی‌شان باز می‌شوند — هیچ کاربری بی‌صدا دسترسی از دست نمی‌دهد. `partner.files` (دانلود مدارک قانونی SOP) عمداً از `partner.read` جداست: دیدن اینکه یک شریک گرید B دارد با برداشتن پروانهٔ کسبش از سامانه یکی نیست.

### استثنای پر‌کاربر

نقش فقط یک **الگو**ست. ستون `permissions` در جدول `users` می‌تواند برای هر کاربر جداگانه پر شود:
- **خالی** ⇒ کاربر از الگوی نقشش پیروی می‌کند (وضعیت همهٔ کاربران پیش‌فرض).
- **پرشده** ⇒ همان فهرست کاملاً **جایگزین** الگو می‌شود.

مدیریت از رابط کاربری، مسیر `#/users` (فقط ادمین). سامانه جلوی قفل‌شدن سازمان را می‌گیرد: ادمین نمی‌تواند خودش را حذف/غیرفعال/تنزل دهد و آخرین دارندهٔ `users.manage` این مجوز را از دست نمی‌دهد.

> 🔒 کنترل دسترسی **سمت سرور** اعمال می‌شود؛ گیت‌های سمت مرورگر فقط برای تجربهٔ کاربری‌اند.

---

## 📂 ۶. ساختار پایگاه‌داده (۱۳ جدول)

| گروه | جداول |
| :--- | :--- |
| احراز هویت | `users` |
| مواد اولیه | `materials` |
| سورس‌ها (هسته) | `vendors`, `vendor_materials`, `evaluations` |
| ریسک (FMEA) | `risk_assessments` |
| آزمایشگاه | `analysis_records` |
| فعالیت‌ها | `activity_logs` |
| شرکای تجاری | `business_partners`, `supplier_evaluations`, `sop_documents` |
| ممیزی | `audit_log` |
| انتخاب سورس منتخب | `source_selections` |

نکات:
- نام جدول ممیزی **مفرد** است: `audit_log` (نه `audit_logs`).
- همهٔ کلیدهای خارجی با رفتار صریح `CASCADE` یا `SET NULL` تعریف شده‌اند. پیوند سورس به شریک تجاری (`vendors.manufacturer_id` / `vendors.supplier_id`) از مهاجرت `20260901140000` به بعد کلید خارجی واقعی است با **`ON DELETE SET NULL`** — یعنی حذف یک شرکت، سورس و سوابق ارزیابی و آزمایشگاهی‌اش را نابود نمی‌کند، فقط پیوند را خالی می‌گذارد. (خودِ API هم تا وقتی سورسی به شریک وصل باشد اجازهٔ حذفش را نمی‌دهد؛ این constraint پشتیبان است.)
- `source_selections` تصمیم انسانی «سورس برندهٔ هر ماده» را با دلیل الزامی نگه می‌دارد (یکتا روی ترکیب ماده و دسته‌بندی).
- هر تغییر در هر ماژول با نسخهٔ قبل و بعد در `audit_log` ثبت می‌شود (الزام Audit Trail صنایع دارویی).

---

## 🩺 ۷. مانیتورینگ

*   **آدرس:** `http://<server>:3000/api/health`
*   **سالم:** `HTTP 200` با `{ "status": "ok", ... }`
*   **ناسالم:** `HTTP 503` با `{ "status": "degraded", ... }`

> ✅ **برای Zabbix / PRTG کافی است.** این endpoint واقعاً یک `SELECT 1` به دیتابیس می‌زند و اگر دیتابیس در دسترس نباشد **۵۰۳** می‌دهد. پس یک بررسی سادهٔ «کد وضعیت ۲۰۰ است؟» هم زنده بودن پروسه و هم دسترسی به دیتابیس را پوشش می‌دهد.
>
> نتیجه تا **۲ ثانیه** کش می‌شود تا مانیتورینگ پرتکرار به دیتابیس فشار نیاورد؛ یعنی یک قطعی حداکثر با دو ثانیه تأخیر گزارش می‌شود.
>
> این تنها مسیری است که احراز هویت نمی‌خواهد، پس ابزار مانیتورینگ به توکن نیاز ندارد.

لاگ‌ها: `pm2 logs` یا `docker compose logs -f pharma-app`.

---

## ✅ ۸. چک‌لیست پیش از استقرار تولیدی

| # | اقدام | چرا |
| :-- | :--- | :--- |
| ۱ | `.env` را با هر چهار متغیر بسازید و آن را کامیت نکنید | بدون آن‌ها compose بالا نمی‌آید و در استقرار دستی سرور با `[FATAL]` متوقف می‌شود. |
| ۲ | `JWT_SECRET` را تصادفی و حداقل ۳۲ کاراکتری بسازید | با `openssl rand -base64 48`. تغییر بعدی این کلید همهٔ نشست‌ها را باطل می‌کند — که اگر لو رفت، اقدام درست همین است. |
| ۳ | `npx prisma migrate deploy` را اجرا کنید | برای نصب تازه جدول‌ها را می‌سازد و برای نصب موجود، سه مهاجرت دادهٔ بخش ۴ را اعمال می‌کند. **این گام را رد نکنید.** |
| ۴ | `NODE_ENV=production` را تأیید کنید | وگرنه سرور Vite dev را بالا می‌آورد. |
| ۵ | رمز هر پنج حساب پیش‌فرض را در اولین ورود عوض کنید | رمزهای پیش‌فرض عمومی‌اند و در همین سند نوشته شده‌اند. |
| ۶ | `VITE_ENABLE_LOCAL_DEMO` را ست **نکنید** | حالت دمو ورود بدون دیتابیس را فعال می‌کند. توجه: این دکمه در قطعی دیتابیس **خودکار هم** ظاهر می‌شود (بخش ۳). |
| ۷ | یک بار با حساب غیرادمین وارد شوید | تأیید می‌کند مهاجرت‌ها و seed درست نشسته‌اند و ماتریس دسترسی کار می‌کند. |
| ۸ | پشتیبان‌گیری منظم از PostgreSQL را برقرار کنید | تنها منبع داده است و مدارک پیوست (SOP و مشخصات) هم داخل همین دیتابیس ذخیره می‌شوند، نه روی فایل‌سیستم — پس یک dump دیتابیس همه‌چیز را می‌گیرد، ولی حجمش بزرگ‌تر از انتظار است. |

### وضعیت امنیتی

**رفع‌شده و قابل اتکا:**

- کلید ثابت JWT حذف شد؛ در تولید نبودنش پروسه را متوقف می‌کند.
- **محدودکنندهٔ نرخ ورود** در خود برنامه: ۸ تلاش ناموفق به‌ازای هر کاربر و ۶۰ به‌ازای هر IP در پنجرهٔ ۱۵ دقیقه‌ای. سقف IP عمداً بالاست تا یک آدرس اداری، همهٔ کارکنان پشتش را قفل نکند. ورود موفق شمارنده را صفر می‌کند.
- **PBKDF2 با ۲۱۰٬۰۰۰ تکرار** و ارتقای خودکار هش‌های قدیمی هنگام ورود.
- هدرهای امنیتی (CSP، `frame-ancestors 'none'`، `X-Frame-Options`، HSTS روی TLS) بدون افزودن وابستگی نوشته شده‌اند — `helmet` عمداً نصب نشده چون این مجموعه همان چیزی است که یک برنامهٔ same-origin لازم دارد و همه‌اش در یک فایل قابل دیدن است.
- رمزهای دیتابیس و کلید JWT از `docker-compose.yml` به `.env` منتقل شده‌اند.
- کنترل دسترسی روی خود endpointها اعمال می‌شود، نه فقط در رابط کاربری.

**بدهی پذیرفته‌شده (واحد IT بداند):**

- **توکن JWT در `localStorage` مرورگر** نگهداری می‌شود، نه کوکی `httpOnly` ⇒ در برابر XSS آسیب‌پذیر است. تغییرش بازنویسی کل جریان احراز هویت است و انجام نشده.
- **ابطال توکن وجود ندارد.** توکن هفت‌روزه است. تغییر نقش و غیرفعال‌کردن حساب **بلافاصله** اثر می‌کنند (سرور در هر درخواست نوشتنی حساب را از دیتابیس می‌خواند)، ولی برای باطل‌کردن فوریِ همهٔ توکن‌های صادرشده تنها راه، عوض‌کردن `JWT_SECRET` و ری‌استارت است.
- **مدارک پیوست به‌صورت base64 داخل ستون‌های دیتابیس** ذخیره می‌شوند، نه در انبار فایل. برای پشتیبان‌گیری و اندازهٔ dump برنامه‌ریزی کنید.
- سامانه باید پشت شبکهٔ داخلی و ترجیحاً پشت HTTPS قرار بگیرد (HSTS فقط روی TLS فعال می‌شود).

---

## 🧰 ۹. عیب‌یابی

| نشانه | علت محتمل | اقدام |
| :--- | :--- | :--- |
| `/api/health` کد ۵۰۳ می‌دهد | دیتابیس در دسترس نیست یا `DATABASE_URL` نامعتبر است | لاگ سرور را ببینید؛ دنبال `[FATAL] DATABASE_URL is missing or invalid` بگردید |
| سرور اصلاً بالا نمی‌آید و `[FATAL]` می‌دهد | `JWT_SECRET` تعریف نشده یا کمتر از ۳۲ کاراکتر است | متن خطا خودش دستور ساخت کلید را می‌دهد |
| بعد از ورود، بلافاصله خارج می‌شود | توکن منقضی (۴۰۱) | دوباره وارد شوید. توجه: ۴۰۳ (عدم مجوز) نباید باعث خروج شود |
| صفحهٔ سفید در مرورگر | پر شدن حافظهٔ محلی مرورگر | صفحهٔ خطا دکمهٔ «پاک‌کردن حافظهٔ محلی و بارگذاری مجدد» دارد؛ هیچ دادهٔ سروری حذف نمی‌شود |
| دکمهٔ «ورود آزمایشی (حالت لوکال)» ناگهان در صفحهٔ ورود ظاهر شده | بک‌اند یا دیتابیس در دسترس نیست | نشانهٔ قطعی است، نه یک قابلیت. `DATABASE_URL` و سرویس Postgres را بررسی کنید؛ تا رفع مشکل کاربران نباید از آن استفاده کنند |
| seed اجرا شد ولی هیچ سورسی نیست | `database/vendors.json` خالی است | رفتار مورد انتظار؛ داده را از رابط کاربری وارد کنید |
| تغییر تأمین‌کنندهٔ یک سورس ذخیره نمی‌شود | مهاجرت‌های بخش ۴ اجرا نشده‌اند | `npx prisma migrate deploy` را اجرا کنید |
| هنگام ذخیره پیام «این رکورد هم‌زمان توسط شخص دیگری تغییر کرده» می‌آید | همین‌طور است — کاربر دیگری زودتر ذخیره کرده | صفحه را تازه کنید و تغییر را دوباره اعمال کنید؛ این پیام از بازنویسی کار آن کاربر جلوگیری کرده است |
| ورود با پیام «تعداد تلاش بیش از حد» رد می‌شود | محدودکنندهٔ نرخ فعال شده (۸ تلاش ناموفق) | ۱۵ دقیقه صبر کنید، یا سرور را ری‌استارت کنید (شمارنده در حافظه است) |
| بعد از بیلد، صفحه بالا نمی‌آید | سرویس از ریشهٔ پروژه اجرا نشده | مسیر `dist` نسبت به دایرکتوری جاری حل می‌شود |

---

**نگهداری این سند:** هر تغییر در `package.json` (اسکریپت‌ها)، `prisma/migrations/` (مهاجرت‌ها)، `prisma/schema.prisma` (جداول)، `src/utils/permissions.ts` (ماتریس دسترسی)، `docker-compose.yml` یا متغیرهای محیطی باید در همین فایل بازتاب پیدا کند.

در محیط تحت مقررات، راهنمای استقرارِ کهنه ریسک عملیاتی است نه ایراد نگارشی: این سند یک‌بار از کد عقب افتاده بود و چیزهایی را توصیف می‌کرد که وجود نداشتند (بازگشت خودکار به فایل JSON) و چیزهایی را هشدار می‌داد که دیگر درست نبودند (کلید ثابت JWT، نبود محدودکنندهٔ نرخ، پورت هاردکد).

> 🗓️ **آخرین راستی‌آزمایی با کد:** کامیت `57fe2e8` — همهٔ دستورها، نام اسکریپت‌ها، متغیرهای محیطی، فهرست مهاجرت‌ها و ماتریس دسترسی در همان تاریخ مستقیماً با سورس تطبیق داده شدند.
