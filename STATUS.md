# STATUS.md — وضعیت زندهٔ کار

> بعد از **هر تغییر** به‌روزرسانی می‌شود. برای ادامه در سشن جدید: اول `CLAUDE.md` بعد این را بخوان.
> آخرین به‌روزرسانی: نوار بالای صفحه — عنوان صفحه، چیپ ساعت، جای پشتیبان‌گیری و Esc

## نوار بالای صفحه (۱۴۰۵/۰۶/۱۴) — چهار ایراد

**اندازه‌گیری اولیه:** در صفحهٔ اصلی، گروه سمت راست هدر عرض **صفر** داشت (دکمهٔ برگشت و مسیر ناوبری فقط وقتی عمق ناوبری > ۱ باشد وجود دارند)، پس همهٔ کنترل‌ها در گوشهٔ چپ جمع می‌شدند و ~۴۶۰px سمت راست خالی بود. خوشهٔ کنترل‌ها **۷۰۰px** بود و هیچ جزئی از آن کوچک نمی‌شد.

**۱. عنوان صفحهٔ جاری سمت راست آمد** (`h1`، از `getViewStateLabel`). **ولی فقط جایی که چیزی دیگر آن را نمی‌گوید:** از `md` به بالا و وقتی مسیر ناوبری وجود دارد، آخرین crumb همان اسم را می‌گوید، و چاپ هر دو باعث شد در ۹۰۰px عنوان به «خرید …» بریده شود و اولین crumb به یک حرف — اندازه‌گیری شد. پس عنوان جای خودش را به مسیر می‌دهد و در صفحهٔ اصلی و صفحه‌های باریک تنها می‌ماند.

**۲. مسیر ناوبری از `lg` به `md` آمد.** بین ۷۶۸ تا ۱۰۲۴ فقط یک دکمهٔ «برگشت» بود بدون هیچ نشانی از «کجا هستم».

**۳. چیپ ساعت از سه فکت به دو فکت رسید.** تاریخ میلادی به tooltip رفت (فکتی است که وقتی لازم شد نگاهش می‌کنند). چیپ از `md` برمی‌گردد ولی **فقط ساعت**؛ تاریخ شمسی از `lg`. علت با اندازه‌گیری: در ۸۲۰px هدر ~۵۴۸px است و با تاریخ، سرریز افقی می‌شد.

**۴. پشتیبان‌گیری به منوی کاربر رفت** و کلید تم فقط در نوار ماند (قبلاً یک تنظیم، دو کنترل دائمی).

**۵. Esc و ARIA برای هر دو پاپ‌اور:** هیچ‌کدام با Esc بسته نمی‌شدند (فقط کلیک روی اسکریم). حالا `aria-haspopup`، `aria-expanded` و `aria-label` دارند و Esc هر دو را می‌بندد.

**نتیجهٔ اندازه‌گیری‌شده:** خوشهٔ کنترل‌ها ۷۰۰ → **۴۸۷px** (در `md` تا `lg`: ۳۳۲px). سرریز افقی هدر در ۱۴۴۰/۱۰۲۴/۹۰۰/۸۲۰/۷۶۸/۶۰۰/۳۹۰ روی صفحهٔ اصلی، دسته‌بندی و جزئیات سورس: **هیچ‌کدام**. تست‌ها ۲۹۶ سبز؛ خطای صفحه ۰.

## بررسی یکپارچه تأمین‌کنندگان (۱۴۰۵/۰۶/۱۴) — موارد ۴ و ۵

**۴. فهرست از کارت به جدول رفت.** شبکهٔ سه‌ستونی کارت تنها شکل فهرستی بود که بقیهٔ برنامه ندارد: مخزن مواد، شرکای تجاری و کاربران همه جدول‌اند با سرستون مرتب‌سازی، ردیف خالی و اسکلت بارگذاری مشترک. کارت‌ها هم اصلاً مرتب نمی‌شدند، پس پیدا کردن پایین‌ترین امتیاز یعنی خواندن همهٔ کارت‌ها.
   - ستون‌ها: تأمین‌کننده (فارسی + لاتین)، نقش با آیکن، کشور، مواد عرضه‌شده (شمارش + دو نام اول + «+n»)، میانگین امتیاز، و ستون پرونده. **هیچ اطلاعاتی از کارت حذف نشد.**
   - مرتب‌سازی روی هر پنج ستون، **قبل از برش صفحه** انجام می‌شود (مرتب‌سازی بیست ردیف روی صفحه، صفحه را مرتب می‌کرد نه فهرست را). مقایسهٔ متن با `Intl.Collator('fa')` مثل بقیهٔ جدول‌ها؛ شرکت بدون امتیاز با `-1` مرتب می‌شود نه جایی دلخواه.
   - «ارزیابی نشده» به‌جای صفر نوشته می‌شود؛ این دو یک چیز نیستند.
   - `TableSkeletonRows` با پراپ تازهٔ `isLoading` که `App` از `isSyncing && db.length === 0` می‌دهد — همان چیزی که به `CategoryView` می‌دهد.
   - دو حالت خالی جدا: «چیزی با این جست‌وجو نیست» با دکمهٔ پاک‌کردن، و «هنوز سورسی ثبت نشده».

**۵. خروجی اکسل فهرست.** `exportSupplierDirectoryToExcel` با همان قالب بقیهٔ خروجی‌ها (هدر 1E3A8A، RTL، حاشیهٔ نازک). ستون‌ها: نام، نام لاتین، نقش، کشور، تعداد مواد، فهرست مواد، میانگین امتیاز (رنگ‌بندی سبز/کهربایی/قرمز، و «ارزیابی نشده» به‌جای عدد جعلی). **خروجی همان چیزی است که جست‌وجو محدودش کرده و به همان ترتیب مرتب‌سازی جدول** — فایل با صفحه یکی است. قبلاً فقط پروندهٔ تک‌شرکت خروجی داشت.

**تست:** `tests/supplierDirectory.test.ts` (۴ مورد) روی کلید گروه‌بندی شرکت‌ها — همان چیزی که تصمیم می‌گیرد یک شرکت یک ردیف باشد یا دو ردیفِ نصفه (ی/ك عربی، نیم‌فاصله، فاصله و حروف بزرگ). کل سوییت **۲۹۶** سبز. مرورگر: مرتب‌سازی، دانلود واقعی فایل، حالت خالی، کلیک ردیف → پرونده، تم روشن/تیره و موبایل ۴۲۰px، خطای صفحه ۰.

## بررسی یکپارچه تأمین‌کنندگان (۱۴۰۵/۰۶/۱۴) — سه ناهماهنگی ظاهری

از پنج موردی که در نقد این ماژول گزارش شد، سه مورد اول اعمال شد (جدول‌شدن فهرست و خروجی اکسل فهرست، به تصمیم کاربر، جداست).

**۱. رنگ teal حذف شد.** سیزده نقطه `bg-teal-50` / `border-teal-100` / `text-teal-600` داشتند و **هیچ‌کدام واریانت `dark:` نداشتند**، پس در تم تیره لکهٔ روشن می‌شدند — همان باگی که قبلاً در کارت نتیجهٔ SOP گرفتیم. همه به `primary` و شفافیت‌های آن رفتند (`bg-primary/10`, `border-primary/20`, `text-primary`, `group-hover:bg-primary`), که در هر دو تم از توکن می‌خواند. teal رنگ معنایی نبود؛ لهجهٔ محلی این یک فایل بود.
   - **سایدبار دست نخورد:** رنگ teal آیتم سایدبار عمدی است (هر ماژول رنگ خودش را دارد، `AppSidebarButton`)، ولی بدنهٔ صفحه نباید آن را به‌عنوان اکسنت تکرار کند.

**۲. مقیاس متن روی چهار توکن نشست.** `text-lg sm:text-xl lg:text-2xl` عنوان پرونده → `text-xl` (هم‌اندازهٔ عنوان صفحه؛ قبلاً محتوا از عنوان صفحه درشت‌تر بود). اعداد آمار `text-2xl` → `text-xl` مثل کاشی‌های بقیهٔ ماژول‌ها. `text-base`/`text-lg` سرفصل‌ها → `text-sm`. `sm:text-[12px]` حذف شد.

**۳. نوار جست‌وجو از `ui/input` می‌آید.** قبلاً `<input>` خام با `placeholder-slate-400`، `bg-transparent` و حلقهٔ focus با رنگ teal داخل یک پنل `backdrop-blur` بود — تنها پنل شیشه‌ای برنامه. آیکن ذره‌بین و دکمهٔ پاک‌کردن درون‌خطی ماندند (استثنای مجاز سیستم طراحی) و دکمهٔ پاک‌کردن `aria-label` گرفت.

**تأیید:** ۲۹۲ تست سبز؛ مرورگر در تم روشن و تیره، فهرست و پروندهٔ تأمین‌کننده، خطای صفحه ۰. `grep teal-` روی فایل: صفر.

## ثبت سورس جدید زیر مجوز (۱۴۰۵/۰۶/۱۴)

**چه بود:** دکمهٔ «ثبت سورس جدید» در داشبورد فقط شرط `currentUser &&` داشت، یعنی به هر حساب واردشده نشان داده می‌شد. سرور از قبل `POST /api/vendors` را با `vendor.create` می‌بست، پس واحدی بدون این مجوز می‌توانست طولانی‌ترین فرم برنامه را پر کند و در آخرین قدم ۴۰۳ بگیرد.

**چه شد:**
- دکمه با `can(currentUser, 'vendor.create')` گیت شد.
- **مسیر فرم هم گیت شد،** چون فرم یک route است: `#/category/<cat>/new` لینکی است که می‌شود برای کسی فرستاد یا در تاریخچهٔ مرورگر ماند. حالا `formMode` قبل از رندر با `vendor.create` (و برای ویرایش با `vendor.edit`) بررسی می‌شود و در غیر این صورت همان `AccessDenied` بقیهٔ ماژول‌ها می‌آید؛ متنش می‌گوید مجوز کجای فرم مدیریت کاربران تنظیم می‌شود.
- مجوز تازه‌ای ساخته نشد: `vendor.create` از قبل در جدول سیاست بود و در دیالوگ دسترسی ستون «ثبت» ردیف سورس‌ها همان است.

**تست:** ۱ مورد تازه در `tests/api.vendorWrites.test.ts` (حساب برنامه‌ریزی ۴۰۳ می‌گیرد و رکوردی ساخته نمی‌شود). کل سوییت **۲۹۲** سبز. مرورگر: حساب `planning` بدون دکمه و با صفحهٔ عدم دسترسی روی لینک مستقیم؛ حساب `admin` بدون تغییر.

## مدیریت کاربران (۱۴۰۵/۰۶/۱۴) — فاز ۵: فعالیت اخیر حساب

**دکمهٔ تازه در ستون عملیات** (آیکن تاریخچه) ده رویداد آخر همان حساب را در یک `FormModal` نشان می‌دهد: تاریخ و ساعت، عملیات، رکورد هدف، ماژول، و بج «مسدود شد / ناموفق» روی تلاش‌های ردشده — چون همان ردیف است که مدیر دنبالش می‌گردد.

- **هیچ endpoint یا ذخیره‌سازی تازه‌ای نیاز نداشت:** هر نوشتنی از قبل می‌نویسد چه کسی آن را انجام داده، پس این فقط `GET /api/audit-logs?userId=<username>&limit=10` است.
- **دکمه فقط وقتی هست که کاربرِ جاری `audit.read` داشته باشد.** `users.manage` این را نمی‌دهد؛ دو مجوز جدا هستند و اگر سرور ۴۰۳ بدهد پیام صریح نمایش داده می‌شود نه خطای عمومی.
- حالت خالی توضیح می‌دهد که ورود هم رویداد ثبت‌شده است، پس «هیچ فعالیتی» یعنی این حساب هرگز وارد نشده.

**تست:** ۲ مورد تازه در `tests/api.auditRead.test.ts` (فیلتر حساب، و اینکه هم `userId` و هم نام ذخیره‌شده را تطبیق می‌دهد). کل سوییت **۲۹۱** سبز. مرورگر: مودال با سه رویداد واقعی، روشن و تیره، خطای صفحه ۰.

**پایان بازطراحی مدیریت کاربران.** پنج فاز، دو مهاجرت سبک (`vendor.select`)، بدون از دست رفتن دسترسی هیچ حساب موجود.

## مدیریت کاربران (۱۴۰۵/۰۶/۱۴) — فاز ۴: ماتریس دسترسی

**تب تازه کنار «فهرست کاربران»:** همهٔ حساب‌ها در برابر همهٔ ماژول‌ها، با همان شورت‌هند `RCUD` که دیالوگ و خروجی اکسل می‌نویسند. فهرست به سؤال «این نفر چه دارد؟» جواب می‌دهد؛ ماتریس به سؤال «چه کسی این را دارد؟» که بازبینی دسترسی از آن شروع می‌شود و فهرست فقط با باز کردن تک‌تک حساب‌ها جوابش را می‌دهد.

- ستون نام و سرستون هر دو **چسبان**‌اند تا در اسکرول افقی گم نشوند؛ ارتفاع جدول تا ۷۰vh و بقیه اسکرول.
- **صفحه‌بندی ندارد، عمداً:** صفحه‌بندی یک بازبینی دسترسی دقیقاً همان ردیفی را پنهان می‌کند که دنبالش هستید. فیلترهای فاز ۳ روی ماتریس هم اعمال می‌شوند.
- خانهٔ بدون دسترسی **خط تیره** می‌گیرد نه خالی می‌ماند؛ تفاوت «دسترسی ندارد» با «پر نشده» تمام حرف این صفحه است.
- زیر نام هر کاربر سمت، و در صورت وجود «غیرفعال» و «سفارشی» می‌آید. کلیک روی نام، همان دیالوگ سطح دسترسی را باز می‌کند.
- ستون امتیازدهی نام کوتاه دپارتمان‌ها را می‌نویسد (بازرگانی، کیفیت، برنامه‌ریزی، مالی)؛ با برچسب کامل مجوز، ردیف ادمین به پنج خط و ~۲۰۰px می‌رسید (اندازه‌گیری شد: حالا ۶۶px).
- راهنمای حروف پایین جدول.

**تأیید:** کل سوییت **۲۸۹** سبز؛ مرورگر در ۱۶۰۰px و تم روشن/تیره، کلیک روی نام دیالوگ را باز کرد، خطای صفحه ۰.

> نکتهٔ تست: اگر سرور توسعه روی همان دیتابیس `vlse` باز باشد، تست‌های API که ردیف‌های audit را می‌شمارند ممکن است شکست بخورند (ورودهای مرورگر رکورد می‌نویسند). قبل از اجرای سوییت، سرور توسعه را ببندید.

## مدیریت کاربران (۱۴۰۵/۰۶/۱۴) — فاز ۳: بازبینی دسترسی

**نوار فیلتر** بالای جدول با سه سؤالی که هر بازبینی دسترسی می‌پرسد: سمت سازمانی، وضعیت حساب (فعال / غیرفعال / هرگز وارد نشده)، و منبع دسترسی (الگوی سمت / سفارشی). فیلتر «سفارشی» دقیقاً همان قاعدهٔ فهرست خالی را می‌خواند: خالی یعنی پیروی از نقش. کنار دکمهٔ حذف فیلترها شمارش «n از m حساب» می‌آید.

**خلاصهٔ تفاوت با الگو** بالای جدول دیالوگ دسترسی: چند مجوز اضافه و چند مجوز کم شده، با نام هر کدام. علامت‌های ریز `+`/`−` روی تیک‌ها از قبل بودند ولی ۸ پیکسل و بدون راهنما؛ حالا یک‌بار با کلمه گفته می‌شود. اگر فهرست دقیقاً همان الگو باشد، همین را می‌گوید.

**خروجی Excel** با همان قالب بقیهٔ ماژول‌ها (هدر 1E3A8A، RTL، حاشیهٔ نازک): یک ردیف به‌ازای هر حساب، یک ستون به‌ازای هر ماژول با همان شورت‌هند `RCUD` صفحه، به‌علاوهٔ نام کامل مجوزهای مؤثر. حساب غیرفعال قرمز و دسترسی سفارشی کهربایی رنگ می‌گیرد. **خروجی همان چیزی است که روی صفحه فیلتر شده**، چون بازبینی معمولاً روی یک واحد یا روی حساب‌های بسته انجام می‌شود.

**تست:** `tests/userAccessExport.test.ts` (۴ مورد) روی دو مشتقی که خروجی به آن‌ها تکیه دارد: شورت‌هند ماژول و فهرست مؤثر. کل سوییت **۲۸۹** سبز. تأیید مرورگری: فیلتر نقش (۵ → ۱ ردیف)، دانلود واقعی فایل، و خلاصهٔ تفاوت در دیالوگ. خطای صفحه: ۰.

## مدیریت کاربران (۱۴۰۵/۰۶/۱۴) — فاز ۲: `vendor.select` و نقش آزمایشگاه

**مجوز تازهٔ `vendor.select`:** تصمیم «این ماده از کدام سورس خریداری می‌شود» تا امروز زیر `vendor.edit` بود، یعنی هرکس می‌توانست شمارهٔ تماس یک سورس را اصلاح کند، می‌توانست سورس برندهٔ یک ماده را هم عوض کند. حالا `PUT /api/source-selections` مجوز خودش را دارد؛ در الگوی نقش‌ها فقط **بازرگانی** و **مدیر** آن را می‌گیرند (بازرگانی سفارش را می‌گذارد، QA ارزیابی می‌کند). گیت کلاینت در `CategoryView` هم به همین مجوز منتقل شد. یک ردیف تازه در `PERMISSION_MODULES` با تیک واحد (ثبت و ویرایش یکی است، حذف ندارد).

**مهاجرت `20260903160000`:** تفکیک مجوز، دسترسی هر حسابی را که فهرست استثنایش `vendor.edit` (یا نام بازنشستهٔ `vendor.write`) را دارد بی‌صدا کم می‌کرد، پس همان‌جا یک‌بار `vendor.select` به آن فهرست‌ها اضافه می‌شود — همان کاری که مهاجرت ۲۰۲۶۰۹۰۳۱۲۰۰۰۰ برای خواندن‌ها کرد. اجرای دوباره‌اش ورودی تکراری نمی‌سازد. حساب‌هایی که از الگوی نقش پیروی می‌کنند نیازی به مهاجرت ندارند.

**نقش `lab` معنا پیدا کرد:** الگویش خالی بود، یعنی هر حساب با این نقش هیچ صفحه‌ای نمی‌دید در حالی که فرم کاربر همچنان نقش را پیشنهاد می‌کرد. حالا سه خواندن + `vendor.analysis` دارد: نتایج روی سورس ثبت می‌شوند، پس دیدن سورس بخشی از کار است. `partner.files` عمداً ندارد — میز آزمایشگاه برای اجرای آزمون به مدارک حقوقی تأمین‌کننده نیاز ندارد.

**تست:** ۳ تست تازه (تفکیک مجوز انتخاب سورس در سطح endpoint · رفتار مهاجرت روی چهار حالت + اجرای دوباره · محدودهٔ نقش آزمایشگاه)، و ماتریس `permissions.test.ts` به‌روزرسانی شد. کل سوییت **۲۸۵** سبز. مهاجرت روی دیتابیس خالی از صفر اجرا شد و دیالوگ دسترسی در مرورگر با ردیف تازه بررسی شد.

## مدیریت کاربران (۱۴۰۵/۰۶/۱۳) — فاز ۱: اجرای واقعی `users.manage`

**چه بود:** هر هفت مسیر `/api/users*` با `requireRole("admin")` بسته بود، نه با `requirePermission("users.manage")`. یعنی مجوز تزئینی بود: دادنش به یک کارشناس هیچ کاری نمی‌کرد و گرفتنش از یک مدیر هم در را نمی‌بست. هم‌زمان سایدبار ماژول را به هر کسی که مجوز را داشت نشان می‌داد، پس صفحه چیزی را پیشنهاد می‌کرد که سرور رد می‌کرد — دقیقاً همان واگرایی‌ای که قاعدهٔ ۱۴ برای جلوگیری از آن نوشته شده.

**چه شد:**
- گارد هر هفت مسیر به `requirePermission("users.manage")` تغییر کرد. مثل همیشه رکورد کاربر از دیتابیس خوانده می‌شود نه از توکن، پس سلب دسترسی فوری اثر می‌کند.
- **یک قدرت نزد نقش ماند:** `refuseAdminGrant` نمی‌گذارد کسی که خودش `admin` نیست نقش «مدیر سیستم» را بدهد — نه با ساخت حساب، نه با `PUT /role`، نه با `PATCH`. دلیل: چند بررسی هنوز خودِ نقش را می‌خوانند و `checkAdminSafety` تعداد مدیران فعال را می‌شمارد؛ تفویض مدیریت حساب‌ها نباید راهی برای ساختن مدیر باشد.
- فرم کاربر گزینهٔ «مدیر سیستم» را فقط به مدیر سیستم نشان می‌دهد (UX؛ کنترل همان ۴۰۳ سرور است). اگر کاربرِ در حال ویرایش خودش ادمین باشد گزینه می‌ماند وگرنه سمتش از فهرست حذف می‌شد.
- `checkAdminSafety` و `checkPermissionSafety` دست‌نخورده‌اند: آخرین مدیر فعال و آخرین دارندهٔ `users.manage` همچنان محافظت می‌شوند.

**تست:** ۳ تست تازه در `tests/api.users.test.ts` (تفویض واقعاً در را باز می‌کند · گرفتن مجوز از یک ادمین در را می‌بندد · تفویض‌شده نمی‌تواند مدیر بسازد)، و عنوان تست قدیمی از «فقط مدیر» به «فقط دارندهٔ مجوز» اصلاح شد. کل سوییت **۲۸۲** سبز. تأیید زنده: حساب بازرگانی با مجوز، `GET /api/users` را ۲۰۰ گرفت و ارتقای کسی به ادمین را ۴۰۳.

**بدون مهاجرت، بدون کاهش دسترسی هیچ حسابی:** الگوی نقش `admin` از قبل شامل `users.manage` است.

## ردیابی تغییرات (۱۴۰۵/۰۶/۱۳) — مرحلهٔ ۵: نمای کلی و خط زمانی

**نوار نمای کلی (۵ کاشی):** کل رویدادها، امروز، بحرانی، مسدودشده، ناموفق. هر کاشی **کنترل است نه تزئین** — کلیک روی آن همان فیلتر را اعمال می‌کند، پس عدد و رکوردهای پشتش دو کار جدا نیستند. قبلاً دو چیپ (کل و بحرانی) کنار دکمهٔ خروجی در هدر بود که فقط عدد نشان می‌داد. سه شمارندهٔ تازه در SQL روی ستون‌های ایندکس‌شده شمرده می‌شوند (`today`, `blocked`, `failed`)، پس نوار پنج `count` هزینه دارد نه یک پیمایش جدول. عمداً داشبورد نیست: بدون نمودار، بدون فلش روند.

**فیلتر نتیجه** به پنل فیلترها اضافه شد (API‌اش در مرحلهٔ ۳ ساخته شده بود). جدا از سطح بحرانیت است چون دو سؤال متفاوت‌اند: بحرانیت یعنی چقدر مهم، نتیجه یعنی اصلاً اجرا شد یا نه.

**خط زمانی** به‌عنوان **نمای مکمل** (جدول همچنان پیش‌فرض و اصلی است): همان صفحه از همان کوئری، گروه‌بندی‌شده بر اساس روز، هر ردیف با ساعت، کاربر، عملیات، رکورد، بج نتیجه/بحرانیت و **خلاصهٔ خود تغییر** (اولین فیلدی که جابه‌جا شد + «و n فیلد دیگر»). سوییچ نما دادهٔ زیر دست را عوض نمی‌کند، فقط چیدمانش را. صفحه‌بندی بین هر دو نما مشترک است.

**یک باگ نمایشی:** جدول برای سه اکشن تازهٔ مرحلهٔ ۳ متن خام انگلیسی (`Update - Blocked`) چاپ می‌کرد، چون `actionLabels` داخل کامپوننت فهرست ناقصی بود. حالا `actionMeta()` اول رنگ‌های محلی، بعد واژگان مشترک `AUDIT_ACTION_LABELS` و در نهایت مقدار خام را برمی‌گرداند؛ هیچ رکوردی بی‌نام نمایش داده نمی‌شود.

**تست و تأیید:** ۲ تست API تازه (شمارش امروز/مسدود/ناموفق)؛ کل سوییت **۲۷۹** سبز. تأیید مرورگری: نوار کاشی‌ها، سوییچ به خط زمانی، فیلتر شدن جدول با کلیک روی کاشی «مسدودشده» (۱ ردیف)، روشن/تیره و موبایل ۴۲۰px. خطای صفحه: ۰.

## ردیابی تغییرات (۱۴۰۵/۰۶/۱۳) — مرحلهٔ ۴: پنجرهٔ جزئیات

**تصمیم دربارهٔ ظاهر:** پرامپت اصلی «کشوی راست» خواسته بود، ولی خواستهٔ کاربر هم‌راستایی با بقیهٔ ماژول‌هاست. پس همان `FormModal` (اندازهٔ `lg`) ماند — همان backdrop، همان انیمیشن، همان Esc/focus trap و همان فوتر — و فقط محتوایش کامل شد. یک اورلی با قواعد بصری متفاوت، بی‌قاعدگی است نه ویژگی.

**سه چیز تازه در پنجره:**
- **بج نتیجه** کنار عنوان (`انجام شد` / `ناموفق` / `مسدود شد`). عمداً از سطح بحرانیت جداست: بحرانیت می‌گوید چقدر مهم است، نتیجه می‌گوید اصلاً اجرا شد یا نه. رکورد قدیمی که ستون نتیجه ندارد **هیچ بجی نمی‌گیرد**، نه بج «موفق» حدسی.
- **«در پی همین اقدام»** — بقیهٔ زنجیرهٔ همان درخواست، قدیمی‌به‌جدید، هر ردیف کلیک‌پذیر و همان پنجره را روی آن رکورد باز می‌کند. اگر رویداد تنها باشد، بخش اصلاً رندر نمی‌شود (عنوانِ خالی که توضیح دهد چیزی نیست، خودش شلوغی است). خواندن زنجیره مستقل است: خطا در آن، خودِ تغییر را از کار نمی‌اندازد.
- **شناسهٔ نشست** کنار شناسهٔ درخواست در بخش فنی؛ فقط وقتی ثبت شده باشد.

**یک تکرار حذف شد:** نگاشتِ «ردیف API → مدل نمایش» به `toAuditLog` منتقل شد و هر دو مصرف‌کننده (جدول و رویداد مرتبط) از همان استفاده می‌کنند، وگرنه یک رکورد بسته به اینکه از کدام فهرست باز شده دو جور توصیف می‌شد.

**رکورد ورود هم شناسهٔ نشست خودش را می‌گیرد** (`setCurrentSession` در هندلر ورود): تنها رکوردی که می‌گوید نشست کِی شروع شد، نباید تنها رکورد بی‌نشستِ آن نشست باشد.

**تست و تأیید:** ۲ تست برای `toAuditLog`؛ کل سوییت **۲۷۷** سبز. تأیید مرورگری روی سرور واقعی: زنجیرهٔ دو رکوردی (ثبت سورس + ارزیابی ریسک) با یک `correlation_id` و یک `session_id`، باز شدن رویداد مرتبط از داخل پنجره، و روشن/تیره هر دو. خطای صفحه: ۰.

**تغییر نکرد:** ستون‌های جدول اصلی (خواستهٔ صریح کاربر از قبل)، مسیر نوشتن، و مجوزها.

## ردیابی تغییرات (۱۴۰۵/۰۶/۱۳) — مرحلهٔ ۳: نتیجهٔ عملیات و شناسهٔ نشست

**مهاجرت `20260903140000_audit_result_and_session`** — دو ستون افزایشی، بدون تغییر ستون موجود و بدون حذف داده:
- `result` (`Success` / `Failed` / `Blocked`) + ایندکس. قبلاً «آیا این کار انجام شد؟» فقط در متن آزاد ستون `action` بیان می‌شد (`Delete - Blocked`، `FAILED_LOGIN`)؛ ممیزی که دنبال تلاش‌های ردشده می‌گشت باید این قرارداد را می‌دانست و عیناً هجی می‌کرد. ردیف‌های موجود در همان مهاجرت با همان قاعده پر می‌شوند.
- `session_id` + ایندکس. توکن حالا `sid` دارد که در لحظهٔ ورود ساخته می‌شود، پس یک رشته تغییر به **یک نشست** نسبت داده می‌شود نه فقط به یک حساب. توکن‌های قدیمی `sid` ندارند و رکوردشان `NULL` می‌ماند — شناسه‌ای که وجود نداشته را نمی‌سازیم.
- **`request_id` عمداً اضافه نشد:** شناسهٔ درخواست همان `correlation_id` است (مرحلهٔ ۲). ستون دوم با همان مقدار فقط زمینهٔ ناهم‌خوانی می‌سازد.

**`resultFor(action, explicit?)`** تنها جای تصمیم است، پس ۴۷ نقطهٔ نوشتن دست نخوردند. `Reject` عمداً «موفق» است: رد شدن در آزمون QC یک تصمیم انجام‌شده است، نه یک عملیات شکست‌خورده — وگرنه تصمیم‌های آزمایشگاه کنار ورودهای ناموفق بایگانی می‌شد.

**یک ادعای نادرست در خودِ trail اصلاح شد:** سه نقطه، رد شدن *ثبت* و *ویرایش* را با اکشن `Delete - Blocked` می‌نوشتند، یعنی لاگ می‌گفت حذفی مسدود شده در حالی که هیچ‌کس قصد حذف نداشت. حالا `Create - Blocked` و `Update - Blocked` (برچسب هر سه در `auditTaxonomy` هست، پس رکوردهای قدیمی هم خوانا می‌مانند). حذف مسدودشدهٔ واقعی در `partners.ts` سر جایش ماند.

**فیلتر تازه:** `GET /api/audit-logs?result=Blocked`.

**تست:** `tests/api.auditOutcome.test.ts` (۷ مورد). کل سوییت **۲۷۵** مورد سبز. مهاجرت روی یک دیتابیس خالی هم از صفر اجرا شد.

## ردیابی تغییرات (۱۴۰۵/۰۶/۱۳) — مرحلهٔ ۲: `correlationId` واقعی

**چه بود:** ستون `correlationId` و ایندکسش وجود داشت، ولی هر نُه نقطه‌ای که پرش می‌کرد در لحظهٔ نوشتن `crypto.randomUUID()` صدا می‌زد. یعنی درخواستی که سه رکورد می‌نوشت، سه زنجیرهٔ تک‌نفره می‌ساخت. زنجیرهٔ «ارزیابی ریسک ذخیره شد ← امتیاز بازمحاسبه شد ← گرید عوض شد» در داده اصلاً وجود نداشت، پس هیچ رابطی نمی‌توانست نشانش دهد.

**چه شد (`src/server/http/requestContext.ts`):**
- شناسه یک‌بار در ابتدای هر درخواست ساخته می‌شود و در `AsyncLocalStorage` می‌نشیند. `createAuditRecord` آن را از همان‌جا برمی‌دارد، پس **هیچ‌کدام از ۴۷ نقطهٔ نوشتن دست نخورد**. علت انتخاب async context به‌جای پارامتر: نوشتن‌ها در عمق repositoryها اتفاق می‌افتد که کاری با HTTP ندارند، و چند تایشان عمداً `await` نمی‌شوند.
- ۱۹ فراخوانی `correlationId: crypto.randomUUID()` حذف شد.
- شناسه در هدر `X-Request-Id` به کلاینت برمی‌گردد. کلاینت هم می‌تواند شناسهٔ خودش را بفرستد، ولی فقط اگر شکل شناسه داشته باشد (`[A-Za-z0-9_-]{8,64}`) — این مقدار در رکورد ممیزی می‌نشیند و متن آزاد نباید در آن بنشیند.
- `GET /api/audit-logs/:id/related` بقیهٔ زنجیره را قدیمی‌به‌جدید برمی‌گرداند؛ خودِ رکورد را برنمی‌گرداند و رکورد بدون زنجیره **هیچ‌چیز** برمی‌گرداند نه «همهٔ رکوردهای بی‌زنجیره».

**تست:** `tests/api.auditCorrelation.test.ts` (۸ مورد). کل سوییت **۲۶۸** مورد سبز.

**تغییر نکرد:** schema (ستون از قبل بود، مهاجرت لازم نشد)، متن هیچ رکورد ممیزی، و رابط کاربری.

## ردیابی تغییرات (۱۴۰۵/۰۶/۱۳) — مرحلهٔ ۱ از بازطراحی: مسیر خواندن

**زمینه:** بازطراحی «مرکز ردیابی و ممیزی» در پنج مرحله تأیید شد. مرحلهٔ ۱ فقط کارایی و درستی مسیر خواندن است؛ ظاهر صفحه در این مرحله عوض نشده.

**چه بود:** `/api/audit-logs/stats` و `/api/audit-logs/filters` هرکدام `getAuditLogs({}, 1, 10000)` می‌زدند — ده هزار ردیف کامل با هر دو JSON `before`/`after` در حافظه — تا سه عدد و دو فهرست یکتا بسازند. جست‌وجو مسیر جدایی داشت که سقف ثابت ۱۰۰ ردیف می‌گرفت و صفحه‌بندی را در حافظه انجام می‌داد، پس `total` اندازهٔ آن سقف بود نه تعداد منطبق‌ها.

**چه شد:**
- `AuditService.whereForPrisma` تنها جای ساخت شرط خواندن است. قبلاً دو نسخه بود و از هم فاصله گرفته بودند: فقط یکی بازهٔ تاریخ را اعمال می‌کرد، پس جست‌وجوی متنی داخل بازهٔ تاریخ بی‌صدا بازه را به «همه» باز می‌کرد.
- `searchAuditLogs` حذف شد؛ متن یک پارامتر `getAuditLogs` است و مثل هر خواندن دیگری در SQL صفحه می‌خورد.
- `getStats()` با `count` و `groupBy` و `getFilterOptions()` با `groupBy` روی ایندکس.
- **خروجی اکسل ناقص بود و نمی‌گفت:** با `limit=10000` درخواست می‌داد و سرور به ۲۰۰ کلمپ می‌کرد، پس فایل حداکثر ۲۰۰ ردیف داشت و ادعا می‌کرد همهٔ منطبق‌هاست. حالا با صفحه‌های ۲۰۰تایی تا سقف ۱۰٬۰۰۰ پیمایش می‌کند و اگر به سقف خورد، بالای صفحه می‌گوید چند رکورد از چند رکورد گرفته شده.
- فیلتر «دستهٔ سریع» حذف شد: نه هیچ کنترلی در UI ستش می‌کرد، نه مسیر PostgreSQL آن را می‌خواند.

**تست:** `tests/api.auditRead.test.ts` (۸ مورد) — صفحه‌بندی SQL، `total` واقعی جست‌وجو، هم‌زمانی فیلتر و جست‌وجو، بازهٔ تاریخ در جست‌وجو، شمارنده‌ها، فهرست یکتا، و گارد ۴۰۱. کل سوییت **۲۶۰** مورد سبز.

**تغییر نکرد:** شکل جدول `audit_log`، `AuditService.createAuditRecord` و هیچ‌کدام از ۴۷ نقطهٔ نوشتن، مجوز `audit.read`، و ظاهر صفحه.

## پنل مقایسهٔ سورس‌های یک ماده (۱۴۰۵/۰۶/۱۲) — بازطراحی

**اندازه‌گیری اولیه:** پنل برای هفت سورس ۱۱۱۵ پیکسل ارتفاع داشت و همان هفت سورس را دو بار فهرست می‌کرد (نمودار میله‌ای + جدول QC). چهار سورس بدون امتیاز نیمی از نمودار و چهار ردیف خط‌تیره در جدول را می‌گرفتند و ستون تحلیل زیر فرمول ~۱۵۰px خالی داشت.

**پیشنهاد تأییدشده و اعمال‌شده:** یک فهرست رتبه‌بندی‌شده؛ هر سورس یک ردیف با امتیاز موتور، میله، امتیاز پایه، گرید، کامل بودن ارزیابی (n/۴)، ضریب ریسک با سطحش، و شمارش QC با ضریب آزمایشگاه. جدول جداگانهٔ QC حذف شد چون داده‌اش به ردیف آمد. سورس‌های بدون امتیاز در یک `details` تاشو با نام و گرید و وضعیت ریسک جمع شدند. دو راهنمای جدا («چطور محاسبه می‌شود؟» و «فرمول محاسبه») در یک راهنما ادغام شد. کارت تصمیم و تاریخ آخرین ارزیابی پایین پنل نشستند.

**نتیجهٔ اندازه‌گیری‌شده:** ارتفاع ۱۱۱۵ → **۶۴۰** پیکسل با همان داده؛ باز کردن فهرست تاشو ۱۰۱۶. موبایل (۴۲۰px) ۹۵۵.

**اطلاعاتی که کامل‌تر شد:** ضریب و سطح ریسک قبلاً فقط برای گزینهٔ پیشنهادی نوشته می‌شد؛ حالا روی هر ردیف است.

**دو باگ که سر راه رفع شد:**
- **تاریخ خام:** «آخرین ارزیابی ثبت‌شده» روی صفحهٔ زنده `1405-06-12T09:54:00.000Z` چاپ می‌کرد. سپردن این مقدار به `Date` بدتر بود و ۷۸۴ می‌داد (۱۴۰۵ به‌عنوان سال میلادی خوانده می‌شد). `formatGroupDate` حالا از روی سال تصمیم می‌گیرد: سال ۱۳۰۰ تا ۱۴۹۹ یعنی شمسیِ نوشته‌شده با خط تیره و فقط جداکننده‌اش عوض می‌شود؛ سال میلادی واقعی تبدیل می‌شود. چهار تست در `tests/comparisonPanel.test.ts`.
- **مقیاس میله:** عنوان می‌گفت «از ۱۰۰» ولی ضرایب امتیاز را تا ۱۱۰ می‌برند (۱۰۴٫۰ در دادهٔ نمونه). مخرج میله حالا بیشینهٔ ۱۰۰ و بالاترین امتیاز گروه است و زیرعنوان فرمول را می‌گوید.

**باقی‌مانده (خارج از این تغییر):** سورس بدون هیچ امتیازی «Grade B» نشان می‌دهد؛ ریشه‌اش در دادهٔ گرید است نه این پنل.

## مخزن شرکای تجاری (۱۴۰۵/۰۶/۱۲) — ستون ارزیابی و جای قاعدهٔ اتصال

**خواستهٔ کاربر:** عنوان «نتیجهٔ ارزیابی SOP» به «نتیجهٔ ارزیابی» تغییر کند، خانه‌اش فقط گرید را بگوید، و برچسب «قابل اتصال به سورس نیست» جای دیگری برود. از سه گزینهٔ پیشنهادی، گزینهٔ الف (ستون مستقل) تأیید شد.

- **خانهٔ ارزیابی فقط چیپ گرید دارد.** متن فارسی گرید و امتیاز ۱۰۰تایی به tooltip رفتند و در پنجرهٔ جزئیات همان‌جا که بودند ماندند. برای تولیدکننده، جملهٔ تکراری «- (فقط مخصوص Supplier)» جایش را به یک خط تیره با tooltip داد.
- **ستون تازهٔ «امکان اتصال به سورس»** با دو حالت «مجاز» و «غیرمجاز»، که مقدارش را از `canSupplySources` می‌خواند — همان تابعی که سرور با آن ۴۲۲ می‌دهد، پس جدول و endpoint نمی‌توانند واگرا شوند. tooltip دلیل دقیق را می‌گوید. ستون قابل مرتب‌سازی است و یک فیلتر تازه («فقط قابل اتصال / فقط غیرمجاز») هم گرفت.
- **خروجی اکسل** همان ستون را گرفت تا گزارش چاپی چیزی را وعده ندهد که فرم رد می‌کند.
- **تست:** `tests/partnerSourceLink.test.ts` — فقط گرید A مجاز است، لیست سیاه و ارزیابی‌نشده مجاز نیستند، تولیدکننده مشمول قاعده نیست، و ستون اکسل همان چیزی را می‌گوید که صفحه.
- **راستی‌آزمایی زنده:** جدول با هفت شریک واقعی؛ سرستون‌ها و مقادیر مطابق انتظار و نام شرکت‌ها دیگر فشرده نمی‌شوند.

## پنجرهٔ جزئیات ردیابی تغییرات (۱۴۰۵/۰۶/۱۲) — فقط «چه بود، چه شد»

**گزارش کاربر (با تصویر):** پنجره برای «ثبت سابقهٔ فعالیت» دو ردیف نشان می‌داد — `activityLogs ۶ → ۷ مورد` و `activityLogCount ۶ → ۷` — با نام فنی، تکراری، و بدون اینکه بگوید آن یک مورد تازه چه بوده. دو ردیف «آدرس IP» و «دستگاه» هم خط تیره بودند. دامنهٔ تأییدشده فقط همین پنجره است؛ جدول اصلی و ستون‌ها دست‌نخورده ماندند.

**پنج تغییر:**
- **فیلد بی‌برچسب چاپ نمی‌شود؛ شمرده می‌شود.** زیر فهرست می‌نویسد «n فیلد فنی دیگر … در داده خام JSON» تا چیزی بی‌صدا حذف نشود.
- **شمارنده‌های مشتق** (`activityLogCount` و هم‌خانواده‌هایش) به `META_KEYS` رفتند، چون فقط طول آرایهٔ ردیف بالایی‌اند.
- **آرایه‌ها با محتوا مقایسه می‌شوند** (`diffCollection`): «۱ مورد افزوده شد: ثبت نتیجهٔ آزمون QC-4471». حداکثر دو مورد نام برده می‌شود و بقیه شمرده. نام مجوزها از `PERMISSION_LABELS` خوانده می‌شود، پس «دسترسی‌ها» به‌جای `score.finance` می‌نویسد «امتیازدهی مالی و حسابداری».
- **ردیف بی‌مقدار رندر نمی‌شود** (`hasRecordedValue`). مقدار جعلیِ پیش‌فرض `127.0.0.1` و `Chrome / Windows` هم حذف شد: رکورد قدیمی نباید آدرس و مرورگری نشان دهد که هرگز وجود نداشته.
- **سرصفحه رکورد را نام می‌برد** («ایجاد · شرکت ۱») با کاربر و زمان؛ شناسهٔ رکورد به بخش فنی رفت. **بلوک «شرح فعالیت» حذف شد** (تأیید کاربر) چون همان جملهٔ دلیل را با عدد تکرار می‌کرد؛ متن توصیف همچنان در دیتابیس و در جست‌وجوی فهرست هست.

**تست:** `tests/auditDiff.test.ts` با ۹ تست: افزودن و حذف از آرایه، بی‌اثر بودن جابه‌جایی، کنار گذاشته شدن شمارنده، شمارش فیلد بی‌برچسب، ردیف اسکالر عادی، کنار ماندن متادیتای رویداد، تشخیص مقدار واقعی از خط تیره، و ترجمهٔ نام مجوز.

**راستی‌آزمایی زنده:** همان رویداد سابقهٔ فعالیت ساخته شد و پنجره فقط یک ردیف نشان داد: «سابقهٔ فعالیت · ۱ مورد افزوده شد · ثبت نتیجهٔ آزمون QC-4471». رویداد ورود و رویداد تغییر دسترسی هم بررسی شدند.

## ماژول مدیریت کاربران (۱۴۰۵/۰۶/۱۲) — دسترسیِ ذخیره‌شده باید همان بماند

**گزارش کاربر:** تغییر سطح دسترسی «واحد مالی» ذخیره نمی‌شد؛ با مراجعهٔ دوباره تنظیمات همان قبلی بود.

**علت واقعی:** خواندن (`vendor.read` و…) دیرتر از فهرست‌های استثنا به مجوزها اضافه شد، پس `effectivePermissions` هر فهرستِ بدون `read` را «قدیمی» فرض می‌کرد و هر چهار خواندن را هنگام خواندن به آن برمی‌گرداند. طبیعی‌ترین محدودسازی برای مالی — خاموش‌کردن همهٔ ماژول‌ها و نگه‌داشتن فقط تیک امتیازدهی — دقیقاً فهرستی بدون `read` می‌سازد. ذخیره انجام می‌شد ولی خوانده‌شده همان قبلی بود. ضمناً `partner.files` هم اضافه می‌شد که اصلاً در الگوی مالی نیست.

**اصلاح:** مهاجرت `20260903120000_expand_legacy_permission_reads` همان ردیف‌های قدیمی را یک‌بار در دیتابیس کامل می‌کند — دقیقاً همان چهار نامی که حدسِ قبلی اضافه می‌کرد، پس دسترسی هیچ حسابی عوض نمی‌شود — و حدس از کد حذف شد. از این پس فهرست ذخیره‌شده عیناً اجرا می‌شود.

**تلهٔ دوم:** فهرست خالی یعنی «از الگوی سمت پیروی کن»، پس برداشتن همهٔ تیک‌ها هم بی‌اثر به نظر می‌رسید. دیالوگ حالا در این حالت هشدار می‌دهد و می‌گوید برای قطع دسترسی باید حساب را غیرفعال کرد.

**تست:** `tests/api.users.test.ts` با ۱۶ تست کل ماژول را از راه HTTP می‌بندد: ذخیره و بازخوانی فهرست، اجرای واقعی محدودیت (۴۰۳ روی `/api/vendors`)، معنی فهرست خالی، باز شدن نام‌های بازنشسته، خودِ SQL مهاجرت روی هر سه حالت، پاک شدن استثناها با تغییر سمت، حذف نام ناشناس، محافظ آخرین مدیر، ادمین‌ویژه بودن ماژول، ثبت در audit، ساخت حساب تازه، قفل‌نشدن ادمین روی خودش، رمز موقت، و غیرفعال/فعال کردن حساب. چهار تست قدیمیِ `permissions.test.ts` که حدس قبلی را رمزگذاری کرده بودند به قرارداد تازه به‌روز شدند.

**راستی‌آزمایی زنده:** در مرورگر سطح دسترسی مالی به «فقط امتیازدهی» تغییر داده شد؛ `PUT` با همان فهرست رفت، ردیف دیتابیس `["score.finance"]` شد و با باز کردن دوبارهٔ دیالوگ فقط همان یک تیک روشن بود.

**ضمناً:** تست «a refused write is itself recorded» گاهی قرمز می‌شد چون رکورد audit بدون await ارسال می‌شود؛ حالا به‌جای مسابقه با آن، منتظرش می‌ماند.

## خروجی اکسل: نقش ماده (۱۴۰۵/۰۶/۱۲)

ستون «نقش ماده» مقدار ذخیره‌شده را چاپ می‌کرد، پس برای ماده‌ای که فرم آن را «Reagent» نشان می‌دهد، در شیت `Reagent / Reactant` می‌آمد (همین‌طور `Packaging Item` در برابر «Packaging»). آن مقدارها عمداً تغییرناپذیرند — هر نام استانداردِ تولیدشده رویشان بنا شده — پس خروجی حالا از `getMaterialRole(...).labelEn` می‌خواند، همان چیزی که همهٔ صفحه‌ها نشان می‌دهند. املای قدیمی (`packaging`, `reagent`) هم از راه جدول ALIAS به برچسب درست می‌رسد، و نقشِ ثبت‌نشده «ثبت‌نشده» می‌ماند نه اینکه به API تبدیل شود (پیش‌فرضِ همان تابع). دو تست؛ هر دو بدون اصلاح قرمز می‌شوند.

## کار هم‌زمان چند کاربر (۱۴۰۵/۰۶/۱۲) — سه بخش، به ترتیب خواستهٔ کاربر

### ۱. دیدن تغییر دیگران بدون رفرش
`GET /api/vendors/changes?since=` اضافه شد: id، مهر زمانی، و تعداد کل — بدون بدنهٔ سورس. کلاینت هر ۳۰ ثانیه و نیز هنگام برگشتن تب می‌پرسد. اگر فرمی dirty باشد هیچ refetchای انجام نمی‌شود و فقط نوار «n تغییر تازه…» با دکمهٔ «نمایش تغییرات» می‌آید؛ وگرنه داده بی‌صدا جایگزین می‌شود و رکورد باز روی صفحه در جا تازه می‌شود. نوشته‌های خود نشست با id فیلتر می‌شوند و حذف از روی `total` فهمیده می‌شود.
**راستی‌آزمایی زنده:** دو کلاینت هم‌زمان؛ صفحهٔ دوم تغییر اولی را در ۲۵ ثانیه گرفت با تنها یک ورودی ناوبری (یعنی بدون reload). با فرم dirty، مقدار تایپ‌شده سر جایش ماند، نوار آمد، و تازه‌سازی فقط با پذیرش نوار انجام شد. چهار تست API.

### ۲. گارد نسخهٔ واقعی (۴۰۹)
کلاینت همان `updatedAt`ی را که خوانده در `expectedUpdatedAt` پس می‌فرستد و هفت مسیر سورس قبل از هر کاری با ۴۰۹ رد می‌کنند. ذخیرهٔ چندمرحله‌ای مهر زمانی را از پاسخ هر مرحله به بعدی می‌دهد و نسخهٔ روی صفحه آخرین مهر را می‌گیرد، پس نوشتهٔ خودِ نشست هیچ‌وقت کهنه دیده نمی‌شود. کالر بدون ادعا دقیقاً رفتار قبلی را دارد. شش تست؛ دو موردِ اصلی بدون گارد قرمز می‌شوند (آزموده شد).

### ۳. تعمیم قفل و ۴۰۹ به شرکا، مواد و انتخاب سورس
قفل از مخزن سورس به `src/server/http/recordLock.ts` منتقل شد و حالا scope دارد. `serializeWrites` روی مسیرهای `:id` شرکا و مواد نشست؛ انتخاب سورس چون کلید ترکیبی دارد قفل را دستی می‌گیرد. `materials` ستون `updated_at` گرفت (مهاجرت `20260903100000_material_updated_at`، افزایشی). فرم شریک و فرم ماده و دیالوگ انتخاب سورس ادعای نسخه می‌فرستند و روی ۴۰۹ فهرست را دوباره می‌خوانند. پنج تست.
**راستی‌آزمایی زنده:** دو ذخیرهٔ پشت‌سرهم در یک نشست، هم برای شریک و هم برای ماده، هر دو با ادعا و هر دو ۲۰۰ — یعنی تعارض کاذب رخ نمی‌دهد.

**باقی‌مانده:** اولویت‌دهی بین کاربران وجود ندارد و FIFO است؛ اگر لازم شد، تصمیم جداگانه‌ای می‌خواهد.

## دو ایراد گزارش‌شدهٔ کاربر (۱۴۰۵/۰۶/۱۲)

### ۱. خوشهٔ چپِ هدر از لبهٔ صفحه بیرون می‌زد
اندازه‌گیری (نه چشم): در بازهٔ ۷۶۸ تا ~۹۵۰ پیکسل، `x` خوشهٔ سمت چپ هدر منفی بود (در ۸۰۰px برابر `-175`) و کل بلوک — شامل جعبهٔ حساب کاربری — بریده و غیرقابل‌دسترس می‌شد. عرض سایدبار عوض نشده بود (همان `272px`): از `md` به بالا سایدبار ثابت است و برای هدر ~۴۶۰ تا ۶۵۰px می‌ماند، در حالی که خودِ خوشه ۷۱۱px نیاز داشت و هیچ‌کدام از آیتم‌هایش کوچک‌شدنی نیست. بزرگ‌ترین قطعه، چیپ ساعت/تاریخ با ۲۷۶px بود.

اصلاح: چیپ ساعت از `sm` به `lg` رفت. حالا در هر عرضی از ۷۶۸ تا ۱۹۲۰ `x = 20` و هیچ عنصری بیرون از قاب نیست (اندازه‌گیری‌شده در ۷۶۸/۸۰۰/۹۰۰/۱۰۲۴/۱۲۸۰/۱۹۲۰).

### ۲. کارت «خلاصه ارزیابی کیفی Supplier» در دارک‌مود روشن می‌شد
کارت با `bg-foreground text-background` ساخته شده بود؛ این توکن‌ها با تم برعکس می‌شوند، پس در دارک‌مود صفحهٔ تیره یک کارت سفید داشت. اول با مقادیر ثابت اسلیت درست شد، ولی آن‌وقت در تم روشن هم تیره می‌ماند و کاربر همین را گزارش کرد. نسخهٔ نهایی توکن است (قاعدهٔ ۷): `bg-muted/50 text-foreground` با کاشی‌های `bg-card` و مرز `border-border`. اندازه‌گیری‌شده — لایت: پس‌زمینهٔ `oklab(0.984 …)` با متن `rgb(15 23 42)` و کاشی سفید؛ دارک: پس‌زمینهٔ `oklab(0.279 …)` با متن `rgb(248 250 252)` و کاشی `rgb(30 41 59)`.

## بلوک برند بالای سایدبار (۱۴۰۵/۰۶/۱۱)

**گزارش کاربر:** عنوان و لوگوی بالای سایدبار کمی ریز است و باید وسط‌چین شود.

**علت بی‌قرینگی:** بلوک یک ردیف `justify-between` بود که لوگو و نام در یک سرش می‌نشستند و دو کنترل (بستن در موبایل، جمع‌کردن در دسکتاپ) در سر دیگرش. یعنی برند هیچ‌وقت وسط نبود و عرضش را با دکمه‌ها تقسیم می‌کرد.

**تغییر:** لوگو **بالای** نام، هر دو وسط‌چین، و کنترل‌ها با `absolute` به گوشه رفتند تا برند تمام عرض را داشته باشد. اندازه‌ها: لوگو ۴۰px → **۴۸px**، عنوان ۱۵px → **۱۶px** (`text-base`)، «VLSE» ۱۲px → **۱۳٫۵px** با `tracking-widest`.

**اندازه‌گیری زنده:** مرکز لوگو ۱۴۶۵ و مرکز عنوان ۱۴۶۵ در برابر مرکز بلوک ۱۴۶۵ — هر دو دقیقاً وسط. در حالت جمع‌شده لوگو ۳۲px عرض دارد در ریل ۷۵px، پس سرریز نمی‌کند (`overflow: false`).

**تأیید:** `tsc` · ESLint ۰ خطا · build · ۲۰۲ تست · اسکرین‌شات هر دو حالت باز و جمع‌شده. ۰ خطای صفحه.


## کارت امتیاز دپارتمان (۱۴۰۵/۰۶/۱۱) — عدد از خط اول جا نماند

**گزارش کاربر:** در صفحهٔ سورس، پارامتر «تطابق کالا با مشخصات فنی درج شده در پکینگ لیست» دوخطی می‌شود و عدد «۵ / ۵» می‌افتد خط پایین.

**علت:** ردیف در `src/components/ScoreBar.tsx` یک `flex justify-between items-center` است. وقتی برچسب دوخطی می‌شود، `items-center` عدد را وسط ارتفاع ردیف می‌نشاند، یعنی کنار خط دومِ برچسب — و عدد بی‌صاحب به‌نظر می‌رسد. برچسب در کارتی به عرض یک‌چهارم صفحه ۴۶ کاراکتر است و یک‌خطی نمی‌شود.

**اندازه‌گیری قبل و بعد** (فاصلهٔ عمودی مرکز عدد از مرکز **خط اولِ** برچسب، روی هر ۱۱ ردیف کارت‌های چهار دپارتمان):

| | ردیف‌های یک‌خطی | ردیف دوخطیِ پکینگ لیست |
|---|---|---|
| قبل | ۰px | **‎+۱۲px** |
| بعد | ۱px | **۱px** |

**رفع:** `items-center` به `items-start` رفت، عدد `shrink-0 whitespace-nowrap` گرفت تا نه فشرده شود نه بشکند، و برچسب و عدد هر دو `leading-snug` گرفتند تا اختلاف خط‌کرسی صفر شود. تنها فایل تغییرکرده `ScoreBar.tsx` است، پس هر جای دیگری هم که نوار امتیاز دارد همین رفتار را می‌گیرد.

**تأیید:** `tsc` · ESLint ۰ خطا · build · ۲۰۲ تست · اندازه‌گیری زندهٔ هر ۱۱ ردیف و اسکرین‌شات کارت برنامه‌ریزی. ۰ خطای صفحه.

**نکته:** خودِ برچسب همچنان دوخطی است و این اجتناب‌ناپذیر است مگر متنش کوتاه شود؛ فرم چاپی برای همین معیار متن کوتاه‌تری دارد («تطابق کالا با مشخصات فنی پکینگ لیست»). یکدست‌کردن این دو، تغییر واژگان است نه چیدمان، و بدون درخواست انجام نشد.


## بررسی یکپارچه تأمین‌کنندگان (۱۴۰۵/۰۶/۱۱) — هدر یکدست و آیکن نقش

**۱. زیرعنوان آرشیو:** «(Vendor Archive Data)» از داخل زیرعنوان به `eyebrow` بالای عنوان رفت (همان شکلی که مواد اولیه و شرکای تجاری دارند). زیرعنوان کوتاه‌تر شد و دیگر در عرض‌های میانی دو خطی نمی‌شود.

**۲. هدر «بررسی یکپارچه»:** همان ایراد آرشیو را داشت — عنوان با `order-1 md:order-2` در ظرف RTL به چپ می‌افتاد — و علاوه بر آن یک `h2` دست‌ساز بود با رنگ سخت‌کدشدهٔ `text-[#6E6E73]`. حالا `PageTitle` با `eyebrow="Supplier Core"` است، عنوان اول در سورس. اندازه‌گیری: **یک `h1`، صفر `h2`، ۲۲px**، مرکز عنوان `x=1065` و چیپ وضعیت در `x=664` یعنی عنوان راست و اقدام چپ.
   - عنوان از «بررسی یکپارچه تامین‌کنندگان (Supplier Core)» به «بررسی یکپارچه تامین‌کنندگان» کوتاه شد چون همان «Supplier Core» حالا eyebrow است.

**۳. آیکن نقش شرکت:** کارت‌ها و بنر پروندهٔ این ماژول برای همه یک آیکن عمومی `Building` می‌گذاشتند، چه تولیدکننده چه فروشنده. حالا نقش از رکورد شریک تجاری خوانده می‌شود (همان `resolveVendorPartner` که هدر جزئیات استفاده می‌کند، پس کارت و هدر نمی‌توانند با هم اختلاف داشته باشند):

| نقش | آیکن | tooltip |
|---|---|---|
| تولیدکننده | `Factory` | «تولیدکننده» |
| فروشنده | `Handshake` | «فروشنده» |
| هر دو (از راه سورس‌های مختلف) | `Building` | «تولیدکننده و فروشنده» |
| بدون رکورد شریک | `Building` | «نقش ثبت‌نشده» |

جفت آیکن همان است که مخزن شرکای تجاری از قبل دارد. **نقش حدس زده نمی‌شود:** شرکتی که رکورد شریک ندارد آیکن خنثی می‌گیرد، چون طبق قاعدهٔ ۴ نقش یک واقعیت ثبت‌شده است نه برچسبی که خالی نماند.

**تأیید:** `tsc` · ESLint ۰ خطا · build · ۲۰۲ تست · اندازه‌گیری زنده با دو رکورد آزمایشی (یک سورس به تولیدکننده و یکی به فروشنده وصل شد): کارت اول `lucide-factory`، دومی `lucide-handshake`، پنج‌تای بی‌رکورد `lucide-building`؛ داخل پروندهٔ همان تولیدکننده هم بنر `lucide-factory` شد. رکوردهای آزمایشی بعدش پاک شدند. ۰ خطای صفحه.

**پیگیری — پاک‌سازی هدر همین ماژول:**
- بنر «PROACTIVE ACTIVE DISCOVERY MODULE» حذف شد. چیزی را نام نمی‌برد که کاربر بتواند روی آن اقدام کند، و در نمای فهرست تنها محتوای ستون اقدام بود؛ با حذفش، آن ستون فقط وقتی رندر می‌شود که واقعاً اقدامی دارد (دکمهٔ بازگشت در نمای پرونده)، نه یک جعبهٔ خالی.
- کپشن لاتین «Supplier Core» جای خود را به **«Supplier Monitoring & Audit»** داد، با آیکن `Activity` که با همان کپشن می‌خواند. عنوان فارسی «بررسی یکپارچه تامین‌کنندگان» دست‌نخورده ماند چون برچسب سایدبار و breadcrumb همین است و عوض‌کردنش این سه را از هم جدا می‌کرد.

**تأیید (این دور):** اندازه‌گیری زنده: هیچ‌جای صفحه رشتهٔ `PROACTIVE ACTIVE DISCOVERY` و `Supplier Core` نیست، یک `h1` و صفر `h2`، و در نمای پرونده دکمهٔ «بازگشت به مانیتور» سر جایش است. `tsc` · ESLint ۰ خطا · build · ۲۰۲ تست · ۰ خطای صفحه.


## آرشیو کل تأمین‌کنندگان (۱۴۰۵/۰۶/۱۱) — جای عنوان و دکمه‌ها

**گزارش کاربر:** عنوان صفحه باید مثل بقیهٔ ماژول‌ها سمت راست باشد و دکمه‌های خروجی سمت چپ.

**علت:** در `views/ArchiveView.tsx` خوشهٔ خروجی‌ها اول در DOM بود با `order-2 md:order-1` و بلوک عنوان دوم با `order-1 md:order-2`. ظرف RTL است، پس `order-1` خروجی‌ها را سمت **راست** می‌نشاند و عنوان به چپ می‌رفت. کامنت‌های خود فایل «Left side: Export Options» و «Right side: Title» می‌نوشتند — یعنی نیت درست بود و کلاس‌های `order` در RTL برعکسش کرده بودند.

**رفع:** عنوان اول در DOM، خروجی‌ها دوم، و هر چهار کلاس `order` حذف شد. همان الگویی که `MaterialRepositoryView` دارد: در RTL فرزند اول سمت راست می‌افتد و وقتی ردیف در عرض کم ستونی می‌شود، همان فرزند بالا می‌ماند.

**اندازه‌گیری مختصات مرکز، قبل و بعد:**

| عرض | قبل (عنوان / دکمه‌ها) | بعد | مواد اولیه (مرجع) |
|---|---|---|---|
| ۱۹۲۰ | ۲۶۴ / ۱۱۳۰ ✗ | **۱۳۸۴ / ۵۱۸** ✓ | ۱۳۶۸ / ۴۴۳ |
| ۱۲۸۰ | ۱۷۶ / ۶۵۶ ✗ | **۸۳۲ / ۳۵۲** ✓ | ۸۷۴ / ۴۱۹ |
| ۷۰۰ و ۴۸۰ (ستونی) | — | عنوان بالا (y ۱۰۰ در برابر ۱۷۷) ✓ | همان الگو |

**تأیید:** `tsc` · ESLint ۰ خطا · build · ۲۰۲ تست · اسکرین‌شات قبل/بعد در سه عرض · آزمون کارکردی: منوی «خروجی تک‌دسته‌ای» باز و با Esc بسته شد، **خروجی اکسل نمای فعلی واقعاً دانلود شد**، و نمای چاپ فهرست باز شد. ۰ خطای صفحه.

**پیگیری — عنوان به `PageTitle` رفت:** هدر آرشیو آخرین جایی بود که یک `h2` دست‌ساز نقش عنوان صفحه را بازی می‌کرد، در حالی که چهار ماژول دیگر از `ui/page-title.tsx` استفاده می‌کنند؛ یعنی ساختار سرفصلِ سند برای صفحه‌خوان روی این صفحه با بقیه فرق داشت. حالا `PageTitle` با `icon={Archive}` است، مثل کاربران و ردیابی تغییرات. اندازه‌گیری در مرورگر: آرشیو، مواد اولیه و کاربران هر سه **یک `h1` و صفر `h2`** دارند و عنوانشان دقیقاً `22px / 900` است. متن عنوان و زیرعنوان دست‌نخورده.
   - تغییر بصری: عنوان از `text-2xl` (۲۴px) به توکن `text-xl` (۲۲px)، زیرعنوان از `text-sm` به `text-xs`، و آیکن از یک گلیف خاکستری کنار متن به کاشی رنگ `primary`.
   - در عرض‌های میانی، زیرعنوان آرشیو دو خطی می‌شود چون نوار خروجی‌ها پهن است؛ بریده نمی‌شود و همان رفتار هر صفحهٔ دیگری با نوار ابزار پهن است.


## فرم خروجی PDF ارزیابی (۱۴۰۵/۰۶/۱۱) — سه ایراد گزارش‌شدهٔ کاربر

`PrintableEvaluationForm` در `src/components/PrintableForms.tsx`. اندازه‌گیری در عرض واقعی A4 (۷۹۴px با `emulateMedia('print')`) انجام شد، نه در پیش‌نمایش پهن — در عرض پهن هیچ عنوانی نمی‌شکند و ایراد اصلاً دیده نمی‌شود.

| مورد | قبل | بعد |
|---|---|---|
| چینش ستون میانی هدر | `text-align: right` | `center` |
| اندازهٔ نام پارامترها | سه اندازه: ۱۴ / ۱۲ / ۱۱px | یک اندازه: ۱۲px |
| عنوان‌های دوخطی | ۲ مورد | ۰ |
| عنوان تأیید واحد برنامه‌ریزی | «تأیید مدیر برنامه» (ناقص) | «تأیید مدیر برنامه‌ریزی» |

**۱. هدر:** ستون میانی `text-center` نداشت و از ظرفِ `text-right` ارث می‌برد. **فرم نمونه در همان فایل این کلاس را دارد** (خط ۳۰۲)، یعنی جاافتادگی بود نه تصمیم.

**۲. پارامترها:** دو مقدار تک‌موردی (`text-[11px]` روی «ارائه مستندات درخواستی» و `text-xs` روی «تطابق کالا با مشخصات فنی پکینگ لیست») حذف شد و هر ۱۵ سلول نام پارامتر یک اندازه گرفتند. اندازه به‌تنهایی کافی نبود: ستون نام در A4 فقط ۱۹۴px بود و «سابقه همکاری و تعداد دفعات خرید» و «تطابق کالا…» می‌شکستند. ستون نام به ۴۲٪ پهن شد (۱۹۴px → ۲۴۴px) و ستون‌های عددی که فقط عدد دارند باریک‌تر شدند.

**۳. عنوان تأیید:** کلمه وسط راه بریده شده بود؛ کامل شد، هم‌شکل سه واحد دیگر. املای «تأیید» با همزه مطابق همان سه‌تا نگه داشته شد.

**تأیید:** `tsc` · ESLint ۰ خطا · build · ۲۰۲ تست · اندازه‌گیری زندهٔ هر ۱۵ سلول (اندازهٔ فونت و تعداد خط بصری با شمارش `top`های یکتای `Range.getClientRects()`، تا اجرای دوجهتهٔ «نتایج Deviation, OOS» اشتباهاً دوخطی شمرده نشود) · **PDF واقعی با `page.pdf({format:'A4'})` قبل و بعد: هر دو ۲ صفحه** — صفحه‌بندی فرم تغییر نکرد.


## بازطراحی رابط، فاز ۸ (۱۴۰۵/۰۶/۱۱) — مقیاس متن روی دسکتاپ

**گزارش کاربر:** با توجه به وسعت صفحهٔ دسکتاپ، فونت‌ها ریز به‌نظر می‌رسند. شمارش تأییدش کرد: از ~۱۰۸۰ عنصر اندازه‌دار بیرون از نمای چاپ، **۸۶۰ تا روی ۱۱ یا ۱۲ پیکسل بودند** (۴۴۳ مورد `text-2xs`، ۴۱۷ مورد `text-xs`) در ظرفی که تا ۱۶۰۰px پهن می‌شود. فاز ۱ فقط کف را درست کرده بود، نه خود مقیاس را.

| توکن | قبل | بعد |
|---|---|---|
| `--text-2xs` | ۱۱px | **۱۲px** |
| `--text-xs` | ۱۲px | **۱۳٫۵px** |
| `--text-sm` | ۱۴px | **۱۵px** |
| `--text-xl` | ۲۰px | **۲۲px** |

`base` و `lg` دست‌نخورده. line-height هم برای فارسی سخاوتمندتر شد. نردبان ۱۲ ← ۱۳٫۵ ← ۱۵ دیده می‌شود؛ ۱۱ ← ۱۲ عملاً یک اندازه بود.

**هیچ فایل کامپوننتی برای اندازه لمس نشد:** ابزارهای Tailwind v4 به `var(--text-…)` ارجاع می‌دهند (در CSS ساخته‌شده: `.text-xs{font-size:var(--text-xs)…}`)، پس یک ویرایش در `@theme` هر ۸۶۰ نقطه را جابه‌جا کرد. مقادیر با `rem` نوشته شدند تا تنظیم اندازهٔ متن مرورگر و سیستم‌عامل اثر کند — با px قبلی بی‌اثر بود.

**چاپ عمداً عقب نگه داشته شد.** `PrintableForms.tsx` از همین توکن‌ها استفاده می‌کند (۵۶ `text-xs` · ۱۷ `text-sm` · ۱۲ `text-lg`) روی برگهٔ A4 با صفحه‌بندی تأییدشده. یک بلوک `@media print` مقادیر قبلی را برمی‌گرداند. اندازه‌گیری زنده: روی صفحه ۱۲/۱۳٫۵/۱۵/۲۲ و با `emulateMedia('print')` همان ۱۱/۱۲/۱۴/۲۰.

**سرشماری سرریز و برش، قبل و بعد، روی ۹ صفحه × ۳ عرض:** فقط چهار نقطه بدتر شد و سه‌تایش رفع شد. یک مورد باقی ماند و **عمداً**:
- **رفع‌شده — لینک‌های breadcrumb:** «صفحه اصلی» و «خرید خارجی» در ۱۲۸۰ بریده می‌شدند. اول سقف عرض را بردم بالا و **کار نکرد** — اندازه‌گیری نشان داد محدودیت از `max-width` نیست، از فشرده‌شدن در ردیف flex است. با `shrink-0` حل شد؛ فشار روی آخرین تکه می‌ماند که نام موجودیت است و tooltip دارد (قاعدهٔ ۱۵).
- **رفع‌نشده — صفحهٔ شرکا در ۷۶۰px:** ۸ سرریز ۲ تا ۶ پیکسلی در برچسب‌های فیلتر. با stash کردن تغییر و اندازه‌گیری دوباره ثابت شد **ردیف فیلتر همان‌جا از قبل له شده** (ستون‌های ۳۸px، برچسب‌های سه‌خطی) و ۱px متن فقط لبه را رد کرد. این یک مشکل چیدمان واکنش‌گراست، نه تایپوگرافی؛ در همین کامیت بازش نکردم.

**تأیید:** `tsc` · ESLint ۰ خطا (۷۰ هشدار) · build · ۲۰۲ تست · ۳۶ اسکرین‌شات قبل/بعد (۹ صفحه × ۱۹۲۰/۱۲۸۰/۷۶۸ + تیره) · اندازه‌گیری واقعی `getComputedStyle` روی سلول جدول، برچسب، فیلد و عنوان صفحه · تست چاپ · ساخت و حذف یک کاربر واقعی. ۰ خطای صفحه.


## باگ: تغییر امتیاز دپارتمان ذخیره نمی‌شد (۱۴۰۵/۰۶/۱۱)

**گزارش کاربر:** در صفحهٔ سورس، هنگام تغییر امتیاز دپارتمان یک بنر قرمز `Validation failed` می‌آمد.

**زنجیرهٔ علت (زنده بازتولید شد):**
1. `EvaluationForm` هنگام ذخیره، `grade` و `status` را هم دوباره حساب می‌کند، پس در `handleUpdateVendor` مقدار `profileChanged` درست می‌شود و **قبل از** درخواست امتیازها، یک `PATCH /profile` در صف می‌نشیند.
2. آن payload کل پروفایل را می‌فرستد، از جمله `ircExpiryDate: null` برای هر سورسی که تاریخ انقضای IRC ندارد.
3. `vendorProfileSchema` این فیلد را `z.string().optional()` تعریف کرده بود. در Zod، `.optional()` یعنی «ممکن است **نباشد**» نه «ممکن است **null** باشد» — پس null رد می‌شد و کل درخواست **۴۰۰** می‌گرفت:
   `{"error":"Validation failed","details":[{"path":["ircExpiryDate"],"message":"expected string, received null"}]}`
4. صف نوشتن ترتیبی است و با اولین شکست متوقف می‌شود (قاعدهٔ ۱۲)، پس درخواست `/scores` **هرگز اجرا نشد**. کاربر بنر قرمز می‌دید و ویرایشش را از دست می‌داد.

**دامنه:** هر سورسی که `irc_expiry_date` ندارد؛ در دیتابیس محلی هر هفت سورس. یعنی حالت استثنایی نبود، حالت عادی بود.

**رفع:** در `src/utils/validation.ts` فیلدهایی که می‌توانند خالی باشند از `.optional()` به `.nullish()` رفتند (یعنی `.nullable().optional()`) — هم در `vendorProfileSchema` و هم `vendorContactSchema` که دقیقاً همین ایراد را داشت و چون صف زودتر می‌مرد هرگز دیده نشده بود.

**چرا سمت سرور و نه کلاینت:** ریشه همان‌جاست و برای هر کالر دیگری هم کار می‌کند؛ اگر فقط کلاینت nullها را حذف می‌کرد، سرور باز هم null را رد می‌کرد و کالر بعدی به همان دیوار می‌خورد.

**تأیید:** دو تست API تازه (`api.vendorWrites`) که **قبل از رفع قرمزند و بعدش سبز** — یکی خودِ null در پروفایل و یکی سناریوی واقعی «ذخیرهٔ امتیاز روی سورس بدون تاریخ IRC». به‌علاوهٔ تست زندهٔ مرورگر: قبل، `PATCH /profile → 400` و هیچ امتیازی در دیتابیس؛ بعد، هر چهار درخواست (`profile`, `contact`, `scores`, `logs`) **۲۰۰** و امتیازها در جدول `evaluations`. `tsc` · ESLint ۰ خطا · build · ۲۰۲ تست.


## بازطراحی رابط، فاز ۷ (۱۴۰۵/۰۶/۱۱) — آخرین تکهٔ تکراری جدول و سه ایراد کوچک

**تصمیم: `DataTable` واحد ساخته نشد.** سه تکهٔ مشترک جدول (سرستون، ردیف خالی، صفحه‌بندی) قبلاً استخراج شده بودند و تنها تکرار باقی‌مانده اسکلت بارگذاری بود. انتزاع کامل روی چهار جدولی که سلول‌هایشان گیت دسترسی و رندر شرطی دارند، diff بزرگ و ریسک regression واقعی می‌سازد در برابر سود بصری تقریباً صفر.

**۱. `ui/table-skeleton-rows.tsx`** — چهار کپی به یک کامپوننت رسید. **باگ دسترس‌پذیری که همین واگرایی لو داد:** سه جدول ردیف‌های ساختگی را `aria-hidden` می‌کردند و جدول کاربران نه، پس صفحه‌خوان پنج ردیف دادهٔ جعلی را مثل حساب واقعی می‌خواند. اندازه‌گیری زنده در مرورگر، قبل و بعد:

| جدول | قبل (پنهان/کل) | بعد |
|---|---|---|
| کاربران | ۰ / ۵ | ۵ / ۵ |
| ردیابی تغییرات | ۷ / ۷ | ۷ / ۷ |
| شرکای تجاری | — | ۵ / ۵ |
| مواد اولیه | — | ۵ / ۵ |

فرمول عرض ستون‌ها عمداً callback هر جدول ماند (یکی‌شان حتی به شمارهٔ ردیف وابسته است)؛ یکسان‌کردنش چهار صفحه را بی‌دلیل عوض می‌کرد. حاشیهٔ ردیف هم داخل کامپوننت نیست چون سه جدول `divide-y` روی `tbody` دارند و حاشیهٔ دوم می‌شد.

**۲. ردیف خطای جدول کاربران** روی همان `TableEmptyRow` نشست با `iconClassName="text-rose-400"` — پراپی که فاز ۴ ساخت و تا امروز مصرف نداشت.

**۳. ۷ `<textarea>` خام → `ui/textarea.tsx`** (که هیچ‌جا import نمی‌شد). سه رنگ حلقهٔ focus مختلف داشتند. `Textarea` حالا کلاس پایه‌اش را از `inputBaseClass` می‌گیرد و فقط قواعد ارتفاع را override می‌کند، پس فیلد و کادر توضیح یک focus مشترک دارند.

**۴. باگ اسکرول‌بار:** بلوک `::-webkit-scrollbar` داخل `@media (prefers-reduced-motion: reduce)` گیر افتاده بود، یعنی اسکرول‌بار باریک ۶px فقط به کسانی می‌رسید که کاهش انیمیشن را روشن کرده بودند. بیرون آمد. (تأیید روی CSS ساخته‌شده: قاعده حالا در سطح بالاست. مرورگر headless اسکرول‌بار overlay دارد و عرض ۰ گزارش می‌کند، پس این مورد **استاتیک** تأیید شد نه بصری.)

**۵. `components.json`** به `tailwind.config.js` اشاره می‌کرد که وجود ندارد (Tailwind v4 با `@theme` کار می‌کند)؛ به رشتهٔ خالی رسید.

**۶. شش پریمیتیو بی‌مصرف حذف شد:** `dialog` (چون هر لایه از `FormModal` می‌آید)، `dropdown-menu`، `select`، `separator`، `tabs`، `table`. هیچ‌کدام حتی یک import نداشتند. قاعدهٔ ۷ در `CLAUDE.md` که به `ui/select.tsx` ارجاع می‌داد بازنویسی شد.

**یک PASS تقلبی که خودم گرفتم:** آزمون اول «رکورد ذخیره شد» را سبز نشان داد، ولی وقتی دیتابیس را نگاه کردم `analysis_records` **خالی** بود. علتش این بود که سلکتور من متن مارکر را در **خودِ textareaِ هنوز باز** پیدا می‌کرد؛ ذخیره اصلاً نرفته بود چون فیلد اجباری «کد QC» را پر نکرده بودم. با پرکردن آن فیلد، رکورد واقعاً در دیتابیس نشست و بعد پاک شد. اگر فقط به سبز شدن تست تکیه می‌کردم، مهاجرت textarea را «تأییدشده» گزارش می‌کردم بدون اینکه یک نوشتن واقعی دیده باشم.

**تأیید:** `tsc` · ESLint ۰ خطا (۷۰ هشدار — دوتا کمتر، چون فایل‌های حذف‌شده هشدار داشتند) · build · ۲۰۰ تست · اسکرین‌شات **حالت بارگذاری** هر چهار جدول با تأخیر مصنوعی روی API · نوشتن واقعی در دیتابیس از راه textarea مهاجرت‌کرده. ۰ خطای صفحه.

**نکتهٔ جانبی (پیش‌از‌این موجود، رفع نشد):** پرچم بارگذاری مخزن مواد `isSyncing && materials.length === 0` است، یعنی به همگام‌سازی **سورس‌ها** گره خورده نه مواد؛ برای دیدن اسکلتش باید درخواست `vendors` هم کند شود. تغییرش منطق است نه ظاهر، پس در این فاز دست نخورد.


## بازطراحی رابط، فاز ۶ (۱۴۰۵/۰۶/۱۱) — رنگ ثابت به توکن

ممیزی «۸۰۴ ابزار رنگ غیرمعنایی» شمرده بود. بیرون از نمای چاپ، عدد واقعی ~۱۰۰ بود و **اکثرش هم قانونی**: پرده‌های پشت مودال (`bg-slate-900/50` که قاعدهٔ ۹ خودش تجویز کرده)، `bg-white/20` روی چیپ‌های رنگی اشباع، و پلاک سفید لوگو در حالت شب.

**۳۲ مورد که واقعاً حالت شب را می‌شکستند عوض شدند:**
- `border-slate-900/10` (مویرگ ۱۰٪ مشکی) روی کارت‌های بررسی یکپارچه، فرم ارزیابی، راهنمای امتیازدهی → `border-border`. این مرز در حالت شب عملاً نامرئی بود و کارت‌ها لبه نداشتند.
- `bg-slate-200` نوار پیشرفت و ریل امتیاز → `bg-muted` · `text-slate-300` جداکننده‌ها → `text-muted-foreground/50` · `text-gray-400/500` → `text-muted-foreground` · `bg-slate-900 text-white` → `bg-foreground text-background`.

**باگ واقعی که پیدا شد — و اسکرین‌شات دارد:** آیتم فعال «ردیابی تغییرات» در سایدبار `bg-slate-800` بود؛ در حالت شب یعنی یک قرص تیره روی سایدبار تیره — کاربر نمی‌فهمید کدام صفحه باز است. حالا `bg-foreground text-background` است و در هر دو تم برعکسِ پس‌زمینه می‌شود. نسبت کنتراست اندازه‌گیری‌شده در مرورگر: **۱۸:۱**. کاشی آیکنش هم مجبور شد از `bg-white/20 text-white` به `bg-background/20 text-background` برود، وگرنه سفید روی سفید می‌شد.

**`AppSidebarButton` از ۵۶ ابزار رنگ سخت‌کدشده به ۱۲ رسید:** جفت‌های `text-muted-foreground dark:text-slate-300` و `bg-muted dark:bg-slate-800` حذف شدند چون توکن دقیقاً همان مقدار را در حالت شب می‌دهد (`--muted` تیره = `#1E293B` = slate-800). جدول ۱۲ واریانت عمداً literal ماند: Tailwind v4 کلاس ساخته‌شده با رشتهٔ داینامیک (`bg-${hue}-600`) را نمی‌بیند و آن رنگ‌ها اصلاً build نمی‌شوند.

**یک اشتباه که خودم گرفتم:** `hover:bg-slate-200` در تب‌های فرم سورس با جایگزینی خودکار شد `hover:bg-muted` که **همان رنگ پایهٔ خودش بود**، یعنی hover بی‌اثر می‌شد. به `hover:bg-accent` اصلاح شد.

**تأیید:** `tsc` · ESLint ۰ خطا · build · ۲۰۰ تست · ۱۴ اسکرین‌شات تمام‌صفحه در روشن و **تیره** روی ۷ صفحه · آزمون کارکردی: ناوبری سایدبار در هر دو تم و اندازه‌گیری کنتراست قرص فعال. ۰ خطای صفحه.


## بازطراحی رابط، فاز ۵ (۱۴۰۵/۰۶/۱۱) — جهت متن: چه چیزی واقعاً مشکل بود

ممیزی گفته بود «۱۴۵ `dir=` دستی را با خواص منطقی RTL جایگزین کن». وقتی کد را شمردم، این عدد **دو چیز کاملاً متفاوت** بود و فقط یکی‌شان مشکل بود:

- **۹۳ مورد `dir="ltr"` درست‌اند و حذف نمی‌شوند.** روی نام کاربری، CAS، IRC، ایمیل و شمارهٔ تلفن نشسته‌اند و *جهتِ محتوا* را اعلام می‌کنند، نه چیدمان را. حذفشان یعنی نمایش غلط متن لاتین در بند فارسی.
- **۳۱ مورد `dir="rtl"` تکراری بودند و حذف شدند.** `index.html` روی `<html>` مقدار `dir="rtl"` دارد، پس ریشهٔ هر نما، هر مودالِ portal‌شده و هر توست همین را از سند به ارث می‌برد. این تکرار این توهم را می‌ساخت که جهت، per-component مدیریت می‌شود.
   - سه جا عمداً نگه داشته شد: `PrintableForms` (نمای چاپ، بیرون از دامنهٔ همهٔ فازها)، `ShamsiDatePicker` و `TooltipContent` رادیکس، که خودشان منطق موقعیت‌دهی دارند و نباید بی‌دلیل دستکاری شوند.

**تبدیل `pr-`/`pl-` به `ps-`/`pe-` را عمداً انجام ندادم** و پیشنهاد هم نمی‌کنم: برنامه تک‌جهته (RTL) است، پس ابزارهای فیزیکی امروز درست کار می‌کنند و تبدیلشان هیچ تغییری برای کاربر ندارد. بدتر: ابزار منطقی داخل جزیره‌های `dir="ltr"` (همان ۹۳ مورد بالا) **برعکس می‌شود**، یعنی یک تبدیل سراسری دقیقاً همان‌جاهایی را می‌شکند که الان درست‌اند. سود صفر در برابر ریسک واقعی.

**تأیید:** `tsc` · ESLint ۰ خطا · build · ۲۰۰ تست · قبل/بعد روی ۹ صفحه با pixel-diff. مهم‌تر از پیکسل، **شمارش عناصر LTR در هر صفحه قبل و بعد دقیقاً یکی است** (خانه ۳۶، مواد ۵، شرکا ۲، کاربران ۷، ممیزی ۱۳، جزئیات ۷، دسته ۲، فرم ۳) یعنی جهت هیچ عنصری عوض نشده. آزمون کارکردی: ورود، پالت فرمان و مودال ماده همگی `direction: rtl` گزارش کردند. تنها اختلاف پیکسلی صفحهٔ ورود، فریمِ انیمیشن `fade-in` بود نه چیدمان.


## بازطراحی رابط، فاز ۴ (۱۴۰۵/۰۶/۱۱) — دو تکهٔ تکراری جدول

چهار نسخهٔ کپی‌شدهٔ `SortHeader` در کد بود (مواد، شرکای تجاری، کاربران، ردیابی تغییرات) که فقط در دو پیکسل padding، رنگ hover و نامِ prop (`sortOrder` در برابر `sortDirection`) فرق داشتند — و همین‌ها نشان می‌دهد کپی‌ها **واگرا شده بودند**. حالت خالی جدول‌ها هم همین‌طور: همان شکلِ آیکن-پیام-اقدام با `py-12` در یکی و `py-14` در دیگری.

**دو کامپوننت تازه در `src/components/ui/`:**
- `sort-header.tsx` — `SortHeader` **جنریک روی نام ستون**، پس هر جدول اتحادیهٔ نوع خودش را نگه می‌دارد و غلط تایپی در کلید ستون هنوز خطای کامپایل است. `aria-sort` و دکمهٔ داخل `<th>` (به‌جای `<th>` کلیک‌پذیر) حالا همه‌جا هست.
- `table-empty-row.tsx` — `TableEmptyRow` با `message` / `action` / `note`. «هیچ رکوردی نیست» و «با این فیلترها چیزی نیست» عمداً دو فراخوانی جدا ماندند؛ پیشنهاد پاک‌کردن فیلترِ ست‌نشده فقط گیج می‌کند.

**۲۴۶ خط حذف شد و ۹۰ خط اضافه.** واردات مردهٔ `ArrowUp`/`ArrowDown`/`ArrowUpDown` در چهار فایل هم پاک شد تا شمار هشدارهای ESLint به همان ۷۲ موردِ از قبل موجود برگردد.

**تغییر بصری پذیرفته‌شده:** ارتفاع سرستون‌ها روی `py-3.5` و رنگ hover روی `bg-accent` یکدست شد؛ جدول شرکا و ردیابی تغییرات ۴ پیکسل بلندتر شدند.

**تأیید:** `tsc` · ESLint ۰ خطا (۷۲ هشدار مثل قبل) · build · ۲۰۰ تست · ۲۴ اسکرین‌شات قبل/بعد شامل **حالت خالی هر چهار جدول** · آزمون کارکردی: روی هر چهار جدول ستون مرتب‌سازی کلیک شد و هم `aria-sort` عوض شد هم ترتیب واقعی ردیف‌ها؛ دکمهٔ «پاک‌کردن جستجو و فیلترها» داخل حالت خالی هم واقعاً فهرست را برگرداند. ۰ خطای صفحه.

**تکهٔ سوم — `ui/page-title.tsx`:** بلوک عنوان چهار صفحهٔ مخزن هم یکی شد. مسئلهٔ واقعی این بود که سه صفحه `h1` می‌زدند و یکی `h2`، با سه وزن فونت مختلف — یعنی ساختار سرفصل سند برای صفحه‌خوان به‌ازای هر صفحه فرق می‌کرد. `PageTitle` فقط بلوک متن است، نه کارت و نوار ابزار دور آن؛ دو شکل موجود را پشتیبانی می‌کند: `eyebrow` (کپشن لاتین بالای عنوان، مواد و شرکا) و `icon` (کاشی آیکن کنار عنوان، کاربران و ردیابی تغییرات). تأیید در مرورگر: هر چهار صفحه دقیقاً **یک `h1` و صفر `h2`** دارند.
   - تغییر بصری: عنوان صفحهٔ کاربران از `text-base` به `text-xl` و زیرعنوانش از `text-2xs` به `text-xs` رفت؛ کاشی آیکن ردیابی تغییرات از خاکستری به رنگ `primary` مثل بقیه.


## بازطراحی رابط، فاز ۳ (۱۴۰۵/۰۶/۱۱) — ورودی‌ها و کشوهای فرم

سومین ردیف P0. `ui/input.tsx` وجود داشت و **فقط یک فایل** آن را import می‌کرد، در برابر ۶۷ `<input>` خام. نتیجه: پنج رنگ حلقهٔ focus مختلف (`blue-500`، `indigo-500`، `fuchsia-500`، `cyan-600`، `primary`)، سه طرح پس‌زمینه و چهار اندازهٔ متن در فیلدهای یک فرم.

**۴۸ ورودی به `Input` مهاجرت کرد** (۶۷ → ۲۰) و **۲۵ `<select>` بومیِ فرم** همان ظاهر را گرفت.

**چرا select کامپوننت نشد:** `ui/select.tsx` رادیکسی است و با `onValueChange` کار می‌کند نه رویداد change؛ عوض‌کردن ۳۷ کشو روی آن **بازنویسی رفتار** بود نه تغییر ظاهر. به‌جایش رشتهٔ کلاسِ پایه از `input.tsx` با نام `inputBaseClass` export شد و کشوهای فرم با `cn(inputBaseClass, …)` همان قیافه را گرفتند — یک تعریف از «ظاهر فیلد»، بدون کامپوننت دوم. کشوهای فشردهٔ نوار ابزار (تعداد در صفحه، چیپ‌های کوچک) دست‌نخورده ماندند چون ارتفاع ۳۸px آن ردیف‌ها را خراب می‌کرد.

**۲۰ ورودی باقی‌مانده:** چک‌باکس (۶)، فایل (۵)، رادیو (۴)، اسلایدر ریسک (۱)، ورودی `CommandPalette` و دو جستجوی ترکیبیِ داخل کادرِ بوردردار (`ArchiveView`، `SupplierAuditView`) که خودشان بدنهٔ فیلد را می‌سازند و `bg-transparent`اند.

**تغییر بصری پذیرفته‌شده:** اندازهٔ متن فیلدها از `text-xs` (۱۲px) به `text-sm` (۱۴px) رفت، چون کف تایپوگرافی فاز ۱ همین را می‌گفت و متن تایپ‌شدهٔ فارسی در ۱۲px داخل کادر تنگ بود. ارتفاع فیلد ۳۸px یکدست شد.

**تأیید:** `tsc` · ESLint ۰ خطا · build · ۲۰۰ تست · ۲۳ اسکرین‌شات قبل/بعد (فرم سورس، فرم ماده، فرم شریک، فرم کاربر، فیلترهای ردیابی تغییرات، صفحهٔ ورود) در سه عرض + تیره · آزمون کارکردی: **ورود واقعی با تایپ در فیلدهای مهاجرت‌کرده**، و **ساخت کاربر واقعی** با فرم مهاجرت‌کرده (نام + نام کاربری + کشوی سمت) که با نقش درست در دیتابیس نشست و بعد پاک شد. ۰ خطای صفحه.

**یک FAIL که باگ نبود:** اولین آزمون نقشِ ذخیره‌شده را غلط خواند چون `selectOption('select')` اولین کشوی صفحه یعنی «تعداد در صفحه» را گرفته بود، نه کشوی فرم. با محدودکردن به کشوی داخل فرم، مقدار `qa` درست ذخیره شد.


## بازطراحی رابط، فاز ۲ج (۱۴۰۵/۰۶/۱۱) — دکمه‌ها، بخش سوم: جزئیات سورس، پوسته و مودال‌ها

**۵۱ دکمه مهاجرت کرد** (۱۲۱ → ۷۰): `VendorDetail` ۲۲ · پوستهٔ `App.tsx` ۱۳ · `ChangePasswordModal` ۵ · `MaterialSelector` ۴ · `LoginView` ۲ · `ErrorBoundary` ۲ · `FormModal` ۲ · `PartnerSelector` ۱. با این، ردیف دوم P0 تمام شد: از ۲۱۲ دکمهٔ خام به ۷۰ رسیدیم و باقی‌مانده‌ها عمداً باقی مانده‌اند.

**۷۰ موردی که می‌مانند** `<button>` هستند ولی «دکمه» نیستند: نمای چاپ (`PrintableForms`)، خانه‌های تقویم شمسی، ردیف‌های `CommandPalette`، آیتم ناوبری سایدبار، سرستون‌های مرتب‌سازی، تب‌ها و چیپ‌های فیلتر، تریگرِ کمبوباکس‌ها و ردیف‌های گزینه در `MaterialSelector`/`PartnerSelector`، آیکن چشمِ داخل فیلد رمز، لینک متنی breadcrumb و «بازگشت به لیست»، ردیف‌های منوی کاربر، و آیکن‌های بستن درون‌خطی. `Button` روی این‌ها یا ارتفاع را می‌شکند یا معنای بصری‌شان را عوض می‌کند.

**دو چیز عمداً دست‌نخورده ماند:** آیکن‌های داخل ورودی رمز عبور (سه‌تا در هر فرم) چون adornment ورودی‌اند نه دکمه؛ و لانچر «جستجوی سریع ⌘K» در سایدبار چون ظاهر و رفتارش یک فیلد جستجوست.

**تأیید:** `tsc` · ESLint ۰ خطا · build · ۲۰۰ تست · ۱۹ اسکرین‌شات قبل/بعد (شامل صفحهٔ ورود) با pixel-diff · آزمون کارکردی مرورگر: **ورود واقعی با دکمهٔ مهاجرت‌کردهٔ `login_submit_btn`**، سوییچ تم، پنل اعلان، تأییدیهٔ حذف سورس و انصرافش، حرکت مرحله‌به‌مرحلهٔ فرآیند ارزیابی، رفتن به فرم ویرایش، مودال تغییر رمز، و **دیالوگ محافظ تغییرات ذخیره‌نشده** با دکمهٔ «بازگشت به فرم». ۰ خطای صفحه. این بار سرور **قبل از** اسکرین‌شات ری‌استارت شد (درس فاز ۲ب).


## بازطراحی رابط، فاز ۲ب (۱۴۰۵/۰۶/۱۱) — دکمه‌ها، بخش دوم: مخزن‌ها

**۵۵ دکمه در چهار مخزن مهاجرت کرد** (۱۹۳ → ۱۲۱): `BusinessPartnerRepositoryView` ۳۱ · `MaterialRepositoryView` ۲۰ · `UsersView` ۱۷ · `AuditTrailView` ۴.

**آنچه دست‌نخورده ماند** (۱۷ مورد که `<button>` هستند ولی «دکمه» نیستند): سرستون‌های مرتب‌سازی جدول، چیپ‌های فیلتر سریع سطح خطا، تب‌های داخل مودال شریک، کارت‌های انتخاب «تولیدکننده/فروشنده»، لینک متنی «شروع ارزیابی» داخل سلول جدول، و آیکن‌های بستنِ درون‌خطی داخل نوار خطا و چیپ فایل. این‌ها ارتفاع سطر یا هویت بصری خودشان را دارند و `Button` آن را می‌شکست.

**رنگ‌های معنادار با `className` ماندند، نه واریانت تازه:** اقدام‌های آبی/کهربایی/سرخ سطر جدول، دکمهٔ سبز «ادامه به ارزیابی SOP» و دکمهٔ کهربایی «بازنشانی کلمه عبور». پایه — ارتفاع، شعاع، حلقهٔ focus — از کامپوننت می‌آید.

**تأیید:** `tsc` · ESLint ۰ خطا · build · ۲۰۰ تست · ۱۶ اسکرین‌شات قبل/بعد در سه عرض + حالت تیره با pixel-diff · آزمون کارکردی مرورگر: باز و بستهٔ فرم کاربر جدید، دیالوگ سطح دسترسی، ویرایش سطر، پاک‌کردن فیلتر ردیابی تغییرات، **دانلود واقعی خروجی اکسل**، و فرم‌های ثبت ماده و شریک تجاری — همه سبز، ۰ خطای صفحه. حلقهٔ focus روی دکمهٔ مهاجرت‌کرده با Tab دیده شد (`--tw-ring-color: #2563EB`).

**تلهٔ روش (دوباره):** اولین دور اسکرین‌شات و آزمون کارکردی روی **کد کهنه** اجرا شد — سرور فرانت را با Vite dev middleware سرو می‌کند و ماژول‌های عوض‌شده را برنداشته بود. کلاس‌های عنصر فوکوس‌شده هنوز `p-1.5` دستی بود در حالی که فایل روی دیسک `Button` داشت. سرور ری‌استارت شد و هر دو دور دوباره اجرا شدند. تست کارکردی در دور اول هم «PASS» می‌داد، چون رفتار عوض نشده بود؛ فقط بازرسی کلاس‌ها این را لو داد.

## بازطراحی رابط، فاز ۲ (۱۴۰۵/۰۶/۱۱) — دکمه‌ها، بخش اول: نماها

دومین ردیف P0. `ui/button.tsx` وجود داشت ولی فقط ۴ فایل آن را import می‌کردند، در برابر **۲۱۲ `<button>` خام**. نتیجه‌اش ارتفاع، شعاع و مهم‌تر از همه **حالت focus** تصادفی در هر صفحه بود.

**دو اندازه به `Button` اضافه شد:** `icon-sm` (۳۲px) و `icon-xs` (۲۸px). ۱۸ مورد `p-1.5` و ۱۵ مورد `p-2` در کد بود که `size="icon"` موجود (۳۶px) به آن‌ها نمی‌خورد؛ بدون این‌ها مهاجرت یک اقدام سطر جدول، ارتفاع سطر را بزرگ می‌کرد. هفت واریانت موجود دست‌نخورده.

**۱۹ دکمه در ۹ فایل نما مهاجرت کرد** (۲۱۲ → ۱۹۳). برنامه ۳۴ تخمین زده بود؛ تفاوت آن‌هایی است که هنگام خواندن کد معلوم شد **دکمه نیستند**: تب‌های کارتابل، چیپ‌های فیلتر، ردیف‌های منوی کشویی، کارت‌های آمار داشبورد، سلول‌های انتخاب نوع شریک و آیکن پاک‌کردن داخل ورودی جستجو. فشردن این‌ها داخل `Button` غلط بود، پس دست‌نخورده ماندند.

**تصمیم‌های قابل بحث:** دکمهٔ «پرونده ممیزی» رنگ فیروزه‌ای و دکمهٔ «ذخیره ارزیابی» رنگ تیرهٔ خودش را نگه داشت — پایه (ارتفاع، شعاع، حلقهٔ focus) از کامپوننت می‌آید و هویت رنگی با `className` می‌ماند. برای رنگ کهربایی که فقط **یک** مصرف داشت واریانت تازه ساخته نشد.

**تأیید:** `tsc` · ESLint ۰ خطا · build · ۲۰۰ تست · ۱۸ اسکرین‌شات قبل/بعد در سه عرض + حالت تیره با pixel-diff · و **آزمون کارکردی واقعی در مرورگر**: خروجی اکسل آرشیو دانلود شد، منوی تک‌دسته‌ای باز شد، پاک‌کردن فیلتر دسته‌بندی کار کرد، رفت‌وبرگشت به جزئیات تأمین‌کننده درست بود، و **حلقهٔ focus روی دکمهٔ مهاجرت‌کرده مرئی است** — همان چیزی که این فاز برایش انجام شد. ۰ خطای صفحه.

**نکتهٔ روش:** pixel-diff نشان داد چهار صفحه از پنج صفحه تقریباً هیچ تغییری ندارند. علتش این بود که بیشتر دکمه‌های مهاجرت‌کرده در نمای پیش‌فرض اصلاً دیده نمی‌شوند (داخل مودال، حالت خالی، یا صفحهٔ جزئیات). اگر فقط به اسکرین‌شات تکیه می‌کردم، «تأیید شد» می‌گفتم بدون اینکه چیزی از کار را دیده باشم.

**باقی‌ماندهٔ این ردیف:** بخش دوم مخزن‌ها (`BusinessPartnerRepositoryView` ۳۸ · `MaterialRepositoryView` ۲۳ · `UsersView` ۱۸ · `AuditTrailView` ۱۰) و بخش سوم `VendorDetail` (۲۴) و پوستهٔ `App.tsx` (۲۱).


## بازطراحی رابط، فاز ۱ (۱۴۰۵/۰۶/۱۱) — کف تایپوگرافی

نخستین ردیف P0 از ممیزی رابط. **۴۴۸ مقدار دلخواه `text-[NNpx]` در ۲۶ فایل به یک توکن نام‌دار `text-2xs` تبدیل شد** و هیچ متنی در رابط زیر ۱۱px نماند.

**چرا این P0 بود:** پرکاربردترین اندازهٔ متن ۱۲px بود و ۴۹۹ نقطه زیر آن — از جمله ۲۴ مورد ۹px. برای فارسی این زیر حد خوانایی است: حرف بزرگ ندارد پس نشانه‌های شکل‌کلمه کمترند، نقطه‌ها و اعراب در اندازهٔ کوچک به هم می‌چسبند، و ارتفاع x نسبی وزیرمتن از فونت‌های لاتینی که این مقیاس از آن‌ها قرض گرفته شده پایین‌تر است.

**دامنه:** ۲۳ مورد ۹px + ۲۲۸ مورد ۱۰px بزرگ شدند (تغییر بصری)؛ ۱۹۷ مورد ۱۱px فقط نام عوض کردند (بدون تغییر بصری). `PrintableForms.tsx` با ۵۱ مورد **بیرون از دامنه** ماند — رجیستر چاپی امضاشونده است و تایپوگرافی چاپ قواعد فیزیکی خودش را دارد. `text-xs` دست‌نخورده ماند چون عوض‌کردنش ۵۵۸ نقطه را یکجا جابه‌جا می‌کرد.

**گیت قبل از هر جایگزینی:** توکن اضافه شد و در CSS خروجی تأیید شد که `.text-2xs` واقعاً ساخته می‌شود و `.text-primary` هنوز ابزار **رنگ** است نه اندازه (تداخل فضای نام `--text-*` در Tailwind v4). بدون این تأیید بقیهٔ کار انجام نمی‌شد.

**تأیید:** `tsc` · ESLint ۰ خطا · build · ۲۰۰ تست سبز · و **۱۸ اسکرین‌شات قبل/بعد** در سه عرض ۱۹۲۰/۱۲۸۰/۷۶۸ به‌علاوهٔ حالت تیره، با pixel-diff برنامه‌ای. هیچ صفحه‌ای ابعادش عوض نشد، سرریز افقی سند صفر ماند و ۰ خطای صفحه.

**دو باگ موجود که در تست پیدا شد و عمداً رفع نشد** (خارج از دامنهٔ این فاز، تغییر هدفمند هدفمند می‌ماند):
- در ۷۶۸px در «بررسی یکپارچه»، متن «N/A» با لینک «بررسی ممیزی» در پای کارت تداخل دارد. **با اسکرین‌شات قبل تأیید شد که از قبل بوده.**
- در ۷۶۸px هدر شلوغ است و چیپ تاریخ/ساعت کمی بیرون viewport می‌افتد. **قبل و بعد دقیقاً یکسان است.**


## یکی‌کردن کتابخانهٔ کامپوننت و blobها (۱۴۰۵/۰۶/۱۱) — آخرین ردیف‌های نقشهٔ راه

### دو کتابخانهٔ کامپوننت → یکی
`src/design-system/` حذف شد. از یازده فایلش **ده تا هیچ‌جا import نمی‌شد**، و یازدهمی (`ThemeSwitcher`) فقط به‌خاطر هوک `useTheme` وارد می‌شد — خودِ کامپوننتش هم استفاده نمی‌شد. یک `cn` تکراری هم داشت (نسخهٔ `src/lib/utils.ts` با `clsx`+`tailwind-merge` کامل‌تر است). `useTheme` به `src/hooks/useTheme.ts` رفت، چون هوک کامپوننت نیست. تنها کتابخانهٔ کامپوننت حالا `src/components/ui/` است (قاعدهٔ ۷ در `CLAUDE.md` به‌روز شد).

### blobها از کوئری‌ها بیرون آمدند
**نکتهٔ اصلی: جدول جداگانه لازم نبود.** Postgres متن بزرگ‌تر از ~۲KB را خودش خارج از ردیف نگه می‌دارد (TOAST) و تا وقتی ستون در `SELECT` نیامده اصلاً لمسش نمی‌کند. پس مشکل «blob در ردیف» نبود، **«blob در SELECT»** بود — و همه‌جا هم فقط برای محاسبهٔ یک بولین:

| کوئری | چه می‌خواند |
|---|---|
| `getBusinessPartnersList` | `include: { documents: true }` ⇒ base64 **هر مدرک SOP هر شریک**، برای ساختن `hasFile` |
| `GET /api/materials` | ردیف کامل هر ماده ⇒ base64 هر فایل مشخصات، برای `hasSpecificationFile` |
| `getVendorsList` | ردیف کامل هر ماده ⇒ همان base64، در حالی که فقط ۵ فیلد لازم بود (روی داغ‌ترین مسیر برنامه) |

هر سه حالا ستون‌ها را صریح نام می‌برند و «کدام‌ها فایل دارند» با یک کوئری کوچکِ `select: { id: true }` جواب داده می‌شود.

**مسیر نوشتن شرکا هم عوض شد و این مهم‌تر است:** ارزیابی و مدارکش delete-و-recreate می‌شدند، یعنی هر فایل ذخیره‌شده باید از دیتابیس خوانده، از داخل تابع عبور داده و دوباره نوشته می‌شد — **اصلاح شمارهٔ تلفن یک فروشنده، هر PDF آپلودشده‌اش را جابه‌جا می‌کرد.** حالا در جا آپدیت می‌شود و مدرکی که فایلش عوض نشده اصلاً دربارهٔ فایلش چیزی نمی‌شنود. ضمناً کلید نامعتبر حالا **قبل از** هر نوشتنی رد می‌شود؛ قبلاً اول همهٔ مدارک حذف می‌شدند و بعد create شکست می‌خورد — یعنی فروشنده بدون هیچ مدرکی می‌ماند.

**۱۱ تست تازه** (`tests/api.partnerFiles.test.ts`) سه قاعدهٔ حفظ فایل را قفل کردند: ویرایش بدون آپلود مجدد فایل را نگه می‌دارد، پاک‌کردن نام فایل آن را حذف می‌کند، آپلود تازه جایگزین می‌شود. **تست منفی گرفتم:** با شکستن قاعدهٔ اول، تست قرمز می‌شود.

**اندازه‌گیری زنده:** با ۱۵۶KB فایل واقعی در دیتابیس، پاسخ فهرست شرکا ۹.۹KB است، هیچ `base64,`ای در هیچ payload فهرستی نیست، `hasFile` برای هر دو مدرک درست است و فایل از endpoint خودش می‌آید. ۰ خطای صفحه. **۲۰۰ تست سبز.**


## بازنشستگی ستون‌های JSON روی `vendors` و افزودن ESLint (۱۴۰۵/۰۶/۱۱)

### دو ستون قدیمی حذف شدند
`risk_assessment` و `analysis_records` همان چیزی را نگه می‌داشتند که جدول‌های نرمال `risk_assessments` و `analysis_records` نگه می‌دارند. هیچ‌جا خوانده نمی‌شدند و هر ذخیره NULL در آن‌ها می‌نوشت. **دلیل اینکه مطمئنیم مرده بودند:** تست‌های نوشتن ریسک و نتایج آزمایشگاهی بعد از حذف ستون‌ها سبز ماندند.

مهاجرت `20260902100000_retire_vendor_legacy_json` اول دادهٔ باقی‌مانده را منتقل می‌کند و **فقط بعد از آن** ستون‌ها را می‌اندازد. اگر چیزی قابل انتقال نباشد `RAISE` می‌کند، id سورس‌ها را نام می‌برد و **هیچ ستونی حذف نمی‌شود** — دپلوی شکست‌خورده قابل جبران است، ارزیابی ریسکِ بی‌صدا گم‌شده نه. رکورد نرمالِ موجود بر JSON اولویت دارد.

**دو فیلتر که از ظاهرشان مهم‌ترند:** `{}` خالی قبلاً به یک ارزیابی ریسک با نمرات پیش‌فرض تبدیل می‌شد، یعنی سورسی که کسی ارزیابی‌اش نکرده بود «ارزیابی‌شده در پایین‌ترین سطح ریسک» نشان داده می‌شد — **ساختن یک رکورد GxP از هیچ**. عنصر غیرشیء داخل آرایهٔ نتایج هم به «Pass» بدل می‌شد. هر دو حالا رد می‌شوند و به‌جای ناپدید شدن، همان گارد بالا را فعال می‌کنند.

`prisma/seed.ts` تنها جایی بود که هنوز این ستون‌ها را می‌نوشت — و **فقط** آن‌ها را می‌نوشت، یعنی هر ارزیابی ریسک یا نتیجهٔ آزمایش که seed می‌شد از همان لحظه برای برنامه نامرئی بود. حالا جدول‌های نرمال را می‌نویسد.

**تأیید روی دیتابیس واقعی با دادهٔ قدیمی:** یک رکورد کامل فیلد‌به‌فیلد منتقل شد، `{}` و `[]` چیزی نساختند، رکورد نرمال موجود دست‌نخورده ماند، و یک مقدار عمداً غیرقابل‌انتقال دپلوی را با پیام حاوی id متوقف کرد و هر دو ستون سر جایشان ماندند.

### ESLint (آخرین ردیف باز از فهرست P0)
`eslint.config.js` عمداً **گیت است، نه راهنمای سبک**. روی ۳۰٬۰۰۰ خط کدِ بدون linter، فعال‌کردن یک preset کامل هزاران finding می‌داد که ظرف یک ساعت خاموش می‌شد. قواعد نگه‌داشته‌شده آن‌هایی‌اند که این کدبیس واقعاً از آن‌ها آسیب دیده:
- **`react-hooks/rules-of-hooks` (error):** قاعدهٔ ۱۰ در `CLAUDE.md` همین باگ را با نثر توضیح می‌دهد؛ حالا اجرا می‌شود. **تست منفی گرفتم:** یک هوک بعد از early-return واقعاً error می‌دهد.
- `react-hooks/exhaustive-deps` (warning)، به‌علاوهٔ خطاهای آشکار preset پایه.
- فرمت‌بندی اصلاً lint نمی‌شود، پس اجرای linter هرگز diff تولید نمی‌کند.

**۰ خطا، ۷۲ هشدار.** هر ۴ هشدار hook بررسی شد و **هیچ‌کدام باگ واقعی نبود** (deps درست‌اند، فقط برای linter قابل اثبات نیستند) — این را می‌گویم چون ادعای «linter باگ پیدا کرد» درست نمی‌بود. بقیه متغیر بلااستفاده‌اند. `--max-warnings` عمداً ست نشده: گیتی که همه یاد بگیرند نادیده بگیرند، دیگر گیت نیست.

سه فایل دورریختنیِ ریشه (`fix.cjs`، `test-crash.cjs`، `test-icons.js`) حذف شدند؛ `fix.cjs` با جایگزینی رشته‌ای `src/App.tsx` را ویرایش می‌کرد.


## صفحه‌بندی فهرست سورس و شکستن باندل (۱۴۰۵/۰۶/۱۱) — P2

### صفحه‌بندی `GET /api/vendors`
- **بدون پارامتر، همان آرایهٔ قبلی برمی‌گردد.** داشبورد، شمارش دسته‌بندی‌ها، بج سایدبار، آرشیو، نمودار مقایسه و خروجی اکسل همگی روی **کل** مجموعه حساب می‌کنند؛ تا وقتی همهٔ این‌ها به aggregate سمت‌سرور تبدیل نشوند، حذف فهرست کامل ممکن نیست. پس صفحه‌بندی چیزی است که کالر **انتخابش می‌کند**: `?page=&limit=` پاسخ را به پاکت `{items,total,page,limit,pages}` تبدیل می‌کند.
- **ترتیب ثابت شد (`name` سپس `id`) و این تزئینی نیست.** بدون آن Postgres هر ترتیبی بخواهد برمی‌گرداند — برای یک پاسخ بی‌ضرر، برای چند صفحه مرگبار: صفحه‌ها همپوشانی و جاافتادگی پیدا می‌کنند. tie-break روی `id` لازم است چون نام یکتا نیست. **تست منفی گرفته شد:** با حذف `orderBy` تست «صفحه‌ها دقیقاً همان فهرست می‌شوند» قرمز می‌شود.
- **کوئری‌های رابطه به idهای همان صفحه محدود شدند**، وگرنه گرفتن ۲۰۰ سورس همچنان همهٔ ارزیابی‌ها/لاگ‌ها/آنالیزهای کل جدول را می‌خواند و بعد دور می‌ریخت.
- مقادیر نامعتبر clamp می‌شوند نه رد (`src/server/http/query.ts`)؛ `limit` سقف ۵۰۰ دارد تا نشود با آن همان پاسخ کل‌جدولی را گرفت.
- کلاینت **تدریجی** می‌خواند (`src/services/vendorPages.ts`): صفحهٔ اول رسم می‌شود و بقیه پشتش می‌آیند. شکست وسط راه پیام جداگانه دارد — فهرست ناقص شبیه فهرست کامل به نظر می‌رسد ولی همهٔ آمار و نمودارها از آن غلط درمی‌آیند.

**باگی که فقط تست زندهٔ مرورگر گرفت:** الحاق صفحه‌ها به state قبلی، effect را غیر idempotent کرد و چون React در حالت توسعه effect را دو بار اجرا می‌کند، **هر سورس دو بار در فهرست می‌آمد** (`V1,V2,V3,V1,V2,…`). حالا هر اجرا در آرایهٔ خودش انباشته می‌کند و همان را منتشر می‌کند (+ پرچم `cancelled` در cleanup). با صفحهٔ ۳تایی روی ۷ سورس دوباره تست شد: ۷ رکورد، هرکدام یک بار.

### code-splitting
| | قبل | بعد |
|---|---|---|
| باندل اصلی | ۲٬۴۴۰ kB (۷۴۳ kB gzip) | **۳۹۰ kB (۹۷ kB gzip)** |
| بارگذاری اول (مجموع) | ۲٬۴۴۰ kB | **۱٬۲۱۳ kB (۳۴۴ kB gzip)** |

- **`xlsx-js-style` (۸۹۰ kB) کاملاً از مسیر اول خارج شد.** بزرگ‌ترین وابستگی پروژه بود و حتی صفحهٔ ورود هزینه‌اش را می‌داد. حالا با کلیک روی دکمهٔ خروجی گرفته می‌شود (`useExcelExport`). چون این کلیک تازه می‌تواند شکست بخورد، هر ۶ نقطهٔ خروجی حالت «در حال آماده‌سازی» و پیام خطا دارند — دکمهٔ خاموشِ بی‌صدا بدترین پاسخ ممکن بود.
- **۷ ماژول به `React.lazy` رفتند** (آرشیو، ردیابی تغییرات، کاربران، مواد، شرکای تجاری، ۳۶۰ تأمین‌کننده، کارتابل) پشت یک `Suspense` در `renderContent`. داشبورد، فهرست دسته‌بندی، جزئیات سورس و فرم آن عمداً در باندل اصلی ماندند — تکه‌کردن مسیر پرتردد فقط یک انتظار را با انتظار دیگر عوض می‌کند.
- **`manualChunks`:** `react` / `ui` (radix+motion+lucide) / `charts` (recharts+d3) جدا شدند تا یک تغییر یک‌خطی در یک view دیگر کل کتابخانه‌ها را برای همهٔ کاربران شبکهٔ داخلی بی‌اعتبار نکند.

**تست زنده:** هر ۷ ماژول lazy واقعاً محتوا رندر کردند (نه اسپینر گیرکرده)، چانک اکسل **قبل** از کلیک بارگذاری نشد و **بعد** از کلیک فایل دانلود شد — ۰ خطای صفحه. ۱۹۱ تست سبز (۱۷ تست تازه).


## ناوبری و کش کوئری فرانت (۱۴۰۵/۰۶/۱۰)
درخواست «روتر و کش کوئری» بود. **کتابخانه‌ای اضافه نشد** و دو دلیل داشت:

- **روتر:** ۸۵۰ خط ناوبری در `App.tsx` یک سیستم طراحی‌شده است، نه کد اتفاقی — قاعدهٔ ۱۰ در `CLAUDE.md`، ۲۲ ارجاع که هرکدام یک نامتغیر را حمل می‌کنند، و ۱۹ تست روی `navRoutes`. تعویض آن با react-router یعنی بازنویسی لایه‌ای که **هیچ تستی روی خودِ انتقال‌ها نداشت**. به‌جای تعویض، **استخراج**: `src/utils/navStack.ts` همان قواعد را به‌صورت توابع خالص `(stack, arg) => stack` درآورد و `tests/navStack.test.ts` با **۱۸ تست** پوشش داد. هر تست یک باگ واقعی گذشته را قفل می‌کند (تعویض‌تب‌نبودنِ drill-down، اینکه صفحهٔ جزئیات هرگز صفحهٔ فرم نیست، جایگزینی ورودی فرم، زندگی `expandedMaterial` داخل ورودی).
- **کش کوئری:** باگی که یک کتابخانهٔ کش قرار بود رفع کند (کش کهنه در برابر سرور) قبلاً با `authWrite` + resync رفع شده بود. آنچه واقعاً تکرار شده بود، چهار نگرانیِ یکسان در سه مجموعه بود که از هم واگرا شده بودند → `src/hooks/useCachedCollection.ts`: کش فقط کش است، خواندن یک مجوز است (رد شدن خطا نیست و کش را خالی می‌کند)، fetch شکست‌خورده کش را نگه می‌دارد، و `reload()` راهِ برگشت مسیر نوشتن است. مواد و شرکای تجاری منتقل شدند؛ `db` عمداً منتقل **نشد** چون کشش تاریخچه را برای بودجهٔ ۵MB هرس می‌کند و از دادهٔ دمو seed می‌شود.

`App.tsx`: ۲۲۴۶ → ۲۱۳۲ خط. `tsc` و `vite build` سبز، **۱۷۴ تست سبز**.

**تست زندهٔ مرورگر (بدون خطای صفحه):** معنای تعویض‌تب (گام ۵ به‌جای push، ۴ ورودی را unwind کرد)، Back/Forward بومی مرورگر، دیپ‌لینک `#/category/foreign/vendor/V1` با استک `home › category:foreign › V1` و breadcrumb «صفحه اصلی › خرید خارجی › شرکت الف» (یعنی stubِ فقط-id واقعاً hydrate شد)، و push/pop تمیز فرم ویرایش.

## تفکیک `server.ts` (۱۴۰۵/۰۶/۱۰) — P2-10
**`server.ts` از ۴٬۵۵۱ خط به ۱۵۱ خط رسید.** هیچ رفتاری عوض نشد؛ همه‌اش جابه‌جایی است و **۱۵۶ تست بعد از هر تکه سبز ماند** — همان تور ایمنی که برای همین کار ساخته شده بود.

### ساختار تازه زیر `src/server/`
| پوشه | چه چیزی |
| :--- | :--- |
| `db/` | `prisma.ts` (تنها انبار داده، fail-fast) · `coerce.ts` (تبدیل تاریخ، مشترک بین دو ریپازیتوری) |
| `domain/` | `sourceRules.ts` (قاعدهٔ IRC و گیت گرید A) · `materialId.ts` · `partnerLink.ts` · `vendorEvaluation.ts` |
| `http/` | `auth.ts` (۱۹۶ خط: requireAuth، requirePermission، requireRole، گاردهای ایمنی ادمین) · `securityHeaders.ts` · `errors.ts` · `requestInfo.ts` |
| `repositories/` | `vendorRepository.ts` (۶۲۷ خط) · `partnerRepository.ts` · `materialRepository.ts` · `userRepository.ts` |
| `routes/` | ۹ روتر: vendors (۱۱۸۷)، auth (۵۰۶)، users (۴۷۷)، materials (۴۲۴)، partners (۲۷۱)، audit (۱۶۴)، sourceSelections (۱۱۷)، health، config |
| `security/` | `passwordService.ts` · `jwtSecret.ts` |

### تصمیم‌ها
- **هر روتر مسیر کاملِ خودش را ثبت می‌کند** (`/api/vendors/...`) نه مسیر نسبی. یعنی ترتیب mount معنا ندارد و هر مسیر را می‌شود با grep روی خودِ URL پیدا کرد.
- **قواعد کسب‌وکار از هندلر بیرون آمدند.** `ircViolation` و `sopSupplierViolation` حکم دربارهٔ کسب‌وکارند نه HTTP، پس در `domain/` نشستند.
- **محدودکنندهٔ نرخ ورود همراه روتر احراز هویت رفت** چون فقط یک endpoint را گارد می‌کند.
- **`getClientIp`/`getUserAgent` جدا شدند** چون هر رکورد audit لازمشان دارد؛ پشت پراکسی `req.ip` آدرس پراکسی است نه شخص.

### روش کار
از پایین به بالا و تکه‌تکه: زیرساخت → ریپازیتوری → میان‌افزار → روترها، با `tsc` بعد از هر جابه‌جایی و کل مجموعهٔ تست بعد از هر تکه. دو کامیت میانی به‌عنوان نقطهٔ امن ثبت شدند. یک بار `ircViolation` وسط جابه‌جایی گم شد و از git برگردانده شد — دلیل اینکه این کار بدون تست‌های API نباید انجام می‌شد.

- تأیید: `tsc` ✓ · `vite build` ✓ · باندل esbuild سرور ✓ · **۱۵۶ تست** ✓.

## بازنویسی `IT_DEPLOYMENT_GUIDE.md` (۱۴۰۵/۰۶/۱۰)
سند از کد عقب افتاده بود و — بدتر از ناقص بودن — چیزهایی را **هشدار می‌داد که دیگر درست نبودند**. در محیط تحت مقررات این ریسک عملیاتی است، چون IT بر اساسش تصمیم می‌گیرد.

**هشدارهایی که دیگر واقعیت نداشتند و حذف شدند:**
| ادعای سند | واقعیت امروز |
| :--- | :--- |
| «`JWT_SECRET` به کلید ثابتِ کامیت‌شده برمی‌گردد» | `resolveJwtSecret()` در production با `[FATAL]` پروسه را متوقف می‌کند |
| «`PORT` خوانده نمی‌شود و ۳۰۰۰ هاردکد است» | خوانده می‌شود؛ مقدار نامعتبر با هشدار به ۳۰۰۰ برمی‌گردد |
| «`helmet` و rate-limit نیستند ⇒ brute-force آزاد است» | محدودکنندهٔ نرخ داخلی: ۸/کاربر و ۶۰/IP در ۱۵ دقیقه |
| «PBKDF2 با ۱۰۰۰ تکرار» | ۲۱۰٬۰۰۰ تکرار با ارتقای خودکار هنگام ورود |
| «`/api/health` هیچ کوئری نمی‌زند؛ برای Zabbix کافی نیست» | `SELECT 1` واقعی می‌زند و ۵۰۳ می‌دهد — یک بررسی ساده کافی است |
| «رمزها در `docker-compose.yml` کامیت شده‌اند» | همه از `.env` با نحو `${VAR:?}` خوانده می‌شوند |
| «Postgres پورت ۵۴۳۲ را منتشر می‌کند و healthcheck ندارد» | منتشر نمی‌شود و healthcheck دارد |
| «خواندن محدود نیست» | سه مجوز خواندن روی ۹ مسیر GET اعمال می‌شوند |
| ماتریس با ۱۳ مجوز و نام‌های `vendor.write` و… | ۲۱ مجوز به تفکیک عملیات؛ نام‌های قدیمی فقط هنگام خواندن باز می‌شوند |

**بخش‌های تازه:** «به‌روزرسانی یک نصب موجود» با ترتیب درست (پشتیبان → مهاجرت → بیلد) و هشدار اینکه مهاجرت‌ها اسکریپت بازگشت ندارند · جدول سه مهاجرت اخیر با «اگر اجرا نشود چه می‌شود» · بخش CI · چهار ردیف تازه در عیب‌یابی (پیام تعارض هم‌زمانی، قفل نرخ ورود، توقف با `[FATAL]`، و پیوند شریکی که بدون مهاجرت ذخیره نمی‌شود) · یادداشت اینکه مدارک پیوست داخل خود دیتابیس‌اند و در برنامهٔ پشتیبان‌گیری باید لحاظ شوند.

**راستی‌آزمایی:** هر دستور با `package.json` تطبیق داده شد (`npm run seed` و `prisma db push` در سند نیستند)، و اعداد مستقیماً از کد شمرده شدند: ۲۱ مجوز · ۱۳ جدول · ۹ مهاجرت · ۶ نقش · ۲۱۰٬۰۰۰ تکرار · ۸/۶۰ تلاش. دو عدد سند یک واحد اختلاف داشت (۲۵ تست API نه ۲۶، و ۱۳۱ تست واحد نه ۱۳۰) که اصلاح شد. سند حالا مهر «آخرین راستی‌آزمایی با کامیت …» دارد.

## تست‌های API — اولین پوشش خودکار روی `server.ts` (۱۴۰۵/۰۶/۱۰)
`server.ts` با ۴٬۴۰۰ خط هیچ تستی نداشت؛ گاردها، مسیریابی و لایهٔ داده فقط دستی بررسی می‌شدند. حالا **۲۵ تست API** دارد که برنامه را مثل مرورگر از راه HTTP و از میان همهٔ میان‌افزارها می‌رانند — نه با صدازدن مستقیم هندلر، چون خودِ گاردها میان‌افزارند و تستی که از آن‌ها رد شود چیزی ثابت نمی‌کند.

**زیرساخت (`tests/helpers/apiHarness.ts`):** سرور واقعی روی پورت آزاد بالا می‌آید (`VERCEL=1` جلوی listen خودش را می‌گیرد، `NODE_ENV=production` میان‌افزار Vite را رد می‌کند)، دیتابیس بین تست‌ها TRUNCATE و دوباره seed می‌شود، و helper درخواست، توکن را همان‌طور که کلاینت می‌چسباند می‌چسباند.
- **بدون `DATABASE_URL` کل مجموعه skip می‌شود، نه fail** — پس `npm test` روی ماشین بدون PostgreSQL هم کار می‌کند (۱۳۱ واحد سبز، ۲۵ skip).
- هش رمز یک‌بار برای کل اجرا حساب می‌شود؛ PBKDF2 با ۲۱۰ هزار تکرار عمداً کند است و پیش از هر تست پنج بار تکرار کردنش زمان اجرا را از **۳۹ به ۲۰ ثانیه** رساند.
- **اجرا باید ترتیبی باشد** (`--test-concurrency=1` در `package.json` و CI): فایل‌های API هرکدام دیتابیس را reset می‌کنند و موازی، جدول‌های همدیگر را truncate می‌کنند و به دلایلی بی‌ربط به کد fail می‌شوند.

**پوشش:** ۴۰۱ در برابر ۴۰۳ · توکن جعلی و توکن با کلید اشتباه · محدودکنندهٔ نرخ (قفل در تلاش نهم، و همکار روی همان IP سالم) · `requireRole` از دیتابیس (ادمینِ تنزل‌یافته با همان توکن ۴۰۳ می‌گیرد) · حساب غیرفعال ۴۰۱ · نبودن endpoint نوشتن audit · ذخیره و ثبت پیوند شریک · گارد حذف شریک در هر دو جهت · هم‌زمانی خوش‌بینانه · مجوز آزمایشگاه و ریسک به تفکیک نقش · «هر دپارتمان فقط خودش» · حذف فقط ادمین · قاعدهٔ IRC و گیت گرید A · دلیل الزامی انتخاب سورس · مجوزهای خواندن.

**دو ناسازگاری که خودِ تست‌ها پیدا کردند:**
1. **رد شدن به‌خاطر IRC نامعتبر ثبت نمی‌شد** در حالی که رد شدن به‌خاطر گرید فروشنده — درست چند خط پایین‌تر — ثبت می‌شد. تلاش مسدودشده هم شاهد است؛ حالا هر دو یکسان ثبت می‌شوند.
2. دو ادعای خودِ من در تست غلط بود (شکل payload آزمایشگاه، و نام ماژول audit انتخاب سورس که فارسی است) — تست‌ها اصلاح شدند نه کد.

**CI:** سرویس `postgres:15` با healthcheck اضافه شد و `prisma migrate deploy` پیش از تست‌ها اجرا می‌شود، وگرنه تست‌های API آنجا فقط skip می‌شدند و پوشش واقعی نداشتیم.

- تأیید: `tsc` ✓ · `vite build` ✓ · **۱۵۶ تست** با دیتابیس ✓ · **۱۳۱ سبز + ۲۵ skip** بدون دیتابیس ✓.

## سه مورد P1 (۱۴۰۵/۰۶/۱۰)
### P1-5: `vendor.risk` صاحب پیدا کرد
- این مجوز در هیچ الگوی نقشی جز `admin` نبود، در حالی که UI فرم ریسک و بک‌لاگ «ریسک ثبت‌نشده» را به QA نشان می‌داد. صفحه کاری را پیشنهاد می‌داد که سرور ردش می‌کرد — دقیقاً همان واگرایی‌ای که جدول سیاست برای جلوگیری از آن ساخته شد.
- FMEA فعالیت کیفی است و QA از قبل نتایج آزمایشگاهی و مواد را دارد، پس **`vendor.risk` به الگوی نقش `qa` اضافه شد**. بقیهٔ نقش‌ها فقط‌خواندنی می‌مانند و استثنای پر‌کاربر همچنان می‌تواند به هرکسی بدهدش. تست ماتریس به‌روزرسانی شد.

### P1-7: پیوند شریک حالا کلید خارجی واقعی است
- تا وقتی ستون‌ها نوشته نمی‌شدند این کار ممکن نبود؛ بعد از مهاجرت قبلی ممکن شد.
- `manufacturer_id`/`supplier_id` روابط Prisma گرفتند و مهاجرت `20260901140000_vendor_partner_fk` دو constraint با **`ON DELETE SET NULL`** ساخت — نه CASCADE: سورس ارزیابی، نتایج آزمایشگاهی و ریسک خودش را دارد و نباید با حذف رکورد شرکت نابود شود. گارد API از قبل جلوی حذف را می‌گیرد، پس این constraint پشتیبان است نه مسیر عادی.
- مهاجرت اول ارجاع‌های یتیم را پاک می‌کند وگرنه constraint روی دادهٔ موجود می‌شکند. تست زنده: `V_DANGLING → bp_deleted_long_ago` پاک شد و `V_OK → bp_a` سالم ماند ✓ · `UPDATE ... supplier_id='bp_ghost'` حالا با خطای دیتابیس رد می‌شود ✓ · حذف شریک، سورس را نمی‌کشد و فقط پیوند را NULL می‌کند ✓.

### P1-6: هم‌زمانی خوش‌بینانه، مستقل از تعداد پروسه
- **`vendors` نه `created_at` داشت نه `updated_at`** در حالی که تقریباً همهٔ جدول‌های دیگر دارند؛ پس هیچ راهی برای فهمیدن اینکه ردیف زیر دست نویسنده عوض شده وجود نداشت. مهاجرت `20260901150000_vendor_timestamps` هر دو را اضافه کرد.
- `saveVendorToDb` حالا پارامتر اختیاری `expectedUpdatedAt` می‌گیرد و پیش از نوشتن یک compare-and-set می‌زند (`updateMany` با `where: { id, updatedAt }`)؛ اگر صفر ردیف بخورد یعنی کس دیگری زودتر ذخیره کرده و `VendorConflictError` پرتاب می‌شود. هر شش هندلر PATCH آن `updatedAt` را که خودشان خوانده‌اند پاس می‌دهند.
- ۲۶ بلوک `catch` یکسان به helper مشترک `sendHandlerError` رفتند که تعارض را **۴۰۹** می‌کند (بقیهٔ رفتار دست‌نخورده). کلاینت بعد از P0-1 هر پاسخ ناموفق را می‌بیند، پس این پیام واقعاً به کاربر می‌رسد.
- **قفل درون‌پروسه حذف نشد** — لایهٔ اول می‌ماند و در استقرار تک‌کانتینری اصلاً نمی‌گذارد تعارض رخ دهد؛ بررسی نسخه لایهٔ دوم برای وقتی است که بیش از یک پروسه هست (ورسل، یا کانتینر دوم).
- تست زنده با شبیه‌سازی چندپروسه‌ای (قفل موقتاً غیرفعال): شش نوشتن هم‌زمان → **۱ موفق و ۵ تای ۴۰۹** با پیام «این رکورد هم‌زمان توسط شخص دیگری تغییر کرده است…» به‌جای پنج نوشتنِ گم‌شده. با قفل فعال (استقرار واقعی): هر شش ۲۰۰، یعنی کاربر ۴۰۹ بی‌مورد نمی‌گیرد. قلاب شبیه‌سازی از کد برداشته شد.
- تأیید: `tsc` ✓ · `vite build` ✓ · **۱۳۱ تست** ✓.

## پیوند سورس ↔ شریک تجاری — سه ایراد با یک ریشه (۱۴۰۵/۰۶/۱۰)
**ریشه:** ستون‌های `manufacturer_id` و `supplier_id` از زمان نرمال‌سازی وجود داشتند و ایندکس هم داشتند، ولی **هیچ مسیر نوشتنی مقداردهی‌شان نمی‌کرد**. `saveVendorToDb` شناسه‌ها را داخل فیلد متنیِ `contact_info` به‌شکل نشانگر `__BP_METAUI__:<mfg>:<sup>` پنهان می‌کرد، و مسیر خواندن **اول ستون** را می‌خواند و اگر خالی بود سراغ نشانگر می‌رفت.

سه پیامدی که زنده بازتولید شدند:
1. **تغییر تأمین‌کننده ذخیره نمی‌شد.** PATCH با همان payload برنامه: `HTTP 200`، نام عوض شد، `supplier_id` سر جایش ماند. رکورد با خودش متناقض می‌شد — اسم «فروشندهٔ ب»، پیوند «فروشندهٔ الف». گرید SOP، گیت گرید A و ستون شریک در اکسل همه از پیوند می‌خوانند، پس همه هنوز الف را نشان می‌دادند. ضمناً `supplierId` اصلاً در payload کلاینت نبود.
2. **سورس تازه ستونش NULL بود.** API پاسخ `"supplierId":"bp_s3"` می‌داد ولی دیتابیس `آدرس | __BP_METAUI__::bp_s3` ذخیره می‌کرد.
3. **گارد حذف شریک کور بود.** با `count({where:{supplierId:id}})` می‌شمرد؛ ستون همیشه NULL بود پس همیشه صفر. حذف «فروشندهٔ ج» که V3 به آن وابسته بود `HTTP 200` گرفت.

**رفع:**
- `saveVendorToDb` ستون‌های واقعی را در هر دو شاخهٔ upsert می‌نویسد و دیگر نشانگر نمی‌سازد.
- مهاجرت `20260901120000_vendor_partner_columns`: نشانگرهای موجود را به ستون‌ها منتقل و متن تماس را تمیز می‌کند؛ فقط ستون‌های NULL را پر می‌کند تا مقدار فعلی (که برنامه نشان می‌داده) بازنویسی نشود؛ و ارجاع‌های مرده را پاک می‌کند. تست زنده روی یک دیتابیس واقعاً «قدیمی»: `(null) | آدرس ⏎ __BP_METAUI__::bp_a` → `bp_a | آدرس` ✓ و `bp_gone` پاک شد ✓.
- `App.tsx` حالا `manufacturerId`/`supplierId` را هم در تشخیص تغییر و هم در payload پروفایل می‌فرستد. audit سرور خودش تغییر را می‌گیرد: `{"supplierId":"bp_a"} → {"supplierId":"bp_b"}` ✓.
- **گارد حذف، شکل قدیمی را هم می‌بیند.** بعد از رفع اولیه، تست نشان داد گارد هنوز رکوردی را که پیوندش فقط در نشانگر است نمی‌بیند (بکاپ قدیمی یا استقرار مهاجرت‌نکرده). یک بررسی `contains` روی متن اضافه شد — LIKE بدون ایندکس، ولی فقط روی عملیات حذف که دستی و نادر است. تست: حذف با وابستگیِ فقط‌نشانگر → ۴۰۰ ✓ · شریک بی‌وابستگی → ۲۰۰ ✓.
- منطق نشانگر از سه جای تکراری به `src/server/domain/partnerLink.ts` منتقل شد (خوانده‌شدن، حذف از متن، و «ستون بر نشانگر مقدم است») + ۸ تست.
- **اصلاح گزارش معماری:** آنجا نوشته بودم «ستون‌ها FK ندارند». درست بود ولی ناقص — ستون‌ها اصلاً نوشته نمی‌شدند. حالا که پر می‌شوند، افزودن FK واقعی (ردیف ۷ نقشهٔ راه) تازه ممکن شده است.
- تأیید: `tsc` ✓ · `vite build` ✓ · **۱۳۱ تست** ✓ (۱۲۳ + ۸) · بازتولید و تأیید زنده روی هر سه سناریو.

## رفع ایرادات P0 (۱۴۰۵/۰۶/۱۰)
> نقطهٔ بازگشت پیش از این کارها: **`ae522cf`** (بخش بعدی را ببین).

### P0-1: نوشتن بی‌صدا شکست می‌خورد — **بحرانی**
- `fetch` روی ۴xx/۵xx اصلاً reject نمی‌کند، پس `.catch()` روی `authFetch` فقط خطای شبکه را می‌دید. هر نوشتنِ سورس، UI را خوش‌بینانه به‌روز می‌کرد، توست موفقیت می‌داد، مقدار را در کش localStorage می‌نوشت و بعد ۴۰۳/۴۲۲/۵۰۰ را کامل نادیده می‌گرفت.
- **کنترل منفی (کد قدیم):** ادمینی که وسط کار دسترسی‌اش گرفته شده بود، IRC را ذخیره کرد → سه ۴۰۳ پشت‌سرهم، **هیچ پیام خطایی**، و مقدار ردشده (`1228123456789012`) در کش ماند در حالی که دیتابیس `NULL` بود.
- **بعد از رفع:** همان سناریو → توست با **پیام دقیق سرور** («عدم دسترسی: سطح دسترسی شما اجازهٔ…») به‌همراه دکمهٔ «بارگذاری دوبارهٔ رکورد»، و کش به `N/A` برگشت یعنی با دیتابیس یکی شد. مسیر مجاز بدون تغییر: سه ۲۰۰ و ذخیرهٔ درست.
- **راه‌حل:** `authWrite` در `services/authFetch.ts` که روی پاسخ ناموفق با پیام سرور throw می‌کند (و اگر سرور پیامی نداد، پیام فارسی به‌ازای هر status). صف `syncQueue` حالا await می‌شود و در صورت شکست `resyncVendorsFromServer()` حقیقت را از سرور می‌خواند — چون وقتی گام سوم از پنج گام رد شود، برگرداندن به `original` یک حالت غلط را با حالت غلط دیگری عوض می‌کند.
- ثبت و حذف سورس هم همین‌طور: ثبتِ ردشده ردیف را از فهرست برمی‌دارد، حذفِ ردشده رکورد را برمی‌گرداند.
- **اصلاح گزارش معماری:** ادعا کرده بودم «مواد و شرکا هم همین الگو را دارند» — **نادرست بود**. هر شش مسیر نوشتن مواد و شرکا از قبل `res.ok` را بررسی می‌کنند، rollback دارند و پیام سرور را نشان می‌دهند. این ایراد فقط مال ماژول سورس بود.
- ۶ تست جدید (`tests/authWrite.test.ts`).

### P0-2: هر کاربر واردشده می‌توانست در ردیابی تغییرات بنویسد
- `POST /api/audit-logs` فقط `requireAuth` داشت و ماژول/عملیات/شدت/before/after را از خود کلاینت می‌گرفت. **حذف شد** (قاعدهٔ ۲: فقط سرور می‌نویسد).
- تنها کالرش `VendorForm` بود و **هر دو رکوردی که می‌نوشت غلط بودند**: یکی تکرار همان چیزی بود که سرور هنگام `POST /api/business-partners` ثبت می‌کند؛ دیگری (`ChangeSupplier`) روی **تغییر فیلد فرم** شلیک می‌شد، پیش از هر ذخیره‌ای — یعنی لغو فرم هم رکوردی به‌جا می‌گذاشت که می‌گفت تأمین‌کننده عوض شد.
- `AuditService.logEvent` که فقط همین endpoint استفاده‌اش می‌کرد حذف شد. برچسب `ChangeSupplier` در تاکسونومی ماند تا رکوردهای قدیمی خوانا بمانند.

### P0-3: `requireRole` توکن را به دیتابیس ترجیح می‌داد
- نقش را مستقیم از JWT می‌خواند. توکن هفت‌روزه است، پس ادمینِ تنزل‌یافته یا **غیرفعال‌شده** تا یک هفته به هر هفت مسیر `/api/users*` دسترسی کامل داشت. حالا مثل `requirePermission` حساب را از دیتابیس می‌خواند؛ حساب نامعتبر ۴۰۱ و نقش ناکافی ۴۰۳ می‌گیرد.

### P0-4: CI اضافه شد
- `.github/workflows/ci.yml`: نصب با bun (هم‌راستا با `bun.lock`)، `prisma generate`، `tsc --noEmit`، تست‌ها، `vite build` و باندل سرور با esbuild — روی هر push و هر PR به main. هر پنج مرحله محلی سبز اجرا شد.
- مهاجرت عمداً در CI اجرا نمی‌شود چون `DATABASE_URL` زنده می‌خواهد.

- تأیید: `tsc` ✓ · `vite build` ✓ · **۱۲۳ تست** ✓ (۱۱۷ + ۶) · تست زندهٔ مرورگری با کنترل منفی · ۰ خطای کنسول.

## 🔖 نقطهٔ بازگشت (checkpoint)
**`ae522cf`** — آخرین وضعیت پایدارِ تأییدشده پیش از شروع رفع ایرادات P0.
در این نقطه: `tsc` سبز · `vite build` سبز · **۱۱۷ تست pass** · تست زندهٔ مرورگری بدون خطای کنسول.

بازگشت به این نقطه:
```bash
git reset --hard ae522cf        # دور ریختن همهٔ کارهای بعدی (تاریخچه بازنویسی می‌شود)
git revert <hash>               # برگرداندن فقط یک کامیت مشخص، بدون بازنویسی تاریخچه (امن‌تر)
git diff ae522cf --stat         # دیدن اینکه از آن نقطه به بعد چه چیزی عوض شده
```
tag محلی `checkpoint-pre-p0` هم روی همین کامیت هست؛ push آن روی این remote با ۴۰۳ رد می‌شود (پراکسی فقط برنچ کاری را می‌پذیرد)، پس **مرجع معتبر همان hash بالاست** که روی `origin/claude/vlse-modules-p4` وجود دارد.

## وضعیت کلی
- **برنچ کاری فعلی:** `claude/vlse-modules-p4` (از `main` تازه، کامیت `ece6323`) — کار جدید اینجا
- **PRها:** #2، #3، #6، #7، #8، #9، **#10** merged — همه در `main`. برنچ `claude/vlse-modules-p3` بسته شد؛ روی آن کامیت جدید نگذار.
- **سلامت:** `tsc` سبز ✓ · `vite build` سبز ✓ · تست‌ها **۱۰۸ pass / ۰ fail** ✓
- **git:** `main` روی `ece6323` است. کار جاری روی `claude/vlse-modules-p4`؛ PR جدید به `main` هنوز باز نشده.

## حالت آزمایشی محلی (Local/Demo Mode) — تست بدون دیتابیس
- برای تست روی Vercel بدون ساخت دیتابیس: env `VITE_ENABLE_LOCAL_DEMO=true` را ست کن (build-time). دکمهٔ «ورود آزمایشی (حالت لوکال)» در صفحهٔ ورود ظاهر می‌شود.
- با کلیک آن، کاربر دموی admin ساخته می‌شود و `app_local_mode=true` در localStorage ست می‌شود. کل داده‌ها فقط در همان مرورگر (localStorage: `app_db`, `app_business_partners`, `app_materials`) ذخیره می‌شوند.
- `authFetch` در این حالت هیچ درخواستی به backend نمی‌زند (پاسخ 503 مصنوعی، بدون reload) → همهٔ loaderها به cache محلی برمی‌گردند و نوشتن‌ها no-op می‌شوند (state از قبل در localStorage persist شده).
- محدودیت‌ها: audit/تاریخچه‌ها (که سمت‌سرور از audit_log بازسازی می‌شوند) در این حالت خالی‌اند. برای نسخهٔ نهایی روی سرور شرکت، این env را **ست نکن** تا فقط ورود عادی با دیتابیس فعال باشد.
- تست زنده: build استاتیک بدون backend → ورود دمو ✓ · بدون خطای صفحه ✓ · persist و رندر داده پس از reload ✓ · بدون حلقهٔ logout ✓.

## دپلوی (Vercel)
- **آداپتر:** `api/handler.ts` → بسته `dist/server.cjs` (از `npm run build`). `vercel.json` rewrite دارد.
- **env لازم:** `DATABASE_URL` (PostgreSQL معتبر، مثل Neon/Supabase) و `JWT_SECRET`.
- **مهاجرت + seed:** جدول‌ها باید با `prisma migrate deploy` ساخته شوند. کاربران پیش‌فرض روی **اولین اجرا** (cold start) اگر جدول users خالی باشد seed می‌شوند (`seedDefaultUsers`).
- **audit در حالت لوکال:** ثبت رویدادها در localStorage (`app_audit_log`، util `services/localAudit.ts`) از هندلرهای مرکزی (سورس/ماده/شریک: create/update/delete) با before/after؛ `AuditTrailView` در حالت لوکال از همین store می‌خواند (فیلتر/صفحه‌بندی/آمار سمت‌کلاینت). diff فیلدها هم کار می‌کند.
- **رفع باگ لاگین Vercel:** `prisma generate` به build + postinstall اضافه شد (بدون آن Prisma Client روی Vercel ساخته نمی‌شد و login کرش می‌کرد → صفحه HTML → خطای Safari «The string did not match the expected pattern»). login handler حالا try/catch دارد و همیشه JSON برمی‌گرداند؛ فرانت هم پاسخ غیر-JSON را با پیام واضح مدیریت می‌کند.
- **⚠️ نکتهٔ لاگین:** دو مسیر seed پسورد متفاوت دارند —
  - محلی (`prisma/seed.ts`): `admin / 123`
  - seed خودکار سرور (`server.ts` DEFAULT_USERS، روی Vercel اجرا می‌شود): `admin / 123456` + `mustChangePassword: true` (اجبار تغییر رمز در اولین ورود).

## رفع شکست بیلد Vercel روی دیتابیس خواب (۱۴۰۵/۰۶/۰۵)
- دیپلوی کامیت `433ef22` شکست خورد. لاگ Vercel: `P1001: Can't reach database server at ep-sparkling-pond-...neon.tech` و بعد `[migrate] Migration failed`.
- **علت دیف من نبود؛ گارد مهاجرتی خودم بود** که در «راه الف» اضافه شده بود و دقیقاً طبق طراحی عمل کرد. ولی طراحی ناقص بود: **Neon در پلن رایگان scale-to-zero دارد** و بعد از چند دقیقه بی‌کاری می‌خوابد؛ اولین اتصال بیدارش می‌کند ولی `prisma migrate deploy` بعد از ~۵ ثانیه تسلیم می‌شود. یعنی هر بیلدی که دیتابیس خواب باشد می‌شکست.
- **رفع:** تلاش مجدد با فاصلهٔ فزاینده (۵ / ۱۵ / ۳۰ ثانیه، مجموعاً ۴ تلاش) **فقط** برای خطاهای اتصال (`P1001`, `ECONNREFUSED`, `ETIMEDOUT`, `ENOTFOUND`). خاصیت ایمنی حفظ شد: خطای واقعی مهاجرت (`P3009`, `P3018`) یا احراز هویت (`P1000`) بدون تلاش مجدد و فوراً بیلد را متوقف می‌کند.
- **مورد دیگری که این ماجرا آشکار کرد:** من محلی فقط `vite build` می‌زدم که **فقط فرانت** را می‌سازد؛ کامیت‌هایی که `server.ts` را لمس می‌کنند باید با `npm run build` کامل (شامل `esbuild` و مهاجرت) و بوت `dist/server.cjs` تست شوند. از این پس همین کار را می‌کنم.
- تست: دیتابیس در دسترس → ۰ ثانیه، exit 0 · هاست ناموجود → ۴ تلاش در ۵۲ ثانیه سپس exit 1 · تعارض مهاجرت (`P3009`) → **۱ ثانیه**، بدون تلاش مجدد · بدون `DATABASE_URL` → skip و exit 0 · `npm run build` کامل + بوت `dist/server.cjs` + `/api/health` = ۲۰۰.

## ردیابی تغییرات — سه بسته (۱۴۰۵/۰۶/۰۹)
### بستهٔ ۱: مرتب‌سازی واقعی (باگ اصلی)
- **مرتب‌سازی ستون‌ها تزئینی بود:** `handleSort` فقط `sortField`/`sortDirection` را عوض می‌کرد؛ این دو نه در کوئری فرستاده می‌شدند نه در وابستگی‌های `fetchLogs` بودند، و سرور همیشه `orderBy: timestamp desc` می‌داد. تست قبل از اصلاح: ترتیب ردیف‌ها بعد از کلیک **دقیقاً یکسان** ماند.
- حالا `sortBy`/`sortDir` به سرور می‌رود و در `AuditService.orderFor` به `orderBy` تبدیل می‌شود (با `timestamp` به‌عنوان tie-breaker). مسیر جستجوی متنی هم همان ترتیب را می‌گیرد، وگرنه با تایپ در جعبهٔ جستجو مرتب‌سازی بی‌صدا از کار می‌افتاد.
- **ستون «سطح ریسک» عمداً غیرقابل‌مرتب‌سازی شد** (به‌جای ادعای دروغ): این ستون متن آزاد است و برای یک سطح دو املا دارد (`Info`/`Information`، قاعدهٔ ۱۶)، پس مرتب‌سازی متنی رتبه‌ای می‌سازد که معنا ندارد. فیلتر سطح ریسک (چیپ‌های بالای جدول) کار واقعی را انجام می‌دهد.
- هدرها `<button>` داخل `<th scope="col">` با `aria-sort` شدند.
- ~~محدودیت: مرتب‌سازی نام کاربر الفبای فارسی را رعایت نمی‌کرد~~ → **رفع شد** (پایین را ببینید).

### بستهٔ ۲: تم تیره و توکن‌ها
- جدول رنگ ۱۵ردیفی `actionLabels` هیچ جفت `dark:` نداشت؛ بج عملیات در تم تیره **۲.۵۲ → ۱۲.۹۲** رسید.
- نُه مورد `#0071E3` به `primary` و همهٔ `slate-*`ها (۳۱ مورد) به توکن تبدیل شدند؛ پنل‌های «خروجی سیستم» در درگاه جزئیات با `bg-foreground/text-background` معکوس می‌شوند.
- diff فیلدها (قلب یک رکورد audit)، کادر شرح فعالیت و چیپ گروه رویداد جفت `dark:` گرفتند: شرح فعالیت ۱۶.۲۲ · مقدار قبلی ۱۳.۶۱ · مقدار جدید ۱۵.۰۷ · چیپ گروه ۱۴.۷۹.
- عنوان صفحه از «Audit Trail» به «ردیابی تغییرات» تغییر کرد (هم در سرصفحه هم در breadcrumb).

### بستهٔ ۳: یکدستی با دو ماژول دیگر
- اسکلت بارگذاری با `aria-busy` به‌جای روکش تار روی صفحهٔ قبلی (تست: ۴۲ عنصر اسکلتی).
- انتخاب تعداد ردیف در صفحه (۱۰/۲۵/۵۰/۱۰۰) — این ماژول بیشترین رکورد را دارد.
- چهار `fetch` خام با توکن دستی به `authFetch` تبدیل شد، پس مدیریت ۴۰۱/۴۰۳ متمرکز می‌ماند.
- نام رکورد با `EntityName` رندر می‌شود (قاعدهٔ ۱۵).
- تست زنده: مرتب‌سازی سرور (date asc/desc و user asc/desc ترتیب‌های متفاوت می‌دهند) ✓ · Tab تا هدر و Enter → `aria-sort=descending` با حلقهٔ فوکوس و تغییر واقعی ترتیب ✓ · تعداد ردیف ۲۵ ✓ · `tsc`/`vite build`/`npm run build`/۱۰۳ تست ✓. رکوردهای آزمایشی حذف شدند.

## ردیابی تغییرات — پوشش کامل + تمیزکردن diff (۱۴۰۵/۰۶/۰۹، سه بستهٔ دوم)
### بستهٔ ۱: پوشش ثبت (ایجاد/ویرایش/حذف)
- **سابقهٔ فعالیت اصلاً ثبت نمی‌شد:** `PATCH /api/vendors/:id/logs` هیچ رکورد audit نمی‌نوشت؛ حالا با ماژول `Source Management` و `entityType: ActivityLog` ثبت می‌شود (Create/Delete بر اساس تغییر تعداد).
- **امتیاز دپارتمان‌ها، ریسک و «سورس منتخب»** بررسی شدند و هر سه ثبت می‌شوند: `Score` (Update)، `Risk Assessment` (Create/Update)، `SourceSelection` با اکشن‌های `CREATE_SOURCE_SELECTION`/`UPDATE_SOURCE_SELECTION` و severity=Warning.
- برچسب فارسی این چهار اکشن (به‌علاوهٔ `Reject` و `ChangeSupplier` که در داده بودند) به `auditTaxonomy.ts` اضافه شد، وگرنه در فیلتر به‌صورت انگلیسی خام دیده می‌شدند (قاعدهٔ ۱۶).

### بستهٔ ۲: متادیتا از داده جدا شد
- `ip/device/eventType` داخل `afterData` تا می‌شدند، پس در diff هر رکورد به‌عنوان «فیلد افزوده» ظاهر می‌شدند. حالا ستون‌های خودشان را دارند (مهاجرت `20260831160000_audit_event_metadata`؛ ۱۳۱ رکورد قدیمی back-fill و کلیدها از JSON پاک شدند — با گارد `jsonb_typeof(...) = 'object'` چون بعضی رکوردها JSON اسکالر داشتند).
- بلوک تکراری `scores` از before/after رکورد امتیاز حذف شد (همان چهار عدد دوبار می‌آمد).

### بستهٔ ۳: درگاه جزئیات مثل یک ماژول ادیت
- **اولویت با قبل/بعد:** diff حالا اولین چیز زیر عنوان «آنچه تغییر کرد» است با بج «N فیلد»؛ متادیتا و کارت‌های تخصصی به `<details>` بستهٔ «اطلاعات فنی رویداد» رفتند.
- `computeFieldDiff` بازگشتی شد: شیء تودرتو پیمایش می‌شود نه stringify، پس تغییر یک عدد یک سطر می‌دهد نه یک خط JSON ناخوانا.
- رویدادهای بدون تغییر داده (ورود/خروج) به‌جای فهرست‌کردن نام/نقش/نام‌کاربری به‌عنوان «افزوده»، یک جملهٔ توضیحی می‌گیرند.
- برچسب فارسی برای کلیدهای انتخاب سورس و ریسک اضافه شد (`vendorId` → «سورس منتخب» و…).
- تست زنده: تغییر تنها نمرهٔ QA (۹۰→۹۵) → درگاه **۲ فیلد** نشان می‌دهد (امتیاز SPS ۷۷→۷۹ و امتیاز کیفی ۹۰→۹۵) به‌جای یک بلوب ✓ · رکورد ورود → «بدون تغییر داده» ✓ · ثبت سورس منتخب و ریسک رکورد می‌سازند ✓ · `tsc`/`npm run build`/۱۰۳ تست ✓. دادهٔ آزمایشی محلی برگردانده و رکوردهای آزمایشی حذف شدند.

## نقش شریک در هدر «بررسی یکپارچه» و برچسب Unknown در آرشیو (۱۴۰۵/۰۶/۱۰)
- **هدر بررسی یکپارچه هر شرکتی را «تولید کننده» می‌نامید.** `mfgName` وقتی رکورد تولیدکننده‌ای پیدا نمی‌شد به نام خودِ گروه fallback می‌کرد، پس یک **فروشنده** — یا شرکتی که اصلاً در شرکای تجاری ثبت نشده — با برچسب «تولید کننده» چاپ می‌شد. نقش، واقعیتی ثبت‌شده دربارهٔ شرکت است (قاعدهٔ ۴)، نه جای پرکردن یک خط خالی.
  - حالا: تولیدکنندهٔ واقعی → «تولیدکننده : …» · فروشندهٔ واقعی → «فروشنده : … | گرید SOP» که وقتی تولیدکننده‌ای نیست **همان خط تیتر صفحه می‌شود** (قبلاً زیرنویس ریز بود) · بدون هیچ رکورد شریک → فقط نام + چیپ «نوع شریک ثبت نشده».
  - `supGrade` نداشته به‌جای «نامشخص» می‌شود «ارزیابی نشده» (نامشخص یعنی گم شده، نه ارزیابی‌نشده).
- **برچسب `Unknown` زیر نام تأمین‌کننده در آرشیو،** نام لاتینِ رکورد بود: ردیف‌های وارداتی مقدار «Unknown»/«N/A» را به‌صورت متن واقعی دارند و زیر نام شرکت طوری چاپ می‌شد که انگار نام انگلیسی شرکت همین است. `cleanPlaceholder` (تعمیم `cleanCountry`) این مقادیر را null می‌کند و خط اصلاً رندر نمی‌شود — در آرشیو، هدر بررسی یکپارچه و کارت‌های فهرست آن.
- تست زنده با سه شرکت (تولیدکننده / فروشنده / بدون رکورد شریک): هدر هر سه درست ✓ · ردیف بدون نام لاتین دیگر «Unknown» ندارد و دو ردیف دیگر نام واقعی‌شان را نگه داشتند ✓ · ۰ خطای کنسول · `tsc`/`vite build`/۱۱۷ تست ✓.

## بازبینی خروجی اکسل — سه بسته (۱۴۰۵/۰۶/۱۰)
### بستهٔ ۱: درستی داده
- **رنگ‌آمیزی ستون امتیاز و ریسک یک ستون جابه‌جا بود، پس هیچ‌وقت اجرا نمی‌شد.** «امتیاز ارزیابی کل» ستون ۱۳ و «سطح ریسک» ستون ۱۴ است، ولی کد استایل `colIndex === 14` و `=== 15` را چک می‌کرد؛ یعنی تست گرید روی سلول ریسک و تست ریسک روی سلول کد QC اجرا می‌شد و **هر دو ستون بی‌رنگ می‌ماندند**. همهٔ شماره‌ها به ثابت‌های نام‌دار `COL.*` تبدیل شدند.
- **رابریک گرید در اکسل، رابریک SOP بود نه رابریک سورس.** یک سورس با امتیاز ۳۵ در اکسل «Blacklist» و در فرم چاپی «Grade D» می‌شد. مقیاس سورس حالا در **`src/utils/vendorRank.ts`** یک‌جا تعریف شده و هم اکسل هم `PrintableForms` از همان می‌خوانند. کپی دستی `calculateOverallScore` با وزن‌های ثابت هم حذف شد و به `vendorUtils` (با `CALCULATION_WEIGHTS` قابل‌تنظیم) وصل شد.
- **`selections` در دو مسیر پاس داده نمی‌شد**، پس ستون «سورس منتخب» در خروجی صفحهٔ دسته‌بندی و در پروندهٔ تک‌تأمین‌کننده همیشه «—» بود — در پرونده، تناقض با شیت ۱ که تعداد منتخب‌ها را می‌شمرد.
- **RTL هیچ‌وقت نوشته نمی‌شد.** `ws['!views'] = [{RTL:true}]` را کتابخانه بی‌صدا دور می‌ریزد؛ فقط view سطح workbook تگ `rightToLeft` تولید می‌کند. با نوشتن فایل و خواندن XML آن تأیید شد. هر چهار خروجی اصلاح شدند (قبلاً فقط پروندهٔ تک‌تأمین‌کننده درست بود).

### بستهٔ ۲: قابل‌کارکردن‌بودن فایل
- `N/A` لاتین (نام ماده/CAS/IRC) به «ثبت‌نشده» تبدیل شد تا با بقیهٔ سلول‌های همان ردیف یکی باشد.
- «ردیف» عدد واقعی شد (قبلاً `.toString()` و هشدار «عدد به‌صورت متن» اکسل) و ستون تازهٔ **«امتیاز عددی (۰-۱۰۰)»** در انتها اضافه شد تا بشود روی امتیاز مرتب‌سازی/میانگین گرفت؛ ستون متنی سر جایش ماند تا ماکرو/پیوت کسی نشکند. سورس ارزیابی‌نشده در این ستون **خالی** است نه صفر.
- `!autofilter` اضافه شد و **ادغام عمودی سلول‌های ماده حذف شد**: اکسل روی شیت دارای merge در ردیف‌های داده درست فیلتر نمی‌کند. گروه‌بندی با همان رنگ‌بندی متناوبِ موجود دیده می‌شود.
- فریز هدر **اضافه نشد**: `xlsx-js-style` عنصر `<pane>` نمی‌نویسد، پس تنظیمش فقط ادعای پین‌شدن بود (با round-trip تأیید شد).

### بستهٔ ۳: صداقت گزارش
- دکمهٔ تازهٔ **«خروجی نمای فعلی (n)»** در آرشیو: همان ردیف‌های روی صفحه با فیلترهای اعمال‌شده. دو دکمهٔ دیگر عمداً کل آرشیو/یک دسته می‌دهند.
- شیت فیلترشده یک **سطر عنوان** بالای هدر می‌گیرد (محدودهٔ فیلتر، تعداد ردیف، تاریخ) — همان چیزی که خروجی چاپی دارد؛ خروجی بدون فیلتر این سطر را ندارد پس هدر همان سطر اول می‌ماند.
- خروجی audit به‌جای جدول واژگان دستی از `auditTaxonomy.ts` می‌خواند (قاعدهٔ ۱۶) و نام ماژول را فارسی می‌نویسد؛ `Information` هم که سرور می‌نویسد و در نگاشت نبود اضافه شد (قبلاً انگلیسی خام صادر می‌شد).
- **`excelExport.ts` تا امروز اصلاً قابل import در تست نبود** (interop سی‌جی‌اس: `Cannot read properties of undefined (reading 'aoa_to_sheet')`) — به همین دلیل باگ ستون‌ها سال‌ها دیده نشد. رفع شد و **۹ تست جدید** روی خودِ ورک‌شیت اضافه شد (`tests/excelExport.test.ts`).
- تأیید: `tsc` ✓ · `vite build` ✓ · **۱۱۷ تست** ✓ (۱۰۸ + ۹).

## بهبود ماژول آرشیو و خروجی چاپی — سه بسته (۱۴۰۵/۰۶/۱۰)
### بستهٔ ۱: ادعاهای نادرست در فرم چاپی
- **سورس ارزیابی‌نشده گرید D می‌گرفت.** `getRankParams` برای امتیاز خالی به پایین‌ترین بازه می‌افتاد، پس رکوردی که هنوز ارزیابی نشده در سند چاپی «گرید D» چاپ می‌شد — یعنی سندی که به ممیز تحویل می‌شود ادعای رد‌شدگی می‌کرد. حالا وقتی نه امتیاز کلی هست و نه گرید واقعی (`A|B|C|D`)، «ارزیابی نشده» چاپ می‌شود.
- همین اشکال در ستون گرید لیست هم بود («— (۰)» و «new (۰)») → حالا «ارزیابی‌نشده».
- «۱ از ۱» که همیشه چاپ می‌شد حذف شد (ادعای شمارهٔ صفحه بود بدون اینکه صفحه‌بندی واقعی باشد).
- همهٔ تاریخ‌ها با `toJalaliDisplay()` شمسی چاپ می‌شوند (قبلاً `2026-08-27` میلادی).
- گیت دکمهٔ چاپ از نقش به `can()` منتقل شد (قاعدهٔ ۱۴)؛ `qa` هم مثل `admin` دسترسی دارد.
- **سطح ریسکِ ارزیابی‌نشده `N/A` چاپ می‌شد** — روی سندی که به ممیز می‌رود مبهم است (نمی‌گوید ریسک ارزیابی شده و نتیجه‌اش مصداق ندارد، یا اصلاً ارزیابی نشده). حالا مثل گرید «ارزیابی نشده» چاپ می‌شود، هم در فرم تک‌سورس و هم در ستون سطح ریسکِ لیست آرشیو (که «ثبت‌نشده» بود) تا یک عبارت واحد بماند.

### بستهٔ ۲: خروجی چاپی خودِ آرشیو
- `PrintableArchiveList` اضافه شد: A4 افقی، `thead` تکرارشونده در هر صفحه (`display: table-header-group`)، `break-inside: avoid` روی ردیف‌ها، خلاصهٔ فیلترِ اعمال‌شده در سربرگ (تا سند بدون متن همراه هم معنا داشته باشد) و سه بلوک امضا.
- ستارهٔ «★ منتخب» در همین خروجی هم می‌آید (از `selectionForVendor`، بدون کپی قاعده).

### بستهٔ ۳: قابل‌استفاده بودن لیست
- منوی خروجی واقعاً با صفحه‌کلید کار می‌کند (Enter باز، Escape بسته، کلیک بیرون بسته) — قبلاً فقط با ماوس.
- فیلتر وضعیت برگشت، جستجو روی نام شریک اضافه شد، و انتخاب تعداد ردیف (۲۰/۵۰/۱۰۰/۲۰۰).
- رنگ‌های ثابت باقی‌مانده به توکن تبدیل شدند. کنتراست زیرعنوان **۳.۷۱ → ۷.۳۴** در تم تیره (۴.۴۳ روشن) — هر دو AA. جداکننده‌های ردیف/کادر روی توکن مشترک `border-border` نشستند (همان چیزی که بقیهٔ ماژول‌ها دارند)؛ نسبت پایینشان جداکنندهٔ تزئینی است و مشمول WCAG 1.4.11 نیست.
- تأیید: `tsc` ✓ · `vite build` ✓ · ۱۰۸ تست ✓ · تست زندهٔ مرورگری روی هر سه بسته با ۰ خطای کنسول.

## نشانهٔ «سورس منتخب» در آرشیو، اکسل و فرم چاپی (۱۴۰۵/۰۶/۱۰)
- **مسئله:** تصمیم «سورس منتخب» فقط در صفحهٔ دسته‌بندی دیده می‌شد. آرشیو — همان چیزی که چاپ و به ممیز تحویل می‌شود — برای یک ماده چند ردیف کاملاً هم‌شکل نشان می‌داد بدون اینکه معلوم باشد کدام‌یک انتخاب رسمی است.
- **قاعدهٔ مشترک در `src/utils/sourceSelection.ts`:** تصمیم با کلید **ماده + دسته** ثبت می‌شود، نه با تأمین‌کننده. پس ستاره به ردیف (تأمین‌کننده+ماده) تعلق دارد نه به شرکت — یک شرکت می‌تواند برای متفورمین منتخب باشد و برای ایبوپروفن نه. علامت‌زدن همهٔ ردیف‌های آن شرکت یک ادعای نادرست روی سند GxP بود. هر سه سطح (لیست، اکسل، چاپ) از همین یک تابع می‌پرسند تا واگرا نشوند.
- **در لیست آرشیو:** چیپ ★ «منتخب» کنار نام، با tooltip حاوی نام ماده، دلیل، ثبت‌کننده و تاریخ — چون ستارهٔ خالی این سؤال را می‌سازد که «منتخب برای چه، به تصمیم چه کسی؟». به‌علاوه چیپ فیلتر **«فقط منتخب‌ها (n)»** که وقتی هیچ انتخابی ثبت نشده اصلاً نمایش داده نمی‌شود تا نتیجهٔ خالی پیشنهاد نکند.
- **در اکسل:** به‌جای گلیف ستاره (که در فیلتر و pivot به‌درد نمی‌خورد) **سه ستون واقعی** در انتهای شیت اضافه شد: «سورس منتخب» (بله/—)، «دلیل انتخاب»، «ثبت‌کنندهٔ انتخاب». عمداً در **انتها** تا موقعیت ستون‌های موجود عوض نشود (فیلتر ذخیره‌شده یا ماکرو کسی نشکند). سلول «بله» پس‌زمینهٔ کهربایی می‌گیرد تا در شیت بلند پیدا شود.
- **در فرم چاپی (PDF):** نوار کهربایی زیر سربرگ با متن «سورس منتخب»، نام ماده، دلیل، ثبت‌کننده و تاریخ. اگر سورس منتخب نباشد **هیچ چیز چاپ نمی‌شود** — نوشتن «منتخب: خیر» روی سندی که امضا و بایگانی می‌شود نویز است، و نبودِ نوار خودش پاسخ است.
- تست زنده: ثبت `s1` به‌عنوان منتخب متفورمین (سه تأمین‌کننده رقیب دارد) → در آرشیو **۱ از ۹** ردیف ستاره گرفت و دو متفورمینِ دیگر بدون ستاره ماندند ✓ · فیلتر → ۹ به ۱ ردیف و همه ستاره‌دار ✓ · اکسل: ۲۰ ستون، سه ستون آخر درست، **۱ از ۹** ردیف «بله» با دلیل و ثبت‌کننده ✓ · فرم چاپی: نوار با دلیل و امضا ✓ · صفر خطای صفحه.
- `tsc`/`npm run build`/۱۰۸ تست سبز.

## رفع یافته‌های ۶ تا ۱۱ (۱۴۰۵/۰۶/۱۰)
شش مورد باقی‌ماندهٔ گزارش تست نهایی. مثل قبل، هر رفع با همان آزمونی که شکست خورده بود دوباره تست شد.

- **۶ — `/api/health` دروغ می‌گفت:** بدون هیچ کوئری‌ای `{"status":"ok"}` برمی‌گرداند، پس با PostgreSQL خاموش هم «سالم» بود در حالی که همهٔ مسیرهای داده ۵۰۰ می‌دادند. حالا `SELECT 1` می‌زند و در صورت شکست **۵۰۳** با `database: "down"` می‌دهد. نتیجه ۲ ثانیه کش می‌شود تا مانیتورِ ثانیه‌ای به یک کوئری در ثانیه تبدیل نشود (۵۰ فراخوانی = ۴۶۰ms). تست: DB بالا → ۲۰۰/`up` · DB خاموش → **۵۰۳**/`down` · برگشت DB → دوباره ۲۰۰ بدون ری‌استارت.
- **۷ — ورودی نامعتبر ۵۰۰ می‌داد و متن داخلی Prisma را لو می‌داد:** سه رفع لایه‌ای — `req.body` خالی به `{}` تبدیل می‌شود (بدنهٔ نبوده باعث TypeError می‌شد)، `materialDataFromBody` هر فیلد را با `asText()` امن می‌خواند (عدد یا آرایه در `nameFa` دیگر `.trim is not a function` نمی‌دهد)، و یک error handler روی `/api` هر خطای پیش‌بینی‌نشده را در لاگ سرور می‌نویسد و به کلاینت پیام عمومی می‌دهد. تست: بدنهٔ خالی / null / آرایه / نوع اشتباه / JSON خراب → همگی **۴۰۰** با پیام فارسی، صفر نشت داخلی.
- **۸ — مسیر API ناموجود ۲۰۰+HTML برمی‌گرداند:** هر دو catch-all (استاتیک و Vite) هر چیزی را با `index.html` جواب می‌دادند. یک هندلر `app.use("/api", …)` **قبل از** آن‌ها ثبت شد و حالا **۴۰۴ JSON** می‌دهد. دیپ‌لینک‌های SPA (`/foo`) همچنان ۲۰۰ می‌مانند.
- **۹ — ۴۰۳ تضمین‌شده برای هر کاربر غیرادمین:** داشبورد `/api/audit-logs` را برای همه صدا می‌زد. حالا اول `can(user,'audit.read')` چک می‌شود. تست: `finance` روی صفحهٔ اصلی → **صفر** فراخوانی ناموفق (قبلاً هر بار یک ۴۰۳).
- **۱۰ — `PORT` خوانده نمی‌شد:** عدد ۳۰۰۰ هاردکد بود، پس مقدار `PORT` در Dockerfile/compose/ecosystem بی‌اثر بود. حالا خوانده و اعتبارسنجی می‌شود (مقدار نامعتبر → هشدار و بازگشت به ۳۰۰۰). تست: `PORT=3200` → سرور روی ۳۲۰۰ بالا آمد و ۳۰۰۰ خالی ماند.
- **۱۱ — سقف صفحه‌بندی audit:** `limit` روی **۲۰۰** و `page` روی حداقل **۱** clamp شد. تست: `limit=999999999` → ۲۰۰ ردیف · `page=-1` و `page=abc` → **۲۰۰** (قبلاً ۵۰۰ با متن خطای Prisma).

**رگرسیون:** هر ۱۵ مسیر بدون صفحهٔ سفید و بدون خطا · فیکس‌های ۱ تا ۵ همگی پابرجا (unauth ۴۰۱ · finance→users ۴۰۳ · CSP موجود · X-Powered-By حذف · **۰ از ۵** نوشتن گم‌شده · ۲۰۰ درخواست هم‌زمان ۲۰۰/۲۰۰) · `tsc`/build سبز · ۱۰۸ تست pass.

## رفع پنج یافتهٔ تست نهایی پیش از تحویل به IT (۱۴۰۵/۰۶/۱۰)
تست کامل امنیت و پایداری روی باندل production گرفته شد (۲۷ آزمون). پنج مورد رد شد و همه رفع شدند؛ هر رفع با **همان آزمونی که قبلاً شکست خورده بود** دوباره تست شد.

### ۱ (بحرانی) — کلید ثابت JWT در مخزن
- `server.ts:983` اگر `JWT_SECRET` ست نبود به یک رشتهٔ ثابتِ داخل کد برمی‌گشت. اثبات‌شده: با آن مقدار یک توکن ادمین ساختم و `GET /api/users` فهرست کامل کاربران را برگرداند (۲۰۰).
- fallback حذف شد. در production نبودِ کلید یا کلید کوتاه‌تر از ۳۲ کاراکتر → **پروسه با exit 1 و پیام فارسی راهنما بالا نمی‌آید** (همان قاعدهٔ fail-fast که `requirePrisma` برای دیتابیس دارد). خارج از production یک کلید تصادفی per-process ساخته می‌شود تا توسعه بدون تنظیمات کار کند و هیچ ثابتی در مخزن نماند.
- تست مجدد: بدون کلید → exit 1 ✓ · کلید کوتاه → exit 1 ✓ · dev → بالا می‌آید با هشدار ✓ · توکن ساخته‌شده با کلید قدیمی → **۴۰۱** ✓.

### ۲ (بحرانی) — از دست رفتن بی‌صدای داده در نوشتن هم‌زمان
- هر شش `PATCH /api/vendors/:id/*` کل رکورد را read-modify-write می‌کنند. دو درخواست هم‌زمان → کندتر نسخهٔ کهنهٔ خودش را می‌نوشت. اثبات‌شده: `/contact` هم‌زمان با `/scores` → **۵ از ۵ بار** تغییر contact کامل گم شد، هر دو درخواست هم ۲۰۰ گرفتند.
- قفل per-vendor (`serializeVendorWrites`) روی هر شش PATCH و روی DELETE اضافه شد: درخواست‌های یک سورس پشت‌سرهم، سورس‌های مختلف هم‌چنان موازی. قفل با رویدادهای `finish` و `close` آزاد می‌شود تا قطع ارتباط کلاینت آن را برای همیشه نگه ندارد.
- **محدودیتی که باید بدانید:** این قفل درون‌پروسه‌ای است و یک نمونهٔ Node را پوشش می‌دهد (همان شکل استقرار: یک کانتینر یا PM2 در fork mode). اگر روزی چند نمونه پشت load balancer اجرا شد، به قفل سطح دیتابیس نیاز است.
- تست مجدد: **۰ از ۵** نوشتن گم‌شده ✓.

### ۳ (مهم) — brute force و ضعف هش رمز
- قبلاً: ۳۰ تلاش ناموفق در **۳۴۳ms** بدون هیچ محدودیتی، و PBKDF2 با فقط **۱۰۰۰ تکرار**.
- محدودکنندهٔ نرخ ورود بدون افزودن وابستگی نوشته شد: **۸ تلاش برای هر نام کاربری** و **۶۰ برای هر IP** در پنجرهٔ ۱۵ دقیقه‌ای، سپس ۱۵ دقیقه مسدودی با هدر `Retry-After` و ثبت Critical در audit. دو آستانه عمداً فاصله دارند: همهٔ کارکنان از یک شبکهٔ داخلی می‌آیند، پس آستانهٔ سخت‌گیرانهٔ IP یعنی یک نفر که رمزش را اشتباه بزند کل ساختمان را قفل می‌کند. ورود موفق شمارنده‌ها را پاک می‌کند.
- PBKDF2 به **۲۱۰٬۰۰۰ تکرار** (توصیهٔ OWASP) رسید؛ اندازه‌گیری: ۱.۱ms → ۱۷۷ms یعنی **۱۵۶ برابر** کندتر برای حدس‌زننده. هش‌های قدیمی نمی‌شکنند: مقدار ذخیره‌شدهٔ بدون برچسب با ۱۰۰۰ تکرار تأیید می‌شود و **در همان لحظهٔ ورود موفق** به فرمت جدید (`pbkdf2$210000$…`) ارتقا می‌یابد — بدون مهاجرت و بدون بازنشانی رمز کسی.
- شاخهٔ پذیرش رمز plaintext از `verifyPassword` حذف شد.
- تست مجدد: تلاش نهم **۴۲۹** ✓ · رمز درست حین مسدودی هم ۴۲۹ ✓ · **همکار روی همان IP ۲۰۰** ✓ · هش قدیمی → ورود موفق و ارتقای خودکار، رمز بدون تغییر ✓.

### ۴ (مهم) — secretها در مخزن
- `docker-compose.yml` هر دو مقدار (`POSTGRES_PASSWORD`, `JWT_SECRET`) را کامیت‌شده داشت. حالا از `.env` خوانده می‌شوند با فرم `${VAR:?پیام}` تا compose در نبود مقدار **صریحاً شکست بخورد** نه اینکه با پیش‌فرض بالا بیاید.
- ضمناً: healthcheck برای postgres + `depends_on: condition: service_healthy` (قبلاً فقط وجود کانتینر را منتظر می‌ماند نه آمادگی دیتابیس)، انتشار پورت ۵۴۳۲ روی هاست بسته شد، و `.env.example` با توضیح هر متغیر و دستور ساخت کلید بازنویسی شد.

### ۵ (متوسط) — نبود هدرهای امنیتی
- CSP، `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, `Permissions-Policy`, `Cross-Origin-*` اضافه و `X-Powered-By` حذف شد. بدون helmet نوشته شد: برای یک اپ same-origin همین مجموعه کافی است، هر مقدار کنار دلیلش دیده می‌شود و وابستگی تازه‌ای به استقرار اضافه نمی‌کند.
- `Strict-Transport-Security` فقط وقتی ست می‌شود که درخواست از HTTPS آمده باشد (مستقیم یا از طریق `X-Forwarded-Proto`)، وگرنه روی سرور داخلیِ HTTP مرورگر را به اسکیمی قفل می‌کرد که سرور سرو نمی‌کند.
- تست رگرسیون زیر CSP: هر ۱۲ مسیر بدون صفحهٔ سفید و **صفر CSP violation**، فونت و استایل اعمال‌شده، خروجی اکسل (دانلود blob) سالم، نمودارها رندر، تم تیره درست ✓.

**وضعیت نهایی:** `tsc` سبز · `npm run build` سبز · **۱۰۸ تست pass** (دو تست قدیمی که رفتار قبلیِ هش را تثبیت می‌کردند بازنویسی شدند و سه تست تازه برای سازگاری هش قدیمی، `needsRehash` و ردِ plaintext اضافه شد) · ۲۰۰ درخواست هم‌زمان همچنان ۲۰۰/۲۰۰.

**یافته‌های ۶ تا ۱۱ گزارش تست (health بی‌اعتبار، ۵۰۰ روی ورودی نامعتبر، ۴۰۴ نبودن مسیر API، ۴۰۳ تکراری داشبورد، بی‌اثر بودن `PORT`، نبود سقف صفحه‌بندی audit) در این مرحله عمداً دست‌نخورده ماندند.**

## ذخیره صفحه را عوض نمی‌کند + ثبت پشت‌سرهم (۱۴۰۵/۰۶/۱۰)
- **مسئله:** `handleAddVendor` در آخرین خطش `handleSelectVendor` را صدا می‌زد، پس بعد از هر ثبت سورس کاربر روی صفحهٔ رکورد تازه می‌افتاد و باید دستی برمی‌گشت. برای کارشناسی که از دیتای قدیمی پشت‌سرهم ثبت می‌کند، هر رکورد یعنی یک بار ترک صفحه و بازگشت. (ثبت ماده و شریک از قبل درست بودند و روی فهرست می‌ماندند — فرم سورس تنها استثنا بود.)

### بستهٔ ۱: بعد از ثبت، همان‌جا بمان
- `handleSelectVendor` از انتهای `handleAddVendor` حذف شد؛ فرم بسته می‌شود و کاربر روی همان دستهٔ سورس می‌ماند.
- برای اینکه دسترسی به رکورد تازه از دست نرود، توست یک **دکمهٔ اقدام** گرفت: «مشاهده و امتیازدهی». هر کس بخواهد یک کلیک، هر کس نخواهد هیچ کلیکی.
- `notify()` پارامتر چهارم `action?: { label, run }` گرفت؛ توستِ دکمه‌دار حداقل ۷ ثانیه می‌ماند و تایمر قبلی پاک می‌شود تا توست دوم اولی را زودتر نبندد.

### بستهٔ ۲: «ذخیره و ثبت بعدی»
- در هر سه فرم (سورس، ماده، شریک تجاری) دکمهٔ دوم اضافه شد: ذخیره می‌کند، فرم را **در جا** خالی می‌کند و روی همان صفحه می‌ماند. فقط در حالت ثبت جدید دیده می‌شود، نه ویرایش.
- آنچه عمداً حفظ می‌شود: دسته و نوع سورس / نقش ماده و فارماکوپه / نوع شریک — چون دسته‌ای که یک‌جا وارد می‌شود معمولاً یک‌جور است و پاک‌کردنشان یعنی تکرار همان دو انتخاب در هر رکورد.
- شمارندهٔ نشست و فهرست آخرین ثبت‌ها نمایش داده می‌شود، وگرنه «ذخیره و ثبت بعدی» صفحه را خالی می‌کرد بدون اینکه چیزی از کار انجام‌شده نشان بدهد. دکمهٔ «انصراف» بعد از اولین ثبت به «پایان» تغییر نام می‌دهد.
- در فرم سورس، `resetForNext` علاوه بر خالی‌کردن، snapshot محافظ تغییرات ذخیره‌نشده را هم بازمی‌گیرد و فوکوس را به فیلد ماده برمی‌گرداند.

### بستهٔ ۳: تثبیت قاعده
- قاعده در `CLAUDE.md` ثبت شد: **ذخیره هرگز صفحه را عوض نمی‌کند؛ ناوبری همیشه دکمه‌ای است که کاربر می‌زند.** تنها استثنا حذف است (رکورد دیگر وجود ندارد).
- بررسی شد که بقیهٔ مسیرها از قبل تبعیت می‌کنند: بعد از ثبت امتیاز، ارزیابی ریسک و نتایج آزمایشگاهی کاربر روی همان صفحهٔ سورس می‌ماند.

**تست زنده:** سه ماده پشت‌سرهم با «ذخیره و ثبت بعدی» → مودال یک بار هم بسته نشد، فرم هر بار خالی شد، شمارنده «۱ → ۲ → ۳ ماده در این نشست ثبت شد»، و هر سه رکورد در دیتابیس ✓. ثبت سورس → URL از `#/category/foreign/new` به `#/category/foreign` رفت (نه به صفحهٔ رکورد) و توست «سورس «…» ثبت شد» با دکمهٔ اقدام آمد؛ کلیک روی دکمه → `#/category/foreign/vendor/vhde9` ✓. `tsc`/`npm run build`/۱۰۶ تست سبز. دادهٔ آزمایشی حذف شد.

## هشدار «تغییرات ذخیره‌نشده» در همهٔ فرم‌ها (۱۴۰۵/۰۶/۱۰)
- **وضعیت قبل (اندازه‌گیری‌شده در مرورگر):** محافظ فقط در فرم سورس وصل بود. تایپ نام در فرم مادهٔ جدید و زدن Esc → مودال **بی‌صدا** بسته می‌شد؛ همین برای فرم شریک تجاری با کلیک بیرون کادر و برای فرم کاربر جدید با Esc. هیچ `beforeunload` هم در برنامه نبود.

### بستهٔ ۱: محافظ داخل خود `FormModal`
- به‌جای وصله‌زدن به هر فرم، دو پراپ اختیاری `unsavedChanges?: () => boolean` و `unsavedLabel?: string` به `FormModal` اضافه شد. Esc و کلیک بیرون کادر — دو راهی که مودال بدون قصد کاربر بسته می‌شود — اگر فرم dirty باشد به‌جای بستن، تأییدیه‌ای **روی همان فرم** بالا می‌آورند (فرم زیرش دیده می‌شود تا کاربر بداند دربارهٔ چه تصمیم می‌گیرد). دکمهٔ «انصراف»/«ذخیره» خود فرم دست‌نخورده است، چون فشردن انصراف خودش یک پاسخ است.
- چون تک‌نقطه‌ای است، **هر مودال آینده هم خودبه‌خود پوشش دارد**.
- هوک مشترک `src/utils/useDirtySnapshot.ts`: مقایسه با snapshotِ لحظهٔ باز شدن، نه «باز بودن فرم». پارامتر `extraDirty` برای حالتی است که چیزی بیرون از شیء فرم زندگی می‌کند (فایل Specification انتخاب‌شده).
- وصل شد به: فرم ماده، فرم شریک تجاری (هم فیلدها هم مدارک SOP)، فرم کاربر، بازنشانی رمز، و دیالوگ سطح دسترسی.

### بستهٔ ۲: صفحهٔ جزئیات سورس و بستن تب
- **`VendorDetail` پراپ `registerNavGuard` را می‌گرفت و هرگز استفاده نمی‌کرد** — پراپ مرده. فرم امتیازدهی و فرم ارزیابی ریسک بخش‌های درون‌صفحه‌ای همین صفحه‌اند، پس ترک صفحه آن‌ها را بی‌صدا می‌برد. هر دو فرم حالا با `onDirtyChange` وضعیت خود را گزارش می‌کنند و صفحه همان محافظ فرم سورس را ثبت می‌کند. سیگنال صادقانه است: `modifiedDepts` (امتیازدهی) و مقایسه با ارزیابی ذخیره‌شده (ریسک)، نه صرفِ باز بودن.
- `beforeunload` سراسری به همان `navGuardRef` وصل شد، پس بستن تب یا F5 هم پوشش دارد. متن آن پیام را مرورگر تعیین می‌کند و قابل تغییر نیست.

### بستهٔ ۳: یکدستی متن‌ها و تأییدیه‌ها
- متن «فرم ویرایش باز است…» برای فرم ثبت *جدید* غلط بود؛ بازنویسی شد و `unsavedLabel` می‌گوید دقیقاً چه چیزی از بین می‌رود («اطلاعات مادهٔ جدید»، «تغییرات این شریک تجاری»، …).
- در هر دو تأییدیه، **پاسخ امن اول و با استایل primary** است («بازگشت به فرم») و «خروج بدون ذخیره» ثانویه — دیالوگی که وسط کار کسی را قطع می‌کند نباید کلیکِ رفلکسی را به دورانداختن کار او بدهد.
- چهار تأییدیهٔ مخرب (حذف ماده، حذف فایل Specification، حذف شریک، لیست سیاه) یکدست شدند: `role="alertdialog"` + `closeOnBackdrop={false}` (قاعدهٔ ۸).

**تست زنده:** فرم دست‌نخورده + Esc → بدون هشدار بسته می‌شود ✓ · تایپ + Esc → هشدار و فرم باز می‌ماند ✓ · «بازگشت به فرم» → متن تایپ‌شده حفظ می‌شود ✓ · «خروج بدون ذخیره» → بسته می‌شود ✓ · فرم شریک + کلیک بیرون کادر → هشدار ✓ · تغییر یک اسلایدر امتیاز و رفتن به صفحهٔ دیگر → هشدار و ماندن در صفحه ✓ · `beforeunload`: صفحهٔ تمیز لغو نمی‌کند، بعد از تغییر امتیاز لغو می‌کند، بعد از بستن فرم دوباره رها می‌کند ✓ (نمایش دیالوگ خود Chromium در حالت headless قابل مشاهده نیست؛ آنچه تست شد رفتار کد ماست).
- `tsc`/`npm run build` سبز · ۱۰۶ تست pass.

## دسترسی CRUD کامل شامل «مشاهده» (۱۴۰۵/۰۶/۱۰)
- **مسئله:** ستون «مشاهده» در دیالوگ سطح دسترسی یک خانهٔ قفل‌شده با متن «هر کاربر واردشده این بخش را می‌بیند» بود، چون خواندن هیچ‌جا محدود نمی‌شد. یعنی «واحد مالی فقط R روی شرکای تجاری» اصلاً قابل بیان نبود.
- **سه مجوز خواندن اضافه شد** — `vendor.read` / `material.read` / `partner.read` — و ستون مشاهده حالا چک‌باکس واقعی است. سرصفحهٔ ستون‌ها حرف CRUD را هم نشان می‌دهد (R / C / U / D) و کنار نام هر ماژول یک بج خلاصه هست که دسترسی فعلی همان ردیف را با همان حروف می‌گوید (مثلاً `RCUD` یا `R` یا `—`).
- **سرور واقعاً اعمال می‌کند** (قاعدهٔ ۱۴ — مجوزی که endpoint اجرا نکند نساز): ۹ مسیر GET گارد گرفتند — فهرست و تاریخچهٔ سورس‌ها، `source-selections`، مواد و فایل Specification، شرکا و تاریخچهٔ ارزیابی و فایل SOP.
- **خواندن و نوشتن به هم قفل‌اند:** تیک‌زدن هر عملیاتِ یک ماژول، مشاهده‌اش را روشن می‌کند و برداشتن تیک مشاهده کل ردیف را خاموش می‌کند — «اجازهٔ ویرایش شرکا ولی بدون دیدنشان» حالتی نیست که برنامه بتواند رندر کند.
- **هیچ حسابی چیزی از دست نمی‌دهد:** هر نقش کاری الگویش هر سه خواندن را دارد. برای استثناهای پر‌کاربرِ ذخیره‌شده (که پیش از وجود مجوز خواندن نوشته شده‌اند و فقط نوشتن دارند) قاعدهٔ `withLegacyReads` اضافه شد: فهرستی که **هیچ** مجوز خواندنی ندارد قدیمی حساب می‌شود و هر سه به آن اضافه می‌شود — همان الگوی «در خواندن گسترش بده، مهاجرت نکن» که برای `material.write` به کار رفت. به‌محض اینکه ادمین یک بار دیالوگ را ذخیره کند فهرست، خواندنِ صریح دارد و این قاعده دیگر اعمال نمی‌شود؛ همین است که حساب فقط‌خواندنی را ممکن می‌کند.
- **UI:** ورودی‌های سایدبار (دسته‌بندی‌ها، آرشیو، شرکا، مواد، بررسی یکپارچه) با `can()` فیلتر می‌شوند و صفحات ماژول به‌جای جدول خالی یا خطای شبکه، کارت `AccessDenied` نشان می‌دهند. loaderهای `App` هم وقتی مجوز نیست اصلاً درخواست نمی‌زنند و کش localStorage را خالی می‌کنند، وگرنه کاربرِ سلب‌دسترسی‌شده همان فهرست قبلی را می‌دید.
- تست زنده با سناریوی دقیق درخواست: `finance` با `["partner.read","score.finance"]` → GET شرکا **۲۰۰** · GET سورس‌ها/مواد/audit **۴۰۳** · POST شریک **۴۰۳** · سایدبار فقط «مخزن شرکای تجاری» را نشان می‌دهد · صفحهٔ مواد کارت عدم دسترسی. `commercial` با الگوی نقش → POST/PUT/DELETE شریک هر سه **۲۰۰** (یعنی CRUD کامل). استثنای قدیمیِ فقط‌نوشتنی → هر سه GET **۲۰۰**. دیالوگ: تیک C روی شرکا بج را از `R` به `RC` می‌برد و برداشتن R کل ردیف را صفر می‌کند.
- **ماتریس در فهرست کاربران:** ستون تازهٔ «دسترسی ماژول‌ها» برای هر کاربر چیپ‌های `<ماژول> <حروف>` نشان می‌دهد (مثلاً `شرکا RCUD`)، خوانده‌شده از `effectivePermissions` یعنی آنچه واقعاً در جریان است نه آنچه تیک خورده. tooltip هر چیپ نام کامل ماژول و عملیات را به فارسی می‌گوید؛ کاربر بدون هیچ دسترسی «بدون دسترسی» می‌گیرد. ستون عمداً مرتب‌سازی ندارد (بین `RCUD` و `R` ترتیبی وجود ندارد که معنا بدهد). ماژولی که خواندنش را از ماژول دیگری قرض گرفته (نتایج آزمایشگاهی و ریسک با `vendor.read` باز می‌شوند) وقتی نوشتنی از خودش ندارد چیپ نمی‌گیرد، وگرنه یک `R` تکراری به هر ردیف اضافه می‌کرد. خروجی واقعی: مدیر سیستم `سورس RCUD … کاربران RCUD` · بازرگانی `سورس RCU مواد R شرکا RCUD` · مالیِ فقط‌خواندنی `شرکا R`.
- **تفکیک `partner.files` از `partner.read`:** دیدن فهرست و گرید یک شریک با «مشاهده» داده می‌شود، ولی گرفتن خودِ مدارک SOP (مجوز کسب‌وکار، معرفی‌نامه، ترجمهٔ رسمی، لگالیزاسیون) مجوز جداگانه دارد — این‌ها اسناد حقوقی‌اند و «می‌بیند شریک گرید B است» با «پروندهٔ حقوقی‌اش را از سامانه برمی‌دارد» یکی نیست. endpoint فایل (`GET /api/business-partners/:id/documents/:key/file`) حالا `partner.files` می‌خواهد.
- برای این‌گونه اختیارات که در قالب CRUD نمی‌گنجند، فیلد عمومی `extras` به `PermissionModule` اضافه شد (به‌جای ستون پنجمی که در همهٔ ردیف‌های دیگر خالی می‌ماند): چک‌باکس خودش زیر نام ماژول می‌آید و حرفش (`F`) به بج خلاصه و ستون فهرست کاربران اضافه می‌شود — مثلاً `شرکا RCUDF`.
- **پیش‌فرض `partner.files`:** فقط `admin`، `commercial` (که مدارک را جمع می‌کند) و `qa` (که طبق رابریک SOP گریدشان می‌دهد). `planning` و `finance` فهرست و گرید شریک را می‌بینند ولی خودِ مدارک را نمی‌گیرند. ادمین می‌تواند برای یک نفر استثنا بگذارد.
- استثناهای **قدیمیِ** ذخیره‌شده (پیش از وجود مجوز خواندن) همچنان مدارک را می‌گیرند، چون `partner.files` در `withLegacyReads` هست — این عمدی است: آن حساب‌ها وقتی ساخته شدند همه چیز برایشان باز بود.
- تست زنده روی هر پنج حساب: فهرست شرکا برای همه **۲۰۰**؛ فایل SOP → admin/commercial/qa **۲۰۰** و planning/finance **۴۰۳**. `finance` با استثنای `partner.files` دوباره **۲۰۰** می‌گیرد. در UI همان کاربر، به‌جای دکمه‌های مشاهده/دانلود متن «بدون مجوز دریافت» می‌بیند (نام فایل عمداً می‌ماند تا بداند مدرکی وجود دارد)، و با مجوز، ۵ دکمهٔ مشاهده و ۵ دکمهٔ دانلود برمی‌گردند.
- `tsc`/`npm run build` سبز · تست‌ها **۱۰۶ pass** (ماتریس مصوب و دو تست جدید برای قاعدهٔ استثنای قدیمی و حساب فقط‌خواندنی به‌روزرسانی شد).

## مدیریت کاربران — سه بسته (۱۴۰۵/۰۶/۰۹)
### بستهٔ ۱: اعتبارسنجی سمت‌سرور در `POST /api/users`
- همهٔ قواعد فقط در فرم React بودند. تست واقعی روی endpoint نشان داد: نام کاربری `X Y!` (با فاصله و `!`)، رمز **یک‌کاراکتری**، نقش `nonsense` و مجوز جعلی `not.a.permission` همگی پذیرفته و ذخیره می‌شدند — و پاسخ، نقش نامعتبر را عیناً برمی‌گرداند در حالی که دیتابیس بی‌صدا `commercial` ذخیره کرده بود (قاعدهٔ ۱۴).
- حالا سرور فرمت نام کاربری، حداقل ۶ کاراکتر رمز، نقش معتبر (۴۰۰ به‌جای coercion بی‌صدا) و آرایه‌بودن permissions را چک می‌کند، ورودی را با `sanitizePermissions` تمیز می‌کند (تنها مسیر نوشتنی که این کار را نمی‌کرد) و **مقدار واقعاً ذخیره‌شده** را برمی‌گرداند.
- تست زنده: هر چهار حالت نامعتبر ۴۰۰ می‌گیرند؛ ورودی معتبر با `["not.a.permission","score.qa"]` فقط `["score.qa"]` ذخیره می‌کند ✓.

### بستهٔ ۲: سه ناسازگاری رفتاری
- **ذخیرهٔ بدون تغییرِ دیالوگ دسترسی، کاربر را برای همیشه «سفارشی» می‌کرد.** دیالوگ با `effectivePermissions` پر می‌شود؛ ذخیره بدون تغییر، همان الگو را به‌عنوان override می‌نوشت و یک رکورد audit با severity=Critical برای هیچ می‌ساخت. حالا اگر پیش‌نویس با الگوی نقش یکی باشد آرایهٔ خالی («از نقش پیروی کن») ارسال می‌شود و دکمهٔ ذخیره وقتی تغییری نیست غیرفعال است.
- **`PUT /api/users/:username/role` مرده و واگرا بود:** هیچ کالری در `src/` نداشت و برخلاف `PATCH` هنگام تغییر نقش استثناها را پاک نمی‌کرد. حالا هم‌رفتار شد (پاک‌کردن استثنا + اعتبارسنجی نقش + ثبت `permissionsReset`).
- **متادیتا از `afterData` جدا شد** در همهٔ endpointهای کاربران و احراز هویت (۱۰ مورد `eventType/ipAddress/userAgent` و `ip/device`) — همان کاری که برای بقیهٔ ماژول‌ها انجام شده بود.

### بستهٔ ۳: گیت دسترسی و یکدستی
- **گیت سایدبار از `role === 'admin'` به `can()` تغییر کرد.** قبلاً صفحه با `can(user,'users.manage')` گیت می‌شد ولی لینک با نقش — یعنی دارندهٔ استثنا هرگز لینک را نمی‌دید. «آرشیو» هم که هیچ endpointای محدودش نمی‌کند از همه پنهان بود. تست زنده: کاربر `qa` آرشیو را می‌بیند ولی ردیابی تغییرات و کاربران را نه؛ با دادن استثنای `audit.read` لینک «ردیابی تغییرات» ظاهر می‌شود ✓.
- جدول کاربران: **هدرهای مرتب‌سازی‌شونده** با `aria-sort` (نام/نام کاربری/سمت/وضعیت/آخرین ورود، با `Intl.Collator('fa')`)، **اسکلت بارگذاری** (۳۰ سلول، `aria-busy`)، **انتخاب تعداد ردیف** ۱۰/۲۵/۵۰/۱۰۰ با صفحه‌بندی، و **چهار کارت آمار** هم‌شکل بقیهٔ مخازن.
- دکمهٔ «پشتیبان‌گیری» در هدر هم از `role === 'admin'` به `can(user,'users.manage')` منتقل شد — همان مخاطب قبلی، ولی از جدول سیاست خوانده می‌شود و استثنای پر‌کاربر را می‌بیند. تست زنده: admin ✓ · `qa` ✗ · `qa` با استثنای `users.manage` ✓. **توجه:** این گیت یک قاعدهٔ نمایشی است نه مرز امنیتی — فایل در مرورگر از `db` ساخته می‌شود و `GET /api/vendors` همان داده را به هر کاربر واردشده می‌دهد؛ مجوز سروری برایش ساخته نشد چون هیچ endpointای اعمالش نمی‌کند (همان اشتباهی که `archive.read` به‌خاطرش حذف شد).
- `text-slate-300` باقی‌مانده به توکن تبدیل شد (قاعدهٔ ۷)، نام کاربر با `EntityName` رندر می‌شود و بج «شما» `shrink-0` در ظرف `flex-wrap` است (قاعدهٔ ۱۵)، و حالت خالی بین «جستجو نتیجه نداد» و «هنوز کاربری تعریف نشده» تفکیک شد.
- `tsc`/`npm run build`/۱۰۳ تست سبز. دادهٔ آزمایشی و رکوردهای audit تست حذف شدند.

## مرتب‌سازی الفبای فارسی نام کاربر در ردیابی تغییرات (۱۴۰۵/۰۶/۰۹)
- **مسئله:** ستون «کاربر» با collation پیش‌فرض دیتابیس (`C.UTF-8`) یعنی ترتیب code point مرتب می‌شد. حروفی که یونیکد بعد از بلوک عربی افزوده (`پ چ ژ گ ک ی`) بعد از همهٔ حروف می‌آمدند — اندازه‌گیری روی نمونهٔ واقعی: `آرش , احمد , اکبر , ایمان , هدی , پرویز , چاوش , کریم , گلی , یاسر` (یعنی «هدی» قبل از «پرویز»).
- **رفع بدون تغییر کوئری:** چون Prisma `COLLATE` را در `orderBy` نمی‌پذیرد، collation به **خودِ ستون** وصل شد (مهاجرت `20260831180000_audit_user_name_persian_collation` → `fa-IR-x-icu`). هر `ORDER BY user_name` — از جمله همانی که Prisma می‌سازد — خودبه‌خود از آن پیروی می‌کند، پس `orderFor` دست‌نخورده ماند.
- **مهاجرت گاردشده است:** ICU در Postgres یک گزینهٔ build است؛ اگر `fa-IR-x-icu`/`fa-x-icu` روی سرور نبود، مهاجرت با یک `NOTICE` رد می‌شود و دیپلوی نمی‌شکند (ترتیب همان قبلی می‌ماند). نوع ستون عمداً `VARCHAR(100)` بازنویسی شد نه `TEXT`، وگرنه سقف طول که `schema.prisma` به آن باور دارد بی‌صدا حذف می‌شد.
- تست زنده روی API: `sortBy=user&sortDir=asc` → `آرش , پرویز , چاوش , سیستم (خودکار) , کریم , گلی , مدیر سیستم` ✓ و `desc` → `یاسر , هدی , واحد کیفیت , مدیر سیستم` ✓. رکوردهای آزمایشی حذف شدند. `tsc`/`npm run build`/۱۰۳ تست سبز.
- **نکته برای دیپلوی:** این مهاجرت باید با `prisma migrate deploy` روی دیتابیس تولید هم اجرا شود، وگرنه فقط محلی درست مرتب می‌شود.

## عرض یکسان برای کل برنامه (۱۴۰۵/۰۶/۰۹)
- **مسئله:** ظرف محتوا در `App.tsx` یک فهرست استثنا بود — فقط `materials` و `audit-trail` عرض ۱۶۰۰px می‌گرفتند و بقیه روی `max-w-5xl` (۱۰۲۴px) قفل بودند. اندازه‌گیری: روی ۱۹۲۰px، **۶۲۴px یعنی ۳۸٪ عرض قابل استفاده** در صفحه اصلی، دسته‌بندی‌ها، شرکای تجاری، آرشیو، کارتابل، کاربران و بررسی یکپارچه هدر می‌رفت (روی ۱۴۴۰px: ۱۴۴px).
- **رفع:** ثابت مشترک `CONTENT_WIDTH` جای شرط را گرفت، پس همهٔ viewها یک عرض دارند و **view جدید هم خودبه‌خود درست است** (دیگر فهرستی نیست که کسی یادش برود به‌روزش کند).
- **چگالی:** شبکهٔ سنجه‌های ردیف دسته‌بندی در `xl` پهن‌تر شد (`[10rem_9rem_9rem]`)، و سقف عرض ستون‌های نام در جدول مواد و شرکا در `xl` بالا رفت تا فضای اضافه به نام برسد نه به فاصلهٔ خالی.
- **صفحهٔ جزئیات سورس:** سقف مستقل `max-w-6xl` برداشته شد و از قاعدهٔ مشترک پیروی می‌کند؛ کارت‌های امتیاز دپارتمان‌ها در `xl` چهارستونه می‌شوند. **ولی بلوک‌های متنی سقف خوانایی `max-w-[75ch]` گرفتند** — خط فارسی در ۱۶۰۰px به ~۱۸۰ کاراکتر می‌رسد و حد خوانا ۶۵–۸۰ است. یعنی عرض به شبکه و جدول می‌رسد، نه به طول خط متن.
- **دو تغییری که پس گرفتم** (چون نتیجه بدتر بود): هشت‌ستونه‌کردن ردیف «کارهای معوق» (چهار کارت داشت و نصف ردیف خالی می‌ماند) و دوستونه‌کردن ردیف اطلاعات تماس در جزئیات سورس (یک کارت دارد).
- تست زنده در ۱۹۲۰/۱۴۴۰/۳۹۰ روی ده مسیر: همه ۱۶۰۰/۱۶۴۸ و ۱۱۶۸/۱۱۶۸ و ۳۹۰/۳۹۰، **بدون سرریز افقی و بدون خطا** ✓ · `tsc`/`vite build`/۱۰۳ تست ✓.

## شرکای تجاری — دادهٔ نمونه فقط در حالت دموی لوکال (۱۴۰۵/۰۶/۰۵)
- `INITIAL_BUSINESS_PARTNERS_DB` (لیست نمونهٔ باندل‌شده) دیگر در حالت عادی fallback نیست. قبلاً هر مرورگرِ بدون کش، شرکای ساختگی را **با گرید و نتیجهٔ SOP** طوری نشان می‌داد که انگار از سرور آمده‌اند — با قاعدهٔ ۱ (PostgreSQL تنها منبع حقیقت) در تضاد بود.
- حالا: کش موجود → همان؛ حالت دموی لوکال (`isLocalMode()`) → دادهٔ نمونه؛ حالت عادی → **آرایهٔ خالی** تا پاسخ سرور برسد (و در این فاصله اسکلت بارگذاری دیده می‌شود، نه دادهٔ جعلی).
- **نکتهٔ ظریف:** آرایهٔ خالیِ ذخیره‌شده «کش» حساب نمی‌شود، وگرنه یک بار اجرای حالت عادی `[]` را در `app_business_partners` می‌نوشت و حالت دمو — که backend ندارد تا پرش کند — برای همیشه خالی می‌ماند.
- **seed سمت سرور دست‌نخورده است:** `seedDefaultBusinessPartners` هنوز روی دیتابیس خالی در اولین راه‌اندازی همین شرکا را درج می‌کند؛ آن مسیر ردیف واقعی می‌سازد، پس مشکل صداقت ندارد.
- تست زنده: حالت عادی با تأخیر روی `/api/business-partners` → `aria-busy=true`، هیچ ردیف نمونه‌ای دیده نمی‌شود، بج سایدبار ۰، و پس از رسیدن داده ۷ ردیف واقعی ✓ · حالت دموی لوکال → ۷ ردیف نمونه ✓ · `tsc`/`vite build`/۱۰۳ تست ✓.

## شرکای تجاری — بستهٔ ۴: حالت‌ها و دسترسی‌پذیری (۱۴۰۵/۰۶/۰۵)
- **اسکلت بارگذاری با پرچم مخصوص خودش:** `partnersLoading` در `App.tsx` (نه `isSyncing` که فقط fetch سورس‌ها را دنبال می‌کند). جدول `aria-busy` می‌گیرد و ۵ ردیف اسکلتی نشان می‌دهد؛ اعداد کارت‌های KPI هم تا رسیدن داده pulse می‌شوند، چون از همان لیستی می‌آیند که هنوز نرسیده.
- **باگ جدی که همین‌جا آشکار شد:** وقتی کش localStorage خالی است، `businessPartners` به لیست seedِ باندل‌شده (`INITIAL_BUSINESS_PARTNERS_DB`) برمی‌گردد. یعنی هم شرط `length === 0` هرگز برقرار نمی‌شد (اسکلت غیرممکن بود)، هم **دادهٔ ساختگی به‌جای دادهٔ سرور نمایش داده می‌شد** تا وقتی پاسخ برسد. حالا اسکلت فقط به `partnersLoading` نگاه می‌کند، پس seed دیگر به‌جای دادهٔ واقعی دیده نمی‌شود (تست: `showsSeedRows: false` هنگام بارگذاری).
- **دو حالت خالی متفاوت:** «هنوز شریک تجاری‌ای ثبت نشده است» با دکمهٔ «ثبت اولین شریک تجاری» (مشروط به `partner.create`) در برابر «هیچ شریکی با این جستجو یا فیلترها پیدا نشد» با دکمهٔ پاک‌کردن فیلترها.
- **هدرها `<button>` داخل `<th scope="col">`** با `aria-sort` و آیکون جهت‌دار؛ ستون **«نتیجهٔ ارزیابی SOP» حالا قابل مرتب‌سازی است** (بر اساس رتبهٔ گرید، تولیدکننده‌ها جدا) — مهم‌ترین ستون این جدول بود و اصلاً مرتب نمی‌شد.
- **مرتب‌سازی فارسی** با `Intl.Collator('fa')` به‌جای مقایسهٔ کدپوینتی.
- **گیت `partner.edit` روی دکمهٔ ویرایش و لینک «شروع ارزیابی»** — قبلاً فقط حذف گیت داشت، پس کاربر بدون مجوز کل فرم را پر می‌کرد و سرور آخرش رد می‌کرد.
- clamp شمارهٔ صفحه، انتخاب تعداد ردیف (۱۰/۲۵/۵۰/۱۰۰)، `EntityName` برای نام شرکا، حلقهٔ فوکوس روی دکمه‌های ردیف، و `caption` مخصوص صفحه‌خوان.
- تست زنده: اسکلت با تأخیر مصنوعی → `aria-busy=true`، ۳۵ عنصر اسکلتی، بدون نمایش seed ✓ · Tab تا هدر SOP و Enter → `aria-sort=ascending` با حلقهٔ فوکوس و ترتیب درست گرید ✓ · مرتب‌سازی فارسی نام‌ها (د، ش، سپس لاتین) ✓ · حالت خالیِ فیلترشده و بازگشت به ۷ ردیف ✓ · `tsc`/`vite build`/۱۰۳ تست ✓.
- **با این، هر چهار بستهٔ گزارش شرکای تجاری انجام شد.**

## شرکای تجاری — بستهٔ ۳: تم تیره و یکدست‌سازی قالب با مخزن مواد (۱۴۰۵/۰۶/۰۵)
- **چیدمان مثل مخزن مواد اولیه شد:** بنر گرادیانی (`from-slate-900 via-slate-800 to-blue-900`) حذف شد — یک زبان بصری دوم برای همان صفحه بود و در تم تیره هم بی‌معنا می‌شد. حالا مثل مخزن مواد: ردیف کارت‌های KPI با همان استایل (`bg-card` + کاشی آیکون + عدد mono)، سپس یک کارت واحد که عنوان صفحه، eyebrow مونو، توضیح، جستجو، خروجی اکسل و دکمهٔ ثبت را کنار هم دارد و فیلترهای کشویی زیر یک خط جداکننده داخل همان کارت‌اند. جدول هم `rounded-2xl shadow-xs` شد.
- کارت‌ها از یک آرایه ساخته می‌شوند (نه پنج بلوک کپی‌شده) و برچسب بلند به‌جای بریده‌شدن در دو خط می‌شکند — «مجاز برای اتصال به سورس» قبلاً «مجاز برای اتصال به …» دیده می‌شد.
- **تم تیره:** ۲۳ الگوی رنگِ هاردکد جفت `dark:` گرفتند (بج‌های نوع/وضعیت/مدرک، دکمه‌های عملیات ردیف، کاشی‌های مودال، کادرهای خطا، تب‌ها) و همهٔ `slate-*`های باقی‌مانده به توکن تبدیل شدند. دو پنل «نتیجهٔ زندهٔ ارزیابی» (در فرم و در جزئیات) که گرادیان ثابت slate بودند، با `bg-foreground/text-background` معکوس می‌شوند — همان الگوی پنل پیش‌نمایش نام استاندارد در مخزن مواد.
- **اندازه‌گیری کنتراست در تم تیره (قبل → بعد):** بج گرید **۱.۰۳ → ۱۳.۳۶** · بج نوع شریک **۲.۵۲ → ۱۴.۳۷** · بج وضعیت سیستم **۲.۳۵ → ۱۴.۶۳** · عدد کارت KPI **۲.۲۶ → ۱۳.۹۸** · برچسب کارت ۵.۷۱ · چیپ «قابل اتصال نیست» ۵.۷۱ · عنوان صفحه ۱۳.۹۸ — همه بالاتر از AA.
- تست: `tsc`/`vite build`/۱۰۳ تست ✓ · اسکرین‌شات فهرست و تب ارزیابی در هر دو تم ✓ · هیچ برچسب کارتی بریده نمی‌شود (`scrollWidth === clientWidth`) ✓.


## شرکای تجاری — بستهٔ ۲: صداقت ارزیابی (۱۴۰۵/۰۶/۰۵)
- **کارت «تامین‌کنندگان تاییدشده» چیزی را تأیید می‌کرد که سیستم رد می‌کند:** گرید A **و B** را می‌شمرد، در حالی که طبق قاعدهٔ ۱۳ فقط گرید A قابل اتصال به سورس است و سرور بقیه را با ۴۲۲ رد می‌کند. حالا از همان `canSupplySources` می‌پرسد و به «مجاز برای اتصال به سورس» تغییر نام داده؛ کارت بعدی «غیرمجاز برای اتصال» است و این دو دقیقاً کل فروشندگان را افراز می‌کنند (تست زنده: ۲ + ۱ = ۳ ✓).
- **واژگان گرید یک‌جا شد (`GRADE_LABELS` / `describeGrade` در `sopEvaluation.ts`).** بج ردیف، گرید B را «Permit Approval» و C را «Expired» نشان می‌داد — این‌ها **وضعیت مدرک**اند نه حکم دربارهٔ فروشنده، و همان فروشنده دو پنل آن‌طرف‌تر «Approved with Monitoring» خوانده می‌شد. حالا همه‌جا واژگان `calculateGradeAndStatus` است: A تاییدشده · B تاییدشده با پایش · C مشروط · Pending Review · Blacklist. دو جدول رنگ محلی (`gradeApprovalLabel` و `getGradeBadgeClass`) هم حذف و به همین منبع وصل شدند (رنگ‌ها جفت `dark:` دارند).
- **ردیف حالا به سؤال واقعی خریدار جواب می‌دهد:** فروشنده‌ای که قابل اتصال به سورس نیست، چیپ «قابل اتصال به سورس نیست» با دلیل در tooltip می‌گیرد — به‌جای اینکه رد‌شدن، بعداً موقع ثبت سورس غافلگیرش کند.
- placeholder جستجو ادعای «تولیدکننده» داشت در حالی که مدل شرکا تخت است (قاعدهٔ ۴) و چنین فیلدی جستجو نمی‌شود؛ به فهرست واقعی فیلدها اصلاح شد.
- ضمن کار: یک `formatFileSize` محلی داخل کامپوننت، نسخهٔ ماژولیِ اضافه‌شده در بستهٔ ۱ را سایه می‌انداخت، پس پیام‌های گارد حجم «3.0 MB» نشان می‌دادند نه «۳ مگابایت». نسخهٔ تکراری حذف شد.
- تست زنده: کارت‌ها ۲/۱ با افراز درست ✓ · ستون SOP: «B · تاییدشده با پایش (۷۰/۱۰۰) + قابل اتصال به سورس نیست» و «A · تاییدشده» ✓ · `tsc`/`vite build`/۱۰۳ تست ✓.
- باقی‌ماندهٔ گزارش شرکای تجاری: بستهٔ ۳ (تم تیره — بج گرید در تم تیره ۱.۰۳:۱ بود؛ بخشی از رنگ‌ها با همین بسته جفت `dark:` گرفتند ولی بنر، کارت‌ها و بج‌های نوع/وضعیت هنوز مانده‌اند) و بستهٔ ۴ (حالت‌ها و دسترسی‌پذیری).

## شرکای تجاری — بستهٔ ۱: درستیِ ذخیره‌سازی (۱۴۰۵/۰۶/۰۵)
- **باگ اصلی:** `handleAddBusinessPartner` و `handleEditBusinessPartner` در `App.tsx` نتیجهٔ درخواست را دور می‌ریختند (`.catch(console.error)`) و `res.ok` را اصلاً نگاه نمی‌کردند. یعنی ۴۰۳/۴۰۰/۴۱۳ سرور، کاربر را با پیام سبز «با موفقیت ذخیره شد» رها می‌کرد؛ تغییر در حافظه می‌ماند و با اولین reload بی‌صدا از بین می‌رفت. برای ثبت ارزیابی تأمین‌کننده در سامانهٔ GxP بدترین حالت ممکن است.
- حالا هر دو مثل ماژول مواد رفتار می‌کنند: خطا → **بازگرداندن state** به snapshot + toast خطا با پیام سرور. `describePartnerFailure` برای ۴۱۳ و ۴۰۳ پیام قابل‌فهم می‌سازد.
- **گارد حجم مدارک SOP سمت کلاینت:** ذخیرهٔ شریک کل رکورد را می‌فرستد و پنج مدرک SOP هرکدام base64 خودشان را حمل می‌کنند، در حالی که سرور `express.json({limit:'10mb'})` دارد و data URL ~۳۳٪ بزرگ‌تر است. سقف‌ها: هر مدرک ۳ مگابایت، مجموع ۶ مگابایت، با پیام صریح و نمایش «مجموع حجم مدارک پیوست: X از Y» در تب ارزیابی.
- **toast دیگر حدس نمی‌زند:** نوع پیام (`success`/`error`) را خودِ فراخوان تعیین می‌کند (`notify()`). قبلاً از روی regex کلمات حدس زده می‌شد و پیام «عدم دسترسی… را نمی‌دهد» هیچ‌کدام را نداشت، پس یک ذخیرهٔ **ردشده** با تیک سبز اعلام می‌شد. regex به‌عنوان fallback برای فراخوان‌های قدیمی مانده (بدهی باقی‌مانده).
- تست زنده: کاربر `qa` (بدون `partner.edit`) → پیام قرمز «عدم دسترسی…» با آیکون هشدار، ردیف به مقدار قبلی برگشت، و پس از reload سرور هم تغییری نکرده بود ✓ · کاربر `admin` → ذخیره موفق و ماندگار پس از reload ✓ (نام آزمایشی برگردانده شد) · `tsc`/`vite build`/`npm run build`/۱۰۳ تست ✓.
- باقی‌ماندهٔ گزارش شرکای تجاری (بستهٔ ۴): حالت‌ها و دسترسی‌پذیری.

## مخزن مواد اولیه — آپلود واقعی فایل Specification (۱۴۰۵/۰۶/۰۵)
- **مسئله:** فرم فقط `e.target.files[0].name` را ذخیره می‌کرد. هیچ فایلی آپلود نمی‌شد و هیچ دانلودی ممکن نبود — یعنی رکورد ادعای مستندسازی داشت بدون پشتوانه، که در سامانهٔ GxP قابل قبول نیست.
- **schema (`20260827200000_material_specification_file`):** سه ستون افزایشی روی `materials` — `specification_file_size`, `specification_file_data` (Text), `specification_uploaded_at`. ستون `specification_file` مثل قبل **نام فایل** است، پس دادهٔ موجود دست‌نخورده می‌ماند.
- **الگوی فایل تنبل (قاعدهٔ ۵)، همان چیزی که مدارک SOP دارند:** لیست مواد هرگز base64 حمل نمی‌کند؛ فقط `specificationFile` (نام)، `specificationFileSize`، `hasSpecificationFile` و `specificationUploadedAt`. blob از endpoint اختصاصی گرفته می‌شود.
- **سه endpoint:** `PUT /api/materials/:id/specification` (مجوز `material.edit`؛ سقف ۷ مگابایت چون `express.json` روی ۱۰mb است و data URL ~۳۳٪ بزرگ‌تر) · `GET /api/materials/:id/specification/file` · `DELETE /api/materials/:id/specification`. هر سه در audit ثبت می‌شوند (حذف با `severity: Warning`) و blob هرگز داخل رکورد audit نوشته نمی‌شود.
- **باگ قدیمی که حین تست پیدا شد و رفع شد:** در `PATCH /api/materials/:id` همهٔ فیلدها با `??` ادغام می‌شدند، پس ارسال `specificationFile: null` برای حذف پیوست، نام قبلی را نگه می‌داشت — یعنی **حذف فایل هیچ‌وقت واقعاً ذخیره نمی‌شد**. حالا برای همین فیلد `"specificationFile" in b` بررسی می‌شود: `null` صریح پاک می‌کند، کلید غایب مقدار فعلی را نگه می‌دارد. اگر نام پاک شود، blob و متادیتایش هم پاک می‌شوند تا فایل یتیم نماند.
- **UI:** در شناسنامهٔ ماده دکمه‌های مشاهده (با `openDocumentPreview`، همان مسیر امنِ شرکا)، دانلود، جایگزینی و حذف؛ خطا به‌صورت درون‌خطی؛ رکورد قدیمی‌ای که فقط نام دارد صریحاً می‌گوید «فقط نام فایل ثبت شده است — فایلی روی سرور نیست». فایل انتخاب‌شده هنگام **ساخت** ماده نگه داشته می‌شود و بعد از ایجاد رکورد آپلود می‌گردد (نمی‌توان فایل را به رکوردی که هنوز وجود ندارد وصل کرد). در حالت آزمایشی لوکال صریحاً گفته می‌شود فایل ذخیره نمی‌شود.
- تست زنده: آپلود ۲۰۰ ✓ · لیست بدون blob ✓ · round-trip بایت‌به‌بایت فایل ✓ · حجم زیاد ۴۱۳ ✓ · data URL نامعتبر ۴۰۰ ✓ · `null` صریح پاک می‌کند و PATCH بی‌ربط فایل را نگه می‌دارد ✓ · حذف ۲۰۰ و سپس ۴۰۴ ✓ · رکوردهای audit ✓ · تست مرورگری: آپلود از خود UI و دانلود برگشتی با همان نام ✓ · `tsc`/`vite build`/`npm run build`/۱۰۳ تست ✓.

## مخزن مواد اولیه — بستهٔ ۴: اعتبارسنجی تکراری‌بودن سمت سرور (۱۴۰۵/۰۶/۰۵)
- **قاعده حالا یک‌جاست و هر دو طرف از آن می‌خوانند:** `src/utils/materialDuplicates.ts` (همان الگوی `permissions.ts` / `auditTaxonomy.ts`). قبلاً فرم مخزن این بررسی را داشت ولی فقط در مرورگر، پس عملاً توصیه بود نه قاعده (قاعدهٔ ۱۴) — هر کلاینت دیگری، از جمله مسیر ذخیرهٔ سورس، می‌توانست ردیف دوم بسازد.
- دو سیگنال: **CAS واقعیِ یکسان** و **ترکیب نقش + نام لاتین + محصول نهایی لاتین**. CAS جایگزین (`N/A`, `-`, `نامشخص`, …) هرگز هویت نیست، وگرنه دومین مادهٔ ثبت‌شده «تکراری» می‌شد. ترکیب نیمه‌پرشده هم هویت نیست.
- `POST /api/materials` و `PATCH /api/materials/:id` در صورت برخورد **۴۰۹** با پیام فارسی و `duplicateOf` برمی‌گردانند و رویداد را با `severity: Warning` در audit ثبت می‌کنند (مثل حذفِ رد‌شده).
- **رکوردهای از قبل تکراری قفل نمی‌شوند:** در PATCH فقط قاعده‌ای اعمال می‌شود که ویرایش، فیلدهای همان قاعده را تغییر داده باشد. پس ویرایش نام روی ردیفی که از قبل CAS مشترک داشت مجاز است (وگرنه هدف‌های اسکریپت `merge-duplicate-materials.ts` غیرقابل‌ویرایش می‌شدند)، ولی تغییر CAS **به** یک CAS موجود رد می‌شود.
- فرم مخزن هم به همین تابع وصل شد تا دو طرف واگرا نشوند (پیام خطای درون‌خطی از همان منبع می‌آید).
- تست: ۹ تست واحد جدید (مجموع **۱۰۳**) · تست زندهٔ endpointها → CAS تکراری ۴۰۹ ✓ · ترکیب تکراری ۴۰۹ ✓ · مادهٔ جدید ۲۰۰ ✓ · PATCH روی فیلد بی‌ربط ۲۰۰ ✓ · PATCH که CAS را به موجود تغییر می‌دهد ۴۰۹ ✓ · سه رکورد Warning در `audit_log` ✓ · `npm run build` کامل (شامل `dist/server.cjs`) ✓.

## مخزن مواد اولیه — بستهٔ ۳: حالت‌ها و دسترسی‌پذیری (۱۴۰۵/۰۶/۰۵)
- **اسکلت بارگذاری:** پراپ `isLoading` (از `App.tsx`، `isSyncing && materials.length === 0` — همان الگوی `CategoryView`). تا رسیدن دادهٔ اول، پنج ردیف اسکلتی هم‌شکل جدول رندر می‌شود و `aria-busy` روی `<table>` می‌نشیند. قبلاً کاربر در همین بازه پیام «هیچ ماده‌ای یافت نشد» می‌دید، یعنی به او گفته می‌شد مخزن خالی است.
- **دو حالت خالیِ متفاوت:** «مخزن مرجع هنوز خالی است» با دکمهٔ «ثبت اولین ماده» (فقط برای دارندهٔ `material.create`؛ برای بقیه جملهٔ صریح دربارهٔ دسترسی) در برابر «هیچ ماده‌ای با این جستجو یا فیلترها پیدا نشد» با دکمهٔ «پاک‌کردن جستجو و فیلترها».
- **هدر ستون‌ها حالا `<button>` داخل `<th scope="col">` است**، نه `<th onClick>`: مرتب‌سازی با کیبورد کار می‌کند، حلقهٔ فوکوس دارد و `aria-sort` روی ستون فعال ست می‌شود. `<caption class="sr-only">` هم اضافه شد.
- حلقهٔ فوکوس روی سه دکمهٔ عملیات هر ردیف و کنترل‌های نوار ابزار.
- **`currentPage` clamp می‌شود** (`page = min(currentPage, totalPages)`): حذف آخرین ردیفِ آخرین صفحه دیگر کاربر را روی صفحهٔ خالی رها نمی‌کند.
- **انتخاب تعداد ردیف در صفحه** (۱۰/۲۵/۵۰/۱۰۰) کنار صفحه‌بندی؛ قبلاً روی ۱۰ ثابت بود.
- نام‌ها با `EntityName` رندر می‌شوند (قاعدهٔ ۱۵) و `whitespace-nowrap` از جدول برداشته شد تا نام بلند به‌جای کشیدن افقی صفحه، در دو خط بشکند.
- تست زنده: با تأخیر مصنوعی روی `/api/**` → `aria-busy=true` و ۴۰ عنصر اسکلتی، بدون ادعای «خالی» ✓ · حالت فیلترشده و دکمهٔ پاک‌کردن (بازگشت به ۷ ردیف) ✓ · Tab تا هدر و Enter → `aria-sort=descending` با حلقهٔ فوکوس ✓ · تغییر تعداد در صفحه ✓ · `tsc`/`build`/۹۴ تست ✓.

## مخزن مواد اولیه — بستهٔ ۲: تم تیره و توکن‌ها (۱۴۰۵/۰۶/۰۵)
- کل `MaterialRepositoryView` صفر `dark:` داشت (همان باگی که در `VendorDetail` رفع شد). حالا هر رنگ روشنِ هاردکد جفت `dark:` دارد: کارت‌های آمار، بج نقش، سه دکمهٔ عملیات ردیف، کاشی‌های آیکون مودال‌ها، چیپ فایل Specification، کادر خطای فرم، هشدار یکپارچگی و مودال‌های حذف.
- `slate-*` باقی‌مانده به توکن تبدیل شد: `hover:bg-slate-200/60` → `hover:bg-accent` · `bg-slate-200` دکمهٔ بستن → `bg-muted hover:bg-accent border-border` · چیپ دستهٔ سورس → `bg-background border-border` · آیکون حالت خالی → `text-muted-foreground/50`.
- **پنل «پیش‌نمایش نام‌های استاندارد»** که گرادیان ثابت `from-slate-900 to-slate-800` بود و در تم تیره در زمینه گم می‌شد، حالا با `bg-foreground text-background` معکوس می‌شود؛ چون این پنل **خروجی سیستم** است نه ورودی، در هر دو تم باید همان تضاد را داشته باشد و توکن‌های foreground/background با هم جابه‌جا می‌شوند.
- **`animate-shake` کلاس مرده بود** (هیچ keyframe به این نام در `index.css` یا Tailwind v4 نبود) — با `fade-in` موجود جایگزین شد، پس کادر خطای فرم بالاخره سیگنال حرکتی دارد.
- تست زندهٔ کنتراست در تم تیره (اندازه‌گیری با canvas، چون Tailwind v4 رنگ را `oklch()` برمی‌گرداند): بج Other 13.98 · API 10.14 · Reagent 10.56 · Packaging 12.55 · عدد کارت 13.98 · برچسب کارت 5.71 · دکمهٔ اصلی 8.24 — همه بالاتر از حد AA (۴.۵). تم روشن هم بازبینی شد و تغییری نکرده است.
- تست: `tsc` ✓ · `vite build` ✓ · ۹۴ تست ✓ · اسکرین‌شات تیره/روشن (جدول و فرم) ✓.

## مخزن مواد اولیه — بستهٔ ۱: درستی داده و صداقت نمایش (۱۴۰۵/۰۶/۰۵)
- **جدول نقش مواد یک‌جا شد:** `src/constants/materialRoles.ts` — همان الگوی `permissions.ts`/`auditTaxonomy.ts`. قبلاً این جدول در `MaterialRepositoryView` و `MaterialSelector` کپی شده بود و واگرا شده بودند.
  - **مقدار ذخیره‌شده (`value`) تغییر نکرد** (`Reagent / Reactant`, `Packaging Item`) چون هر ردیف موجود و هر نام استاندارد تولیدشده از همان استفاده می‌کند؛ فقط برچسب‌ها عوض شدند: API/ماده موثره · Intermediate/حدواسط · Solvent/حلال · Reagent/واکنشگر · Excipient/اکسپیانت · Packaging/بسته‌بندی · Other/متفرقه.
  - `getMaterialRole()` املاهای قدیمی (`Reagent`, `Packaging`, …) را هم به همان ردیف می‌نگارد، وگرنه بی‌صدا در سطل API می‌افتادند.
- **کارت‌های آمار درست شمردند:** قبلاً پنج کارت بود و `Packaging Item` و `Other` هیچ‌جا شمرده نمی‌شدند، ضمن اینکه کارت «Reagent / Other» فقط Reagent را می‌شمرد. حالا هفت کارت از روی همان جدول ساخته می‌شود و جمعشان دقیقاً برابر «مجموع مواد» است (تست زنده: ۴+۱+۱+۱ = ۷ ✓).
- **مرتب‌سازی فارسی:** `Intl.Collator('fa', {numeric:true})` جای مقایسهٔ کدپوینتی. ترتیب واقعی حالا آ، آت، ا، ای، ب، پ، م است. ستون فعال و جهت آن هم با آیکون جهت‌دار و `aria-sort` نشان داده می‌شود (قبلاً هر شش ستون یک آیکون خنثای یکسان داشتند).
- **جستجو نام‌های استاندارد و IUPAC را هم پوشش می‌دهد** — جستجوی «بسته‌بندی - بلیستر» که همان نامی است که بقیهٔ برنامه نشان می‌دهد، قبلاً هیچ نتیجه‌ای نمی‌داد.
- **ستون «سورس‌های مرتبط»:** همان عددی که تصمیم حذف را می‌گیرد، حالا در خود جدول دیده می‌شود و قابل مرتب‌سازی است. مرجع نهایی همچنان سرور است (`DELETE /api/materials/:id` روی `vendorMaterial` گارد دارد)؛ شمارش کلاینت برای سورس بدون `materialId` روی ماهیت ماده تطبیق می‌دهد تا با گارد سرور واگرا نشود.
- برچسب دستهٔ سورس در مودال حذف از `categoryLabels` می‌آید (قبلاً کلید انگلیسی خام مثل `foreign` نمایش داده می‌شد).
- تست: `tsc` ✓ · `vite build` ✓ · ۹۴ تست ✓ · تست زندهٔ مرورگری روی دادهٔ واقعی (کارت‌ها، ترتیب فارسی، جستجوی نام استاندارد، مرتب‌سازی ستون سورس‌ها با `aria-sort=ascending`) ✓.
- گزارش مخزن مواد کامل شد: بسته‌های ۱ تا ۴ به‌علاوهٔ آپلود واقعی فایل Specification. هیچ مورد بازی از آن گزارش نمانده است.

## قانون SOP: فقط فروشندهٔ گرید A (۱۴۰۵/۰۶/۰۵)
- `canSupplySources(partner)` در `src/utils/sopEvaluation.ts` — منبع واحدِ تصمیم، که هم `PartnerSelector` و هم `server.ts` از آن می‌خوانند.
- **UI:** فروشندهٔ غیرمجاز پنهان نمی‌شود بلکه خاکستری و غیرقابل‌کلیک می‌ماند، با دلیل زیر نامش و شمارش در فوتر («۷ مورد یافت شد · ۱ مورد طبق SOP قابل انتخاب نیست»). پنهان‌کردن باعث می‌شد کاربر فکر کند ثبت نشده و رکورد تکراری بسازد.
- **سرور:** `POST /api/vendors` و `PATCH /api/vendors/:id/profile` با **۴۲۲** رد می‌کنند و رویداد را با `severity: Warning` در audit می‌نویسند.
- **استثناها:** تولیدکننده (SOP ندارد) · فروشندهٔ ازقبل‌متصل به همان سورس (وگرنه رکورد قدیمی غیرقابل‌ویرایش می‌شد) · فروشندهٔ ارزیابی‌نشده با پیام جداگانه.
- **⚠️ ناسازگاری داده که در تست پیدا شد:** ستون `grade` با امتیاز ذخیره‌شده نمی‌خواند — `bp_sup_2` امتیاز ۸۰ دارد ولی `grade='B'`، در حالی که رابریک ≥۸۰ را A می‌داند. کلاینت روی load اصلاح می‌کند و A نشان می‌دهد؛ نسخهٔ اول گارد سرور ستون را می‌خواند و همان فروشنده را رد می‌کرد. حالا سرور هم گرید را با `calculateGradeAndStatus` از امتیاز مشتق می‌کند.
- تست زنده: POST با گرید A → ۲۰۰ · با گرید B → **۴۲۲** با پیام فارسی · فقط تولیدکننده → ۲۰۰ · PATCH به گرید B → ۴۲۲ · PATCH با نگه‌داشتن فروشندهٔ خودش → ۲۰۰ · رکورد قدیمی روی فروشندهٔ B همچنان قابل ذخیره · دو ردیف audit ثبت شد. UI: کیمیا فارمد (۷۰) غیرفعال با دلیل، Sinoway (۸۰) و Biesterfeld (۹۵) فعال، کلیک روی ردیف مسدود بی‌اثر.

## استقلال تولیدکننده و فروشنده در فرم سورس (۱۴۰۵/۰۶/۰۵)
- طبق قاعدهٔ ۴ پروژه فروشنده به تولیدکننده وابسته نیست، ولی **UI هنوز مدل قدیمی را نشان می‌داد**. با انتخاب یک تولیدکننده سه پیام هم‌زمان می‌آمد: جعبهٔ سبز «تأمین مستقیم و بی‌واسطه از کارخانه سازنده (Direct Manufacturer Sourcing)»، بج «خرید بی‌واسطه»، و بج دوم روی کارت تماس؛ به‌علاوهٔ جملهٔ «نیازی به ارزیابی فروشنده واسطه ندارد».
- جای همه، **یک کارت واحد** نشست که برای هر دو نوع یکسان است و فقط یک بج نوع دارد («تولیدکننده» / «فروشنده»). رنگ سبز دیگر یعنی «ارزیابی تأییدشده»، نه «تولیدکننده انتخاب شد».
- placeholder «-- جستجو و انتخاب فروشندهٔ واسطه --» → «جستجو و انتخاب تولیدکننده یا فروشنده». برچسب‌های `PartnerSelector` هم از «کارخانه تولیدی مرجع (Manufacturer)» و «فروشنده / بازرگانی واسطه (Supplier)» به «تولیدکننده» و «فروشنده» ساده شدند.
- عنوان کارت تماس: «… تولیدکنندهٔ مرجع (تکمیل خودکار - Read-Only)» → «اطلاعات تماس تولیدکننده».
- **گرید دروغین رفع شد:** کد `grade || 'A'` و `status || 'تایید شده'` داشت، پس فروشندهٔ ارزیابی‌نشده در این فرم «Grade A / تایید شده» دیده می‌شد. حالا «ارزیابی نشده» و «—» نشان می‌دهد (تست زنده با یک فروشندهٔ بدون ارزیابی).
- **بازمانده‌های کد حذف شد:** `BusinessPartner.manufacturerId` از `types.ts` (ستونی به این نام در `business_partners` وجود ندارد) و سه مقدارش در `db_business_partners.ts`؛ خط ۶۷ `VendorForm` که از یک Supplier فیلد `manufacturerId` می‌خواند؛ متغیر بی‌مصرف `isDirectPurchase`.
- **حالت مردهٔ `optional` در `PartnerSelector` پاک شد:** یک شاخهٔ کامل «خرید بی‌واسطه» (گزینهٔ dropdown + چیپ سبز + برچسب «اختیاری، در صورت خرید مستقیم خالی بگذارید») که **هیچ صداکننده‌ای** فعالش نمی‌کرد. پراپ‌های `selectedManufacturerId`/`manufacturerId` هم که فقط در یک متغیر خوانده‌نشده جمع می‌شدند حذف شدند.
- تست زنده: با تولیدکننده → هیچ واژهٔ «بی‌واسطه/واسطه/Direct» روی صفحه نمانده؛ با فروشندهٔ ارزیابی‌شده → گرید و امتیاز واقعی؛ با فروشندهٔ ارزیابی‌نشده → «ارزیابی نشده». بدون خطای کنسول.

## اعتبارسنجی کد IRC در فرم ثبت سورس (۱۴۰۵/۰۶/۰۵)
- کد IRC **اختیاری** می‌ماند، ولی اگر پر شود باید دقیقاً **۱۶ رقم عددی** باشد؛ وگرنه ثبت انجام نمی‌شود و خطا زیر همان فیلد نشان داده می‌شود (`role="alert"` + اسکرول و فوکوس، مثل خطاهای مادهٔ اولیه و شریک).
- ورودی فقط رقم می‌پذیرد: حروف/خط‌تیره/فاصله حذف می‌شوند (پیست‌کردن کد با جداکننده کار می‌کند)، ارقام فارسی/عربی به لاتین تبدیل می‌شوند، و طول روی ۱۶ قفل است. شمارندهٔ «n/۱۶» کنار برچسب.
- **رکوردهای قدیمی قفل نمی‌شوند:** مقدارهای موروثی مثل `N/A` فقط هشدار کهربایی می‌گیرند و تا وقتی دست نخورده‌اند مانع ذخیره نیستند (`isLegacyIrcUntouched`)؛ به‌محض ویرایش، قاعدهٔ ۱۶ رقم اعمال می‌شود.
- تست زنده: با ماده و شریک انتخاب‌شده و IRC پنج‌رقمی → **هیچ POSTی ارسال نشد**، فوکوس روی `#vf-irc` و پیام «کد IRC باید دقیقاً ۱۶ رقم باشد؛ ۵ رقم وارد شده است» · با ۱۶ رقم → `POST /api/vendors` = ۲۰۰ و رفتن به صفحهٔ سورس · تایپ `12ab-34 56` → `123456` · تایپ `۱۲۳۴` → `1234` · ۸۸ تست سبز.

## صفحهٔ سورس — بستهٔ ۱: دارک‌مود و خوانایی (۱۴۰۵/۰۶/۰۵)
- **`VendorDetail.tsx` اصلاً دارک‌مود نداشت:** صفر مورد `dark:` در برابر ~۴۰ پس‌زمینهٔ رنگیِ فقط-روشن. حالا **۲۱۳ جفت `dark:`** به‌صورت قاعده‌مند اضافه شد: `bg-{c}-50→dark:bg-{c}-950/30` · `bg-{c}-100→dark:bg-{c}-900/40` · `border-{c}-100..300→dark:border-{c}-800` · `text-{c}-700..950→dark:text-{c}-300` · `text-{c}-500|600→dark:text-{c}-400` (+ معادل‌های hover).
- **باگ خوانایی اندازه‌گیری‌شده:** برچسب تأمین‌کننده در کارت تماس `rgb(0,79,59)` روی `rgb(30,41,59)` بود = **۱٫۵۲:۱**. حالا `rgb(94,233,181)` = **۹٫۶۱:۱**.
- **جزیرهٔ روشن:** کادر هشدار انقضای مجوز در دارک‌مود `rgb(255,251,235)` بود روی صفحهٔ `rgb(11,17,32)`. حالا پس‌زمینهٔ تیرهٔ کهربایی با متن `rgb(255,210,48)` = **۱۳٫۶۱:۱**. همین برای پنل «لیست سیاه» (rose) هم درست شد.
- **۱۶ کلاس Tailwind با پلهٔ ناموجود** که هیچ استایلی اعمال نمی‌کردند: `focus:ring-indigo-505` ×۴، `border-indigo-250` ×۴، `text-slate-505` ×۲، `text-slate-605`، `text-indigo-850`، `text-red-905`، `border-amber-250`، `bg-teal-55`، `bg-orange-105` → به نزدیک‌ترین پلهٔ معتبر یا توکن تبدیل شدند.
- خاکستری‌های خام (`hover:bg-slate-200`، `bg-slate-200/300`، `border-slate-50`، `border-slate-900/5|10`، `text-slate-300`) به توکن‌های `accent`/`muted`/`border` رفتند.
- تست زنده: لایت بدون هیچ تغییری (مقایسهٔ اسکرین‌شات قبل/بعد) · دارک روی سورس عادی و سورس لیست‌سیاه · ۰ خطای کنسول · ۸۸ تست سبز.
## حذف ۸ `alert()` باقی‌ماندهٔ برنامه (۱۴۰۵/۰۶/۰۵)
برخلاف مورد فرم ریسک (که از مسیر UI غیرقابل‌دسترس بود)، این هشت مورد **واقعاً به کاربر نشان داده می‌شدند**. الگو همان `UsersView` است: `role="alert"` قرمز برای خطا، `role="status"` برای اطلاع.

- **`VendorDetail` — ثبت نتیجهٔ آزمایش (۲):** «تاریخ آزمایش» و «کد آزمایشگاهی» حالا پیام درون‌خطی بالای دکمه‌های فرم می‌دهند و با انصراف پاک می‌شوند.
- **`VendorDetail` — ویرایش ردیف آزمایش (۲):** پیام داخل همان ردیف جدول، بالای دکمه‌های ذخیره/انصراف؛ با شروع ویرایش ردیف دیگر پاک می‌شود.
- **`VendorDetail` — رد/بازگردانی سورس (۲):** پیام زیر textarea دلیل، داخل باکس تصمیم کیفی.
- **`AuditTrailView` — خروجی اکسل (۲):** «رکوردی یافت نشد» و «خطا در تهیهٔ خروجی» به دو حالت جدا تفکیک شدند — اولی `status` کهربایی (نتیجهٔ خالی یک یافته است نه خرابی) و دومی `alert` قرمز؛ هر دو زیر همان دکمه‌ای که شروعشان کرده.
- تأیید زنده روی هر پنج مسیر با **صفر دیالوگ بومی**: ثبت آزمایش بدون کد QC · ویرایش ردیف با کد خالی · رد سورس بدون دلیل · خروجی با فیلتر بی‌نتیجه · خروجی با درخواست قطع‌شده (`route.abort`).
- **حالا هیچ `alert()` بومی در کل `src` باقی نمانده.**

## فرم ارزیابی ریسک — حذف `alert()` بومی (۱۴۰۵/۰۶/۰۵)
- `alert('شما دسترسی ثبت ارزیابی ریسک را ندارید.')` با پیام درون‌خطی `role="alert"` بالای نوار نتیجه جایگزین شد (همان الگوی فرم ثبت سورس): RTL، تم‌پذیر، و بعد از بسته‌شدن ناپدید نمی‌شود.
- ⚠️ **نکتهٔ مهم که حین تست روشن شد:** `VendorDetail` این فرم را فقط وقتی رندر می‌کند که کاربر `vendor.risk` داشته باشد (`canRisk` در خط ۳۴ و گیت دکمهٔ باز‌کردن در خط ۱۱۲۵). یعنی این `alert()` عملاً **از مسیر UI غیرقابل‌دسترس** بود و فقط یک تور ایمنی است. با کاربر `qa` تست شد: فرم اصلاً باز نمی‌شود.
- به همین دلیل، نسخهٔ اولی که نوشتم (دکمهٔ disabled + یادداشت دائمی «دسترسی ندارید») **UI مرده** می‌ساخت و پس گرفته شد؛ فقط تور ایمنی درون‌خطی ماند.
- تأیید زنده با ادمین: مسیر عادی سالم است — صفر دیالوگ بومی، کارت موفقیت نمایش داده شد، ارزیابی ذخیره و روی صفحه ماند، ۰ خطای کنسول.
- **باقی‌ماندهٔ `alert()` در برنامه (تأیید نشده):** ۶ مورد در `VendorDetail` (اعتبارسنجی فرم نتایج آزمایشگاه و باکس رد/بازگردانی سورس) و ۲ مورد در `AuditTrailView` (خروجی اکسل).

## فرم ارزیابی ریسک — یکدست‌سازی با بقیهٔ فرم‌ها (۱۴۰۵/۰۶/۰۵)
- **مشکل:** `RiskAssessmentForm.tsx` صرف‌نظر از تم، همیشه تیره رندر می‌شد: `bg-slate-900` برای پوسته، `bg-slate-800/40` برای پنل‌ها، `text-white`/`text-slate-300` برای متن، و glow کهربایی (`shadow-[0_0_20px_rgba(245,158,11,0.1)]`). چون داخل ویزارد صفحهٔ سورس رندر می‌شود، روی صفحهٔ روشن یک جزیرهٔ تیره بود. صفر مورد `dark:` داشت.
- **اصلاح:** همان قراردادهای `EvaluationForm` (فرم خواهرش در همان ویزارد) — پوسته `bg-card border-border rounded-2xl p-6 md:p-8 shadow-sm`، پنل‌های فیلد `bg-muted border-border`، متن `text-foreground`/`text-muted-foreground`، سلکت‌ها `bg-background border-border focus:ring-ring`، و دکمهٔ ثبت با `bg-primary text-primary-foreground` (هم‌شکل دکمهٔ «مرحله بعد» درست زیرش).
- **ماتریس ریسک:** سلول‌های رنگی با آلفای پایین (`/25`) نگه داشته شدند چون روی هر دو زمینه کار می‌کنند؛ عدد داخل سلول `text-foreground` شد و حلقهٔ سلول فعال از `ring-white` به `ring-ring` تغییر کرد تا در تم روشن هم دیده شود.
- **کهربایی به‌عنوان رنگ معنایی ریسک ماند** ولی با جفت روشن/تیره: `text-amber-600 dark:text-amber-400`. همین برای سطح ریسک (سبز/کهربایی/قرمز).
- تأیید زنده: لایت → پوسته `rgb(255,255,255)` روی صفحهٔ `rgb(244,247,250)`؛ دارک → `rgb(30,41,59)` روی `rgb(11,17,32)`؛ سلکت‌ها در هر دو تم با متن خوانا. ۰ خطای کنسول · ۹۴ تست سبز.
- باقی‌مانده (خارج از این تغییر): این فرم هنوز برای خطای دسترسی از `alert()` بومی استفاده می‌کند — همان الگویی که در فرم ثبت سورس به خطای درون‌خطی تبدیل شد.

## تغییر عنوان «گرید SOP» به «گرید ارزیابی» (۱۴۰۵/۰۶/۰۵)
هر ۴ نقطه‌ای که این عنوان را حمل می‌کرد:
- `VendorDetail.tsx:439` — «گرید SOP فروشنده:» → «گرید ارزیابی فروشنده:» (تأیید زنده: «گرید ارزیابی فروشنده: A»).
- `excelExport.ts:525` — سرستون خروجی اکسل شرکا.
- `excelExport.ts:924` — ردیف خلاصهٔ SOP در پروندهٔ تک‌تأمین‌کننده.
- `sopEvaluation.ts:200` — متن رد ۴۲۲: «گرید ارزیابی این فروشنده X است؛ طبق دستورالعمل فقط گرید A قابل انتخاب است.» (این جمله هم سمت سرور و هم در `PartnerSelector` نمایش داده می‌شود.)

**دست‌نخورده ماند** (عمداً): واژهٔ SOP آنجا که به خودِ *مدرک* یا *فرآیند* اشاره دارد — «مدارک SOP»، «ارزیابی SOP انجام نشده»، «امتیاز کل SOP» و مانند آن — چون SOP آنجا اصطلاح واقعی GMP است نه عنوان گرید. اگر تغییر گسترده‌تری لازم است باید جداگانه تصمیم گرفته شود.

## اسکریپت ادغام مواد تکراری (۱۴۰۵/۰۶/۰۵)
`scripts/merge-duplicate-materials.ts` — پاک‌سازی ردیف‌های مادهٔ تکراری که مسیر ذخیرهٔ قدیمی سورس می‌ساخت.

```bash
DATABASE_URL=... ./node_modules/.bin/tsx scripts/merge-duplicate-materials.ts          # اجرای آزمایشی
DATABASE_URL=... ./node_modules/.bin/tsx scripts/merge-duplicate-materials.ts --apply  # اجرای واقعی
```

- **بدون `--apply` هیچ چیزی نوشته نمی‌شود** و فقط گزارش می‌دهد چه کاری انجام خواهد داد.
- فقط ردیف‌های تولیدشده (`^mat_`) در مدخل مخزن ادغام می‌شوند؛ **دو مدخل دست‌ساز هرگز ادغام نمی‌شوند**، هر چقدر هم شبیه باشند.
- تطبیق «یک ماده بودن» فقط با سیگنال قوی: CAS واقعی، یا نام فارسی/انگلیسی یکسان. مقدارهای جایگزین (`N/A` و…) به‌حساب نمی‌آیند. اگر یک ردیف به **بیش از یک** مدخل بخورد، رد می‌شود تا دستی بررسی شود.
- هر ادغام در یک تراکنش: انتقال لینک سورس‌ها، انتقال `evaluations`، حذف ردیف تکراری، و **ثبت رکورد audit** (قاعدهٔ ۲). اگر سورسی از قبل هم به تکراری و هم به مدخل اصلی وصل باشد (تخطی از یکتایی `vendor_materials`)، لینک تکراری حذف می‌شود نه منتقل.
- **باگی که همین تست گرفت:** `audit_id` از نوع `VarChar(50)` است و یک `material_id` تولیدشده به‌تنهایی پُرش می‌کرد (`P2000`). تراکنش درست rollback شد و شناسه کوتاه شد؛ نام ماده در `description` می‌آید.
- تست کامل روی دیتای ساختگی: ۲ ادغام، ۱ لینک منتقل، ۱ لینک متضاد حذف، ۱ ارزیابی منتقل، ۱ ردیف بی‌همتا دست‌نخورده، و **اجرای دوم بی‌اثر (idempotent)**. تأیید UI: سورسی که به تکراری وصل بود حالا نام استاندارد را نشان می‌دهد و مخزن فقط یک «استون» دارد.

## نام استاندارد ماده در صفحهٔ سورس (۱۴۰۵/۰۶/۰۵)
- **علائم:** در «مشخصات فنی و اطلاعات عمومی» به‌جای نام استاندارد، فقط نام سادهٔ ماده می‌آمد (مثلاً «استون» به‌جای «حلال - استون (برای متادون هیدروکلراید)»).
- **ریشه:** منطق نمایش درست بود ولی رکوردی که سورس به آن **لینک** است همیشه همان مدخل مخزن نیست. سورس‌هایی که قبل از اصلاح `materialId` ذخیره شده‌اند به مادهٔ تکراریِ ساخته‌شده از payload سورس وصل‌اند که فقط name/CAS/IRC دارد و `standard_name_fa|en` ندارد؛ پس fallback به نام ساده می‌افتاد. با ساختن همان سناریو در دیتابیس محلی بازتولید شد.
- **اصلاح:** منطق به `src/utils/materialNames.ts` منتقل شد (`resolveMaterialNames`) با ترتیب: رکورد لینک‌شده → اگر نام استاندارد نداشت، مدخل مخزن برای همان ماده که دارد (تطبیق با نام فارسی/انگلیسی/نام استاندارد/CAS واقعی) → نام ساده. CAS جایگزین (`N/A`) عمداً تطبیق نمی‌دهد وگرنه هر دو مادهٔ بدون CAS یکی حساب می‌شدند.
- ۶ تست جدید (`tests/materialNames.test.ts`) — مجموع **۹۴ تست**. تأیید زنده روی سه حالت: لینک به تکراری، لینک به مدخل واقعی، و ماده‌ای که اصلاً نام استاندارد ندارد.

## صفحهٔ سورس — بسته‌های ۳ و ۴ (۱۴۰۵/۰۶/۰۵)
- **حذف واژگان منسوخ «خرید بی‌واسطه»:** چیپ سبز هدر و کل باکس کارت تماس (با جملهٔ «این سورس فاقد فروشنده واسطه بوده…») حذف شدند. این ادعا از روی `role === 'manufacturer'` ساخته می‌شد، در حالی که طبق قاعدهٔ ۴ تولیدکننده و فروشنده مستقل‌اند و این پیوند چیزی دربارهٔ وجود یا نبود واسطه نمی‌گوید. متغیر به `partnerIsManufacturer` تغییر نام داد چون تمایز آیکون/رنگ تولیدکننده و فروشنده گزارهٔ درستی است و ماند. گرید ستون‌بندی هم که فقط برای همان باکس دو‌ستونه می‌شد، تک‌ستونه شد.
- **برچسب فیلدها فارسی شد:** `CAS Number` → «شمارهٔ CAS» · `IRC Code`/`IVC Code` → «کد IRC»/«کد IVC» · `کد سیستم / Unique ID` → «کد داخلی سامانه».
- **شناسهٔ داخلی از رتبهٔ فیلد ثبتی پایین آمد:** قبلاً با قاب و رنگ فیروزه‌ای هم‌وزن CAS و IRC بود؛ حالا متن ساده و کم‌رنگ با tooltip «کد ثبتی رگولاتوری نیست».
- **حالت خالی کارت تماس:** وقتی شریک تلفن/ایمیل/آدرس/رابط ندارد، به‌جای فضای سفید، جمله‌ای می‌گوید اطلاعات در مخزن شرکا ثبت نشده و از همان‌جا قابل تکمیل است.
- **ویزارد به مرحلهٔ جدید اسکرول می‌کند:** `setEvalStage` حالا بعد از تغییر مرحله، بلوک استپر را به دید می‌آورد. تأیید زنده: `scrollTop` از ۰ به ۶۸۹ رفت و در هر دو مرحله `scrollTop === maxScroll` بود (یعنی تا انتهای ممکن اسکرول شد؛ استپر در ۴۱۱px می‌ماند چون محتوای زیرش کمتر از یک صفحه است).
- تست زنده: هیچ ردی از واژگان منسوخ و برچسب انگلیسی روی صفحه نماند · حالت خالی در لایت و دارک · ۰ خطای کنسول · ۸۸ تست سبز.

## صفحهٔ سورس — بستهٔ ۲: هدر (۱۴۰۵/۰۶/۰۵)
- **بج گرید دقیقاً وقتی مخفی می‌شد که وجود داشت:** شرط `!isScored && <GradeBadge/>` یعنی سورسِ امتیازخورده — همان که گرید دارد — هیچ گریدی نشان نمی‌داد. شرط حذف شد و متغیر بی‌مصرف `isScored` هم پاک شد. تأیید: سورس ۸۶ حالا «Grade A» دارد، سورس بدون امتیاز «جدید».
- **حلقهٔ امتیاز برچسب‌دار شد.** عدد وسط حلقه برای ادمین امتیاز کل وزنی است و برای کاربرِ تک‌دپارتمانی امتیاز همان دپارتمان؛ بدون کپشن، دو نفر یک عدد را یک چیز می‌خواندند. حالا زیر حلقه «امتیاز کل» / «امتیاز کیفیت» / «بدون امتیاز» می‌آید. تأیید زنده: ادمین `86 · امتیاز کل`، کاربر qa `91 · امتیاز کیفیت` روی همان سورس.
- **تایپوگرافی فارسی:** فاصلهٔ قبل از دونقطه در هدر حذف شد (`تأمین‌کننده:`، `کشور:`)، و برچسب مبهم `Grade :` به «گرید SOP فروشنده:» تغییر کرد تا با گرید خودِ سورس اشتباه نشود.
- **نکتهٔ محیط:** برای کاربر qa چهار خطای ۴۰۳ در کنسول دیده می‌شود؛ با stash کردن تغییرات تأیید شد که **از قبل وجود داشته** و ربطی به این بسته ندارد (فراخوانی endpointهای ادمین‌ویژه؛ طبق قاعدهٔ ۱۴ باعث خروج کاربر نمی‌شود).



## دسته‌بندی‌ها — دور دوم: دو باگ و پنج بهسازی (۱۴۰۵/۰۶/۰۵)
- **باگ ۱ — نبود نشانهٔ فوکوس:** سربرگ گروه و ردیف‌های سورس `role="button"` + `tabIndex={0}` بودند بدون هیچ استایل فوکوس، و چون `div`اند حلقهٔ پیش‌فرض مرورگر هم به ارث نمی‌رسید. حالا `focus-visible:ring-2 ring-ring ring-inset`. تأیید با Tab واقعی: `rgb(37,99,235) 0 0 0 2px inset` و `:focus-visible` مچ می‌شود.
- **باگ ۲ — پیام گمراه‌کننده هنگام بارگذاری:** تا رسیدن داده، شرط «۰ گروه» فعال می‌شد و صفحه «نتیجه‌ای یافت نشد» نشان می‌داد. `isLoading={isSyncing && db.length === 0}` از `App` پاس داده می‌شود و سه اسکلت هم‌شکل کارت گروه رندر می‌شود؛ پیام خالی فقط بعد از رسیدن داده. تأیید با کش پاک + تأخیر ۷ ثانیه‌ای روی `/api/vendors`: `skeleton: true, saysNotFound: false`.
- **موبایل:** سه ستون گرید/امتیاز/ریسک `hidden sm:flex` بودند و روی گوشی هیچ‌کدام دیده نمی‌شد؛ یک ردیف فشرده با همان مقادیر زیر نام سورس اضافه شد (دادهٔ جدید نیست، همان دسکتاپ است).
- **تراکم:** گام عمودی کارت گروه از **۱۰۴px به ۷۱px** رسید (نام و CAS در یک خط، padding کمتر) — بدون حذف هیچ محتوایی.
- **هم‌ترازی:** سه عرض ثابت مستقل (`w-28/32`, `w-24/28`, `w-24/28`) با یک `grid-cols-[8rem_7rem_7rem]` مشترک جایگزین شد تا ستون‌ها بین ردیف‌ها هم‌تراز شوند (برای لیست سیاه تک‌ستونه).
- **حذف تکرار:** بج «نام ماده اولیه» و نام ماده از ردیف‌های داخل گروه حذف شد؛ نام تأمین‌کننده خط اول شد.
- **نوار چسبان:** دو ردیف (تیتر+خروجی / کارت جست‌وجو) در یک ردیف ادغام شد و کارت دور نوار ابزار حذف شد؛ ارتفاع از **~۲۰۰px به ۱۰۵px**. چیپ‌ها خط خودشان را دارند چون wrap می‌شوند.
- تست زنده: دسکتاپ/موبایل، بدون سرریز افقی، ۰ خطای کنسول، ۸۸ تست سبز.

## یکدست‌سازی بصری صفحهٔ دسته‌بندی‌ها (۱۴۰۵/۰۶/۰۵)
> تصمیم کاربر: **هیچ اطلاعات جدیدی اضافه نشود**؛ فقط یکدستی با بقیهٔ نرم‌افزار و خوانایی.

- **پالت:** ۲۴ رنگ هاردکد در پنل مقایسه (`#0071E3` آبیِ اپل، `#4096FF`، `#6E6E73`، `#424245`) به توکن‌های `primary`/`muted-foreground`/`foreground` تبدیل شد؛ به‌علاوهٔ نقطه‌رنگ گرید B در `MaterialGroup` و ۴ مورد باقی‌مانده در `ArchiveView`/`SupplierAuditView`. حالا هیچ نمای دسته‌بندی رنگ خارج از توکن ندارد.
- **باگ خوانایی دارک‌مود:** خط فرمول موتور با `text-[#424245]` رندر می‌شد، یعنی `rgb(66,66,69)` روی کارت تقریباً مشکی — عملاً نامرئی (کنتراست ~۱٫۲:۱). بعد از تبدیل به توکن: `rgb(248,250,252)`.
- **کف اندازهٔ قلم:** ۳۰ مورد `text-[10px]`/`text-[9px]` در دو فایل به `text-[11px]` رسید (فارسی در ۹–۱۰px عملاً خوانا نیست).
- **مقیاس گردی یکدست شد:** پنل مقایسه هم‌زمان از `rounded`/`sm`/`md`/`lg`/`xl`/`full` استفاده می‌کرد؛ حالا کارت `xl`، کنترل `lg`، چیپ `full`.
- **نوار ابزار:** دکمهٔ پاک‌کردن جستجو با آیکون ذره‌بین فقط **۴px** فاصله داشت (اندازه‌گیری شد) و مثل یک لکه دیده می‌شد؛ به انتهای دیگر فیلد منتقل شد (فاصله: ۱۹۸px) · CTA از «خروجی اکسل خرید خارجی (XLSX)» به «خروجی اکسل» · حذف چوخان تزئینی کنار برچسب مرتب‌سازی (خودِ select چوخان دارد) + اتصال `label htmlFor` · حذف زیرعنوان انگلیسی `FOREIGN PURCHASE` (همان کاری که در سایدبار شد) · چیپ‌های انگلیسیِ دستهٔ نمونه فارسی شد.
- **کاراکترهای متنی `⚠` و `★`** با آیکون lucide جایگزین شدند (در فونت‌ها و تم‌های مختلف یکسان رندر نمی‌شدند).
- **چیپ با شمارش صفر** دیگر کلیک‌پذیر نیست (`opacity-45 pointer-events-none`) — قبلاً «لیست سیاه: ۰» فیلتری بود که فقط می‌توانست فهرست خالی بدهد.
- **جدول QC روی موبایل:** `w-full` باعث می‌شد جدول فشرده شود و سرستون‌ها دوخطی شوند؛ با `min-w-[420px]` اسکرول به ظرف موجود سپرده شد.
- تست زنده: دارک/لایت/موبایل · بدون سرریز افقی · ۰ خطای کنسول · ۸۸ تست سبز.

## IRC روی سورس ذخیره می‌شود، نه روی ماده (۱۴۰۵/۰۶/۰۵)
> رفع باگی که هنگام تست بالا پیدا شد و قاعدهٔ ۳ را نقض می‌کرد.

- **باگ:** جدول `vendors` ستون `irc` نداشت؛ `saveVendorToDb` مقدار IRC را به `generateMaterialId(cas, irc, …)` می‌داد، پس ثبت هر سورس با IRC یک **ردیف مادهٔ تکراری** می‌ساخت (`mat_103_90_2_1228123456789012` کنار `M-1` برای همان «پاراستامول») و سورس به آن نسخهٔ تکراری وصل می‌شد — یعنی هم IRC عملاً روی ماده می‌نشست، هم مخزن مواد آلوده می‌شد.
- **مهاجرت `20260827134500_vendor_irc`:** ستون `vendors.irc` اضافه شد و مقدارها از مادهٔ متصل **backfill** شدند (فقط کدهای واقعی؛ `N/A`/`NA`/`-` رد می‌شوند).
- `saveVendorToDb` حالا به `v.materialId` (همان مادهٔ انتخاب‌شده از مخزن) وصل می‌کند و id مشتق فقط fallbackِ payloadهای قدیمی است — و IRC دیگر جزئی از هویت ماده نیست. رکورد ماده هم **فقط ساخته** می‌شود، هرگز از payload سورس بازنویسی نمی‌شود.
- `getVendorsList` مقدار را از خود سورس می‌خواند و برای رکوردهای قدیمی به IRC ماده fallback می‌کند.
- **گارد سمت سرور (قاعدهٔ ۱۴):** `ircViolation()` در `POST /api/vendors` و `PATCH /api/vendors/:id/profile` → **۴۲۲**. خالی و `N/A` مجازند و مقدارِ تغییرنکرده هم استثناست تا رکورد قدیمی غیرقابل‌ویرایش نشود.
- **باگ جانبی که همین‌جا رو شد:** upsert لینک `vendor_materials` روی id مصنوعی `link_<v>_<m>` انجام می‌شد، پس برای لینک‌هایی که id خودشان را دارند (seed/import) به‌جای update سعی می‌کرد ردیف دوم برای همان جفت بسازد و به unique constraint می‌خورد؛ حالا روی کلید یکتای `(vendorId, materialId)` upsert می‌شود. قبلاً پنهان بود چون materialId مشتق باعث می‌شد `deleteMany` اول لینک قبلی را پاک کند.
- تست زنده: مهاجرت روی دیتای موجود اجرا و backfill درست انجام شد (`V9 → 1228999988887777`، رکورد `N/A` خالی ماند) · `POST` با IRC پنج‌رقمی = **۴۲۲**، با ۱۶ رقم = ۲۰۰ و **هیچ مادهٔ تکراری ساخته نشد** (تعداد مواد ۲ ماند و سورس به `M-1` واقعی وصل شد) · `PATCH /profile` با `N/A` تکراری = ۲۰۰، با `777` = ۴۲۲، با ۱۶ رقم = ۲۰۰ و لینک قدیمی `L1` دست‌نخورده ماند · ثبت از UI: IRC روی صفحهٔ سورس نمایش داده شد · ۸۸ تست سبز.


## تقویم شمسی `ShamsiDatePicker` (۱۴۰۵/۰۶/۰۵)
- **قرارگیری با portal:** پاپ‌اور `absolute top-full` بود، پس هر ancestor با overflow آن را می‌برید (`overflow-x-auto` جدول رکوردهای آزمایشگاه در `VendorDetail:1508`، و پنل `overflow-hidden` در `FormModal:175`) و هرگز به بالا flip نمی‌کرد. حالا `createPortal` به `body` + موقعیت از `getBoundingClientRect()` تریگر، با flip خودکار و clamp افقی، و به‌روزرسانی روی scroll/resize. (همان دلیل قاعدهٔ ۸.)
- **توکن‌های تم:** کل پالت ایندیگوی هاردکد (`indigo-300/500/600/50/800`, `shadow-indigo-600/20`) به `primary`/`accent`/`ring`/`popover` تبدیل شد. `border-slate-250` **کلاس واقعی Tailwind نبود** ⇒ آن دو المان اصلاً بوردر نداشتند؛ در این فایل و ۲ مورد `VendorDetail` به `border-border` تغییر کرد.
- **کیبورد:** Escape می‌بندد و فوکوس را برمی‌گرداند؛ کلیدهای جهت/Home/End بین روزها حرکت می‌کنند (با عبور از مرز ماه)؛ هنگام باز شدن فوکوس روی روز انتخاب‌شده می‌نشیند.
- **منطق:** حلقهٔ «امروز» در گرید · «برو به امروز» از `jalaali.toJalaali` (به‌جای `Intl('fa-IR')` + تبدیل دستی ارقام) · فهرست سال نسبت به سال جاری (به‌جای ۱۳۸۰–۱۴۲۰ ثابت) · حذف عدد میلادی ۸px و خط راهنما (معادل در tooltip و زیر input هست) · sync ماه با `value` بیرونی.
- تست زنده: با فضای کافی به پایین باز می‌شود (y=368، پایین ۷۳۷ از ۹۰۰) · در viewport ۵۶۰px به بالای تریگر flip می‌کند (y=8) · Escape می‌بندد · ArrowLeft+ArrowDown از ۰۵ به ۱۳ می‌رود و Enter ثبت می‌کند.

## فرم ثبت سورس (۱۴۰۵/۰۶/۰۵)
- **اعتبارسنجی از `alert()` بومی به خطای درون‌خطی تبدیل شد.** قبلاً فرم خالی یک پنجرهٔ بومی مرورگر می‌داد که نه RTL می‌شد، نه تم می‌گرفت، و بعد از بستن هیچ نشانی از فیلد مشکل‌دار نمی‌ماند. حالا `role="alert"` زیر همان فیلد + اسکرول خودکار + فوکوس روی کنترل.
- **دو اسکرول‌بار تودرتو حذف شد:** بدنهٔ فرم `max-h-[80vh] overflow-y-auto` داشت (۹۲۱px محتوا در پنل ۷۶۰px، داخل صفحهٔ ۸۸۷px). این یک **صفحه** است نه مودال (قاعدهٔ ۸b) پس سقف حذف شد. اندازه‌گیری بعد: **یک** ناحیهٔ اسکرول.
- **قفل رنگ:** ۱۸ رنگ هاردکد `#0071E3`/`#0025D2` در این فرم و ۸ مورد در `MaterialSelector`/`PartnerSelector` با توکن `primary`/`ring` جایگزین شد. قبلاً دکمهٔ ثبت `rgb(37,99,235)` و لینک «ثبت تأمین‌کننده جدید» `rgb(0,113,227)` بودند. بعد از تغییر: هیچ نودی با آن آبی نمانده.
- **کنترست ورودی‌ها:** ورودی‌ها `bg-card` بودند یعنی دقیقاً هم‌رنگ کارت پشتشان (سفید روی سفید، و در دارک‌مود `rgb(30,41,59)` روی همان). به `bg-background` تغییر کرد.
- **دسترس‌پذیری:** `label[for]` از **۰** به ۳ رسید (فیلدهای واقعی؛ سلکتورهای سفارشی دکمه‌اند و برچسب متنی دارند).
- **پاکسازی:** em-dash ۱→۰ · حذف `(Material Master)`/`(Source Type)`/`(SOP Evaluation Summary)`/`(Contact Person)`/`(Email)` · حذف شماره‌گذاری تکراری («بخش اول:» کنار بج ۱) · گزینه‌های وضعیت نمونه فارسی شد · باکس «وضعیت و گرید اولیه» از شکل فیلد به یک جملهٔ راهنما تبدیل شد · ۶ `input readOnly` مشخصات شریک به متن ساده تبدیل شد (۶→۰).
- تست زنده: فرم خالی → خطای درون‌خطی و صفر دیالوگ بومی؛ انتخاب ماده و شریک → خطا پاک شد؛ ثبت → رکورد ساخته شد و به صفحهٔ همان سورس رفت. دارک/موبایل بدون خطای کنسول.
- **باقی‌مانده (خارج از دامنهٔ این بررسی):** `#0071E3` هنوز در ۸ فایل دیگر هست (ArchiveView، AuditTrailView، MaterialGroup، SupplierAuditView، ScoreBar، ScoringGuide، PrintableForms، MaterialsComparisonSection).

## سایدبار، عنوان و لوگو (۱۴۰۵/۰۶/۰۵)
- **لوگو در دارک‌مود:** همان باگ صفحهٔ ورود؛ نشان سرمه‌ای روی کارت تیره محو می‌شد. پلیت روشن `dark:` گرفت.
- **سلسله‌مراتب عنوان برعکس بود:** تیتر انگلیسی ۱۴px در **۳ خط**، و نام فارسی زیرش با مونو ۱۰px. حالا «سامانهٔ ارزیابی تامین‌کنندگان» تیتر است و `VLSE` زیرنویس کوچک. ارتفاع بلوک برند ۱۰۸px → **۸۱px**.
- **۱۲ زیرنویس انگلیسی منو حذف شد** (`FOREIGN PURCHASE`, `MATERIALS MASTER`, `AUDIT TRAIL CENTER`, …) و پراپ `sub` از `AppSidebarButton` برداشته شد. عنوان‌های گروه هم دیگر جفت فارسی/انگلیسی نیستند.
- **⚠️ پیش‌بینی اشتباه و اصلاحش:** فکر می‌کردم حذف زیرنویس‌ها ارتفاع منو را ۸۸۰→۷۳۶ می‌آورد. اندازه‌گیری بعد از تغییر: **همان ۸۸۰** — چون ارتفاع هر ردیف را آیکن ۳۲px + padding تعیین می‌کرد نه متن. سپس آیکن ۳۲→۲۸، `py-2.5`→`py-2`، فاصلهٔ آیتم‌ها و عنوان گروه‌ها کم شد → ردیف ۵۲→۴۴px و مجموع **۸۲۰px**. دسکتاپ (۸۲۰ در دسترس): **دیگر اسکرول نمی‌خورد**. موبایل ۳۹۰×۸۴۴: ۷۲۴ در برابر ۷۱۴ — ۱۰px اسکرول باقی می‌ماند.
- **سه انیمیشن دائمی حذف شد** (بج زنگوله، دو بج هشدار سایدبار)؛ هیچ‌کدام `prefers-reduced-motion` را رعایت نمی‌کردند و تغییر حالتی نشان نمی‌دادند.
- **چیپ «سیستم فعال»** که همیشه سبز بود، جایش را چیپی گرفت که **فقط در حالت لوکال** ظاهر می‌شود («حالت لوکال — بدون پایگاه‌داده»). وضعیتی که عوض نمی‌شود وضعیت نیست.
- «پشتیبان‌گیری (JSON)» → «پشتیبان‌گیری» (نام فرمت در tooltip باقی است).
- تست زنده: `animate-pulse` = ۰، زیرنویس انگلیسی در منو = ۰، دارک/روشن/موبایل بدون خطای کنسول.

## بازطراحی داشبورد اصلی (۱۴۰۵/۰۶/۰۵)
- **باگ داده:** ردیف KPI روی `db.length` حساب می‌شد ولی فقط ۴ گرید را نشان می‌داد، پس درصدها به ۶۶٪ می‌رسید و **سورس‌های بدون گرید بی‌صدا حذف می‌شدند** — در حالی که دونات کنارش همان برش را نشان می‌داد. کارت «بدون گرید» اضافه شد و حالا شش کارت کل جمعیت را پوشش می‌دهند.
- **باگ جمعیت:** «کل تامین‌کنندگان» رکوردهای نمونه را می‌شمرد ولی «کارهای معوق» عمداً حذفشان می‌کرد. حالا هر دو از `sourceVendors` مشترک می‌خوانند و زیرنویس کارت تعداد نمونهٔ کنارگذاشته را می‌گوید.
- **نوار پیشرفت زیر KPIها حذف شد:** هر عدد را نسبت به کلی می‌سنجید که خودش درصدش بود، و رنگ همه `bg-primary` بود؛ کارت «کل» نواری همیشه‌پُر از خودش داشت. بج درصد کافی است.
- **خانهٔ یتیم موبایل:** ۵ کارت در گرید ۲ستونه یک خانهٔ خالی می‌ساخت؛ با ۶ کارت دقیقاً ۳ ردیف کامل می‌شود.
- **فید فعالیت:** رویدادهای ورود/خروج فیلتر شدند (`module !== 'احراز هویت'`)؛ قبلاً هر ۵ ردیف «ورود موفقیت‌آمیز admin» بود و هیچ تغییر دادهٔ واقعی دیده نمی‌شد. حالا limit=40 گرفته و ۵ تغییر واقعی نشان می‌دهد.
- **اولویت صفحه:** «کارهای معوق» از جایگاه دوم/نصف‌عرض به **بالای صفحه و تمام‌عرض** آمد؛ Hero تکراری (عنوانی که در سایدبار و تب مرورگر هم بود) به یک سطر جمع شد.
- **یکنواختی چیدمان شکسته شد:** فید فعالیت از کارت به فهرست تمام‌عرض تبدیل شد؛ نرخ آزمایشگاه کنار دونات نشست.
- **واژگان:** شش برچسب انگلیسی تزئینی حذف شد ((Grade Mix)، (Action Center)، (Recent Activity)، (Lab Pass)، (CATEGORIES)، ابروی VLSE)؛ «Pass/Reject» → «قبول/مردود»؛ «Approved» در کارت نمونه → «تایید شده»؛ دسته‌های خالی کم‌رنگ‌تر شدند.
- تست زنده با ۹ سورس/۷ آزمون: ۶ کارت KPI (بدون یتیم موبایل)، ۰ ردیف لاگین در فید، ۰ پرانتز انگلیسی در HomeView، مجموع کارت‌ها = کل جمعیت، دارک‌مود و موبایل سالم، بدون خطای کنسول.

## صفحهٔ ورود + رفع ریشه‌ای دارک‌مود (۱۴۰۵/۰۶/۰۵)
- **باگ سراسری:** `@custom-variant dark` در `index.css` تعریف نشده بود، پس Tailwind v4 به‌طور پیش‌فرض `dark:` را به **`prefers-color-scheme` سیستم‌عامل** وصل می‌کرد، نه کلاس `.dark` که سوییچر تم می‌گذارد. توکن‌ها (CSS ساده زیر `.dark`) درست عوض می‌شدند و برای همین به چشم نیامده بود، ولی **هر `dark:bg-…`/`dark:text-…` دست‌نویس** در کل کدبیس برای کاربری با سیستم‌عامل روشن بی‌اثر بود. اثبات: `dark:bg-white` با OS روشن `rgba(0,0,0,0)` و با OS تیره `rgb(255,255,255)`. با یک خط `@custom-variant dark (&:where(.dark, .dark *))` رفع شد.
- **صفحهٔ ورود (`LoginView.tsx`) بازنویسی شد:** رنگ‌های hardcode → توکن (قاعدهٔ ۷)؛ `dir="ltr"` انگلیسی → RTL و فارسی؛ `min-h-screen` → `min-h-[100dvh]`؛ کنترست متن راهنما اصلاح شد.
- افزوده: دکمهٔ نمایش/پنهان رمز، `autoFocus` روی نام کاربری، اسپینر و دکمهٔ غیرفعال حین بررسی، هشدار Caps Lock، و راهنمای «بازنشانی رمز فقط توسط مدیر سیستم».
- لوگو ۱۱۲px → ۸۰px؛ در دارک‌مود روی پلیت سفید می‌نشیند (لوگوی سرمه‌ای روی کارت تیره تقریباً نامرئی بود).
- سه پیام خطای انگلیسیِ `/api/auth/login` در `server.ts` فارسی شد (بقیهٔ پیام‌های همان مسیر از قبل فارسی بودند).
- تست زنده: دارک/روشن هر دو درست؛ autofocus ✓؛ toggle رمز `password`→`text` ✓؛ حین ارسال دکمه غیرفعال + اسپینر + `aria-busy` ✓؛ خطای فارسی ✓؛ هشدار Caps Lock با رویداد واقعی ✓؛ داشبورد و ماژول کاربران در دارک‌مود بدون خطا و با تینت درست.

## هم‌سان‌سازی فرم ثبت شریک داخل فرم سورس (۱۴۰۵/۰۶/۰۴)
- فرم سریعِ «ثبت تأمین‌کننده جدید» مجموعه و ترتیب فیلدهای خودش را داشت (از جمله «نام انگلیسی» که فرم اصلی اصلاً نمی‌گیرد) و همان رکورد `business_partners` را می‌نوشت.
- حالا **دقیقاً** فیلدها، ترتیب، برچسب‌ها، placeholderها و نوع کنترل فرم ماژول شرکای تجاری را دارد: نوع موجودیت → نام → کشور → شهر → آدرس کامل → نام رابط → شماره تماس → ایمیل رسمی → وبسایت → وضعیت (رادیو Active/Inactive).
- `nameEn` حذف شد (فرم اصلی ندارد)؛ «لیست سیاه» از انتخاب‌های وضعیت حذف شد چون در ماژول شرکا اقدام جداگانه با دلیل الزامی است.
- **باگ:** ۵ مدرک SOP به‌صورت پیش‌فرض روی `Approved` بودند، یعنی هر فروشندهٔ ساخته‌شده از اینجا بدون هیچ ارزیابی‌ای با ۱۰۰/۱۰۰ و گرید A متولد می‌شد. حالا مثل فرم اصلی «ارزیابی نشده» شروع می‌شود (امتیاز ۰).
- تست زنده: فهرست برچسب‌های دو فرم بایت‌به‌بایت یکسان شد (تولیدکننده و فروشنده)، ساخت شریک از داخل فرم سورس کار می‌کند و بلافاصله در فیلد انتخاب می‌شود، بدون خطای کنسول.

## برچسب‌های فارسی در «سورس‌های متصل به این شریک» (۱۴۰۵/۰۶/۰۴)
- ستون «دسته» کلید خام دیتابیس را نشان می‌داد (`foreign`, `packaging`, …). حالا از `categoryLabels` می‌خواند → «خرید خارجی»، «اقلام بسته بندی» و بقیه.
- ستون «گرید / وضعیت» یک کپی محلی از بج بود که برای سورس بدون گرید به وضعیت خام انگلیسی (`new`, `approved`) برمی‌گشت؛ با `GradeBadge` مشترک جایگزین شد («جدید»، «در حال ارزیابی»، «Grade A» …). تابع محلی `gradeBadgeCls` حذف شد.
- بررسی شد که این تنها جای برنامه بود که `category` خام رندر می‌شد.
- تست زنده با دو سورس متصل به BASF: «خرید خارجی + جدید» و «اقلام بسته بندی + Grade A»، بدون خطای کنسول.

## رفع باگ مودال بلند (۱۴۰۵/۰۶/۰۴)
- **علامت:** در «سطح دسترسی» ماژول کاربران، صفحه اسکرول نمی‌شد و دکمهٔ «ذخیره سطح دسترسی» دیده نمی‌شد.
- **ریشه:** `min-height:auto` روی flex item. wrapper کالر ۹۷۵px بود در حالی که پنل ۷۳۶px (=۹۲vh)؛ `overflow-hidden` پنل فوتر را می‌برید و بدنهٔ `overflow-y-auto` هم اسکرول نمی‌کرد چون ارتفاعش از پنل بیشتر بود.
- **رفع مرکزی در `FormModal`:** `[&>*]:min-h-0` روی پنل → سرریز به همان ناحیهٔ اسکرول داخلی می‌رسد. همهٔ دیالوگ‌های برنامه با یک تغییر درست شدند.
- تست زنده: قبل → wrapper 975px، بدنه بدون اسکرول، دکمه در y=991 (بیرون از viewport 800). بعد → wrapper 734px، بدنه ۸۳۸ در ۵۹۷ اسکرول می‌کند، دکمه در y=750 و قابل کلیک. تغییر مجوز ذخیره و پس از بازگشایی **persist** شد. دیالوگ‌های `md` و `lg` بدون تغییر رفتار.

## آخرین تغییر — فیلترهای Audit (۱۴۰۵/۰۶/۰۴)
- **ریشهٔ مشکل:** فرم فیلتر فهرست ماژول‌های خودش را دستی داشت و از ۷ گزینه فقط «مدیریت کاربران» با مقدار واقعی `audit_log.module` یکی بود؛ بقیه هیچ رکوردی برنمی‌گرداندند. همچنین `severity=Info` در برابر مقدار ذخیره‌شدهٔ `Information`، فیلتر کاربر با `userName` در برابر شرط `userId` سرور، و «گروه رویداد» که مسیر PostgreSQL اصلاً اعمالش نمی‌کرد (`eventType` ستون نیست).
- **راه‌حل:** `src/utils/auditTaxonomy.ts` — واژگان مشترک سرور/کلاینت (ماژول‌ها، عملیات، گروه‌ها، هم‌ارزی Info/Information).
- گروه رویداد = مجموعه‌ای از ماژول‌ها، با `module IN (…)` سمت سرور (و وقتی ماژول مشخص انتخاب شده غیرفعال می‌شود).
- بازهٔ تاریخ: `ShamsiDatePicker` به‌جای متن آزاد + تبدیل شمسی→ISO (پایانِ بازه شاملِ کل روز). تاریخ نامعتبر دیگر ۵۰۰ نمی‌دهد.
- جست‌وجوی متنی حالا بازهٔ تاریخ را هم اعمال می‌کند (قبلاً بی‌صدا نادیده می‌گرفت).
- کاربران/ماژول‌های دموی MOCK از dropdownها حذف شدند؛ ستون ماژول برچسب فارسی می‌گیرد.
- ستون «تاریخ و ساعت» دو خطی شد. `ShamsiDatePicker` خالی روی «امروز» باز می‌شود (قبلاً فروردین ۱۴۰۳).
- **حذف رکوردهای دموی Audit:** ۵ رکورد ساختگی `MOCK_AUDIT_LOGS` (از جمله «رد صلاحیت بچ ۹۰۴۲ به علت OOS») که وقتی دیتابیس خالی بود نمایش داده می‌شدند حذف شدند — در Audit Trail برای GMP، رکورد جعلی از رکورد واقعی قابل تشخیص نبود. آمار خالی حالا ۰ نشان می‌دهد و پیام «هنوز هیچ رویدادی ثبت نشده است» جای فهرست جعلی را می‌گیرد (متن حالت «فیلتر بی‌نتیجه» جداست).
- تست زنده روی Postgres: همهٔ فیلترها (کاربر/ماژول/گروه/عملیات/سطح/بازهٔ تاریخ/جست‌وجو+بازه) مقدار درست برگرداندند؛ UI: ماژول ۱ ردیف، گروه احراز هویت ۸، Information ۸، بدون خطای کنسول.

## آنچه انجام شده (به ترتیب)
### زیرساخت دیتابیس (فاز ۲)
1. ✅ نرمال‌سازی schema به ۱۲ جدول per-module + enum + index + FK (`6b7f0b6`)
2. ✅ اتصال endpointها به Postgres + حذف کامل fallback فایل JSON + مهاجرت users به DB (`235ef55`)
3. ✅ استفاده از جداول نرمال برای risk/analysis/activity (به‌جای ستون Text) (`ce1f90e`)
4. ✅ ادغام دو مدل audit موازی به `AuditLog` واحد (`022a4db`)
5. ✅ انتقال Business Partners به backend (`3c48fb9`) + رفع باگ حلقهٔ reload (`1af6ea3`)
6. ✅ پوشش کامل audit سمت‌سرور برای همهٔ ماژول‌ها (`937b5b0`)
7. ✅ Polish حالت‌های loading/error/toast (theme-aware) (`476af71`)

### ماژول سورس‌ها
8. ✅ مرتب‌سازی + فیلتر کلیک‌پذیر + هشدار IRC در لیست (`5fc05b7`)
9. ✅ تاریخچه و روند نمرات (از audit) + guard صفحه‌بندی (`422939f`)
10. ✅ ویزارد ارزیابی هدایت‌شده: امتیازدهی → ریسک → آزمایشگاه (`3d19c92`, `93625a0`)
10b. ✅ کاهش friction ناوبری: بازگشت از جزئیات سورس، همان مادهٔ باز‌شده را بازیابی می‌کند (A) هایلایت گذرای بصری روی مادهٔ بازگشتی، (B) ذخیرهٔ `expandedMaterial` در `viewHistory` (پایدار در reload)، (C) رفتار آکاردئون واقعی (فقط یک ماده هم‌زمان باز).

### ناوبری سراسری (بازطراحی — گروه‌های الف/ب/ج)
10c. ✅ **الف — پایه:**
  - `scrollContainerRef` + reset اسکرول روی هر تغییر view (با double-rAF، چون بعضی صفحات بعد از mount خودشان اسکرول را جابه‌جا می‌کردند).
  - **Breadcrumb زنده** در هدر از `getViewStateLabel` (که قبلاً کد مرده بود) + `goToHistoryIndex` برای پرش مستقیم به هر سطح. برچسب `business-partners` که جا افتاده بود اضافه شد.
  - سایدبار در صفحهٔ جزئیات دیگر active state را از دست نمی‌دهد (شرط `&& !selectedVendor` حذف شد).
  - **رفع باگ:** کلیک روی سایدبار وقتی یک سورس باز بود، ورودی تکراری push می‌کرد و «برگشت» دوباره وارد همان سورس می‌شد. حالا ناوبری سطح‌بالا مثل «تعویض تب» است: اگر مقصد در stack باشد به آن unwind می‌شود.
  - سقف `MAX_VIEW_HISTORY = 25` + ذخیرهٔ فقط snapshot سبک از vendor در localStorage (id/name/material) — جلوگیری از رشد بی‌حد و پر شدن quota.
10d. ✅ **ب — Back سخت‌افزاری/مرورگر:** الگوی sentinel روی `history.pushState` + شنوندهٔ `popstate`؛ تا وقتی جایی برای برگشت هست event مصرف و sentinel دوباره arm می‌شود، در ریشه اجازهٔ خروج داده می‌شود. (هوک‌ها عمداً بالای early-return‌های لاگین قرار دارند تا ترتیب هوک پایدار بماند؛ `goBackRef` برای شکستن وابستگی ترتیبی.)
10e. ✅ **ج — محافظ داده:** `registerNavGuard` در `VendorDetail` هنگام باز بودن فرم ویرایش؛ هر ناوبری پشت دیالوگ تأیید (`ماندن در صفحه` / `خروج بدون ذخیره`) می‌رود و `beforeunload` هم بستن تب را پوشش می‌دهد.
- **تست زنده:** با Playwright روی Postgres محلی تأیید شد — breadcrumb، Back مرورگر (depth ۳→۲)، حفظ باز بودن + هایلایت ماده هنگام بازگشت، `scrollTop=0` بعد از ناوبری، bounded ماندن depth بعد از ۴ بار جابه‌جایی تب، و هر سه مسیر دیالوگ محافظ.
10f. ✅ **د — مسیریابی hash و دیپ‌لینک** (`src/utils/navRoutes.ts` + ۹ تست واحد در `tests/navRoutes.test.ts`):
  - URL منبع حقیقت شد: `#/category/foreign/vendor/V1`، `#/category/foreign?m=Paracetamol` و … . لینک هر سورس **قابل ارسال به همکار** است.
  - الگوی sentinel حذف و جای آن ورودی‌های واقعی تاریخچهٔ مرورگر نشست ⇒ **دکمهٔ Forward هم کار می‌کند** (که با sentinel ممکن نبود) و منوی تاریخچهٔ مرورگر معنادار شد.
  - refresh دقیقاً همان‌جا برمی‌گردد؛ هر تب موقعیت مستقل خودش را دارد (تداخل کلید مشترک `app_viewHistory` بین تب‌ها رفع شد).
  - دیپ‌لینک فقط `id` دارد ⇒ حالت loading تا رسیدن `db`، و پیام «سورس یافت نشد» برای لینک به رکورد حذف‌شده. hash نامعتبر → خانه.
  - محافظ تغییرات ذخیره‌نشده با Back **واقعی** مرورگر هم یکپارچه شد (URL تا زمان تصمیم کاربر سر جایش می‌ماند).
  - **تست زنده:** encode/decode همهٔ viewها، Back/Forward، reload، دیپ‌لینک در مرورگر تازه (بدون localStorage) با breadcrumb درست، لینک نامعتبر، و hash بی‌معنی — همه سبز.

10g. ✅ **رفع باگ «مردودی که پاک نمی‌شود»** (`src/utils/vendorState.ts` + ۱۳ تست در `tests/vendorState.test.ts`):
  - **علت ۱ — قفل یک‌طرفهٔ `grade`:** با ثبت نتیجهٔ Reject، `normalizeAndCleanVendor` هم `status` و هم `grade` را `'rejected'` مهر می‌زد؛ با حذف آن نتیجه فقط `status` برمی‌گشت و `grade` قفل می‌ماند. چون همهٔ شمارنده‌ها `grade==='rejected' || status==='rejected'` را می‌خواندند، دونات و کارت لیست سیاه یک مردودی نشان می‌دادند. برای سورس‌ها بدتر بود: `grade` کهنه، `status` را هم به عقب می‌کشید.
  - **علت ۲ (کشف‌شده حین تست) — رقابت PATCHهای موازی:** `handleUpdateVendor` شش endpoint را هم‌زمان صدا می‌زد و هرکدام کل vendor را read-modify-write می‌کند؛ `/profile` نسخهٔ کهنهٔ `analysisRecords` را برمی‌گرداند و **نتیجهٔ حذف‌شده بعد از reload زنده می‌شد**. با صف `syncQueue` ترتیبی شد.
  - حالا «مردود بودن» از واقعیت‌ها **محاسبه** می‌شود (`isVendorRejected`) و `grade` فقط خروجی است. یک predicate مشترک در ۱۲ نقطه (دونات، کارت‌ها، بج سایدبار، فیلترها، آرشیو، اکسل) جایگزین شرط‌های دستی شد.
  - دلایلِ «مردود در آزمون QC» بازتاب رکوردها هستند و به‌تنهایی وضعیت را نگه نمی‌دارند ⇒ دادهٔ قدیمیِ قفل‌شده خودترمیم می‌شود.
  - **تست زنده:** ثبت Reject → کارت‌ها ۱ مردودی · حذف نتیجه → کارت‌ها ۰ · **reload → همچنان ۰** (DB تأیید شد).

10h. ✅ **بررسی ماژول شرکای تجاری (همان کلاس باگ)** — نتیجه: **باگ سورس‌ها را ندارد.**
  - `computeSupplierEvaluation` یک مشتق‌گیری خالص از مدارک است و در `handleSubmit` روی **هر ذخیره** دوباره محاسبه می‌شود؛ فرم ویرایش هم `evalDocs` را از `partner.evaluation.documents` بارگذاری می‌کند (بررسی شد: پاسخ API مدارک و وضعیت‌ها را کامل دارد و فقط blob فایل lazy است) ⇒ مسیرِ «مدرک عوض شود ولی گرید کهنه بماند» وجود ندارد.
  - **سخت‌سازی انجام شد:** `reconcileSupplierEvaluation` روی load (هم API و هم cache) اجرا می‌شود تا اگر رکوردی از نسخهٔ قدیمی یا ویرایش مستقیم DB با مدارک خودش ناسازگار باشد، خودترمیم شود. `updatedAt/updatedBy` عمداً حفظ می‌شود تا محاسبهٔ مجدد شبیه «ارزیابی تازه» به نظر نرسد (نکتهٔ GxP).
  - ۷ تست جدید در `tests/sopEvaluation.test.ts`.

10i. ✅ **تعیین تکلیف رابریک SOP (تصمیم کاربر: گزینهٔ ۲ — رابریک فعلی معتبر است):**
  - رابریک رسمی: مرزهای **۸۰ / ۶۰ / ۴۰ / ۳۰** با گریدهای `A, B, C, Pending Review, Blacklist`. گرید `D` وجود ندارد.
  - تست `businessRules.test.ts` که سال‌ها fail بود و رابریک متفاوتی (۹۰/۷۵/۶۰ با A/B/C/D) را ادعا می‌کرد، به رابریک واقعی اصلاح شد + تست مرز `Not Evaluated` اضافه شد ⇒ **کل سوئیت حالا سبز است (۳۹ pass / ۰ fail).**
  - واژگان مردهٔ `'D'` حذف شد از: `SOPGrade` (types)، `gradeApprovalLabel`، `getGradeBadgeClass`، فیلتر آمار، و `sopResultLabel` در اکسل. همچنین statusهای دست‌نیافتنی `Conditional Approval` و `Rejected` از `SOPSupplierStatus` پاک شدند.
  - **حذف رابریک تکراری:** `computeNewSupplierEval` در `App.tsx` نسخهٔ دومی از قوانین نمره‌دهی داشت (کپی دستی مرزها و امتیاز مدارک)؛ حالا از `computeSupplierEvaluation` و `SOP_DOCUMENTS_DEF` مشترک استفاده می‌کند تا دو نسخه از هم فاصله نگیرند.
  - ⚠️ توجه: `getRankParams` در `App.tsx`/`PrintableForms.tsx` هم گرید `D` دارد ولی **مربوط به مقیاس گرید سورس‌هاست** (A: ۸۰-۱۰۰ … D: ۰-۳۹)، نه SOP — عمداً دست‌نخورده ماند.
  - تست زنده: لیست شرکا با گریدهای A/B و برچسب‌های Approved / Permit Approval و امتیازهای ۹۰، ۸۰، ۷۰ درست رندر شد؛ هیچ «Grade D» سرگردانی در صفحه نیست.

10j. ✅ **یکدست‌سازی باز و بسته شدن فرم‌ها (پیشنهاد ترکیبی، تأییدشده)** — سه کامیت:
  - **`src/components/FormModal.tsx`** پوستهٔ مشترک همهٔ دیالوگ‌ها: portal، backdrop، Esc، قفل اسکرول، focus trap و بازگرداندن focus، و **انیمیشن متقارن ورود/خروج** با `AnimatePresence`. سه اندازه `sm/md/lg` جای ۹ عرض دلخواه. عمداً «پوسته» است نه فرم قالبی، پس مهاجرت = حذف boilerplate.
  - **همهٔ ۱۵ مودال** منتقل شدند (مواد، شرکا، تأییدیه‌های حذف/لیست‌سیاه، تغییر رمز، انتخابگر ماده، دیالوگ تغییرات ذخیره‌نشده). تنها overlay دستی باقی‌مانده اسکریم سایدبار موبایل است (که دیالوگ نیست). `design-system/Dialog.tsx` و `Drawer.tsx` بلااستفاده بودند و حذف شدند.
  - **فرم سورس صفحه شد** با مسیر اختصاصی `#/category/<cat>/new` و `#/category/<cat>/vendor/<id>/edit` ⇒ Back، breadcrumb، لینک قابل اشتراک و دیپ‌لینک رایگان. محافظ تغییرات ذخیره‌نشده به `VendorForm` منتقل شد و حالا **dirty واقعی** را می‌سنجد نه صرف باز بودن فرم. مودال «ثبت تأمین‌کننده» دیگر مودالِ داخل مودال نیست.
  - **بهبود رفتاری:** تأییدیه‌های مخرب با کلیک روی backdrop بسته نمی‌شوند (دکمهٔ صریح لازم است) ولی Esc لغو می‌کند — قبلاً هیچ‌کدام Esc نداشتند.

  **چهار باگ که حین همین کار پیدا و رفع شد:**
  1. children یک `FormModal` حتی وقتی بسته است ارزیابی می‌شوند (برخلاف portal شرطی قبلی) ⇒ مودال جزئیات با خواندن `selectedMaterial.id` صفحه را crash می‌کرد. گارد شد.
  2. کالر معمولاً رکورد را هم‌زمان با بستن پاک می‌کند ⇒ پنل خالی fade out می‌شد. `FormModal` آخرین children را تا پایان انیمیشن نگه می‌دارد.
  3. `handleSelectVendor` هنگام push ورودی قبلی را spread می‌کرد ⇒ رکورد تازه‌ساخته `formMode: 'create'` را ارث می‌برد و دوباره **فرم** رندر می‌شد نه رکورد. حالا `formMode` پاک می‌شود و ورودی فرم **جایگزین** می‌شود.
  4. **`getVendorsList` هرگز `materialId` را برنمی‌گرداند** ⇒ ویرایش هر سورس موجود با پیام «ماده را انتخاب کنید» رد می‌شد. یعنی **ویرایش سورس عملاً کار نمی‌کرد** (باگ از پیش موجود). رفع شد.

  **تست:** ۴۴ تست واحد سبز (۵ تست جدید مسیر فرم). تست زنده: هر دو فرم مودال (باز/قفل اسکرول/Esc/backdrop/بازگشت focus)، تأییدیهٔ مخرب، صفحهٔ فرم (URL، Back، دیپ‌لینک در مرورگر تازه، breadcrumb چهارسطحی، محافظ dirty با هر دو پاسخ)، ثبت سورس تا دیتابیس، و ویرایش سورس تا دیتابیس.

### ماژول مواد
11. ✅ persist فیلدهای غنی مواد به Postgres (بدون IRC) (`407eef5`, `a1c4b8f`)

### ماژول شرکای تجاری
12. ✅ مدل تخت: تولیدکننده/فروشنده مستقل (حذف وابستگی) (`fb739bb`)
13. ✅ تاریخچه و روند ارزیابی SOP فروشنده (از audit) (`d6e5b96`)
14. ✅ lazy-load فایل‌های SOP (حذف base64 از لیست) (`ca830af`)
15. ✅ گردش‌کار Blacklist با دلیل الزامی + گارد انتخاب (`3dd8e78`)
16. ✅ یکدست‌سازی theme / dark mode (`23b3980`)

### معماری فرانت — شکستن `App.tsx`
17. ✅ **`src/App.tsx`: ۷۹۵۶ → ۱۶۶۸ خط.** ~۷۹۰ خط کد مرده حذف شد (`UnusedFallback*`، استاب‌های `LegacyPrintable*`) و ۱۴ ماژول استخراج شد:
    - `components/views/`: `HomeView`, `CategoryView`, `MaterialGroup`, `MaterialsComparisonSection`, `ArchiveView`, `SupplierAuditView`
    - `components/vendor/`: `VendorDetail`, `VendorForm`, `EvaluationForm`, `RiskAssessmentForm` (+`RiskHeatmap`)
    - `constants/`: `categories.ts`, `evaluationLayout.ts`, `categoryCardStyles.ts` · `utils/scoreUtils.ts`
    - **کد فقط جابه‌جا شد، بازنویسی نشد** — هیچ تغییر رفتاری. props (به‌ویژه `registerNavGuard`) دست‌نخورده.
    - خودِ `App()` عمداً یکپارچه ماند (state ناوبری + `renderContent`) — شکستنش قواعد ترتیب هوک‌ها را لمس می‌کند و مرحلهٔ جداست.
    - تأیید: `tsc` ✓ · `vite build` ✓ · ۴۴ تست ✓ · تست زندهٔ مرورگری روی هر ۹ نمای جابه‌جاشده با **۰ خطای کنسول** (داشبورد، لیست دسته، باز شدن ماده، جزئیات سورس، فرم ارزیابی، فرم ریسک، صفحهٔ فرم سورس + round-trip ذخیره، آرشیو، Supplier 360).

### رفع باگ نمایش دوگانهٔ تولیدکننده/فروشنده
18. ✅ **صفحهٔ جزئیات سورس یک شریک را دو بار نشان می‌داد.** `VendorDetail` جداگانه یک «تولیدکننده» و یک «فروشنده» resolve می‌کرد؛ برای سورسِ فروشنده‌ای هر دو به **یک رکورد واحد** می‌رسیدند و آدرس/تلفن/ایمیل تکرار می‌شد. تیتر صفحه هم برچسب «تولید کننده :» را hardcode کرده بود.
    - helper مشترک `src/utils/vendorPartner.ts` → `resolveVendorPartner` (+ `cleanCountry`, `formatLocation`, `formatContactLine`). **نقش از `partner.type` می‌آید، نه از اینکه id در کدام فیلد نشسته** — رکوردهای قدیمی می‌توانند id یک Supplier را در `manufacturerId` داشته باشند.
    - `VendorDetail`: تیتر با برچسب پویا؛ کارت تماس از دو باکس به **یک** باکس. کارت سبز «خرید بی‌واسطه» فقط وقتی شریک واقعاً تولیدکننده است.
    - `excelExport`: چهار ستون `تولید کننده | آدرس | فروشنده | آدرس` → سه ستون `تأمین‌کننده | نوع تأمین‌کننده | آدرس و اطلاعات تماس` (عرض ستون‌ها هم اصلاح شد).
    - `PrintableForms`: قالب ثابت فرم GxP حفظ شد، ولی خانه‌ها درست پر می‌شوند — نام در خانهٔ نقشِ خودش، خانهٔ دیگر «ثبت‌نشده» یا «خرید بی‌واسطه». دیگر یک نام در دو خانه تکرار نمی‌شود.
    - `MaterialGroup`: `getPartnerDetails` محلی حذف و به helper مشترک وصل شد.
    - **هیچ تغییری در schema، migration یا endpoint نبود** — این صرفاً باگ نمایش بود؛ همهٔ اطلاعات تماس از قبل روی رکورد `BusinessPartner` است.
    - تأیید: `tsc` ✓ · `build` ✓ · **۵۴ تست** (۱۰ تست جدید در `tests/vendorPartner.test.ts`) ✓ · تست زندهٔ مرورگری روی هر سه حالت (سورسِ فروشنده‌ای، تولیدکننده‌ای، بدون شریک) در جزئیات + لیست دسته + خروجی اکسل + فرم چاپی، با **۰ خطای کنسول**.

### رفع باگ «نمی‌شود در فرم‌ها تایپ کرد»
19. ✅ **نشانگر تایپ با هر کاراکتر از فیلد می‌پرید** — در فرم ثبت ماده، فرم شرکای تجاری و مودال «ثبت تأمین‌کننده جدید» داخل فرم سورس. **رگرسیونِ کار یکدست‌سازی فرم‌ها بود** (ردیف ۱۷).
    - علت: افکت فوکوس/Esc در `FormModal` وابستگی `[open, onClose]` داشت، ولی هر ۱۱ کالر `onClose` را arrow inline می‌دهند و state فیلدها در همان والد است. پس هر کاراکتر → رندر والد → identity تازهٔ `onClose` → cleanup افکت → `restoreFocusRef.current?.focus?.()` فوکوس را بیرون می‌کشید و ۴۰ms بعد `panelRef.focus()` آن را روی پنل می‌گذاشت.
    - اصلاح در **یک نقطه**: `onClose` در `onCloseRef` نگه داشته می‌شود و deps افکت `[open]` شد ⇒ افکت یک بار روی باز شدن و یک بار روی بسته شدن اجرا می‌شود (همان «capture on open, restore on close» که از ابتدا هدف بود). هیچ کالری لازم نشد `useCallback` بگیرد.
    - سخت‌سازی: بازگرداندن فوکوس فقط وقتی که فوکوس هنوز داخل پنل (یا روی `body`) است — مودال فوکوسِ بیرون را نمی‌دزدد.
    - `PartnerSelector`: تایم‌اوت ۵۰ms فوکوس جستجو حالا در cleanup پاک می‌شود.
    - تأیید: `tsc` ✓ · `build` ✓ · ۵۴ تست ✓ · تست زندهٔ مرورگری با **کیبورد واقعی** (`type()` نه `fill()`) روی هر سه فرم، با **کنترل منفی**: revert موقت `FormModal.tsx` + ری‌استارت سرور ⇒ هر سه FAIL با متن به‌هم‌ریخته (`پاراستامول تست` → `مولتستپاراستا`، `activeElement=DIV`). عدم رگرسیون Esc/backdrop/بازگشت فوکوس/قفل اسکرول هم سبز. ۰ خطای کنسول.
    - ⚠️ **تلهٔ تکرارشونده:** اولین کنترل منفی به‌غلط PASS داد چون Vite ماژول کهنه سرو می‌کرد. بعد از هر تغییر فایل **حتماً سرور را ری‌استارت کن**.

### رفع مودال جزئیات صفحهٔ Audit
20. ✅ **کلیک روی رکورد در جدول Audit، پنجرهٔ جزئیات را بدجا و زیر هدر اصلی باز می‌کرد.**
    - علت: `AuditTrailView` **تنها مودال دست‌سازِ باقی‌مانده** بود (`fixed inset-0 z-50` به‌جای `FormModal`). هر view داخل `motion.div` صفحه‌گردانی رندر می‌شود که روی **`filter`** انیمیت می‌کند؛ یک `filter` غیر-`none` هم containing block برای فرزندان `fixed` می‌سازد (پس `inset-0` به جعبهٔ صفحه نگاشت می‌شد، نه viewport) و هم stacking context (پس `z-50` زیر هدرِ `z-10` حبس می‌شد). هر دو نشانه یک علت داشتند.
    - بررسی شد که بقیهٔ overlayها مصون‌اند: `FormModal` و `PrintableForms` هر دو `createPortal` می‌زنند و `CommandPalette` بیرون از `<main>` است.
    - اصلاح: overlay دست‌ساز با `FormModal` (size=`lg`) جایگزین شد؛ markup هدر/بدنه/فوتر عیناً ماند و گارد `{selectedLog && …}` حفظ شد. **Esc، focus trap، قفل اسکرول و `role="dialog"`** هم که قبلاً نبودند رایگان اضافه شدند.
    - `CLAUDE.md` قاعدهٔ ۸: دلیل CSSی اجباری‌بودن قاعده ثبت شد تا دوباره کشف نشود.
    - تأیید عددی (نه چشمی) با `getBoundingClientRect` + `elementFromPoint`، و **کنترل منفی** با revert + ری‌استارت سرور:

    | سنجه | قبل | بعد |
    |---|---|---|
    | مرکز عمودی پنل vs viewport | ۵۹۶ vs ۵۰۰ | ۵۰۰ vs ۵۰۰ |
    | کاملاً داخل viewport | ✗ (bottom=۱۰۴۶ > ۱۰۰۰) | ✓ |
    | در نوار هدر، کدام بالاست | `HEADER.sticky z-10` | خودِ مودال |
    | `role="dialog"` | ندارد | دارد |

    - محتوای دیف (`computeFieldDiff`) سالم بود و تغییری لازم نداشت؛ ردیف‌های تغییر/افزوده/حذف و JSON خام درست رندر می‌شوند. ۰ خطای کنسول.

### ماژول کاربر — امنیت و صحت نشست (موارد ۱ و ۲ گزارش ماژول کاربر)
21. ✅ **بستن ترفیع خودسرانهٔ نقش (P0).** هر شش endpoint `/api/users/*` فقط `requireAuth` داشتند و **هیچ بررسی نقشی نداشتند** ⇒ یک کاربر `finance` می‌توانست نقش ادمین را عوض کند یا برای خودش حساب ادمین بسازد.
    - middleware جدید `requireRole(...roles)` در `server.ts` (الگویش از قبل در `/api/audit-logs` بود و فقط به این مسیرها اعمال نشده بود) روی هر شش مسیر.
    - `GET /api/vendors` که **هیچ احراز هویتی نداشت** حالا `requireAuth` دارد. افکت لود سورس‌ها در `App.tsx` به `currentUser` مشروط شد تا در صفحهٔ ورود حلقهٔ reload نسازد (تلهٔ ثبت‌شده در `CLAUDE.md`).
22. ✅ **سه نقص صحت نشست:**
    - **خروج حالا در Audit ثبت می‌شود** — `handleLogout` قبلاً فقط localStorage را پاک می‌کرد و `POST /api/auth/logout` را صدا نمی‌زد؛ audit trail ورود داشت ولی خروج نداشت (نقص کامل‌بودن ALCOA+). درخواست ناموفق هم کاربر را در حالت لاگین گیر نمی‌اندازد.
    - **اعتبارسنجی نقش هنگام boot** — `GET /api/auth/me` حالا نقش/نام را **از دیتابیس** می‌خواند نه از JWT (توکن هفت‌روزه است و صرفاً وضعیت زمان ورود را تکرار می‌کرد)، و برای حساب حذف‌شده ۴۰۱ می‌دهد. کلاینت یک بار در هر لود آن را صدا می‌زند و `currentUser` کش‌شده را همگام می‌کند.
    - `ChangePasswordModal` از `authFetch` استفاده می‌کند (قبلاً `fetch` خام با هدر دستی، بدون مدیریت ۴۰۱).
23. ✅ **رفع باگ از پیش موجود: فقط ادمین می‌توانست وارد برنامه شود.** `HomeView` فید `/api/audit-logs` (ادمین‌ویژه) را برای همه صدا می‌زند و `authFetch` هر **۴۰۳** را «انقضای نشست» می‌گرفت ⇒ کاربر غیرادمین بلافاصله بعد از ورود بیرون انداخته می‌شد. اصلاح با تفکیک معنایی: `requireAuth` برای توکن نامعتبر **۴۰۱** برمی‌گرداند (قبلاً ۴۰۳)، `requireRole` برای عدم مجوز **۴۰۳**؛ و `authFetch` فقط روی ۴۰۱ خروج می‌کند و ۴۰۳ را به کالر برمی‌گرداند.
    - **این باگ مال تغییرات ما نبود** — با revert و تست دوباره تأیید شد که روی کد قبلی هم `commercial` نمی‌توانست وارد شود.

    تأیید: `tsc` ✓ · `build` ✓ · ۵۴ تست ✓ · تست زندهٔ API و مرورگری:

    | سنجه | نتیجه |
    |---|---|
    | `finance` روی ۴ مسیر کاربران | ۴۰۳ (قبلاً باز بود) |
    | `admin` روی `GET /api/users` | ۲۰۰ |
    | `GET /api/vendors` بدون توکن / با توکن | ۴۰۱ / ۲۰۰ |
    | توکن خراب / عدم مجوز | ۴۰۱ / ۴۰۳ (تفکیک‌شده) |
    | ورود کاربر غیرادمین | ✓ (قبلاً بیرون انداخته می‌شد) |
    | رکورد LOGOUT بعد از خروج | ۰ → ۱ |
    | تغییر نقش در DB → reload | نقش و منوی GOVERNANCE به‌روز شد |
    | تغییر رمز از منو | موفق، رمز واقعاً عوض شد |
    | حلقهٔ reload در صفحهٔ ورود | ۰ ناوبری در ۴ ثانیه |

### ماژول مدیریت کاربران (مورد ۳ گزارش ماژول کاربر)
24. ✅ **صفحهٔ «مدیریت کاربران» ساخته شد** (`src/components/UsersView.tsx`، مسیر `#/users`، ادمین‌ویژه، ورودی در سایدبار + منوی کاربر).
    - لیست کاربران با سمت، وضعیت، آخرین ورود و نشان «تغییر رمز در ورود بعدی»؛ جستجو؛ ایجاد کاربر؛ ویرایش نام/سمت؛ **فعال/غیرفعال‌سازی**؛ **بازنشانی کلمه عبور**؛ حذف کامل (با هشدار صریح که غیرفعال‌سازی گزینهٔ درست‌تر است).
    - همهٔ دیالوگ‌ها با `FormModal` (قاعدهٔ ۸)؛ تأییدیهٔ حذف `role="alertdialog"` + `closeOnBackdrop={false}`. audit فقط سمت‌سرور (قاعدهٔ ۲) — این view هیچ رکورد audit نمی‌زند.
    - endpointهای موجود از قبل بودند و هیچ‌وقت صدا زده نمی‌شدند؛ فقط `POST /api/users/:username/reset-password` جدید اضافه شد (رمز موقت + اجبار تغییر در ورود بعدی؛ **خودِ رمز در audit ثبت نمی‌شود**، فقط واقعهٔ بازنشانی).
25. ✅ **دو ستون مردهٔ schema زنده شدند.** `mapUserRow` در `server.ts` هر دو را دور می‌ریخت — به همین دلیل هیچ‌جای برنامه نمی‌توانست ببیندشان:
    - `is_active` → حالا در `GET /api/users` برمی‌گردد، در **login** بررسی می‌شود (حساب غیرفعال ۴۰۳ می‌گیرد و تلاشش در audit ثبت می‌شود) و در `GET /api/auth/me` باعث ۴۰۱ می‌شود تا نشستِ در جریان هم قطع شود، نه اینکه تا انقضای توکن هفت‌روزه ادامه یابد.
    - `last_login_at` → هنگام ورود موفق نوشته می‌شود و در ستون «آخرین ورود» نمایش داده می‌شود.
26. ✅ **محافظ قفل‌شدن سازمان (`checkAdminSafety` در `server.ts`).** روی `PATCH /api/users/:username`، `PUT /api/users/:u/role` و `DELETE`:
    - ادمین نمی‌تواند خودش را حذف، غیرفعال یا تنزل دهد.
    - آخرین **مدیر فعال** قابل حذف/غیرفعال/تنزل نیست.

    تأیید: `tsc` ✓ · `build` ✓ · **۵۴ تست** (۲ تست مسیر `#/users` اضافه شد) ✓ · تست زندهٔ API و مرورگری با **۰ خطای کنسول**:

    | سنجه | نتیجه |
    |---|---|
    | ایجاد کاربر از UI و ورود با آن | ✓ |
    | بازنشانی رمز → رمز قدیمی ۴۰۱، جدید ۲۰۰ | ✓ |
    | غیرفعال‌سازی → ورود ۴۰۳ با پیام فارسی | ✓ |
    | `last_login_at` بعد از ورود | ثبت شد (قبلاً همیشه خالی) |
    | حذف/غیرفعال/تنزلِ خود ادمین | ۴۰۰ در هر سه |
    | تنزل آخرین مدیر فعال | ۴۰۰ |
    | تایپ در فرم (کیبورد واقعی) | فوکوس حفظ شد |
    | کاربر غیرادمین روی `#/users` | صفحهٔ «عدم دسترسی»؛ سایدبار و منو مخفی |

### سطوح دسترسی نقش‌محور (اعمال ماتریس تأییدشده)
27. ✅ **جدول سیاست واحد `src/utils/permissions.ts`** که **هم سرور و هم کلاینت** از آن می‌خوانند — واگرایی UI/سرور از نظر ساختاری ناممکن شد.
    - ماتریس: `admin` همه · `commercial` ثبت/ویرایش سورس + شرکای تجاری · `qa` نتایج آزمایشگاهی + مواد اولیه · `planning`/`finance` فقط امتیاز دپارتمان خودشان · `lab` فقط‌خواندنی.
    - تغییر نسبت به قبل: `commercial` شرکای تجاری را گرفت · `qa` مواد اولیه را گرفت · ارزیابی ریسک ادمین‌ویژه شد.
28. ✅ **`requirePermission()` روی ۱۴ مسیر نوشتنی** (سورس/ماده/شریک/آزمایش/ریسک/حذف). قبلاً همهٔ این‌ها فقط `requireAuth` داشتند و هر کاربری با curl می‌توانست انجامشان دهد.
29. ✅ **گارد per-department امتیازدهی.** `PATCH /vendors/:id/scores` کل شیء را جایگزین می‌کند، پس گارد ساده کافی نبود؛ payload با مقدار ذخیره‌شده مقایسه می‌شود و تغییر دپارتمان دیگر ۴۰۳ می‌گیرد.
    - ⚠️ **باگی که تست زنده گرفت:** فرم همیشه هر چهار دپارتمان را می‌فرستد و برای غیرقابل‌ویرایش‌ها `0` می‌گذارد؛ روی سورس بدون امتیاز، «ثبت‌نشده → ۰» تغییر حساب می‌شد و **کاربر مالی اصلاً نمی‌توانست سورس جدید را امتیاز دهد**. با یکی‌شمردن «ثبت‌نشده» و «صفر» رفع شد + تست رگرسیون.
30. ✅ **کلاینت هم‌راستا شد** — شرط‌های پراکندهٔ `role === 'admin'` جای خود را به `can()`/`canScoreDepartment()` دادند. دو دکمهٔ «ثبت ماده جدید» و «ثبت شریک تجاری جدید» **اصلاً گارد نداشتند** (باگ از پیش موجود) و گارد گرفتند.

    تأیید: `tsc` ✓ · `build` ✓ · **۷۱ تست** (۱۷ تست جدید؛ هر سلول ماتریس یک assert) ✓ · تست زندهٔ API با توکن هر ۵ نقش روی ۶ مسیر + ۵ سناریوی امتیازدهی + تست مرورگری هر نقش، با ۰ خطای کنسول.

### تنظیم سطح دسترسی پر‌کاربر با چک‌باکس
31. ✅ **ستون مردهٔ `users.permissions` بالاخره خوانده می‌شود.** نقش = الگو، استثنا = پر‌کاربر. **خالی یعنی «از نقش پیروی کن»** — دقیقاً وضعیت همهٔ کاربران موجود، پس **صفر مهاجرت داده**.
32. ✅ **۱۳ چک‌باکس در ۴ گروه** (سورس‌ها · ارزیابی · مخازن · مدیریتی) در دیالوگ «سطح دسترسی» از لیست کاربران؛ با نشان «افزوده به سمت»/«سلب‌شده از سمت» روی هر تفاوت، دکمهٔ «بازگشت به پیش‌فرض سمت»، و بج «سفارشی» در لیست.
33. ✅ **امتیازدهی چهار مجوز جدا شد** (`score.commercial/qa/planning/finance`) — چیزی که با نقش ممکن نبود: یک کاربر می‌تواند هم‌زمان دو دپارتمان را امتیاز دهد.
34. ✅ **`requirePermission` رکورد کاربر را از DB می‌خواند نه از JWT.** توکن هفت‌روزه فقط `role` دارد؛ خواندن از آن یعنی تغییر دسترسی تا یک هفته بی‌اثر بماند. **تست زنده تأیید کرد که تغییر روی همان توکن و بدون ورود مجدد اعمال می‌شود.**
35. ✅ محافظ‌ها: `checkPermissionSafety` (آخرین دارندهٔ `users.manage` و خودِ کاربر) · تغییر نقش استثناها را پاک می‌کند (با هشدار در فرم) · نام‌های ناشناخته sanitize می‌شوند و فهرستِ کاملاً ناشناخته به‌جای «هیچ دسترسی» به الگوی نقش برمی‌گردد.

    تأیید: `tsc` ✓ · `build` ✓ · **۸۰ تست** (۲۶ تست سیاست) ✓ · تست زندهٔ API: اعطا → ۲۰۰ روی همان مسیری که ۴۰۳ بود، سلب → ۴۰۳، ریست → ۴۰۳، نام ناشناخته ذخیره نشد، هر سه محافظ ۴۰۰ · تست مرورگری: ۱۳ چک‌باکس، بج‌ها، ذخیره، و دیدن دکمهٔ جدید توسط کاربر هدف. ۰ خطای کنسول.

### پروفایل و نشست در منوی کاربر (مورد ۴ گزارش ماژول کاربر)
36. ✅ منوی کاربر از سه دکمه به یک پنل واقعی تبدیل شد: **ورود قبلی** · **اعتبار نشست** (با هشدار نارنجی زیر ۲۴ ساعت) · **تعداد مجوزها** (+ نشان «سفارشی») · **فعالیت اخیر من**.
    - **«ورود قبلی» یعنی دفعهٔ قبل، نه همین حالا** — مقدار `lastLoginAt` **قبل از** بازنویسی خوانده و در پاسخ login برگردانده می‌شود؛ `/api/auth/me` عمداً آن را نمی‌فرستد تا با reload خراب نشود (چون merge به‌صورت `{...prev, ...fresh}` است).
    - **`GET /api/auth/my-activity`** (جدید، `requireAuth`): خواندن فعالیت خودِ کاربر امتیاز ویژه نیست، ولی فیلتر **از توکن** گرفته می‌شود نه از query string — تست شد که `?userId=admin` نادیده گرفته می‌شود.
    - `src/utils/session.ts`: خواندن `exp` از payload توکن **بدون تأیید امضا** — فقط برای برچسب، هرگز برای تصمیم (سرور هر درخواست را خودش تأیید می‌کند).
    - ⚠️ **باگی که تست مرورگری گرفت:** منو برای ادمین «(سفارشی)» نشان می‌داد. علت: کلاینت فقط فهرست **مؤثر** را می‌گیرد، پس `hasCustomPermissions` سمت‌کلاینت هیچ‌وقت درست کار نمی‌کند. با فرستادن `permissionsCustom` از سرور رفع شد.

    تأیید: `tsc` ✓ · `build` ✓ · **۸۱ تست** ✓ · تست زندهٔ API (self-scope و نادیده‌گرفتن `userId`، و درست بودن `previousLoginAt` در ورود دوم) + تست مرورگری کل پنل، با ۰ خطای کنسول.

### ظرفیت ۱۰۰۰+ رکورد — بنچمارک واقعی و اصلاح
37. ✅ **بنچمارک با ۱۲۷۶ سورس واقعی** (۷۲۲۱ لاگ، ۴۸۰۰ رکورد آزمایش) روی Postgres محلی. اعداد **اندازه‌گیری‌شده‌اند، نه تخمین**.
38. ✅ **`getVendorsList(vendorId?)` فیلترپذیر شد.** `getVendorById` قبلاً کل دیتابیس را می‌خواند و بعد `.find()` می‌کرد — **۱۶ endpoint** این مسیر را می‌رفتند. حالا `where` می‌خورد (ایندکس‌هایش از قبل بود).
39. ✅ **رفع O(n²):** `vendorMaterials.find()` داخل حلقهٔ سورس‌ها ⇒ در ۱۲۰۰ رکورد بیش از یک میلیون مقایسه در هر درخواست. با `Map` جایگزین شد.
40. ✅ **`getRankingSnapshot()`** — محاسبهٔ رتبه دو بار کل دیتابیس را می‌خواند تا یک عدد بدهد؛ حالا فقط id/scores/category را می‌گیرد. منطق `rankVendor` دست‌نخورده.
41. ✅ **کش `app_db` سبک و محافظت‌شده.** تاریخچه (لاگ/آزمایش/rawScores) از کش حذف شد (هیچ‌وقت از کش خوانده نمی‌شد) و آرایه‌ها خالی می‌مانند تا شکل رکورد نشکند؛ نوشتن در `try/catch` و در صورت شکست، کش پاک و برنامه ادامه می‌دهد.
42. ✅ **`ErrorBoundary` در ریشه** (`src/main.tsx`) — قبلاً هیچ‌جا نبود؛ هر throw کل درخت را unmount می‌کرد و صفحهٔ سفید می‌داد.

    | سنجه (۱۲۷۶ رکورد) | قبل | بعد |
    |---|---|---|
    | ذخیرهٔ کامل (۶ PATCH) | **۲.۸۳ s** | **۰.۲۴ s** (~۱۲×) |
    | `PATCH /scores` | ۰.۸۰ s | ۰.۰۶ s |
    | `PATCH /contact` | ۰.۴۲ s | ۰.۰۲۵ s |
    | حجم `app_db` | ۶.۳۴ MB | **۲.۱۳ MB** (−۶۶٪) |
    | سقف تخمینی سورس (Chromium ۹.۹MB) | ~۱۹۹۰ | ~۵۹۳۰ |
    | سقف تخمینی سورس (Firefox/Safari ۵MB) | ~۱۰۰۶ | ~۲۹۹۵ |

    - **تصحیح گزارش اولیه:** سهمیهٔ واقعی Chromium اندازه‌گیری شد **۹.۹MB** نه ۵MB؛ پس ادعای «دیوار در ۹۳۰ رکورد» فقط برای Firefox/Safari درست بود. در Chrome دیوار حدود ۲۰۰۰ بود.
    - تأیید: خروجی `GET /api/vendors` **بایت‌به‌بایت یکسان** ماند (۰ رکورد متفاوت از ۱۲۷۶) · پر کردن عمدی سهمیه ⇒ برنامه زنده ماند، ۰ crash · ۸۱ تست ✓

### انتخاب سورس برندهٔ هر ماده (بخش «مقایسهٔ منابع» در صفحهٔ دسته‌بندی)
43. ✅ **تاریخ جعلی حذف شد.** کد قبلی اگر آخرین ارزیابی قدیمی‌تر از ۱۴۰۴ بود، عمداً **تاریخ امروز** را نشان می‌داد («تا کاملاً به‌روز به‌نظر برسد»). در یک سامانهٔ GxP این ادعای نادرستِ تازگی داده است. حالا `getLatestGroupUpdateDate()` می‌تواند `null` برگرداند و در نبود ارزیابی «ارزیابی ثبت نشده» نمایش داده می‌شود.
44. ✅ **نمودار با رتبه‌بندی هم‌خوان شد.** میله‌ها قبلاً `score` پایه را رسم می‌کردند در حالی که ترتیب ردیف‌ها با `engineScore` (خروجی موتور) بود ⇒ گاهی میلهٔ کوتاه‌تر بالای فهرست بود. حالا محور همان `engineScore` است و امتیاز پایه در پرانتز `(پایه {score})` می‌آید.
45. ✅ **حالت «هم‌تراز».** با `DECISIVE_MARGIN = 3`، اگر فاصلهٔ دو سورس اول کمتر از ۳ امتیاز باشد به‌جای اعلام برنده، «هم‌تراز — تصمیم انسانی لازم است» نشان داده می‌شود.
46. ✅ **نشانگر کامل‌بودن داده.** تعداد دپارتمان‌های امتیازدهی‌شده (`n/۴`) و هشدار نبود ارزیابی ریسک، تا پیشنهاد بر پایهٔ دادهٔ ناقص با پیشنهاد کامل اشتباه گرفته نشود.
47. ✅ **ثبت تصمیم (`source_selections`).** جدول جدید + مهاجرت `20260825142747_source_selections` (یکتا روی `materialKey+category`، CASCADE روی vendor) و endpointهای `GET`/`PUT /api/source-selections` با `requirePermission('vendor.write')`. **دلیل الزامی** (حداقل طول، در غیر این صورت ۴۰۰ با پیام فارسی) و ثبت در `audit_log` (`CREATE_SOURCE_SELECTION`). اگر انتخاب انسانی با پیشنهاد موتور فرق داشته باشد، برچسب «متفاوت با پیشنهاد» نمایش داده می‌شود.
    - **عمداً تغییر نکرد:** فرمول امتیازدهی `FmeaService`. تغییر آن یعنی تغییر معیار تصمیم کیفی و باید تصمیم صریح و جداگانهٔ کاربر باشد.
    - تأیید: تست زنده مرورگری (۱۰ سنجه، همه ✓، ۰ خطای کنسول) + تست API (دلیل کوتاه → ۴۰۰، ذخیرهٔ معتبر → ۲۰۰، بازخوانی درست، ردیف audit ثبت شد) + گیت دسترسی (کاربر finance دکمهٔ ثبت را نمی‌بیند ولی انتخاب ثبت‌شده را می‌بیند) · ۸۱ تست ✓ · `tsc` ✓ · `build` ✓

48. ✅ **خلوت‌سازی پنل «موتور تحلیل سیستم».** سه بولت توضیحی، همان سه عددی را که خط فرمول زیرشان می‌گفت دوباره به‌صورت متن تکرار می‌کردند — و این پنل زیر **هر ماده** تکرار می‌شود.
    - نتیجه همیشه دیده می‌شود: سه چیپ فشرده (`پایه ۹۰` · `ریسک Low ×1.05` · `آزمایشگاه ×1.00`) + خط فرمول با اعداد واقعی.
    - متن آموزشی پشت دکمهٔ «چطور محاسبه می‌شود؟» جمع شد — با فعال‌کردن state ‏`showEngineGuide` که از قبل تعریف شده بود ولی به هیچ دکمه‌ای وصل نبود (کد مرده).
    - چیپ تکراری «سورس منتخب» از هدر بخش حذف شد (جعبهٔ پایین همان نام را به‌علاوهٔ دلیل و ثبت‌کننده دارد).
    - **اصلاح یک ادعای نادرست:** متن قبلی نداشتن ارزیابی ریسک را «ضریب خنثی» می‌نامید، در حالی که `FmeaService.riskModifiers.Default = 0.95` یک **جریمهٔ محتاطانه** است نه ۱.۰۰. حالا هم tooltip و هم متن راهنما همین را می‌گویند و چیپ ریسک/آزمایشگاهِ ثبت‌نشده کهربایی نمایش داده می‌شود.
    - **تصحیح برآورد خودم:** پیش‌بینی کرده بودم ارتفاع پنل ~۶۰٪ کم شود؛ اندازه‌گیری زنده نشان داد **ارتفاع تغییر نمی‌کند** (۸۵۵px قبل و بعد). دلیلش این است که ستون راست در `items-stretch` است و ارتفاعش را ستون چپ (نمودار + جدول آزمایشگاه) تعیین می‌کند، نه محتوای خودش. بردِ واقعی **حجم متن** است: ۵۸۷ → **۲۴۶** کاراکتر (−۵۸٪). عارضهٔ جانبی مطلوب: باز کردن راهنما هم چون در فضای خالی موجود باز می‌شود، هیچ ارتفاعی به صفحه اضافه نمی‌کند.

### خوانایی نام موجودیت‌ها (سراسری)
49. ✅ **علت مشترک پیدا شد، نه یک باگ موضعی.** در ۲۰+ نقطه `truncate` روی یک ظرف `flex` بود نه روی خود متن. `text-overflow` به فرزندان flex اعمال نمی‌شود ⇒ متن **بدون «…» سخت بریده می‌شد** و کاربر نمی‌فهمید نام ناقص است.
50. ✅ **زیرساخت:** `src/components/EntityName.tsx` + `src/hooks/useIsOverflowing.ts`. با `line-clamp` (نه `truncate` که `nowrap` تحمیل می‌کند) و tooltip **فقط وقتی متن واقعاً بریده شده**. `src/components/ui/tooltip.tsx` که کد مرده بود (هیچ import نداشت) زنده و اصلاح شد: `TooltipPrimitive.Portal` اضافه شد (تلهٔ `filter` — قاعدهٔ ۸)، `TooltipProvider` در `main.tsx`.
51. ✅ **نمودار مقایسه (نقطهٔ گزارش‌شدهٔ کاربر):** سقف `max-w-[180px]` که با ۳ بجِ `shrink-0` مشترک بود حذف شد؛ نام و بج‌ها در ظرف `flex-wrap` هم‌سطح شدند تا بج‌ها به خط بعد بروند نه اینکه نام را له کنند.

    | | قبل | بعد |
    |---|---|---|
    | عرض جعبهٔ نام | ۱۸۰px | **۲۳۷px** |
    | عرض لازم برای متن | ۳۰۹px | ۲۳۷px |
    | نتیجه | **۴۲٪ نام نامرئی** | کل ۴۹ کاراکتر خوانا |

52. ✅ **۸ فایل دیگر:** `ArchiveView`, `HomeView`, `PartnerSelector`, `App` (breadcrumb)، `SupplierAuditView`, `VendorDetail`, `CommandPalette`. عمداً دست‌نخورده: نام **فایل**‌ها و برچسب‌های ثابت سایدبار.
53. ✅ **باگی که فقط با تست زنده پیدا شد:** هوک درست اندازه می‌گرفت (`303>295`) ولی یک اندازه‌گیری سوم `0 0 0 0` آن را برمی‌گرداند. علت: وصل‌شدن tooltip عنصر را جابه‌جا می‌کند، React گرهٔ قبلی را unmount می‌کند و `ResizeObserver` روی گرهٔ جدا‌شده می‌ماند. با **callback ref** + دور‌ریختن اندازه‌گیریِ گرهٔ جداشده/صفر رفع شد. (در طرح اولیه نوشته بودم این حلقه رخ نمی‌دهد — اشتباه بود.)
    - تأیید: **کنترل منفی اجرا شد** (روی کد قدیم tooltip باز نمی‌شد و آرشیو ۱ برش بی‌صدا داشت) · بعد از اصلاح: ۵ صفحه × ۰ برش بی‌صدا، dark mode ۰، ستون‌های آرشیو هم‌تراز، tooltip بالای هدر می‌نشیند (`elementFromPoint`) و به `body` پورتال می‌شود · ۰ خطای کنسول · ۸۱ تست ✓

### مهاجرت خودکار در بیلد
54. ✅ **`scripts/migrate-if-configured.mjs`** — هیچ‌چیز مهاجرت‌ها را اجرا نمی‌کرد (نه Dockerfile، نه بیلد Vercel)، پس هر استقرار تازه به دیتابیسی بدون جدول وصل می‌شد و همهٔ مسیرها ۵۰۰ می‌دادند. حالا `npm run build` قبل از کامپایل این اسکریپت را صدا می‌زند.
    - **عمداً شرطی است:** اگر `DATABASE_URL` نبود یا مقدارش placeholder بود، رد می‌شود و بیلد **موفق** می‌ماند. بدون این شرط، بیلد لپ‌تاپ و هر preview بدون دیتابیس می‌شکست — از جمله PR #10 که الان سبز است.
    - **ولی خطای واقعی را نمی‌بخشد:** اگر دیتابیس تنظیم شده باشد و مهاجرت شکست بخورد، exit غیرصفر می‌دهد و بیلد را می‌خواباند. انتشار برنامه‌ای که schema‌اش با کدش نمی‌خواند بدتر از انتشار‌نشدن است.
    - اعتبارسنجی URL عمداً از `isValidPostgresUrl` در `server.ts` تکرار شده، چون این اسکریپت قبل از ساخته‌شدن باندل سرور اجرا می‌شود.
    - تأیید: بیلد بدون `DATABASE_URL` ✓ سبز · ۵ مقدار نامعتبر/placeholder ✓ همه skip · دیتابیس خالی ⇒ **۱۴ جدول** ساخته شد (۱۳ + `_prisma_migrations`) · اجرای دوم ⇒ «No pending migrations» (idempotent) · دیتابیس غیرقابل‌دسترس ⇒ **exit 1** · سرور تولیدی روی همان دیتابیس تازه: `/api/health` ✓، ورود `admin/123456` ✓ با `mustChangePassword=true`، `GET /api/vendors` → **۲۰۰** (نه ۵۰۰)، هر ۵ کاربر پیش‌فرض seed شدند · ۸۱ تست ✓
    - `IT_DEPLOYMENT_GUIDE.md` اصلاح شد: ادعای «مهاجرت خودکار اجرا نمی‌شود» دیگر درست نیست، ولی در `docker-compose.yml` فعلی متغیر فقط روی سرویس است نه روی build، پس آنجا هنوز یک بار دستی لازم است — همین تفکیک در سند نوشته شد.

### سطح دسترسی به تفکیک عملیات (ثبت/ویرایش/حذف)
55. ✅ **مجوزهای یکجا تفکیک شدند.** `material.write` یک مجوز برای POST+PATCH+DELETE+status بود؛ حالا `material.create` / `material.edit` / `material.delete`. همین‌طور `partner.*` و `vendor.create`/`vendor.edit`. مجموع ۱۳ → ۱۷ مجوز، و ۱۲ گارد endpoint به‌روز شد.
56. ✅ **دو مجوز نمایشی رفع شد.** `audit.read` در فرم بود ولی هیچ‌جا اعمال نمی‌شد؛ چهار endpoint ممیزی به‌جایش شرط دستی `role !== 'admin'` داشتند — همان چیزی که قاعدهٔ ۱۴ ممنوع کرده. حالا هر چهار endpoint `requirePermission('audit.read')` دارند. `archive.read` **حذف شد** چون هیچ endpointای نداشت که اعمالش کند.
57. ✅ **فرم به جدول ماژول×عملیات تبدیل شد** (`PERMISSION_MODULES`): هر ردیف یک ماژول، هر ستون یک عملیات، به‌علاوهٔ تیک «همه» با حالت indeterminate. **خانه‌ای که سرور اعمالش نمی‌کند چک‌باکس نمی‌گیرد** — قفل می‌شود با دلیل (`LOCKED_REASONS`): `✓` یعنی همه می‌بینند، `—` یعنی چنین عملیاتی وجود ندارد. سه ماژول تفکیک‌ناپذیر (آزمایشگاه/ریسک/کاربران) یک تیک واحد می‌گیرند.
58. ✅ **سازگاری به‌عقب بدون مهاجرت.** `LEGACY_PERMISSIONS` نام‌های بازنشسته را هنگام خواندن باز می‌کند، پس کاربری که `material.write` ذخیره‌شده دارد دقیقاً همان دسترسی قبلی را نگه می‌دارد. دیتابیس دست نخورد.
59. ✅ **دکمهٔ ویرایش ماده که هیچ‌وقت گیت نداشت** گیت شد (`material.edit`) — قبلاً برای همه دیده می‌شد.
    - تأیید زنده (API): کاربر با فقط `material.create` → POST **۲۰۰**، PATCH **۴۰۳**، DELETE **۴۰۳** · همان کاربر با `material.write` ذخیره‌شده → PATCH **۲۰۰**، DELETE **۲۰۰** (سازگاری) · غیرادمین با `audit.read` → **۲۰۰** (قبلاً همیشه ۴۰۳) و بدون آن → **۴۰۳**.
    - تأیید مرورگری: هر ۷ ردیف درست رندر شد، تیک «همه» کار می‌کند (۱→۵)، ۰ خطای کنسول. یک ایراد حین همین تست پیدا و رفع شد: ردیف کاربران دو چک‌باکس برای یک مجوز نشان می‌داد.
    - ۸۳ تست (۲ مورد جدید برای باز‌شدن نام‌های بازنشسته) ✓ · `tsc` ✓ · `build` ✓

### بررسی یکپارچه تأمین‌کنندگان (Supplier 360)
60. ✅ **`ircExpiryDate` نوشته می‌شد ولی هرگز خوانده نمی‌شد.** `PATCH /contact` آن را ذخیره و در audit ثبت می‌کرد، ولی `getVendorsList` اصلاً در شیء vendor نمی‌گذاشتش. یعنی **کارت «IRC نزدیک انقضا» در داشبورد همیشه صفر بود** و تاریخ انقضا در صفحهٔ جزئیات همیشه خالی. یک خط در سریال‌سازی سرور.
61. ✅ **میانگین امتیاز به مجوز وصل شد، نه نقش.** ماژول نیمه‌مهاجرت بود: کارت‌های دپارتمان از `canScoreDepartment` استفاده می‌کردند ولی عدد اصلی در ۵ نقطه `currentUser.role === 'admin'` بود. کاربری با نقش `commercial` که مجوزش به `score.qa` تغییر کرده بود، امتیاز بازرگانی را می‌دید که اجازهٔ ثبتش را نداشت. حالا یک `myDepartments` مشترک تصمیم می‌گیرد.
62. ✅ **تشخیص شریک روی کل گروه، بدون کد مرده.** قبلاً فقط `vendors[0]` را نگاه می‌کرد و به `partner.manufacturerId` تکیه داشت — فیلدی که **در دیتابیس وجود ندارد** (schema: «suppliers no longer reference a manufacturer»). حالا `resolveVendorPartner` روی همهٔ سورس‌های گروه اجرا می‌شود.
63. ✅ **چهار سنجهٔ سطح‌شرکت** که قبلاً فقط per-material وجود داشتند: نرخ قبولی آزمایشگاه · بالاترین ریسک با توزیعش · وضعیت IRC · **تک‌منبع بودن** (موادی که این شرکت تنها تأمین‌کنندهٔ آن‌هاست — ریسک تداوم تأمین که هیچ ماژول دیگری نشان نمی‌داد).
    - **تصحیح یک پیش‌بینی:** گزارش داده بودم گروه‌بندی با نام متنی باگ است. اندازه‌گیری روی داده نشان داد **نیست**: ۶۷ گروه با نام، ۶۷ با نرمال‌سازی کامل فارسی، و ۶۹ با هویت شریک — چون **۷۴ از ۷۶ سورس هیچ لینک شریکی ندارند**، گروه‌بندی با شریک وضع را بدتر می‌کرد. پس بازنویسی انجام نشد؛ فقط `supplierKey()` نرمال‌سازی ی/ي، ک/ك و نیم‌فاصله را به‌عنوان بیمهٔ دادهٔ واقعی اضافه کرد (امروز صفر تغییر).
    - تأیید زنده با دادهٔ ساختگی: آزمایشگاه **۷۵٪** (۸ تست، ۶ قبول، ۲ مردود روی ۴ ماده) · ریسک **بالا** (۱ بالا/۱ متوسط/۱ پایین/۵ بدون ارزیابی) · IRC **۲ منقضی** · تک‌منبع **۱** با نام ماده. هر چهار سنجه هم در حالت خالی و هم در حالت پر تست شدند · ۰ خطای کنسول · ۸۳ تست ✓

64. ✅ **ادغام‌های ۵ تا ۷ (پیشنهادهای همان گزارش):**
    - **مدارک SOP از مخزن شرکا** — قبلاً فقط یک رشتهٔ گرید نشان داده می‌شد. حالا امتیاز کل، تاریخ و ثبت‌کنندهٔ آخرین ارزیابی، و **وضعیت تک‌تک ۵ مدرک** (تأییدشده/تأیید موقت/منقضی/ارائه‌نشده) با لینک مستقیم به رکورد شریک. اگر سورس به هیچ فروشنده‌ای وصل نباشد، همین را صریح می‌گوید به‌جای نمایش خالی.
    - **سورس منتخب** — «n از m» ماده‌ای که این شرکت تصمیم ثبت‌شدهٔ سورس برایشان است (`GET /api/source-selections`).
    - **خروجی پروندهٔ تک‌تأمین‌کننده** (`exportSupplierDossierToExcel`) — سه شیت: خلاصهٔ ممیزی (آزمایشگاه/ریسک/SOP/تداوم تأمین)، اقلام تأمین‌شده با همان فرمت آرشیو (از `buildCategoryWorksheet` بازاستفاده شد)، و سوابق آزمایشگاه. ممیزی همیشه تک‌تأمین‌کننده انجام می‌شود و تا الان یعنی خروجی کل آرشیو و فیلتر دستی.
    - تأیید زنده: کارت SOP با دادهٔ واقعی (Grade C · ۵۵ از ۱۰۰ · ۵ مدرک با ۴ وضعیت متفاوت) · سورس منتخب ۱ از ۸ · لینک به مخزن شرکا ✓ · **فایل اکسل واقعاً دانلود شد** و با باز کردن zip تأیید شد: ۳ شیت با ۴۷/۱۵۳/۱۶ سلول و همهٔ بخش‌های خلاصه پر · ۰ خطای کنسول · ۸۳ تست ✓
      - نکته: اولین اعتبارسنجی من غلط بود — دنبال `<t>` (shared strings) گشتم ولی `xlsx-js-style` مقادیر را inline در `<v>` می‌نویسد. فایل از اول سالم بود.

### کارتابل اقدامات (`#/tasks/<key>`)
65. ✅ **کارت‌های اقدامات معلق حالا وارد فهرست می‌شوند، نه وسط یک رکورد.** رفتار قبلی `onSelectVendor(a.items[0])` بود — یعنی پرش مستقیم به **اولین** رکورد از دوازده‌تا، بدون اینکه کاربر بفهمد کجاست یا بقیه چه‌اند.
66. ✅ **کارت SOP اصلاً کلیک‌پذیر نبود** — `items: []` داشت، پس عددی نشان می‌داد که هرگز قابل باز کردن نبود.
67. ✅ **فهرست IRC از صفحهٔ اصلی برداشته شد** (۶۴ خط). این بخش هرچه بک‌لاگ بزرگ‌تر می‌شد صفحهٔ اصلی را شلوغ‌تر می‌کرد — برعکس کاری که داشبورد باید بکند. نکته: این فهرست **تا یک ساعت پیش همیشه خالی بود** چون `ircExpiryDate` هرگز خوانده نمی‌شد (ردیف ۶۰)؛ به‌محض رفع آن باگ، مشکل شلوغی ظاهر شد.
68. ✅ **یک صفحه با چهار تب، نه چهار صفحه** (`WorklistView`). تعامل هر چهار یکی است و کاربری که بک‌لاگ را خالی می‌کند معمولاً بین آن‌ها جابه‌جا می‌شود. هر تب آدرس خودش را دارد (`#/tasks/risk`) پس قابل ارسال به همکار است.
    - **بازگشت به فهرست:** رکورد روی استک ناوبری push می‌شود، پس «برگشت» به همان فهرست با همان بک‌لاگ برمی‌گردد — نه به صفحهٔ اصلی.
    - **مقصد هر ردیف متفاوت است:** سورس‌ها → پروندهٔ سورس · SOP → مخزن شرکای تجاری.
    - **مرتب‌سازی بر اساس فوریت:** در IRC، منقضی‌شده‌ها بالای نزدیک‌به‌انقضا و بعد بر اساس روز باقی‌مانده.
    - **آگاه به مجوز:** اگر کاربر مجوز اقدام نداشته باشد (`vendor.risk`, `partner.edit`, `vendor.edit`) فهرست را می‌بیند ولی با یادداشت صریح «فقط برای مشاهده».
    - تأیید زنده: هر ۹ سنجه سبز — حذف فهرست از صفحهٔ اصلی، کلیک‌پذیر شدن SOP، کارت→کارتابل، رندر ۹ ردیف، تعویض تب→URL، ترتیب فوریت، ردیف→پرونده، **برگشت→همان فهرست**، و دیپ‌لینک در مرورگر تازه · ۰ خطای کنسول · **۸۸ تست** (۵ تست جدید مسیر) ✓

69. ✅ **پاک‌سازی صفحهٔ اصلی + ساعت هدر:**
    - باکس «در انتظار ارزیابی اولیه کیفی» حذف شد (۳۳ خط). کارکردش با تب «سورس‌های ارزیابی‌نشده» در کارتابل پوشش داده می‌شود و آنجا **کل** فهرست را می‌دهد، نه ۳ مورد اول.
    - **فرمت تاریخ** به ترتیب گفتاری فارسی درآمد: `۴ شهریور ۱۴۰۵ · چهارشنبه`. قبلاً `toLocaleDateString` با هر چهار جزء، خروجی `۱۴۰۵ شهریور ۴, چهارشنبه` می‌داد — سال اول و روز هفته پشت ویرگول. حالا اجزا جدا گرفته و کنار هم چیده می‌شوند (`formatSystemDate`)، که نیاز به `replace` روی رشتهٔ فرمت‌شده را هم از بین می‌برد.
    - ساعت از وسط هدر به **گوشهٔ چپ، کنار باکس حساب کاربری** منتقل شد.
    - تأیید زنده: ۴ سنجه سبز (ترتیب تاریخ، حذف باکس، موقعیت افقی ساعت، نزدیکی به باکس کاربر) · ۰ خطای کنسول · ۸۸ تست ✓

70. ✅ **ساعت هدر: دقیقه‌ای به‌جای ثانیه‌ای + تاریخ میلادی.**
    - ساعت روی `App` زندگی می‌کند، پس هر تیک **کل درخت را re-render می‌کرد** — شصت بار در دقیقه برای رقمی که در یک سامانهٔ ارزیابی تأمین‌کننده کسی نمی‌خواند. ثانیه حذف شد و به‌روزرسانی دقیقه‌ای شد.
    - **تیک روی مرز دقیقه زمان‌بندی می‌شود، نه هر ۶۰ ثانیه ثابت** (`setTimeout` با فاصلهٔ محاسبه‌شده تا دقیقهٔ بعد)، وگرنه ساعت نمایش‌داده‌شده تا ۵۹ ثانیه از دقیقهٔ واقعی عقب می‌افتاد.
    - تاریخ میلادی به شکل ISO (`2026-08-26`) کنار آن اضافه شد — برای مکاتبات با تأمین‌کنندگان خارجی. عمداً ISO و نه فرمت محلی، تا با day-first/month-first اشتباه خوانده نشود.
    - تأیید زنده و **کمّی**: با `MutationObserver` روی خود عنصر ساعت — در ۶ ثانیه **۰ تغییر** (قبلاً ~۶). و تست جداگانه تأیید کرد ساعت **دقیقاً روی مرز دقیقه** جلو می‌رود (۱۲:۲۴ → ۱۲:۲۵)، چون «بهینه‌ای که دیگر به‌روز نمی‌شود» بدتر از مشکل اولیه بود · ۰ خطای کنسول · ۸۸ تست ✓

## کارهای باقی‌مانده (نقشهٔ راه)

### ماژول شرکای تجاری (بقیهٔ پیشنهادها)
- [x] #۲ نمای «سورس‌های متصل به این شریک» در جزئیات (تولیدکننده و فروشنده) ✅
- [x] #۵ خروجی Excel شرکا (فرمت یکسان با آرشیو) ✅
- [x] ستون «نتیجهٔ ارزیابی SOP»: برچسب بر اساس گرید (A→Approved, B→Permit Approval, C→Expired, D→Black List) ✅ (`04d82cf`) — حذف badge گرید تکراری + یکدست‌سازی در نمای جزئیات و خروجی Excel ✅
- [ ] #۴ اعتبارسنجی ایمیل/وبسایت/تلفن

### ماژول‌های دیگر (پیشنهادها از نقشهٔ اولیه)
- [x] **ریسک (FMEA):** ماتریس ریسک بصری زنده (heatmap ۵×۵ اهمیت×احتمال) در فرم ارزیابی ✅ (`cac47bd`) — کنتراست اعداد + نمایش بعد سوم (Detectability) و RPN نهایی زیر ماتریس ✅
- [x] **ریسک (FMEA):** تاریخچهٔ چند ارزیابی + روند SRI/RPN (بازسازی از audit) — audit اختصاصی «Risk Assessment» در POST vendors + endpoint `GET /api/vendors/:id/risk-history` + کارت تاریخچه در نمای سورس ✅ (تست زنده تأیید شد)
- [x] **آزمایشگاه:** نوار خلاصه (Pass/مشروط/Reject/نرخ قبولی) + **نمودار خطی روند کیفی نتایج** (جایگزین timeline قبلی) ✅ (تست زنده تأیید شد)
- [x] **آزمایشگاه:** اتوماسیون Reject→blacklist بازطراحی شد ✅ (تست زنده UI تأیید شد):
  - **نمونه‌ها:** یک نتیجهٔ Reject → خودکار به لیست سیاه (go/no-go).
  - **سورس/تأمین‌کننده:** دیگر خودکار رد نمی‌شوند (یک سورس می‌تواند چند نتیجه داشته باشد). به‌جای آن **باکس تصمیم‌گیری کیفی (فقط ادمین/QA)** با **دلیل الزامی** که در audit (severity Critical، reasonForChange) و در `rejectionReasons` سورس ثبت می‌شود؛ قابلیت بازگردانی از لیست سیاه با دلیل.
  - رفع badge شمارش لیست سیاه + یکدست‌سازی ۴ نقطهٔ فیلتر (سورس رد‌شده فقط در لیست سیاه، خارج از دستهٔ خرید).
- [ ] **آزمایشگاه:** پیوست فایل به رکورد آزمایش
- [x] **Audit:** نمای diff بصری فیلد‌به‌فیلد در جزئیات رویداد (فقط فیلدهای تغییریافته، برچسب فارسی، قبل→بعد رنگی، JSON خام جمع‌شونده) ✅ (تست زنده تأیید شد)
- [x] **Audit:** خلوت‌سازی UI — حذف ردیف ۹ دکمهٔ رنگی، جمع‌کردن ۷ فیلتر در پنل «فیلترها»، کاهش چیپ آمار به ۲، ادغام ستون تاریخ/ساعت و بردن سمت زیر نام کاربر ✅ (تست زنده)
- [x] **Audit:** خروجی Excel لیست audit (فرمت یکسان با شرکای تجاری: هدر 1E3A8A، Segoe UI، border نازک، RTL، رنگ سطح بحرانیت) + ستون «خلاصهٔ تغییرات» (قبل→بعد). خروجی همهٔ رکوردهای فیلترشده (نه فقط صفحه)؛ در حالت لوکال از store محلی. ✅ (تست زنده)
- [x] **داشبورد:** نمودار حلقوی توزیع گرید + مرکز اقدامات معلق (کلیک‌پذیر) + آخرین فعالیت‌های audit + نرخ قبولی آزمایشگاه ✅ (تست زنده)
- [x] **سایدبار:** Command Palette (⌘K) جستجوی سراسری + حالت جمع‌شونده آیکون‌فقط (persist) + badge هشدار قرمز کنار Audit ✅ (تست زنده)
- [ ] **داشبورد/سایدبار (بقیه):** heatmap ریسک پرتفولیو، KPI با دلتا/روند، دسترسی‌محورکردن آیتم‌ها

### فرابخشی (P0 امنیت — Critical از فاز ۱)
> وضعیت هر ردیف با خواندن کد تأیید شده است (نه از حافظه). ارجاع‌ها دقیق‌اند تا سشن بعدی دنبال نگردد.

- [ ] **`JWT_SECRET` پیش‌فرض hardcode** — `server.ts:770` → `process.env.JWT_SECRET || "internal-regulatory-compliance-secret-key-321"`. اگر env ست نشود هر کسی که مخزن را دیده می‌تواند توکن معتبر بسازد. باید بدون مقدار پیش‌فرض باشد و در نبودش fail-fast کند (مثل `requirePrisma`).
- [ ] **secretها داخل `docker-compose.yml`** — خط ۱۴ `JWT_SECRET=...` و خط ۲۴ `POSTGRES_PASSWORD=SecurePassword123`.
- [ ] **بدون `helmet` و `express-rate-limit`** — هیچ‌کدام در `package.json` نیستند ⇒ brute-force روی login هیچ محدودیتی ندارد.
- [ ] **PBKDF2 با ۱۰۰۰ تکرار** (`src/server/security/passwordService.ts:11`) → ~۶۰۰٬۰۰۰ یا argon2. ⚠️ هش‌های موجود **قابل ارتقای درجا نیستند**؛ نیاز به rehash در اولین ورود موفق + نسخهٔ الگوریتم در رکورد. `verifyPassword` هنوز رمز ساده (plaintext) را هم می‌پذیرد و باید حذف شود.
- [ ] **JWT در `localStorage`** → httpOnly cookie (در برابر XSS).
- [ ] **ESLint + CI** — نه `eslint.config`ای هست نه `.github/workflows`. یعنی هیچ گیتی جز اجرای دستی، typecheck/test/build را قبل از merge بررسی نمی‌کند.
- [x] ~~رفع باگ تست `SOP grade boundary`~~ ✅ — با تثبیت رابریک ۸۰/۶۰/۴۰/۳۰ و حذف گرید `D` رفع شد؛ کل سوییت سبز است.
- [x] ~~شکستن `App.tsx`~~ ✅ — ۷۹۵۶ → **۱۸۳۲** خط (ردیف‌های ۱۷ و بعد).
- [ ] **شکستن `server.ts`** — هنوز **۳۴۳۲ خط** است (مونولیت) + لایهٔ data (React Query) پیاده نشده.
- [ ] **code-splitting** — باندل هنوز یک فایل **~۲.۳MB** است (`dist/assets/index-*.js`).

## نکات مهم برای ادامه
- الگوی «تاریخچه از audit» برای هر موجودیتی که audit before/after دارد قابل تکرار است.
- برای هر ماژول: اگر هنوز localStorage-only است (مثل قبلِ مواد/شرکا)، اول به backend وصلش کن.
- قبل از تغییر بزرگ UI، ساختار فعلی را بخوان و درک/پیشنهاد را با کاربر تأیید کن (کاربر این را ترجیح می‌دهد).

## تم (Dark/Light) — یکدست‌سازی سراسری
- دکمهٔ تغییر تم (ماه/خورشید) در هدر + ردیف در منوی کاربر؛ انتخاب در localStorage (`theme`) ذخیره می‌شود و بار اول `prefers-color-scheme` را احترام می‌گذارد.
- **مهاجرت ۱۵۵۹ رنگ ثابت به theme tokens** در ۱۴ فایل: `bg-white→bg-card`, `bg-slate-50/100→bg-muted`, `text-slate-800/700→text-foreground`, `text-slate-600/500/400→text-muted-foreground`, `border-slate-100/200→border-border`, `hover:bg-slate-*→hover:bg-accent`.
- استثناها (دست‌نخورده): پنل‌های عمداً تیره (`bg-slate-800/900`)، رنگ‌های معنایی (emerald/blue/amber/rose)، overlayهای سفید روی دکمه‌های رنگی (`bg-white/20`)، فرم چاپ و صفحهٔ ورود، `print:bg-white`.
