# STATUS.md — وضعیت زندهٔ کار

> بعد از **هر تغییر** به‌روزرسانی می‌شود. برای ادامه در سشن جدید: اول `CLAUDE.md` بعد این را بخوان.
> آخرین به‌روزرسانی: مخزن مواد اولیه — آپلود واقعی فایل Specification

## وضعیت کلی
- **برنچ کاری فعلی:** `claude/vlse-modules-p3` (از main تازه) — کار جدید اینجا
- **PRها:** #2، #3، #6، #7، #8 merged · **#9 باز است** (کار جاری: رفع باگ‌ها + ماژول کاربران + سطوح دسترسی)
- **سلامت:** `tsc` سبز ✓ · `vite build` سبز ✓ · تست‌ها **۱۰۳ pass / ۰ fail** ✓
- **git:** کار جاری روی `claude/vlse-modules-p3` push شده (جلوتر از main؛ PR بعدی به main)

## حالت آزمایشی محلی (Local/Demo Mode) — تست بدون دیتابیس
- برای تست روی Vercel بدون ساخت دیتابیس: env `VITE_ENABLE_LOCAL_DEMO=true` را ست کن (build-time). دکمهٔ «ورود آزمایشی (حالت لوکال)» در صفحهٔ ورود ظاهر می‌شود.
- با کلیک آن، کاربر دموی admin ساخته می‌شود و `app_local_mode=true` در localStorage ست می‌شود. کل داده‌ها فقط در همان مرورگر (localStorage: `app_db`, `app_business_partners`, `app_materials`) ذخیره می‌شوند.
- `authFetch` در این حالت هیچ درخواستی به backend نمی‌زند (پاسخ 503 مصنوعی، بدون reload) → همهٔ loaderها به cache محلی برمی‌گردند و نوشتن‌ها no-op می‌شوند (state از قبل در localStorage persist شده).
- محدودیت‌ها: audit/تاریخچه‌ها (که سمت‌سرور از audit_log بازسازی می‌شوند) در این حالت خالی‌اند. برای نسخهٔ نهایی روی سرور شرکت، این env را **ست نکن** تا فقط ورود عادی با دیتابیس فعال باشد.
- تست زنده: build استاتیک بدون backend → ورود دمو ✓ · بدون خطای صفحه ✓ · persist و رندر داده پس از reload ✓ · بدون حلقهٔ logout ✓.

## دپلوی (Vercel)
- **آداپتر:** `api/handler.ts` → بسته `dist/server.cjs` (از `npm run build`). `vercel.json` rewrite دارد.
- **env لازم:** `DATABASE_URL` (PostgreSQL معتبر، مثل Neon/Supabase) و `JWT_SECRET`.
- **مهاجرت + seed:** جدول‌ها باید با `prisma migrate deploy` ساخته شوند. کاربران پیش‌فرض روی **اولین اجرا** (cold start) اگر جدول users خالی باشد seed می‌شوند (`seedDefaultUsers`).
- **audit در حالت لوکال:** ثبت رویدادها در localStorage (`app_audit_log`، util `services/localAudit.ts`) از هندلرهای مرکزی (سورس/ماده/شریک: create/update/delete) با before/after؛ `AuditTrailView` در حالت لوکال از همین store می‌خواند (فیلتر/صفحه‌بندی/آمار سمت‌کلاینت). diff فیلدها هم کار می‌کند.
- **رفع باگ لاگین Vercel:** `prisma generate` به build + postinstall اضافه شد (بدون آن Prisma Client روی Vercel ساخته نمی‌شد و login کرش می‌کرد → صفحه HTML → خطای Safari «The string did not match the expected pattern»). login handler حالا try/catch دارد و همیشه JSON برمی‌گرداند؛ فرانت هم پاسخ غیر-JSON را با پیام واضح مدیریت می‌کند.
- **⚠️ نکتهٔ لاگین:** دو مسیر seed پسورد متفاوت دارند —
  - محلی (`prisma/seed.ts`): `admin / 123`
  - seed خودکار سرور (`server.ts` DEFAULT_USERS، روی Vercel اجرا می‌شود): `admin / 123456` + `mustChangePassword: true` (اجبار تغییر رمز در اولین ورود).

## رفع شکست بیلد Vercel روی دیتابیس خواب (۱۴۰۵/۰۶/۰۵)
- دیپلوی کامیت `433ef22` شکست خورد. لاگ Vercel: `P1001: Can't reach database server at ep-sparkling-pond-...neon.tech` و بعد `[migrate] Migration failed`.
- **علت دیف من نبود؛ گارد مهاجرتی خودم بود** که در «راه الف» اضافه شده بود و دقیقاً طبق طراحی عمل کرد. ولی طراحی ناقص بود: **Neon در پلن رایگان scale-to-zero دارد** و بعد از چند دقیقه بی‌کاری می‌خوابد؛ اولین اتصال بیدارش می‌کند ولی `prisma migrate deploy` بعد از ~۵ ثانیه تسلیم می‌شود. یعنی هر بیلدی که دیتابیس خواب باشد می‌شکست.
- **رفع:** تلاش مجدد با فاصلهٔ فزاینده (۵ / ۱۵ / ۳۰ ثانیه، مجموعاً ۴ تلاش) **فقط** برای خطاهای اتصال (`P1001`, `ECONNREFUSED`, `ETIMEDOUT`, `ENOTFOUND`). خاصیت ایمنی حفظ شد: خطای واقعی مهاجرت (`P3009`, `P3018`) یا احراز هویت (`P1000`) بدون تلاش مجدد و فوراً بیلد را متوقف می‌کند.
- **مورد دیگری که این ماجرا آشکار کرد:** من محلی فقط `vite build` می‌زدم که **فقط فرانت** را می‌سازد؛ کامیت‌هایی که `server.ts` را لمس می‌کنند باید با `npm run build` کامل (شامل `esbuild` و مهاجرت) و بوت `dist/server.cjs` تست شوند. از این پس همین کار را می‌کنم.
- تست: دیتابیس در دسترس → ۰ ثانیه، exit 0 · هاست ناموجود → ۴ تلاش در ۵۲ ثانیه سپس exit 1 · تعارض مهاجرت (`P3009`) → **۱ ثانیه**، بدون تلاش مجدد · بدون `DATABASE_URL` → skip و exit 0 · `npm run build` کامل + بوت `dist/server.cjs` + `/api/health` = ۲۰۰.

## مخزن مواد اولیه — آپلود واقعی فایل Specification (۱۴۰۵/۰۶/۰۵)
- **مسئله:** فرم فقط `e.target.files[0].name` را ذخیره می‌کرد. هیچ فایلی آپلود نمی‌شد و هیچ دانلودی ممکن نبود — یعنی رکورد ادعای مستندسازی داشت بدون پشتوانه، که در سامانهٔ GxP قابل قبول نیست.
- **schema (`20260827200000_material_specification_file`):** سه ستون افزایشی روی `materials` — `specification_file_size`, `specification_file_data` (Text), `specification_uploaded_at`. ستون `specification_file` مثل قبل **نام فایل** است، پس دادهٔ موجود دست‌نخورده می‌ماند.
- **الگوی فایل تنبل (قاعدهٔ ۵)، همان چیزی که مدارک SOP دارند:** لیست مواد هرگز base64 حمل نمی‌کند؛ فقط `specificationFile` (نام)، `specificationFileSize`، `hasSpecificationFile` و `specificationUploadedAt`. blob از endpoint اختصاصی گرفته می‌شود.
- **سه endpoint:** `PUT /api/materials/:id/specification` (مجوز `material.edit`؛ سقف ۷ مگابایت چون `express.json` روی ۱۰mb است و data URL ~۳۳٪ بزرگ‌تر) · `GET /api/materials/:id/specification/file` · `DELETE /api/materials/:id/specification`. هر سه در audit ثبت می‌شوند (حذف با `severity: Warning`) و blob هرگز داخل رکورد audit نوشته نمی‌شود.
- **باگ قدیمی که حین تست پیدا شد و رفع شد:** در `PATCH /api/materials/:id` همهٔ فیلدها با `??` ادغام می‌شدند، پس ارسال `specificationFile: null` برای حذف پیوست، نام قبلی را نگه می‌داشت — یعنی **حذف فایل هیچ‌وقت واقعاً ذخیره نمی‌شد**. حالا برای همین فیلد `"specificationFile" in b` بررسی می‌شود: `null` صریح پاک می‌کند، کلید غایب مقدار فعلی را نگه می‌دارد. اگر نام پاک شود، blob و متادیتایش هم پاک می‌شوند تا فایل یتیم نماند.
- **UI:** در شناسنامهٔ ماده دکمه‌های مشاهده (با `openDocumentPreview`، همان مسیر امنِ شرکا)، دانلود، جایگزینی و حذف؛ خطا به‌صورت درون‌خطی؛ رکورد قدیمی‌ای که فقط نام دارد صریحاً می‌گوید «فقط نام فایل ثبت شده است — فایلی روی سرور نیست». فایل انتخاب‌شده هنگام **ساخت** ماده نگه داشته می‌شود و بعد از ایجاد رکورد آپلود می‌گردد (نمی‌توان فایل را به رکوردی که هنوز وجود ندارد وصل کرد). در حالت آزمایشی لوکال صریحاً گفته می‌شود فایل ذخیره نمی‌شود.
- تست زنده: آپلود ۲۰۰ ✓ · لیست بدون blob ✓ · round-trip بایت‌به‌بایت فایل ✓ · حجم زیاد ۴۱۳ ✓ · data URL نامعتبر ۴۰۰ ✓ · `null` صریح پاک می‌کند و PATCH بی‌ربط فایل را نگه می‌دارد ✓ · حذف ۲۰۰ و سپس ۴۰۴ ✓ · رکوردهای audit ✓ · تست مرورگری: آپلود از خود UI و دانلود برگشتی با همان نام ✓ · `tsc`/`vite build`/`npm run build`/۱۰۳ تست ✓.

## مخزن مواد اولیه — بستهٔ ۴: اعتبارسنجی تکراری‌بودن سمت سرور (۱۴۰۵/۰۶/۰۵)
- **قاعده حالا یک‌جاست و هر دو طرف از آن می‌خوانند:** `src/utils/materialDuplicates.ts` (همان الگوی `permissions.ts` / `auditTaxonomy.ts`). قبلاً فرم مخزن این بررسی را داشت ولی فقط در مرورگر، پس عملاً توصیه بود نه قاعده (قاعدهٔ ۱۴) — هر کلاینت دیگری، از جمله مسیر ذخیرهٔ سورس، می‌توانست ردیف دوم بسازد.
- دو سیگنال: **CAS واقعیِ یکسان** و **ترکیب نقش + نام لاتین + محصول نهایی لاتین**. CAS جایگزین (`N/A`, `-`, `نامشخص`, …) هرگز هویت نیست، وگرنه دومین مادهٔ ثبت‌شده «تکراری» می‌شد. ترکیب نیمه‌پرشده هم هویت نیست.
- `POST /api/materials` و `PATCH /api/materials/:id` در صورت برخورد **۴۰۹** با پیام فارسی و `duplicateOf` برمی‌گردانند و رویداد را با `severity: Warning` در audit ثبت می‌کنند (مثل حذفِ رد‌شده).
- **رکوردهای از قبل تکراری قفل نمی‌شوند:** در PATCH فقط قاعده‌ای اعمال می‌شود که ویرایش، فیلدهای همان قاعده را تغییر داده باشد. پس ویرایش نام روی ردیفی که از قبل CAS مشترک داشت مجاز است (وگرنه هدف‌های اسکریپت `merge-duplicate-materials.ts` غیرقابل‌ویرایش می‌شدند)، ولی تغییر CAS **به** یک CAS موجود رد می‌شود.
- فرم مخزن هم به همین تابع وصل شد تا دو طرف واگرا نشوند (پیام خطای درون‌خطی از همان منبع می‌آید).
- تست: ۹ تست واحد جدید (مجموع **۱۰۳**) · تست زندهٔ endpointها → CAS تکراری ۴۰۹ ✓ · ترکیب تکراری ۴۰۹ ✓ · مادهٔ جدید ۲۰۰ ✓ · PATCH روی فیلد بی‌ربط ۲۰۰ ✓ · PATCH که CAS را به موجود تغییر می‌دهد ۴۰۹ ✓ · سه رکورد Warning در `audit_log` ✓ · `npm run build` کامل (شامل `dist/server.cjs`) ✓.

## مخزن مواد اولیه — بستهٔ ۳: حالت‌ها و دسترسی‌پذیری (۱۴۰۵/۰۶/۰۵)
- **اسکلت بارگذاری:** پراپ `isLoading` (از `App.tsx`، `isSyncing && materials.length === 0` — همان الگوی `CategoryView`). تا رسیدن دادهٔ اول، پنج ردیف اسکلتی هم‌شکل جدول رندر می‌شود و `aria-busy` روی `<table>` می‌نشیند. قبلاً کاربر در همین بازه پیام «هیچ ماده‌ای یافت نشد» می‌دید، یعنی به او گفته می‌شد مخزن خالی است.
- **دو حالت خالیِ متفاوت:** «مخزن مرجع هنوز خالی است» با دکمهٔ «ثبت اولین ماده» (فقط برای دارندهٔ `material.create`؛ برای بقیه جملهٔ صریح دربارهٔ دسترسی) در برابر «هیچ ماده‌ای با این جستجو یا فیلترها پیدا نشد» با دکمهٔ «پاک‌کردن جستجو و فیلترها».
- **هدر ستون‌ها حالا `<button>` داخل `<th scope="col">` است**، نه `<th onClick>`: مرتب‌سازی با کیبورد کار می‌کند، حلقهٔ فوکوس دارد و `aria-sort` روی ستون فعال ست می‌شود. `<caption class="sr-only">` هم اضافه شد.
- حلقهٔ فوکوس روی سه دکمهٔ عملیات هر ردیف و کنترل‌های نوار ابزار.
- **`currentPage` clamp می‌شود** (`page = min(currentPage, totalPages)`): حذف آخرین ردیفِ آخرین صفحه دیگر کاربر را روی صفحهٔ خالی رها نمی‌کند.
- **انتخاب تعداد ردیف در صفحه** (۱۰/۲۵/۵۰/۱۰۰) کنار صفحه‌بندی؛ قبلاً روی ۱۰ ثابت بود.
- نام‌ها با `EntityName` رندر می‌شوند (قاعدهٔ ۱۵) و `whitespace-nowrap` از جدول برداشته شد تا نام بلند به‌جای کشیدن افقی صفحه، در دو خط بشکند.
- تست زنده: با تأخیر مصنوعی روی `/api/**` → `aria-busy=true` و ۴۰ عنصر اسکلتی، بدون ادعای «خالی» ✓ · حالت فیلترشده و دکمهٔ پاک‌کردن (بازگشت به ۷ ردیف) ✓ · Tab تا هدر و Enter → `aria-sort=descending` با حلقهٔ فوکوس ✓ · تغییر تعداد در صفحه ✓ · `tsc`/`build`/۹۴ تست ✓.

## مخزن مواد اولیه — بستهٔ ۲: تم تیره و توکن‌ها (۱۴۰۵/۰۶/۰۵)
- کل `MaterialRepositoryView` صفر `dark:` داشت (همان باگی که در `VendorDetail` رفع شد). حالا هر رنگ روشنِ هاردکد جفت `dark:` دارد: کارت‌های آمار، بج نقش، سه دکمهٔ عملیات ردیف، کاشی‌های آیکون مودال‌ها، چیپ فایل Specification، کادر خطای فرم، هشدار یکپارچگی و مودال‌های حذف.
- `slate-*` باقی‌مانده به توکن تبدیل شد: `hover:bg-slate-200/60` → `hover:bg-accent` · `bg-slate-200` دکمهٔ بستن → `bg-muted hover:bg-accent border-border` · چیپ دستهٔ سورس → `bg-background border-border` · آیکون حالت خالی → `text-muted-foreground/50`.
- **پنل «پیش‌نمایش نام‌های استاندارد»** که گرادیان ثابت `from-slate-900 to-slate-800` بود و در تم تیره در زمینه گم می‌شد، حالا با `bg-foreground text-background` معکوس می‌شود؛ چون این پنل **خروجی سیستم** است نه ورودی، در هر دو تم باید همان تضاد را داشته باشد و توکن‌های foreground/background با هم جابه‌جا می‌شوند.
- **`animate-shake` کلاس مرده بود** (هیچ keyframe به این نام در `index.css` یا Tailwind v4 نبود) — با `fade-in` موجود جایگزین شد، پس کادر خطای فرم بالاخره سیگنال حرکتی دارد.
- تست زندهٔ کنتراست در تم تیره (اندازه‌گیری با canvas، چون Tailwind v4 رنگ را `oklch()` برمی‌گرداند): بج Other 13.98 · API 10.14 · Reagent 10.56 · Packaging 12.55 · عدد کارت 13.98 · برچسب کارت 5.71 · دکمهٔ اصلی 8.24 — همه بالاتر از حد AA (۴.۵). تم روشن هم بازبینی شد و تغییری نکرده است.
- تست: `tsc` ✓ · `vite build` ✓ · ۹۴ تست ✓ · اسکرین‌شات تیره/روشن (جدول و فرم) ✓.

## مخزن مواد اولیه — بستهٔ ۱: درستی داده و صداقت نمایش (۱۴۰۵/۰۶/۰۵)
- **جدول نقش مواد یک‌جا شد:** `src/constants/materialRoles.ts` — همان الگوی `permissions.ts`/`auditTaxonomy.ts`. قبلاً این جدول در `MaterialRepositoryView` و `MaterialSelector` کپی شده بود و واگرا شده بودند.
  - **مقدار ذخیره‌شده (`value`) تغییر نکرد** (`Reagent / Reactant`, `Packaging Item`) چون هر ردیف موجود و هر نام استاندارد تولیدشده از همان استفاده می‌کند؛ فقط برچسب‌ها عوض شدند: API/ماده موثره · Intermediate/حدواسط · Solvent/حلال · Reagent/واکنشگر · Excipient/اکسپیانت · Packaging/بسته‌بندی · Other/متفرقه.
  - `getMaterialRole()` املاهای قدیمی (`Reagent`, `Packaging`, …) را هم به همان ردیف می‌نگارد، وگرنه بی‌صدا در سطل API می‌افتادند.
- **کارت‌های آمار درست شمردند:** قبلاً پنج کارت بود و `Packaging Item` و `Other` هیچ‌جا شمرده نمی‌شدند، ضمن اینکه کارت «Reagent / Other» فقط Reagent را می‌شمرد. حالا هفت کارت از روی همان جدول ساخته می‌شود و جمعشان دقیقاً برابر «مجموع مواد» است (تست زنده: ۴+۱+۱+۱ = ۷ ✓).
- **مرتب‌سازی فارسی:** `Intl.Collator('fa', {numeric:true})` جای مقایسهٔ کدپوینتی. ترتیب واقعی حالا آ، آت، ا، ای، ب، پ، م است. ستون فعال و جهت آن هم با آیکون جهت‌دار و `aria-sort` نشان داده می‌شود (قبلاً هر شش ستون یک آیکون خنثای یکسان داشتند).
- **جستجو نام‌های استاندارد و IUPAC را هم پوشش می‌دهد** — جستجوی «بسته‌بندی - بلیستر» که همان نامی است که بقیهٔ برنامه نشان می‌دهد، قبلاً هیچ نتیجه‌ای نمی‌داد.
- **ستون «سورس‌های مرتبط»:** همان عددی که تصمیم حذف را می‌گیرد، حالا در خود جدول دیده می‌شود و قابل مرتب‌سازی است. مرجع نهایی همچنان سرور است (`DELETE /api/materials/:id` روی `vendorMaterial` گارد دارد)؛ شمارش کلاینت برای سورس بدون `materialId` روی ماهیت ماده تطبیق می‌دهد تا با گارد سرور واگرا نشود.
- برچسب دستهٔ سورس در مودال حذف از `categoryLabels` می‌آید (قبلاً کلید انگلیسی خام مثل `foreign` نمایش داده می‌شد).
- تست: `tsc` ✓ · `vite build` ✓ · ۹۴ تست ✓ · تست زندهٔ مرورگری روی دادهٔ واقعی (کارت‌ها، ترتیب فارسی، جستجوی نام استاندارد، مرتب‌سازی ستون سورس‌ها با `aria-sort=ascending`) ✓.
- گزارش مخزن مواد کامل شد: بسته‌های ۱ تا ۴ به‌علاوهٔ آپلود واقعی فایل Specification. هیچ مورد بازی از آن گزارش نمانده است.

## قانون SOP: فقط فروشندهٔ گرید A (۱۴۰۵/۰۶/۰۵)
- `canSupplySources(partner)` در `src/utils/sopEvaluation.ts` — منبع واحدِ تصمیم، که هم `PartnerSelector` و هم `server.ts` از آن می‌خوانند.
- **UI:** فروشندهٔ غیرمجاز پنهان نمی‌شود بلکه خاکستری و غیرقابل‌کلیک می‌ماند، با دلیل زیر نامش و شمارش در فوتر («۷ مورد یافت شد · ۱ مورد طبق SOP قابل انتخاب نیست»). پنهان‌کردن باعث می‌شد کاربر فکر کند ثبت نشده و رکورد تکراری بسازد.
- **سرور:** `POST /api/vendors` و `PATCH /api/vendors/:id/profile` با **۴۲۲** رد می‌کنند و رویداد را با `severity: Warning` در audit می‌نویسند.
- **استثناها:** تولیدکننده (SOP ندارد) · فروشندهٔ ازقبل‌متصل به همان سورس (وگرنه رکورد قدیمی غیرقابل‌ویرایش می‌شد) · فروشندهٔ ارزیابی‌نشده با پیام جداگانه.
- **⚠️ ناسازگاری داده که در تست پیدا شد:** ستون `grade` با امتیاز ذخیره‌شده نمی‌خواند — `bp_sup_2` امتیاز ۸۰ دارد ولی `grade='B'`، در حالی که رابریک ≥۸۰ را A می‌داند. کلاینت روی load اصلاح می‌کند و A نشان می‌دهد؛ نسخهٔ اول گارد سرور ستون را می‌خواند و همان فروشنده را رد می‌کرد. حالا سرور هم گرید را با `calculateGradeAndStatus` از امتیاز مشتق می‌کند.
- تست زنده: POST با گرید A → ۲۰۰ · با گرید B → **۴۲۲** با پیام فارسی · فقط تولیدکننده → ۲۰۰ · PATCH به گرید B → ۴۲۲ · PATCH با نگه‌داشتن فروشندهٔ خودش → ۲۰۰ · رکورد قدیمی روی فروشندهٔ B همچنان قابل ذخیره · دو ردیف audit ثبت شد. UI: کیمیا فارمد (۷۰) غیرفعال با دلیل، Sinoway (۸۰) و Biesterfeld (۹۵) فعال، کلیک روی ردیف مسدود بی‌اثر.

## استقلال تولیدکننده و فروشنده در فرم سورس (۱۴۰۵/۰۶/۰۵)
- طبق قاعدهٔ ۴ پروژه فروشنده به تولیدکننده وابسته نیست، ولی **UI هنوز مدل قدیمی را نشان می‌داد**. با انتخاب یک تولیدکننده سه پیام هم‌زمان می‌آمد: جعبهٔ سبز «تأمین مستقیم و بی‌واسطه از کارخانه سازنده (Direct Manufacturer Sourcing)»، بج «خرید بی‌واسطه»، و بج دوم روی کارت تماس؛ به‌علاوهٔ جملهٔ «نیازی به ارزیابی فروشنده واسطه ندارد».
- جای همه، **یک کارت واحد** نشست که برای هر دو نوع یکسان است و فقط یک بج نوع دارد («تولیدکننده» / «فروشنده»). رنگ سبز دیگر یعنی «ارزیابی تأییدشده»، نه «تولیدکننده انتخاب شد».
- placeholder «-- جستجو و انتخاب فروشندهٔ واسطه --» → «جستجو و انتخاب تولیدکننده یا فروشنده». برچسب‌های `PartnerSelector` هم از «کارخانه تولیدی مرجع (Manufacturer)» و «فروشنده / بازرگانی واسطه (Supplier)» به «تولیدکننده» و «فروشنده» ساده شدند.
- عنوان کارت تماس: «… تولیدکنندهٔ مرجع (تکمیل خودکار - Read-Only)» → «اطلاعات تماس تولیدکننده».
- **گرید دروغین رفع شد:** کد `grade || 'A'` و `status || 'تایید شده'` داشت، پس فروشندهٔ ارزیابی‌نشده در این فرم «Grade A / تایید شده» دیده می‌شد. حالا «ارزیابی نشده» و «—» نشان می‌دهد (تست زنده با یک فروشندهٔ بدون ارزیابی).
- **بازمانده‌های کد حذف شد:** `BusinessPartner.manufacturerId` از `types.ts` (ستونی به این نام در `business_partners` وجود ندارد) و سه مقدارش در `db_business_partners.ts`؛ خط ۶۷ `VendorForm` که از یک Supplier فیلد `manufacturerId` می‌خواند؛ متغیر بی‌مصرف `isDirectPurchase`.
- **حالت مردهٔ `optional` در `PartnerSelector` پاک شد:** یک شاخهٔ کامل «خرید بی‌واسطه» (گزینهٔ dropdown + چیپ سبز + برچسب «اختیاری، در صورت خرید مستقیم خالی بگذارید») که **هیچ صداکننده‌ای** فعالش نمی‌کرد. پراپ‌های `selectedManufacturerId`/`manufacturerId` هم که فقط در یک متغیر خوانده‌نشده جمع می‌شدند حذف شدند.
- تست زنده: با تولیدکننده → هیچ واژهٔ «بی‌واسطه/واسطه/Direct» روی صفحه نمانده؛ با فروشندهٔ ارزیابی‌شده → گرید و امتیاز واقعی؛ با فروشندهٔ ارزیابی‌نشده → «ارزیابی نشده». بدون خطای کنسول.

## اعتبارسنجی کد IRC در فرم ثبت سورس (۱۴۰۵/۰۶/۰۵)
- کد IRC **اختیاری** می‌ماند، ولی اگر پر شود باید دقیقاً **۱۶ رقم عددی** باشد؛ وگرنه ثبت انجام نمی‌شود و خطا زیر همان فیلد نشان داده می‌شود (`role="alert"` + اسکرول و فوکوس، مثل خطاهای مادهٔ اولیه و شریک).
- ورودی فقط رقم می‌پذیرد: حروف/خط‌تیره/فاصله حذف می‌شوند (پیست‌کردن کد با جداکننده کار می‌کند)، ارقام فارسی/عربی به لاتین تبدیل می‌شوند، و طول روی ۱۶ قفل است. شمارندهٔ «n/۱۶» کنار برچسب.
- **رکوردهای قدیمی قفل نمی‌شوند:** مقدارهای موروثی مثل `N/A` فقط هشدار کهربایی می‌گیرند و تا وقتی دست نخورده‌اند مانع ذخیره نیستند (`isLegacyIrcUntouched`)؛ به‌محض ویرایش، قاعدهٔ ۱۶ رقم اعمال می‌شود.
- تست زنده: با ماده و شریک انتخاب‌شده و IRC پنج‌رقمی → **هیچ POSTی ارسال نشد**، فوکوس روی `#vf-irc` و پیام «کد IRC باید دقیقاً ۱۶ رقم باشد؛ ۵ رقم وارد شده است» · با ۱۶ رقم → `POST /api/vendors` = ۲۰۰ و رفتن به صفحهٔ سورس · تایپ `12ab-34 56` → `123456` · تایپ `۱۲۳۴` → `1234` · ۸۸ تست سبز.

## صفحهٔ سورس — بستهٔ ۱: دارک‌مود و خوانایی (۱۴۰۵/۰۶/۰۵)
- **`VendorDetail.tsx` اصلاً دارک‌مود نداشت:** صفر مورد `dark:` در برابر ~۴۰ پس‌زمینهٔ رنگیِ فقط-روشن. حالا **۲۱۳ جفت `dark:`** به‌صورت قاعده‌مند اضافه شد: `bg-{c}-50→dark:bg-{c}-950/30` · `bg-{c}-100→dark:bg-{c}-900/40` · `border-{c}-100..300→dark:border-{c}-800` · `text-{c}-700..950→dark:text-{c}-300` · `text-{c}-500|600→dark:text-{c}-400` (+ معادل‌های hover).
- **باگ خوانایی اندازه‌گیری‌شده:** برچسب تأمین‌کننده در کارت تماس `rgb(0,79,59)` روی `rgb(30,41,59)` بود = **۱٫۵۲:۱**. حالا `rgb(94,233,181)` = **۹٫۶۱:۱**.
- **جزیرهٔ روشن:** کادر هشدار انقضای مجوز در دارک‌مود `rgb(255,251,235)` بود روی صفحهٔ `rgb(11,17,32)`. حالا پس‌زمینهٔ تیرهٔ کهربایی با متن `rgb(255,210,48)` = **۱۳٫۶۱:۱**. همین برای پنل «لیست سیاه» (rose) هم درست شد.
- **۱۶ کلاس Tailwind با پلهٔ ناموجود** که هیچ استایلی اعمال نمی‌کردند: `focus:ring-indigo-505` ×۴، `border-indigo-250` ×۴، `text-slate-505` ×۲، `text-slate-605`، `text-indigo-850`، `text-red-905`، `border-amber-250`، `bg-teal-55`، `bg-orange-105` → به نزدیک‌ترین پلهٔ معتبر یا توکن تبدیل شدند.
- خاکستری‌های خام (`hover:bg-slate-200`، `bg-slate-200/300`، `border-slate-50`، `border-slate-900/5|10`، `text-slate-300`) به توکن‌های `accent`/`muted`/`border` رفتند.
- تست زنده: لایت بدون هیچ تغییری (مقایسهٔ اسکرین‌شات قبل/بعد) · دارک روی سورس عادی و سورس لیست‌سیاه · ۰ خطای کنسول · ۸۸ تست سبز.
## حذف ۸ `alert()` باقی‌ماندهٔ برنامه (۱۴۰۵/۰۶/۰۵)
برخلاف مورد فرم ریسک (که از مسیر UI غیرقابل‌دسترس بود)، این هشت مورد **واقعاً به کاربر نشان داده می‌شدند**. الگو همان `UsersView` است: `role="alert"` قرمز برای خطا، `role="status"` برای اطلاع.

- **`VendorDetail` — ثبت نتیجهٔ آزمایش (۲):** «تاریخ آزمایش» و «کد آزمایشگاهی» حالا پیام درون‌خطی بالای دکمه‌های فرم می‌دهند و با انصراف پاک می‌شوند.
- **`VendorDetail` — ویرایش ردیف آزمایش (۲):** پیام داخل همان ردیف جدول، بالای دکمه‌های ذخیره/انصراف؛ با شروع ویرایش ردیف دیگر پاک می‌شود.
- **`VendorDetail` — رد/بازگردانی سورس (۲):** پیام زیر textarea دلیل، داخل باکس تصمیم کیفی.
- **`AuditTrailView` — خروجی اکسل (۲):** «رکوردی یافت نشد» و «خطا در تهیهٔ خروجی» به دو حالت جدا تفکیک شدند — اولی `status` کهربایی (نتیجهٔ خالی یک یافته است نه خرابی) و دومی `alert` قرمز؛ هر دو زیر همان دکمه‌ای که شروعشان کرده.
- تأیید زنده روی هر پنج مسیر با **صفر دیالوگ بومی**: ثبت آزمایش بدون کد QC · ویرایش ردیف با کد خالی · رد سورس بدون دلیل · خروجی با فیلتر بی‌نتیجه · خروجی با درخواست قطع‌شده (`route.abort`).
- **حالا هیچ `alert()` بومی در کل `src` باقی نمانده.**

## فرم ارزیابی ریسک — حذف `alert()` بومی (۱۴۰۵/۰۶/۰۵)
- `alert('شما دسترسی ثبت ارزیابی ریسک را ندارید.')` با پیام درون‌خطی `role="alert"` بالای نوار نتیجه جایگزین شد (همان الگوی فرم ثبت سورس): RTL، تم‌پذیر، و بعد از بسته‌شدن ناپدید نمی‌شود.
- ⚠️ **نکتهٔ مهم که حین تست روشن شد:** `VendorDetail` این فرم را فقط وقتی رندر می‌کند که کاربر `vendor.risk` داشته باشد (`canRisk` در خط ۳۴ و گیت دکمهٔ باز‌کردن در خط ۱۱۲۵). یعنی این `alert()` عملاً **از مسیر UI غیرقابل‌دسترس** بود و فقط یک تور ایمنی است. با کاربر `qa` تست شد: فرم اصلاً باز نمی‌شود.
- به همین دلیل، نسخهٔ اولی که نوشتم (دکمهٔ disabled + یادداشت دائمی «دسترسی ندارید») **UI مرده** می‌ساخت و پس گرفته شد؛ فقط تور ایمنی درون‌خطی ماند.
- تأیید زنده با ادمین: مسیر عادی سالم است — صفر دیالوگ بومی، کارت موفقیت نمایش داده شد، ارزیابی ذخیره و روی صفحه ماند، ۰ خطای کنسول.
- **باقی‌ماندهٔ `alert()` در برنامه (تأیید نشده):** ۶ مورد در `VendorDetail` (اعتبارسنجی فرم نتایج آزمایشگاه و باکس رد/بازگردانی سورس) و ۲ مورد در `AuditTrailView` (خروجی اکسل).

## فرم ارزیابی ریسک — یکدست‌سازی با بقیهٔ فرم‌ها (۱۴۰۵/۰۶/۰۵)
- **مشکل:** `RiskAssessmentForm.tsx` صرف‌نظر از تم، همیشه تیره رندر می‌شد: `bg-slate-900` برای پوسته، `bg-slate-800/40` برای پنل‌ها، `text-white`/`text-slate-300` برای متن، و glow کهربایی (`shadow-[0_0_20px_rgba(245,158,11,0.1)]`). چون داخل ویزارد صفحهٔ سورس رندر می‌شود، روی صفحهٔ روشن یک جزیرهٔ تیره بود. صفر مورد `dark:` داشت.
- **اصلاح:** همان قراردادهای `EvaluationForm` (فرم خواهرش در همان ویزارد) — پوسته `bg-card border-border rounded-2xl p-6 md:p-8 shadow-sm`، پنل‌های فیلد `bg-muted border-border`، متن `text-foreground`/`text-muted-foreground`، سلکت‌ها `bg-background border-border focus:ring-ring`، و دکمهٔ ثبت با `bg-primary text-primary-foreground` (هم‌شکل دکمهٔ «مرحله بعد» درست زیرش).
- **ماتریس ریسک:** سلول‌های رنگی با آلفای پایین (`/25`) نگه داشته شدند چون روی هر دو زمینه کار می‌کنند؛ عدد داخل سلول `text-foreground` شد و حلقهٔ سلول فعال از `ring-white` به `ring-ring` تغییر کرد تا در تم روشن هم دیده شود.
- **کهربایی به‌عنوان رنگ معنایی ریسک ماند** ولی با جفت روشن/تیره: `text-amber-600 dark:text-amber-400`. همین برای سطح ریسک (سبز/کهربایی/قرمز).
- تأیید زنده: لایت → پوسته `rgb(255,255,255)` روی صفحهٔ `rgb(244,247,250)`؛ دارک → `rgb(30,41,59)` روی `rgb(11,17,32)`؛ سلکت‌ها در هر دو تم با متن خوانا. ۰ خطای کنسول · ۹۴ تست سبز.
- باقی‌مانده (خارج از این تغییر): این فرم هنوز برای خطای دسترسی از `alert()` بومی استفاده می‌کند — همان الگویی که در فرم ثبت سورس به خطای درون‌خطی تبدیل شد.

## تغییر عنوان «گرید SOP» به «گرید ارزیابی» (۱۴۰۵/۰۶/۰۵)
هر ۴ نقطه‌ای که این عنوان را حمل می‌کرد:
- `VendorDetail.tsx:439` — «گرید SOP فروشنده:» → «گرید ارزیابی فروشنده:» (تأیید زنده: «گرید ارزیابی فروشنده: A»).
- `excelExport.ts:525` — سرستون خروجی اکسل شرکا.
- `excelExport.ts:924` — ردیف خلاصهٔ SOP در پروندهٔ تک‌تأمین‌کننده.
- `sopEvaluation.ts:200` — متن رد ۴۲۲: «گرید ارزیابی این فروشنده X است؛ طبق دستورالعمل فقط گرید A قابل انتخاب است.» (این جمله هم سمت سرور و هم در `PartnerSelector` نمایش داده می‌شود.)

**دست‌نخورده ماند** (عمداً): واژهٔ SOP آنجا که به خودِ *مدرک* یا *فرآیند* اشاره دارد — «مدارک SOP»، «ارزیابی SOP انجام نشده»، «امتیاز کل SOP» و مانند آن — چون SOP آنجا اصطلاح واقعی GMP است نه عنوان گرید. اگر تغییر گسترده‌تری لازم است باید جداگانه تصمیم گرفته شود.

## اسکریپت ادغام مواد تکراری (۱۴۰۵/۰۶/۰۵)
`scripts/merge-duplicate-materials.ts` — پاک‌سازی ردیف‌های مادهٔ تکراری که مسیر ذخیرهٔ قدیمی سورس می‌ساخت.

```bash
DATABASE_URL=... ./node_modules/.bin/tsx scripts/merge-duplicate-materials.ts          # اجرای آزمایشی
DATABASE_URL=... ./node_modules/.bin/tsx scripts/merge-duplicate-materials.ts --apply  # اجرای واقعی
```

- **بدون `--apply` هیچ چیزی نوشته نمی‌شود** و فقط گزارش می‌دهد چه کاری انجام خواهد داد.
- فقط ردیف‌های تولیدشده (`^mat_`) در مدخل مخزن ادغام می‌شوند؛ **دو مدخل دست‌ساز هرگز ادغام نمی‌شوند**، هر چقدر هم شبیه باشند.
- تطبیق «یک ماده بودن» فقط با سیگنال قوی: CAS واقعی، یا نام فارسی/انگلیسی یکسان. مقدارهای جایگزین (`N/A` و…) به‌حساب نمی‌آیند. اگر یک ردیف به **بیش از یک** مدخل بخورد، رد می‌شود تا دستی بررسی شود.
- هر ادغام در یک تراکنش: انتقال لینک سورس‌ها، انتقال `evaluations`، حذف ردیف تکراری، و **ثبت رکورد audit** (قاعدهٔ ۲). اگر سورسی از قبل هم به تکراری و هم به مدخل اصلی وصل باشد (تخطی از یکتایی `vendor_materials`)، لینک تکراری حذف می‌شود نه منتقل.
- **باگی که همین تست گرفت:** `audit_id` از نوع `VarChar(50)` است و یک `material_id` تولیدشده به‌تنهایی پُرش می‌کرد (`P2000`). تراکنش درست rollback شد و شناسه کوتاه شد؛ نام ماده در `description` می‌آید.
- تست کامل روی دیتای ساختگی: ۲ ادغام، ۱ لینک منتقل، ۱ لینک متضاد حذف، ۱ ارزیابی منتقل، ۱ ردیف بی‌همتا دست‌نخورده، و **اجرای دوم بی‌اثر (idempotent)**. تأیید UI: سورسی که به تکراری وصل بود حالا نام استاندارد را نشان می‌دهد و مخزن فقط یک «استون» دارد.

## نام استاندارد ماده در صفحهٔ سورس (۱۴۰۵/۰۶/۰۵)
- **علائم:** در «مشخصات فنی و اطلاعات عمومی» به‌جای نام استاندارد، فقط نام سادهٔ ماده می‌آمد (مثلاً «استون» به‌جای «حلال - استون (برای متادون هیدروکلراید)»).
- **ریشه:** منطق نمایش درست بود ولی رکوردی که سورس به آن **لینک** است همیشه همان مدخل مخزن نیست. سورس‌هایی که قبل از اصلاح `materialId` ذخیره شده‌اند به مادهٔ تکراریِ ساخته‌شده از payload سورس وصل‌اند که فقط name/CAS/IRC دارد و `standard_name_fa|en` ندارد؛ پس fallback به نام ساده می‌افتاد. با ساختن همان سناریو در دیتابیس محلی بازتولید شد.
- **اصلاح:** منطق به `src/utils/materialNames.ts` منتقل شد (`resolveMaterialNames`) با ترتیب: رکورد لینک‌شده → اگر نام استاندارد نداشت، مدخل مخزن برای همان ماده که دارد (تطبیق با نام فارسی/انگلیسی/نام استاندارد/CAS واقعی) → نام ساده. CAS جایگزین (`N/A`) عمداً تطبیق نمی‌دهد وگرنه هر دو مادهٔ بدون CAS یکی حساب می‌شدند.
- ۶ تست جدید (`tests/materialNames.test.ts`) — مجموع **۹۴ تست**. تأیید زنده روی سه حالت: لینک به تکراری، لینک به مدخل واقعی، و ماده‌ای که اصلاً نام استاندارد ندارد.

## صفحهٔ سورس — بسته‌های ۳ و ۴ (۱۴۰۵/۰۶/۰۵)
- **حذف واژگان منسوخ «خرید بی‌واسطه»:** چیپ سبز هدر و کل باکس کارت تماس (با جملهٔ «این سورس فاقد فروشنده واسطه بوده…») حذف شدند. این ادعا از روی `role === 'manufacturer'` ساخته می‌شد، در حالی که طبق قاعدهٔ ۴ تولیدکننده و فروشنده مستقل‌اند و این پیوند چیزی دربارهٔ وجود یا نبود واسطه نمی‌گوید. متغیر به `partnerIsManufacturer` تغییر نام داد چون تمایز آیکون/رنگ تولیدکننده و فروشنده گزارهٔ درستی است و ماند. گرید ستون‌بندی هم که فقط برای همان باکس دو‌ستونه می‌شد، تک‌ستونه شد.
- **برچسب فیلدها فارسی شد:** `CAS Number` → «شمارهٔ CAS» · `IRC Code`/`IVC Code` → «کد IRC»/«کد IVC» · `کد سیستم / Unique ID` → «کد داخلی سامانه».
- **شناسهٔ داخلی از رتبهٔ فیلد ثبتی پایین آمد:** قبلاً با قاب و رنگ فیروزه‌ای هم‌وزن CAS و IRC بود؛ حالا متن ساده و کم‌رنگ با tooltip «کد ثبتی رگولاتوری نیست».
- **حالت خالی کارت تماس:** وقتی شریک تلفن/ایمیل/آدرس/رابط ندارد، به‌جای فضای سفید، جمله‌ای می‌گوید اطلاعات در مخزن شرکا ثبت نشده و از همان‌جا قابل تکمیل است.
- **ویزارد به مرحلهٔ جدید اسکرول می‌کند:** `setEvalStage` حالا بعد از تغییر مرحله، بلوک استپر را به دید می‌آورد. تأیید زنده: `scrollTop` از ۰ به ۶۸۹ رفت و در هر دو مرحله `scrollTop === maxScroll` بود (یعنی تا انتهای ممکن اسکرول شد؛ استپر در ۴۱۱px می‌ماند چون محتوای زیرش کمتر از یک صفحه است).
- تست زنده: هیچ ردی از واژگان منسوخ و برچسب انگلیسی روی صفحه نماند · حالت خالی در لایت و دارک · ۰ خطای کنسول · ۸۸ تست سبز.

## صفحهٔ سورس — بستهٔ ۲: هدر (۱۴۰۵/۰۶/۰۵)
- **بج گرید دقیقاً وقتی مخفی می‌شد که وجود داشت:** شرط `!isScored && <GradeBadge/>` یعنی سورسِ امتیازخورده — همان که گرید دارد — هیچ گریدی نشان نمی‌داد. شرط حذف شد و متغیر بی‌مصرف `isScored` هم پاک شد. تأیید: سورس ۸۶ حالا «Grade A» دارد، سورس بدون امتیاز «جدید».
- **حلقهٔ امتیاز برچسب‌دار شد.** عدد وسط حلقه برای ادمین امتیاز کل وزنی است و برای کاربرِ تک‌دپارتمانی امتیاز همان دپارتمان؛ بدون کپشن، دو نفر یک عدد را یک چیز می‌خواندند. حالا زیر حلقه «امتیاز کل» / «امتیاز کیفیت» / «بدون امتیاز» می‌آید. تأیید زنده: ادمین `86 · امتیاز کل`، کاربر qa `91 · امتیاز کیفیت` روی همان سورس.
- **تایپوگرافی فارسی:** فاصلهٔ قبل از دونقطه در هدر حذف شد (`تأمین‌کننده:`، `کشور:`)، و برچسب مبهم `Grade :` به «گرید SOP فروشنده:» تغییر کرد تا با گرید خودِ سورس اشتباه نشود.
- **نکتهٔ محیط:** برای کاربر qa چهار خطای ۴۰۳ در کنسول دیده می‌شود؛ با stash کردن تغییرات تأیید شد که **از قبل وجود داشته** و ربطی به این بسته ندارد (فراخوانی endpointهای ادمین‌ویژه؛ طبق قاعدهٔ ۱۴ باعث خروج کاربر نمی‌شود).



## دسته‌بندی‌ها — دور دوم: دو باگ و پنج بهسازی (۱۴۰۵/۰۶/۰۵)
- **باگ ۱ — نبود نشانهٔ فوکوس:** سربرگ گروه و ردیف‌های سورس `role="button"` + `tabIndex={0}` بودند بدون هیچ استایل فوکوس، و چون `div`اند حلقهٔ پیش‌فرض مرورگر هم به ارث نمی‌رسید. حالا `focus-visible:ring-2 ring-ring ring-inset`. تأیید با Tab واقعی: `rgb(37,99,235) 0 0 0 2px inset` و `:focus-visible` مچ می‌شود.
- **باگ ۲ — پیام گمراه‌کننده هنگام بارگذاری:** تا رسیدن داده، شرط «۰ گروه» فعال می‌شد و صفحه «نتیجه‌ای یافت نشد» نشان می‌داد. `isLoading={isSyncing && db.length === 0}` از `App` پاس داده می‌شود و سه اسکلت هم‌شکل کارت گروه رندر می‌شود؛ پیام خالی فقط بعد از رسیدن داده. تأیید با کش پاک + تأخیر ۷ ثانیه‌ای روی `/api/vendors`: `skeleton: true, saysNotFound: false`.
- **موبایل:** سه ستون گرید/امتیاز/ریسک `hidden sm:flex` بودند و روی گوشی هیچ‌کدام دیده نمی‌شد؛ یک ردیف فشرده با همان مقادیر زیر نام سورس اضافه شد (دادهٔ جدید نیست، همان دسکتاپ است).
- **تراکم:** گام عمودی کارت گروه از **۱۰۴px به ۷۱px** رسید (نام و CAS در یک خط، padding کمتر) — بدون حذف هیچ محتوایی.
- **هم‌ترازی:** سه عرض ثابت مستقل (`w-28/32`, `w-24/28`, `w-24/28`) با یک `grid-cols-[8rem_7rem_7rem]` مشترک جایگزین شد تا ستون‌ها بین ردیف‌ها هم‌تراز شوند (برای لیست سیاه تک‌ستونه).
- **حذف تکرار:** بج «نام ماده اولیه» و نام ماده از ردیف‌های داخل گروه حذف شد؛ نام تأمین‌کننده خط اول شد.
- **نوار چسبان:** دو ردیف (تیتر+خروجی / کارت جست‌وجو) در یک ردیف ادغام شد و کارت دور نوار ابزار حذف شد؛ ارتفاع از **~۲۰۰px به ۱۰۵px**. چیپ‌ها خط خودشان را دارند چون wrap می‌شوند.
- تست زنده: دسکتاپ/موبایل، بدون سرریز افقی، ۰ خطای کنسول، ۸۸ تست سبز.

## یکدست‌سازی بصری صفحهٔ دسته‌بندی‌ها (۱۴۰۵/۰۶/۰۵)
> تصمیم کاربر: **هیچ اطلاعات جدیدی اضافه نشود**؛ فقط یکدستی با بقیهٔ نرم‌افزار و خوانایی.

- **پالت:** ۲۴ رنگ هاردکد در پنل مقایسه (`#0071E3` آبیِ اپل، `#4096FF`، `#6E6E73`، `#424245`) به توکن‌های `primary`/`muted-foreground`/`foreground` تبدیل شد؛ به‌علاوهٔ نقطه‌رنگ گرید B در `MaterialGroup` و ۴ مورد باقی‌مانده در `ArchiveView`/`SupplierAuditView`. حالا هیچ نمای دسته‌بندی رنگ خارج از توکن ندارد.
- **باگ خوانایی دارک‌مود:** خط فرمول موتور با `text-[#424245]` رندر می‌شد، یعنی `rgb(66,66,69)` روی کارت تقریباً مشکی — عملاً نامرئی (کنتراست ~۱٫۲:۱). بعد از تبدیل به توکن: `rgb(248,250,252)`.
- **کف اندازهٔ قلم:** ۳۰ مورد `text-[10px]`/`text-[9px]` در دو فایل به `text-[11px]` رسید (فارسی در ۹–۱۰px عملاً خوانا نیست).
- **مقیاس گردی یکدست شد:** پنل مقایسه هم‌زمان از `rounded`/`sm`/`md`/`lg`/`xl`/`full` استفاده می‌کرد؛ حالا کارت `xl`، کنترل `lg`، چیپ `full`.
- **نوار ابزار:** دکمهٔ پاک‌کردن جستجو با آیکون ذره‌بین فقط **۴px** فاصله داشت (اندازه‌گیری شد) و مثل یک لکه دیده می‌شد؛ به انتهای دیگر فیلد منتقل شد (فاصله: ۱۹۸px) · CTA از «خروجی اکسل خرید خارجی (XLSX)» به «خروجی اکسل» · حذف چوخان تزئینی کنار برچسب مرتب‌سازی (خودِ select چوخان دارد) + اتصال `label htmlFor` · حذف زیرعنوان انگلیسی `FOREIGN PURCHASE` (همان کاری که در سایدبار شد) · چیپ‌های انگلیسیِ دستهٔ نمونه فارسی شد.
- **کاراکترهای متنی `⚠` و `★`** با آیکون lucide جایگزین شدند (در فونت‌ها و تم‌های مختلف یکسان رندر نمی‌شدند).
- **چیپ با شمارش صفر** دیگر کلیک‌پذیر نیست (`opacity-45 pointer-events-none`) — قبلاً «لیست سیاه: ۰» فیلتری بود که فقط می‌توانست فهرست خالی بدهد.
- **جدول QC روی موبایل:** `w-full` باعث می‌شد جدول فشرده شود و سرستون‌ها دوخطی شوند؛ با `min-w-[420px]` اسکرول به ظرف موجود سپرده شد.
- تست زنده: دارک/لایت/موبایل · بدون سرریز افقی · ۰ خطای کنسول · ۸۸ تست سبز.

## IRC روی سورس ذخیره می‌شود، نه روی ماده (۱۴۰۵/۰۶/۰۵)
> رفع باگی که هنگام تست بالا پیدا شد و قاعدهٔ ۳ را نقض می‌کرد.

- **باگ:** جدول `vendors` ستون `irc` نداشت؛ `saveVendorToDb` مقدار IRC را به `generateMaterialId(cas, irc, …)` می‌داد، پس ثبت هر سورس با IRC یک **ردیف مادهٔ تکراری** می‌ساخت (`mat_103_90_2_1228123456789012` کنار `M-1` برای همان «پاراستامول») و سورس به آن نسخهٔ تکراری وصل می‌شد — یعنی هم IRC عملاً روی ماده می‌نشست، هم مخزن مواد آلوده می‌شد.
- **مهاجرت `20260827134500_vendor_irc`:** ستون `vendors.irc` اضافه شد و مقدارها از مادهٔ متصل **backfill** شدند (فقط کدهای واقعی؛ `N/A`/`NA`/`-` رد می‌شوند).
- `saveVendorToDb` حالا به `v.materialId` (همان مادهٔ انتخاب‌شده از مخزن) وصل می‌کند و id مشتق فقط fallbackِ payloadهای قدیمی است — و IRC دیگر جزئی از هویت ماده نیست. رکورد ماده هم **فقط ساخته** می‌شود، هرگز از payload سورس بازنویسی نمی‌شود.
- `getVendorsList` مقدار را از خود سورس می‌خواند و برای رکوردهای قدیمی به IRC ماده fallback می‌کند.
- **گارد سمت سرور (قاعدهٔ ۱۴):** `ircViolation()` در `POST /api/vendors` و `PATCH /api/vendors/:id/profile` → **۴۲۲**. خالی و `N/A` مجازند و مقدارِ تغییرنکرده هم استثناست تا رکورد قدیمی غیرقابل‌ویرایش نشود.
- **باگ جانبی که همین‌جا رو شد:** upsert لینک `vendor_materials` روی id مصنوعی `link_<v>_<m>` انجام می‌شد، پس برای لینک‌هایی که id خودشان را دارند (seed/import) به‌جای update سعی می‌کرد ردیف دوم برای همان جفت بسازد و به unique constraint می‌خورد؛ حالا روی کلید یکتای `(vendorId, materialId)` upsert می‌شود. قبلاً پنهان بود چون materialId مشتق باعث می‌شد `deleteMany` اول لینک قبلی را پاک کند.
- تست زنده: مهاجرت روی دیتای موجود اجرا و backfill درست انجام شد (`V9 → 1228999988887777`، رکورد `N/A` خالی ماند) · `POST` با IRC پنج‌رقمی = **۴۲۲**، با ۱۶ رقم = ۲۰۰ و **هیچ مادهٔ تکراری ساخته نشد** (تعداد مواد ۲ ماند و سورس به `M-1` واقعی وصل شد) · `PATCH /profile` با `N/A` تکراری = ۲۰۰، با `777` = ۴۲۲، با ۱۶ رقم = ۲۰۰ و لینک قدیمی `L1` دست‌نخورده ماند · ثبت از UI: IRC روی صفحهٔ سورس نمایش داده شد · ۸۸ تست سبز.


## تقویم شمسی `ShamsiDatePicker` (۱۴۰۵/۰۶/۰۵)
- **قرارگیری با portal:** پاپ‌اور `absolute top-full` بود، پس هر ancestor با overflow آن را می‌برید (`overflow-x-auto` جدول رکوردهای آزمایشگاه در `VendorDetail:1508`، و پنل `overflow-hidden` در `FormModal:175`) و هرگز به بالا flip نمی‌کرد. حالا `createPortal` به `body` + موقعیت از `getBoundingClientRect()` تریگر، با flip خودکار و clamp افقی، و به‌روزرسانی روی scroll/resize. (همان دلیل قاعدهٔ ۸.)
- **توکن‌های تم:** کل پالت ایندیگوی هاردکد (`indigo-300/500/600/50/800`, `shadow-indigo-600/20`) به `primary`/`accent`/`ring`/`popover` تبدیل شد. `border-slate-250` **کلاس واقعی Tailwind نبود** ⇒ آن دو المان اصلاً بوردر نداشتند؛ در این فایل و ۲ مورد `VendorDetail` به `border-border` تغییر کرد.
- **کیبورد:** Escape می‌بندد و فوکوس را برمی‌گرداند؛ کلیدهای جهت/Home/End بین روزها حرکت می‌کنند (با عبور از مرز ماه)؛ هنگام باز شدن فوکوس روی روز انتخاب‌شده می‌نشیند.
- **منطق:** حلقهٔ «امروز» در گرید · «برو به امروز» از `jalaali.toJalaali` (به‌جای `Intl('fa-IR')` + تبدیل دستی ارقام) · فهرست سال نسبت به سال جاری (به‌جای ۱۳۸۰–۱۴۲۰ ثابت) · حذف عدد میلادی ۸px و خط راهنما (معادل در tooltip و زیر input هست) · sync ماه با `value` بیرونی.
- تست زنده: با فضای کافی به پایین باز می‌شود (y=368، پایین ۷۳۷ از ۹۰۰) · در viewport ۵۶۰px به بالای تریگر flip می‌کند (y=8) · Escape می‌بندد · ArrowLeft+ArrowDown از ۰۵ به ۱۳ می‌رود و Enter ثبت می‌کند.

## فرم ثبت سورس (۱۴۰۵/۰۶/۰۵)
- **اعتبارسنجی از `alert()` بومی به خطای درون‌خطی تبدیل شد.** قبلاً فرم خالی یک پنجرهٔ بومی مرورگر می‌داد که نه RTL می‌شد، نه تم می‌گرفت، و بعد از بستن هیچ نشانی از فیلد مشکل‌دار نمی‌ماند. حالا `role="alert"` زیر همان فیلد + اسکرول خودکار + فوکوس روی کنترل.
- **دو اسکرول‌بار تودرتو حذف شد:** بدنهٔ فرم `max-h-[80vh] overflow-y-auto` داشت (۹۲۱px محتوا در پنل ۷۶۰px، داخل صفحهٔ ۸۸۷px). این یک **صفحه** است نه مودال (قاعدهٔ ۸b) پس سقف حذف شد. اندازه‌گیری بعد: **یک** ناحیهٔ اسکرول.
- **قفل رنگ:** ۱۸ رنگ هاردکد `#0071E3`/`#0025D2` در این فرم و ۸ مورد در `MaterialSelector`/`PartnerSelector` با توکن `primary`/`ring` جایگزین شد. قبلاً دکمهٔ ثبت `rgb(37,99,235)` و لینک «ثبت تأمین‌کننده جدید» `rgb(0,113,227)` بودند. بعد از تغییر: هیچ نودی با آن آبی نمانده.
- **کنترست ورودی‌ها:** ورودی‌ها `bg-card` بودند یعنی دقیقاً هم‌رنگ کارت پشتشان (سفید روی سفید، و در دارک‌مود `rgb(30,41,59)` روی همان). به `bg-background` تغییر کرد.
- **دسترس‌پذیری:** `label[for]` از **۰** به ۳ رسید (فیلدهای واقعی؛ سلکتورهای سفارشی دکمه‌اند و برچسب متنی دارند).
- **پاکسازی:** em-dash ۱→۰ · حذف `(Material Master)`/`(Source Type)`/`(SOP Evaluation Summary)`/`(Contact Person)`/`(Email)` · حذف شماره‌گذاری تکراری («بخش اول:» کنار بج ۱) · گزینه‌های وضعیت نمونه فارسی شد · باکس «وضعیت و گرید اولیه» از شکل فیلد به یک جملهٔ راهنما تبدیل شد · ۶ `input readOnly` مشخصات شریک به متن ساده تبدیل شد (۶→۰).
- تست زنده: فرم خالی → خطای درون‌خطی و صفر دیالوگ بومی؛ انتخاب ماده و شریک → خطا پاک شد؛ ثبت → رکورد ساخته شد و به صفحهٔ همان سورس رفت. دارک/موبایل بدون خطای کنسول.
- **باقی‌مانده (خارج از دامنهٔ این بررسی):** `#0071E3` هنوز در ۸ فایل دیگر هست (ArchiveView، AuditTrailView، MaterialGroup، SupplierAuditView، ScoreBar، ScoringGuide، PrintableForms، MaterialsComparisonSection).

## سایدبار، عنوان و لوگو (۱۴۰۵/۰۶/۰۵)
- **لوگو در دارک‌مود:** همان باگ صفحهٔ ورود؛ نشان سرمه‌ای روی کارت تیره محو می‌شد. پلیت روشن `dark:` گرفت.
- **سلسله‌مراتب عنوان برعکس بود:** تیتر انگلیسی ۱۴px در **۳ خط**، و نام فارسی زیرش با مونو ۱۰px. حالا «سامانهٔ ارزیابی تامین‌کنندگان» تیتر است و `VLSE` زیرنویس کوچک. ارتفاع بلوک برند ۱۰۸px → **۸۱px**.
- **۱۲ زیرنویس انگلیسی منو حذف شد** (`FOREIGN PURCHASE`, `MATERIALS MASTER`, `AUDIT TRAIL CENTER`, …) و پراپ `sub` از `AppSidebarButton` برداشته شد. عنوان‌های گروه هم دیگر جفت فارسی/انگلیسی نیستند.
- **⚠️ پیش‌بینی اشتباه و اصلاحش:** فکر می‌کردم حذف زیرنویس‌ها ارتفاع منو را ۸۸۰→۷۳۶ می‌آورد. اندازه‌گیری بعد از تغییر: **همان ۸۸۰** — چون ارتفاع هر ردیف را آیکن ۳۲px + padding تعیین می‌کرد نه متن. سپس آیکن ۳۲→۲۸، `py-2.5`→`py-2`، فاصلهٔ آیتم‌ها و عنوان گروه‌ها کم شد → ردیف ۵۲→۴۴px و مجموع **۸۲۰px**. دسکتاپ (۸۲۰ در دسترس): **دیگر اسکرول نمی‌خورد**. موبایل ۳۹۰×۸۴۴: ۷۲۴ در برابر ۷۱۴ — ۱۰px اسکرول باقی می‌ماند.
- **سه انیمیشن دائمی حذف شد** (بج زنگوله، دو بج هشدار سایدبار)؛ هیچ‌کدام `prefers-reduced-motion` را رعایت نمی‌کردند و تغییر حالتی نشان نمی‌دادند.
- **چیپ «سیستم فعال»** که همیشه سبز بود، جایش را چیپی گرفت که **فقط در حالت لوکال** ظاهر می‌شود («حالت لوکال — بدون پایگاه‌داده»). وضعیتی که عوض نمی‌شود وضعیت نیست.
- «پشتیبان‌گیری (JSON)» → «پشتیبان‌گیری» (نام فرمت در tooltip باقی است).
- تست زنده: `animate-pulse` = ۰، زیرنویس انگلیسی در منو = ۰، دارک/روشن/موبایل بدون خطای کنسول.

## بازطراحی داشبورد اصلی (۱۴۰۵/۰۶/۰۵)
- **باگ داده:** ردیف KPI روی `db.length` حساب می‌شد ولی فقط ۴ گرید را نشان می‌داد، پس درصدها به ۶۶٪ می‌رسید و **سورس‌های بدون گرید بی‌صدا حذف می‌شدند** — در حالی که دونات کنارش همان برش را نشان می‌داد. کارت «بدون گرید» اضافه شد و حالا شش کارت کل جمعیت را پوشش می‌دهند.
- **باگ جمعیت:** «کل تامین‌کنندگان» رکوردهای نمونه را می‌شمرد ولی «کارهای معوق» عمداً حذفشان می‌کرد. حالا هر دو از `sourceVendors` مشترک می‌خوانند و زیرنویس کارت تعداد نمونهٔ کنارگذاشته را می‌گوید.
- **نوار پیشرفت زیر KPIها حذف شد:** هر عدد را نسبت به کلی می‌سنجید که خودش درصدش بود، و رنگ همه `bg-primary` بود؛ کارت «کل» نواری همیشه‌پُر از خودش داشت. بج درصد کافی است.
- **خانهٔ یتیم موبایل:** ۵ کارت در گرید ۲ستونه یک خانهٔ خالی می‌ساخت؛ با ۶ کارت دقیقاً ۳ ردیف کامل می‌شود.
- **فید فعالیت:** رویدادهای ورود/خروج فیلتر شدند (`module !== 'احراز هویت'`)؛ قبلاً هر ۵ ردیف «ورود موفقیت‌آمیز admin» بود و هیچ تغییر دادهٔ واقعی دیده نمی‌شد. حالا limit=40 گرفته و ۵ تغییر واقعی نشان می‌دهد.
- **اولویت صفحه:** «کارهای معوق» از جایگاه دوم/نصف‌عرض به **بالای صفحه و تمام‌عرض** آمد؛ Hero تکراری (عنوانی که در سایدبار و تب مرورگر هم بود) به یک سطر جمع شد.
- **یکنواختی چیدمان شکسته شد:** فید فعالیت از کارت به فهرست تمام‌عرض تبدیل شد؛ نرخ آزمایشگاه کنار دونات نشست.
- **واژگان:** شش برچسب انگلیسی تزئینی حذف شد ((Grade Mix)، (Action Center)، (Recent Activity)، (Lab Pass)، (CATEGORIES)، ابروی VLSE)؛ «Pass/Reject» → «قبول/مردود»؛ «Approved» در کارت نمونه → «تایید شده»؛ دسته‌های خالی کم‌رنگ‌تر شدند.
- تست زنده با ۹ سورس/۷ آزمون: ۶ کارت KPI (بدون یتیم موبایل)، ۰ ردیف لاگین در فید، ۰ پرانتز انگلیسی در HomeView، مجموع کارت‌ها = کل جمعیت، دارک‌مود و موبایل سالم، بدون خطای کنسول.

## صفحهٔ ورود + رفع ریشه‌ای دارک‌مود (۱۴۰۵/۰۶/۰۵)
- **باگ سراسری:** `@custom-variant dark` در `index.css` تعریف نشده بود، پس Tailwind v4 به‌طور پیش‌فرض `dark:` را به **`prefers-color-scheme` سیستم‌عامل** وصل می‌کرد، نه کلاس `.dark` که سوییچر تم می‌گذارد. توکن‌ها (CSS ساده زیر `.dark`) درست عوض می‌شدند و برای همین به چشم نیامده بود، ولی **هر `dark:bg-…`/`dark:text-…` دست‌نویس** در کل کدبیس برای کاربری با سیستم‌عامل روشن بی‌اثر بود. اثبات: `dark:bg-white` با OS روشن `rgba(0,0,0,0)` و با OS تیره `rgb(255,255,255)`. با یک خط `@custom-variant dark (&:where(.dark, .dark *))` رفع شد.
- **صفحهٔ ورود (`LoginView.tsx`) بازنویسی شد:** رنگ‌های hardcode → توکن (قاعدهٔ ۷)؛ `dir="ltr"` انگلیسی → RTL و فارسی؛ `min-h-screen` → `min-h-[100dvh]`؛ کنترست متن راهنما اصلاح شد.
- افزوده: دکمهٔ نمایش/پنهان رمز، `autoFocus` روی نام کاربری، اسپینر و دکمهٔ غیرفعال حین بررسی، هشدار Caps Lock، و راهنمای «بازنشانی رمز فقط توسط مدیر سیستم».
- لوگو ۱۱۲px → ۸۰px؛ در دارک‌مود روی پلیت سفید می‌نشیند (لوگوی سرمه‌ای روی کارت تیره تقریباً نامرئی بود).
- سه پیام خطای انگلیسیِ `/api/auth/login` در `server.ts` فارسی شد (بقیهٔ پیام‌های همان مسیر از قبل فارسی بودند).
- تست زنده: دارک/روشن هر دو درست؛ autofocus ✓؛ toggle رمز `password`→`text` ✓؛ حین ارسال دکمه غیرفعال + اسپینر + `aria-busy` ✓؛ خطای فارسی ✓؛ هشدار Caps Lock با رویداد واقعی ✓؛ داشبورد و ماژول کاربران در دارک‌مود بدون خطا و با تینت درست.

## هم‌سان‌سازی فرم ثبت شریک داخل فرم سورس (۱۴۰۵/۰۶/۰۴)
- فرم سریعِ «ثبت تأمین‌کننده جدید» مجموعه و ترتیب فیلدهای خودش را داشت (از جمله «نام انگلیسی» که فرم اصلی اصلاً نمی‌گیرد) و همان رکورد `business_partners` را می‌نوشت.
- حالا **دقیقاً** فیلدها، ترتیب، برچسب‌ها، placeholderها و نوع کنترل فرم ماژول شرکای تجاری را دارد: نوع موجودیت → نام → کشور → شهر → آدرس کامل → نام رابط → شماره تماس → ایمیل رسمی → وبسایت → وضعیت (رادیو Active/Inactive).
- `nameEn` حذف شد (فرم اصلی ندارد)؛ «لیست سیاه» از انتخاب‌های وضعیت حذف شد چون در ماژول شرکا اقدام جداگانه با دلیل الزامی است.
- **باگ:** ۵ مدرک SOP به‌صورت پیش‌فرض روی `Approved` بودند، یعنی هر فروشندهٔ ساخته‌شده از اینجا بدون هیچ ارزیابی‌ای با ۱۰۰/۱۰۰ و گرید A متولد می‌شد. حالا مثل فرم اصلی «ارزیابی نشده» شروع می‌شود (امتیاز ۰).
- تست زنده: فهرست برچسب‌های دو فرم بایت‌به‌بایت یکسان شد (تولیدکننده و فروشنده)، ساخت شریک از داخل فرم سورس کار می‌کند و بلافاصله در فیلد انتخاب می‌شود، بدون خطای کنسول.

## برچسب‌های فارسی در «سورس‌های متصل به این شریک» (۱۴۰۵/۰۶/۰۴)
- ستون «دسته» کلید خام دیتابیس را نشان می‌داد (`foreign`, `packaging`, …). حالا از `categoryLabels` می‌خواند → «خرید خارجی»، «اقلام بسته بندی» و بقیه.
- ستون «گرید / وضعیت» یک کپی محلی از بج بود که برای سورس بدون گرید به وضعیت خام انگلیسی (`new`, `approved`) برمی‌گشت؛ با `GradeBadge` مشترک جایگزین شد («جدید»، «در حال ارزیابی»، «Grade A» …). تابع محلی `gradeBadgeCls` حذف شد.
- بررسی شد که این تنها جای برنامه بود که `category` خام رندر می‌شد.
- تست زنده با دو سورس متصل به BASF: «خرید خارجی + جدید» و «اقلام بسته بندی + Grade A»، بدون خطای کنسول.

## رفع باگ مودال بلند (۱۴۰۵/۰۶/۰۴)
- **علامت:** در «سطح دسترسی» ماژول کاربران، صفحه اسکرول نمی‌شد و دکمهٔ «ذخیره سطح دسترسی» دیده نمی‌شد.
- **ریشه:** `min-height:auto` روی flex item. wrapper کالر ۹۷۵px بود در حالی که پنل ۷۳۶px (=۹۲vh)؛ `overflow-hidden` پنل فوتر را می‌برید و بدنهٔ `overflow-y-auto` هم اسکرول نمی‌کرد چون ارتفاعش از پنل بیشتر بود.
- **رفع مرکزی در `FormModal`:** `[&>*]:min-h-0` روی پنل → سرریز به همان ناحیهٔ اسکرول داخلی می‌رسد. همهٔ دیالوگ‌های برنامه با یک تغییر درست شدند.
- تست زنده: قبل → wrapper 975px، بدنه بدون اسکرول، دکمه در y=991 (بیرون از viewport 800). بعد → wrapper 734px، بدنه ۸۳۸ در ۵۹۷ اسکرول می‌کند، دکمه در y=750 و قابل کلیک. تغییر مجوز ذخیره و پس از بازگشایی **persist** شد. دیالوگ‌های `md` و `lg` بدون تغییر رفتار.

## آخرین تغییر — فیلترهای Audit (۱۴۰۵/۰۶/۰۴)
- **ریشهٔ مشکل:** فرم فیلتر فهرست ماژول‌های خودش را دستی داشت و از ۷ گزینه فقط «مدیریت کاربران» با مقدار واقعی `audit_log.module` یکی بود؛ بقیه هیچ رکوردی برنمی‌گرداندند. همچنین `severity=Info` در برابر مقدار ذخیره‌شدهٔ `Information`، فیلتر کاربر با `userName` در برابر شرط `userId` سرور، و «گروه رویداد» که مسیر PostgreSQL اصلاً اعمالش نمی‌کرد (`eventType` ستون نیست).
- **راه‌حل:** `src/utils/auditTaxonomy.ts` — واژگان مشترک سرور/کلاینت (ماژول‌ها، عملیات، گروه‌ها، هم‌ارزی Info/Information).
- گروه رویداد = مجموعه‌ای از ماژول‌ها، با `module IN (…)` سمت سرور (و وقتی ماژول مشخص انتخاب شده غیرفعال می‌شود).
- بازهٔ تاریخ: `ShamsiDatePicker` به‌جای متن آزاد + تبدیل شمسی→ISO (پایانِ بازه شاملِ کل روز). تاریخ نامعتبر دیگر ۵۰۰ نمی‌دهد.
- جست‌وجوی متنی حالا بازهٔ تاریخ را هم اعمال می‌کند (قبلاً بی‌صدا نادیده می‌گرفت).
- کاربران/ماژول‌های دموی MOCK از dropdownها حذف شدند؛ ستون ماژول برچسب فارسی می‌گیرد.
- ستون «تاریخ و ساعت» دو خطی شد. `ShamsiDatePicker` خالی روی «امروز» باز می‌شود (قبلاً فروردین ۱۴۰۳).
- **حذف رکوردهای دموی Audit:** ۵ رکورد ساختگی `MOCK_AUDIT_LOGS` (از جمله «رد صلاحیت بچ ۹۰۴۲ به علت OOS») که وقتی دیتابیس خالی بود نمایش داده می‌شدند حذف شدند — در Audit Trail برای GMP، رکورد جعلی از رکورد واقعی قابل تشخیص نبود. آمار خالی حالا ۰ نشان می‌دهد و پیام «هنوز هیچ رویدادی ثبت نشده است» جای فهرست جعلی را می‌گیرد (متن حالت «فیلتر بی‌نتیجه» جداست).
- تست زنده روی Postgres: همهٔ فیلترها (کاربر/ماژول/گروه/عملیات/سطح/بازهٔ تاریخ/جست‌وجو+بازه) مقدار درست برگرداندند؛ UI: ماژول ۱ ردیف، گروه احراز هویت ۸، Information ۸، بدون خطای کنسول.

## آنچه انجام شده (به ترتیب)
### زیرساخت دیتابیس (فاز ۲)
1. ✅ نرمال‌سازی schema به ۱۲ جدول per-module + enum + index + FK (`6b7f0b6`)
2. ✅ اتصال endpointها به Postgres + حذف کامل fallback فایل JSON + مهاجرت users به DB (`235ef55`)
3. ✅ استفاده از جداول نرمال برای risk/analysis/activity (به‌جای ستون Text) (`ce1f90e`)
4. ✅ ادغام دو مدل audit موازی به `AuditLog` واحد (`022a4db`)
5. ✅ انتقال Business Partners به backend (`3c48fb9`) + رفع باگ حلقهٔ reload (`1af6ea3`)
6. ✅ پوشش کامل audit سمت‌سرور برای همهٔ ماژول‌ها (`937b5b0`)
7. ✅ Polish حالت‌های loading/error/toast (theme-aware) (`476af71`)

### ماژول سورس‌ها
8. ✅ مرتب‌سازی + فیلتر کلیک‌پذیر + هشدار IRC در لیست (`5fc05b7`)
9. ✅ تاریخچه و روند نمرات (از audit) + guard صفحه‌بندی (`422939f`)
10. ✅ ویزارد ارزیابی هدایت‌شده: امتیازدهی → ریسک → آزمایشگاه (`3d19c92`, `93625a0`)
10b. ✅ کاهش friction ناوبری: بازگشت از جزئیات سورس، همان مادهٔ باز‌شده را بازیابی می‌کند (A) هایلایت گذرای بصری روی مادهٔ بازگشتی، (B) ذخیرهٔ `expandedMaterial` در `viewHistory` (پایدار در reload)، (C) رفتار آکاردئون واقعی (فقط یک ماده هم‌زمان باز).

### ناوبری سراسری (بازطراحی — گروه‌های الف/ب/ج)
10c. ✅ **الف — پایه:**
  - `scrollContainerRef` + reset اسکرول روی هر تغییر view (با double-rAF، چون بعضی صفحات بعد از mount خودشان اسکرول را جابه‌جا می‌کردند).
  - **Breadcrumb زنده** در هدر از `getViewStateLabel` (که قبلاً کد مرده بود) + `goToHistoryIndex` برای پرش مستقیم به هر سطح. برچسب `business-partners` که جا افتاده بود اضافه شد.
  - سایدبار در صفحهٔ جزئیات دیگر active state را از دست نمی‌دهد (شرط `&& !selectedVendor` حذف شد).
  - **رفع باگ:** کلیک روی سایدبار وقتی یک سورس باز بود، ورودی تکراری push می‌کرد و «برگشت» دوباره وارد همان سورس می‌شد. حالا ناوبری سطح‌بالا مثل «تعویض تب» است: اگر مقصد در stack باشد به آن unwind می‌شود.
  - سقف `MAX_VIEW_HISTORY = 25` + ذخیرهٔ فقط snapshot سبک از vendor در localStorage (id/name/material) — جلوگیری از رشد بی‌حد و پر شدن quota.
10d. ✅ **ب — Back سخت‌افزاری/مرورگر:** الگوی sentinel روی `history.pushState` + شنوندهٔ `popstate`؛ تا وقتی جایی برای برگشت هست event مصرف و sentinel دوباره arm می‌شود، در ریشه اجازهٔ خروج داده می‌شود. (هوک‌ها عمداً بالای early-return‌های لاگین قرار دارند تا ترتیب هوک پایدار بماند؛ `goBackRef` برای شکستن وابستگی ترتیبی.)
10e. ✅ **ج — محافظ داده:** `registerNavGuard` در `VendorDetail` هنگام باز بودن فرم ویرایش؛ هر ناوبری پشت دیالوگ تأیید (`ماندن در صفحه` / `خروج بدون ذخیره`) می‌رود و `beforeunload` هم بستن تب را پوشش می‌دهد.
- **تست زنده:** با Playwright روی Postgres محلی تأیید شد — breadcrumb، Back مرورگر (depth ۳→۲)، حفظ باز بودن + هایلایت ماده هنگام بازگشت، `scrollTop=0` بعد از ناوبری، bounded ماندن depth بعد از ۴ بار جابه‌جایی تب، و هر سه مسیر دیالوگ محافظ.
10f. ✅ **د — مسیریابی hash و دیپ‌لینک** (`src/utils/navRoutes.ts` + ۹ تست واحد در `tests/navRoutes.test.ts`):
  - URL منبع حقیقت شد: `#/category/foreign/vendor/V1`، `#/category/foreign?m=Paracetamol` و … . لینک هر سورس **قابل ارسال به همکار** است.
  - الگوی sentinel حذف و جای آن ورودی‌های واقعی تاریخچهٔ مرورگر نشست ⇒ **دکمهٔ Forward هم کار می‌کند** (که با sentinel ممکن نبود) و منوی تاریخچهٔ مرورگر معنادار شد.
  - refresh دقیقاً همان‌جا برمی‌گردد؛ هر تب موقعیت مستقل خودش را دارد (تداخل کلید مشترک `app_viewHistory` بین تب‌ها رفع شد).
  - دیپ‌لینک فقط `id` دارد ⇒ حالت loading تا رسیدن `db`، و پیام «سورس یافت نشد» برای لینک به رکورد حذف‌شده. hash نامعتبر → خانه.
  - محافظ تغییرات ذخیره‌نشده با Back **واقعی** مرورگر هم یکپارچه شد (URL تا زمان تصمیم کاربر سر جایش می‌ماند).
  - **تست زنده:** encode/decode همهٔ viewها، Back/Forward، reload، دیپ‌لینک در مرورگر تازه (بدون localStorage) با breadcrumb درست، لینک نامعتبر، و hash بی‌معنی — همه سبز.

10g. ✅ **رفع باگ «مردودی که پاک نمی‌شود»** (`src/utils/vendorState.ts` + ۱۳ تست در `tests/vendorState.test.ts`):
  - **علت ۱ — قفل یک‌طرفهٔ `grade`:** با ثبت نتیجهٔ Reject، `normalizeAndCleanVendor` هم `status` و هم `grade` را `'rejected'` مهر می‌زد؛ با حذف آن نتیجه فقط `status` برمی‌گشت و `grade` قفل می‌ماند. چون همهٔ شمارنده‌ها `grade==='rejected' || status==='rejected'` را می‌خواندند، دونات و کارت لیست سیاه یک مردودی نشان می‌دادند. برای سورس‌ها بدتر بود: `grade` کهنه، `status` را هم به عقب می‌کشید.
  - **علت ۲ (کشف‌شده حین تست) — رقابت PATCHهای موازی:** `handleUpdateVendor` شش endpoint را هم‌زمان صدا می‌زد و هرکدام کل vendor را read-modify-write می‌کند؛ `/profile` نسخهٔ کهنهٔ `analysisRecords` را برمی‌گرداند و **نتیجهٔ حذف‌شده بعد از reload زنده می‌شد**. با صف `syncQueue` ترتیبی شد.
  - حالا «مردود بودن» از واقعیت‌ها **محاسبه** می‌شود (`isVendorRejected`) و `grade` فقط خروجی است. یک predicate مشترک در ۱۲ نقطه (دونات، کارت‌ها، بج سایدبار، فیلترها، آرشیو، اکسل) جایگزین شرط‌های دستی شد.
  - دلایلِ «مردود در آزمون QC» بازتاب رکوردها هستند و به‌تنهایی وضعیت را نگه نمی‌دارند ⇒ دادهٔ قدیمیِ قفل‌شده خودترمیم می‌شود.
  - **تست زنده:** ثبت Reject → کارت‌ها ۱ مردودی · حذف نتیجه → کارت‌ها ۰ · **reload → همچنان ۰** (DB تأیید شد).

10h. ✅ **بررسی ماژول شرکای تجاری (همان کلاس باگ)** — نتیجه: **باگ سورس‌ها را ندارد.**
  - `computeSupplierEvaluation` یک مشتق‌گیری خالص از مدارک است و در `handleSubmit` روی **هر ذخیره** دوباره محاسبه می‌شود؛ فرم ویرایش هم `evalDocs` را از `partner.evaluation.documents` بارگذاری می‌کند (بررسی شد: پاسخ API مدارک و وضعیت‌ها را کامل دارد و فقط blob فایل lazy است) ⇒ مسیرِ «مدرک عوض شود ولی گرید کهنه بماند» وجود ندارد.
  - **سخت‌سازی انجام شد:** `reconcileSupplierEvaluation` روی load (هم API و هم cache) اجرا می‌شود تا اگر رکوردی از نسخهٔ قدیمی یا ویرایش مستقیم DB با مدارک خودش ناسازگار باشد، خودترمیم شود. `updatedAt/updatedBy` عمداً حفظ می‌شود تا محاسبهٔ مجدد شبیه «ارزیابی تازه» به نظر نرسد (نکتهٔ GxP).
  - ۷ تست جدید در `tests/sopEvaluation.test.ts`.

10i. ✅ **تعیین تکلیف رابریک SOP (تصمیم کاربر: گزینهٔ ۲ — رابریک فعلی معتبر است):**
  - رابریک رسمی: مرزهای **۸۰ / ۶۰ / ۴۰ / ۳۰** با گریدهای `A, B, C, Pending Review, Blacklist`. گرید `D` وجود ندارد.
  - تست `businessRules.test.ts` که سال‌ها fail بود و رابریک متفاوتی (۹۰/۷۵/۶۰ با A/B/C/D) را ادعا می‌کرد، به رابریک واقعی اصلاح شد + تست مرز `Not Evaluated` اضافه شد ⇒ **کل سوئیت حالا سبز است (۳۹ pass / ۰ fail).**
  - واژگان مردهٔ `'D'` حذف شد از: `SOPGrade` (types)، `gradeApprovalLabel`، `getGradeBadgeClass`، فیلتر آمار، و `sopResultLabel` در اکسل. همچنین statusهای دست‌نیافتنی `Conditional Approval` و `Rejected` از `SOPSupplierStatus` پاک شدند.
  - **حذف رابریک تکراری:** `computeNewSupplierEval` در `App.tsx` نسخهٔ دومی از قوانین نمره‌دهی داشت (کپی دستی مرزها و امتیاز مدارک)؛ حالا از `computeSupplierEvaluation` و `SOP_DOCUMENTS_DEF` مشترک استفاده می‌کند تا دو نسخه از هم فاصله نگیرند.
  - ⚠️ توجه: `getRankParams` در `App.tsx`/`PrintableForms.tsx` هم گرید `D` دارد ولی **مربوط به مقیاس گرید سورس‌هاست** (A: ۸۰-۱۰۰ … D: ۰-۳۹)، نه SOP — عمداً دست‌نخورده ماند.
  - تست زنده: لیست شرکا با گریدهای A/B و برچسب‌های Approved / Permit Approval و امتیازهای ۹۰، ۸۰، ۷۰ درست رندر شد؛ هیچ «Grade D» سرگردانی در صفحه نیست.

10j. ✅ **یکدست‌سازی باز و بسته شدن فرم‌ها (پیشنهاد ترکیبی، تأییدشده)** — سه کامیت:
  - **`src/components/FormModal.tsx`** پوستهٔ مشترک همهٔ دیالوگ‌ها: portal، backdrop، Esc، قفل اسکرول، focus trap و بازگرداندن focus، و **انیمیشن متقارن ورود/خروج** با `AnimatePresence`. سه اندازه `sm/md/lg` جای ۹ عرض دلخواه. عمداً «پوسته» است نه فرم قالبی، پس مهاجرت = حذف boilerplate.
  - **همهٔ ۱۵ مودال** منتقل شدند (مواد، شرکا، تأییدیه‌های حذف/لیست‌سیاه، تغییر رمز، انتخابگر ماده، دیالوگ تغییرات ذخیره‌نشده). تنها overlay دستی باقی‌مانده اسکریم سایدبار موبایل است (که دیالوگ نیست). `design-system/Dialog.tsx` و `Drawer.tsx` بلااستفاده بودند و حذف شدند.
  - **فرم سورس صفحه شد** با مسیر اختصاصی `#/category/<cat>/new` و `#/category/<cat>/vendor/<id>/edit` ⇒ Back، breadcrumb، لینک قابل اشتراک و دیپ‌لینک رایگان. محافظ تغییرات ذخیره‌نشده به `VendorForm` منتقل شد و حالا **dirty واقعی** را می‌سنجد نه صرف باز بودن فرم. مودال «ثبت تأمین‌کننده» دیگر مودالِ داخل مودال نیست.
  - **بهبود رفتاری:** تأییدیه‌های مخرب با کلیک روی backdrop بسته نمی‌شوند (دکمهٔ صریح لازم است) ولی Esc لغو می‌کند — قبلاً هیچ‌کدام Esc نداشتند.

  **چهار باگ که حین همین کار پیدا و رفع شد:**
  1. children یک `FormModal` حتی وقتی بسته است ارزیابی می‌شوند (برخلاف portal شرطی قبلی) ⇒ مودال جزئیات با خواندن `selectedMaterial.id` صفحه را crash می‌کرد. گارد شد.
  2. کالر معمولاً رکورد را هم‌زمان با بستن پاک می‌کند ⇒ پنل خالی fade out می‌شد. `FormModal` آخرین children را تا پایان انیمیشن نگه می‌دارد.
  3. `handleSelectVendor` هنگام push ورودی قبلی را spread می‌کرد ⇒ رکورد تازه‌ساخته `formMode: 'create'` را ارث می‌برد و دوباره **فرم** رندر می‌شد نه رکورد. حالا `formMode` پاک می‌شود و ورودی فرم **جایگزین** می‌شود.
  4. **`getVendorsList` هرگز `materialId` را برنمی‌گرداند** ⇒ ویرایش هر سورس موجود با پیام «ماده را انتخاب کنید» رد می‌شد. یعنی **ویرایش سورس عملاً کار نمی‌کرد** (باگ از پیش موجود). رفع شد.

  **تست:** ۴۴ تست واحد سبز (۵ تست جدید مسیر فرم). تست زنده: هر دو فرم مودال (باز/قفل اسکرول/Esc/backdrop/بازگشت focus)، تأییدیهٔ مخرب، صفحهٔ فرم (URL، Back، دیپ‌لینک در مرورگر تازه، breadcrumb چهارسطحی، محافظ dirty با هر دو پاسخ)، ثبت سورس تا دیتابیس، و ویرایش سورس تا دیتابیس.

### ماژول مواد
11. ✅ persist فیلدهای غنی مواد به Postgres (بدون IRC) (`407eef5`, `a1c4b8f`)

### ماژول شرکای تجاری
12. ✅ مدل تخت: تولیدکننده/فروشنده مستقل (حذف وابستگی) (`fb739bb`)
13. ✅ تاریخچه و روند ارزیابی SOP فروشنده (از audit) (`d6e5b96`)
14. ✅ lazy-load فایل‌های SOP (حذف base64 از لیست) (`ca830af`)
15. ✅ گردش‌کار Blacklist با دلیل الزامی + گارد انتخاب (`3dd8e78`)
16. ✅ یکدست‌سازی theme / dark mode (`23b3980`)

### معماری فرانت — شکستن `App.tsx`
17. ✅ **`src/App.tsx`: ۷۹۵۶ → ۱۶۶۸ خط.** ~۷۹۰ خط کد مرده حذف شد (`UnusedFallback*`، استاب‌های `LegacyPrintable*`) و ۱۴ ماژول استخراج شد:
    - `components/views/`: `HomeView`, `CategoryView`, `MaterialGroup`, `MaterialsComparisonSection`, `ArchiveView`, `SupplierAuditView`
    - `components/vendor/`: `VendorDetail`, `VendorForm`, `EvaluationForm`, `RiskAssessmentForm` (+`RiskHeatmap`)
    - `constants/`: `categories.ts`, `evaluationLayout.ts`, `categoryCardStyles.ts` · `utils/scoreUtils.ts`
    - **کد فقط جابه‌جا شد، بازنویسی نشد** — هیچ تغییر رفتاری. props (به‌ویژه `registerNavGuard`) دست‌نخورده.
    - خودِ `App()` عمداً یکپارچه ماند (state ناوبری + `renderContent`) — شکستنش قواعد ترتیب هوک‌ها را لمس می‌کند و مرحلهٔ جداست.
    - تأیید: `tsc` ✓ · `vite build` ✓ · ۴۴ تست ✓ · تست زندهٔ مرورگری روی هر ۹ نمای جابه‌جاشده با **۰ خطای کنسول** (داشبورد، لیست دسته، باز شدن ماده، جزئیات سورس، فرم ارزیابی، فرم ریسک، صفحهٔ فرم سورس + round-trip ذخیره، آرشیو، Supplier 360).

### رفع باگ نمایش دوگانهٔ تولیدکننده/فروشنده
18. ✅ **صفحهٔ جزئیات سورس یک شریک را دو بار نشان می‌داد.** `VendorDetail` جداگانه یک «تولیدکننده» و یک «فروشنده» resolve می‌کرد؛ برای سورسِ فروشنده‌ای هر دو به **یک رکورد واحد** می‌رسیدند و آدرس/تلفن/ایمیل تکرار می‌شد. تیتر صفحه هم برچسب «تولید کننده :» را hardcode کرده بود.
    - helper مشترک `src/utils/vendorPartner.ts` → `resolveVendorPartner` (+ `cleanCountry`, `formatLocation`, `formatContactLine`). **نقش از `partner.type` می‌آید، نه از اینکه id در کدام فیلد نشسته** — رکوردهای قدیمی می‌توانند id یک Supplier را در `manufacturerId` داشته باشند.
    - `VendorDetail`: تیتر با برچسب پویا؛ کارت تماس از دو باکس به **یک** باکس. کارت سبز «خرید بی‌واسطه» فقط وقتی شریک واقعاً تولیدکننده است.
    - `excelExport`: چهار ستون `تولید کننده | آدرس | فروشنده | آدرس` → سه ستون `تأمین‌کننده | نوع تأمین‌کننده | آدرس و اطلاعات تماس` (عرض ستون‌ها هم اصلاح شد).
    - `PrintableForms`: قالب ثابت فرم GxP حفظ شد، ولی خانه‌ها درست پر می‌شوند — نام در خانهٔ نقشِ خودش، خانهٔ دیگر «ثبت‌نشده» یا «خرید بی‌واسطه». دیگر یک نام در دو خانه تکرار نمی‌شود.
    - `MaterialGroup`: `getPartnerDetails` محلی حذف و به helper مشترک وصل شد.
    - **هیچ تغییری در schema، migration یا endpoint نبود** — این صرفاً باگ نمایش بود؛ همهٔ اطلاعات تماس از قبل روی رکورد `BusinessPartner` است.
    - تأیید: `tsc` ✓ · `build` ✓ · **۵۴ تست** (۱۰ تست جدید در `tests/vendorPartner.test.ts`) ✓ · تست زندهٔ مرورگری روی هر سه حالت (سورسِ فروشنده‌ای، تولیدکننده‌ای، بدون شریک) در جزئیات + لیست دسته + خروجی اکسل + فرم چاپی، با **۰ خطای کنسول**.

### رفع باگ «نمی‌شود در فرم‌ها تایپ کرد»
19. ✅ **نشانگر تایپ با هر کاراکتر از فیلد می‌پرید** — در فرم ثبت ماده، فرم شرکای تجاری و مودال «ثبت تأمین‌کننده جدید» داخل فرم سورس. **رگرسیونِ کار یکدست‌سازی فرم‌ها بود** (ردیف ۱۷).
    - علت: افکت فوکوس/Esc در `FormModal` وابستگی `[open, onClose]` داشت، ولی هر ۱۱ کالر `onClose` را arrow inline می‌دهند و state فیلدها در همان والد است. پس هر کاراکتر → رندر والد → identity تازهٔ `onClose` → cleanup افکت → `restoreFocusRef.current?.focus?.()` فوکوس را بیرون می‌کشید و ۴۰ms بعد `panelRef.focus()` آن را روی پنل می‌گذاشت.
    - اصلاح در **یک نقطه**: `onClose` در `onCloseRef` نگه داشته می‌شود و deps افکت `[open]` شد ⇒ افکت یک بار روی باز شدن و یک بار روی بسته شدن اجرا می‌شود (همان «capture on open, restore on close» که از ابتدا هدف بود). هیچ کالری لازم نشد `useCallback` بگیرد.
    - سخت‌سازی: بازگرداندن فوکوس فقط وقتی که فوکوس هنوز داخل پنل (یا روی `body`) است — مودال فوکوسِ بیرون را نمی‌دزدد.
    - `PartnerSelector`: تایم‌اوت ۵۰ms فوکوس جستجو حالا در cleanup پاک می‌شود.
    - تأیید: `tsc` ✓ · `build` ✓ · ۵۴ تست ✓ · تست زندهٔ مرورگری با **کیبورد واقعی** (`type()` نه `fill()`) روی هر سه فرم، با **کنترل منفی**: revert موقت `FormModal.tsx` + ری‌استارت سرور ⇒ هر سه FAIL با متن به‌هم‌ریخته (`پاراستامول تست` → `مولتستپاراستا`، `activeElement=DIV`). عدم رگرسیون Esc/backdrop/بازگشت فوکوس/قفل اسکرول هم سبز. ۰ خطای کنسول.
    - ⚠️ **تلهٔ تکرارشونده:** اولین کنترل منفی به‌غلط PASS داد چون Vite ماژول کهنه سرو می‌کرد. بعد از هر تغییر فایل **حتماً سرور را ری‌استارت کن**.

### رفع مودال جزئیات صفحهٔ Audit
20. ✅ **کلیک روی رکورد در جدول Audit، پنجرهٔ جزئیات را بدجا و زیر هدر اصلی باز می‌کرد.**
    - علت: `AuditTrailView` **تنها مودال دست‌سازِ باقی‌مانده** بود (`fixed inset-0 z-50` به‌جای `FormModal`). هر view داخل `motion.div` صفحه‌گردانی رندر می‌شود که روی **`filter`** انیمیت می‌کند؛ یک `filter` غیر-`none` هم containing block برای فرزندان `fixed` می‌سازد (پس `inset-0` به جعبهٔ صفحه نگاشت می‌شد، نه viewport) و هم stacking context (پس `z-50` زیر هدرِ `z-10` حبس می‌شد). هر دو نشانه یک علت داشتند.
    - بررسی شد که بقیهٔ overlayها مصون‌اند: `FormModal` و `PrintableForms` هر دو `createPortal` می‌زنند و `CommandPalette` بیرون از `<main>` است.
    - اصلاح: overlay دست‌ساز با `FormModal` (size=`lg`) جایگزین شد؛ markup هدر/بدنه/فوتر عیناً ماند و گارد `{selectedLog && …}` حفظ شد. **Esc، focus trap، قفل اسکرول و `role="dialog"`** هم که قبلاً نبودند رایگان اضافه شدند.
    - `CLAUDE.md` قاعدهٔ ۸: دلیل CSSی اجباری‌بودن قاعده ثبت شد تا دوباره کشف نشود.
    - تأیید عددی (نه چشمی) با `getBoundingClientRect` + `elementFromPoint`، و **کنترل منفی** با revert + ری‌استارت سرور:

    | سنجه | قبل | بعد |
    |---|---|---|
    | مرکز عمودی پنل vs viewport | ۵۹۶ vs ۵۰۰ | ۵۰۰ vs ۵۰۰ |
    | کاملاً داخل viewport | ✗ (bottom=۱۰۴۶ > ۱۰۰۰) | ✓ |
    | در نوار هدر، کدام بالاست | `HEADER.sticky z-10` | خودِ مودال |
    | `role="dialog"` | ندارد | دارد |

    - محتوای دیف (`computeFieldDiff`) سالم بود و تغییری لازم نداشت؛ ردیف‌های تغییر/افزوده/حذف و JSON خام درست رندر می‌شوند. ۰ خطای کنسول.

### ماژول کاربر — امنیت و صحت نشست (موارد ۱ و ۲ گزارش ماژول کاربر)
21. ✅ **بستن ترفیع خودسرانهٔ نقش (P0).** هر شش endpoint `/api/users/*` فقط `requireAuth` داشتند و **هیچ بررسی نقشی نداشتند** ⇒ یک کاربر `finance` می‌توانست نقش ادمین را عوض کند یا برای خودش حساب ادمین بسازد.
    - middleware جدید `requireRole(...roles)` در `server.ts` (الگویش از قبل در `/api/audit-logs` بود و فقط به این مسیرها اعمال نشده بود) روی هر شش مسیر.
    - `GET /api/vendors` که **هیچ احراز هویتی نداشت** حالا `requireAuth` دارد. افکت لود سورس‌ها در `App.tsx` به `currentUser` مشروط شد تا در صفحهٔ ورود حلقهٔ reload نسازد (تلهٔ ثبت‌شده در `CLAUDE.md`).
22. ✅ **سه نقص صحت نشست:**
    - **خروج حالا در Audit ثبت می‌شود** — `handleLogout` قبلاً فقط localStorage را پاک می‌کرد و `POST /api/auth/logout` را صدا نمی‌زد؛ audit trail ورود داشت ولی خروج نداشت (نقص کامل‌بودن ALCOA+). درخواست ناموفق هم کاربر را در حالت لاگین گیر نمی‌اندازد.
    - **اعتبارسنجی نقش هنگام boot** — `GET /api/auth/me` حالا نقش/نام را **از دیتابیس** می‌خواند نه از JWT (توکن هفت‌روزه است و صرفاً وضعیت زمان ورود را تکرار می‌کرد)، و برای حساب حذف‌شده ۴۰۱ می‌دهد. کلاینت یک بار در هر لود آن را صدا می‌زند و `currentUser` کش‌شده را همگام می‌کند.
    - `ChangePasswordModal` از `authFetch` استفاده می‌کند (قبلاً `fetch` خام با هدر دستی، بدون مدیریت ۴۰۱).
23. ✅ **رفع باگ از پیش موجود: فقط ادمین می‌توانست وارد برنامه شود.** `HomeView` فید `/api/audit-logs` (ادمین‌ویژه) را برای همه صدا می‌زند و `authFetch` هر **۴۰۳** را «انقضای نشست» می‌گرفت ⇒ کاربر غیرادمین بلافاصله بعد از ورود بیرون انداخته می‌شد. اصلاح با تفکیک معنایی: `requireAuth` برای توکن نامعتبر **۴۰۱** برمی‌گرداند (قبلاً ۴۰۳)، `requireRole` برای عدم مجوز **۴۰۳**؛ و `authFetch` فقط روی ۴۰۱ خروج می‌کند و ۴۰۳ را به کالر برمی‌گرداند.
    - **این باگ مال تغییرات ما نبود** — با revert و تست دوباره تأیید شد که روی کد قبلی هم `commercial` نمی‌توانست وارد شود.

    تأیید: `tsc` ✓ · `build` ✓ · ۵۴ تست ✓ · تست زندهٔ API و مرورگری:

    | سنجه | نتیجه |
    |---|---|
    | `finance` روی ۴ مسیر کاربران | ۴۰۳ (قبلاً باز بود) |
    | `admin` روی `GET /api/users` | ۲۰۰ |
    | `GET /api/vendors` بدون توکن / با توکن | ۴۰۱ / ۲۰۰ |
    | توکن خراب / عدم مجوز | ۴۰۱ / ۴۰۳ (تفکیک‌شده) |
    | ورود کاربر غیرادمین | ✓ (قبلاً بیرون انداخته می‌شد) |
    | رکورد LOGOUT بعد از خروج | ۰ → ۱ |
    | تغییر نقش در DB → reload | نقش و منوی GOVERNANCE به‌روز شد |
    | تغییر رمز از منو | موفق، رمز واقعاً عوض شد |
    | حلقهٔ reload در صفحهٔ ورود | ۰ ناوبری در ۴ ثانیه |

### ماژول مدیریت کاربران (مورد ۳ گزارش ماژول کاربر)
24. ✅ **صفحهٔ «مدیریت کاربران» ساخته شد** (`src/components/UsersView.tsx`، مسیر `#/users`، ادمین‌ویژه، ورودی در سایدبار + منوی کاربر).
    - لیست کاربران با سمت، وضعیت، آخرین ورود و نشان «تغییر رمز در ورود بعدی»؛ جستجو؛ ایجاد کاربر؛ ویرایش نام/سمت؛ **فعال/غیرفعال‌سازی**؛ **بازنشانی کلمه عبور**؛ حذف کامل (با هشدار صریح که غیرفعال‌سازی گزینهٔ درست‌تر است).
    - همهٔ دیالوگ‌ها با `FormModal` (قاعدهٔ ۸)؛ تأییدیهٔ حذف `role="alertdialog"` + `closeOnBackdrop={false}`. audit فقط سمت‌سرور (قاعدهٔ ۲) — این view هیچ رکورد audit نمی‌زند.
    - endpointهای موجود از قبل بودند و هیچ‌وقت صدا زده نمی‌شدند؛ فقط `POST /api/users/:username/reset-password` جدید اضافه شد (رمز موقت + اجبار تغییر در ورود بعدی؛ **خودِ رمز در audit ثبت نمی‌شود**، فقط واقعهٔ بازنشانی).
25. ✅ **دو ستون مردهٔ schema زنده شدند.** `mapUserRow` در `server.ts` هر دو را دور می‌ریخت — به همین دلیل هیچ‌جای برنامه نمی‌توانست ببیندشان:
    - `is_active` → حالا در `GET /api/users` برمی‌گردد، در **login** بررسی می‌شود (حساب غیرفعال ۴۰۳ می‌گیرد و تلاشش در audit ثبت می‌شود) و در `GET /api/auth/me` باعث ۴۰۱ می‌شود تا نشستِ در جریان هم قطع شود، نه اینکه تا انقضای توکن هفت‌روزه ادامه یابد.
    - `last_login_at` → هنگام ورود موفق نوشته می‌شود و در ستون «آخرین ورود» نمایش داده می‌شود.
26. ✅ **محافظ قفل‌شدن سازمان (`checkAdminSafety` در `server.ts`).** روی `PATCH /api/users/:username`، `PUT /api/users/:u/role` و `DELETE`:
    - ادمین نمی‌تواند خودش را حذف، غیرفعال یا تنزل دهد.
    - آخرین **مدیر فعال** قابل حذف/غیرفعال/تنزل نیست.

    تأیید: `tsc` ✓ · `build` ✓ · **۵۴ تست** (۲ تست مسیر `#/users` اضافه شد) ✓ · تست زندهٔ API و مرورگری با **۰ خطای کنسول**:

    | سنجه | نتیجه |
    |---|---|
    | ایجاد کاربر از UI و ورود با آن | ✓ |
    | بازنشانی رمز → رمز قدیمی ۴۰۱، جدید ۲۰۰ | ✓ |
    | غیرفعال‌سازی → ورود ۴۰۳ با پیام فارسی | ✓ |
    | `last_login_at` بعد از ورود | ثبت شد (قبلاً همیشه خالی) |
    | حذف/غیرفعال/تنزلِ خود ادمین | ۴۰۰ در هر سه |
    | تنزل آخرین مدیر فعال | ۴۰۰ |
    | تایپ در فرم (کیبورد واقعی) | فوکوس حفظ شد |
    | کاربر غیرادمین روی `#/users` | صفحهٔ «عدم دسترسی»؛ سایدبار و منو مخفی |

### سطوح دسترسی نقش‌محور (اعمال ماتریس تأییدشده)
27. ✅ **جدول سیاست واحد `src/utils/permissions.ts`** که **هم سرور و هم کلاینت** از آن می‌خوانند — واگرایی UI/سرور از نظر ساختاری ناممکن شد.
    - ماتریس: `admin` همه · `commercial` ثبت/ویرایش سورس + شرکای تجاری · `qa` نتایج آزمایشگاهی + مواد اولیه · `planning`/`finance` فقط امتیاز دپارتمان خودشان · `lab` فقط‌خواندنی.
    - تغییر نسبت به قبل: `commercial` شرکای تجاری را گرفت · `qa` مواد اولیه را گرفت · ارزیابی ریسک ادمین‌ویژه شد.
28. ✅ **`requirePermission()` روی ۱۴ مسیر نوشتنی** (سورس/ماده/شریک/آزمایش/ریسک/حذف). قبلاً همهٔ این‌ها فقط `requireAuth` داشتند و هر کاربری با curl می‌توانست انجامشان دهد.
29. ✅ **گارد per-department امتیازدهی.** `PATCH /vendors/:id/scores` کل شیء را جایگزین می‌کند، پس گارد ساده کافی نبود؛ payload با مقدار ذخیره‌شده مقایسه می‌شود و تغییر دپارتمان دیگر ۴۰۳ می‌گیرد.
    - ⚠️ **باگی که تست زنده گرفت:** فرم همیشه هر چهار دپارتمان را می‌فرستد و برای غیرقابل‌ویرایش‌ها `0` می‌گذارد؛ روی سورس بدون امتیاز، «ثبت‌نشده → ۰» تغییر حساب می‌شد و **کاربر مالی اصلاً نمی‌توانست سورس جدید را امتیاز دهد**. با یکی‌شمردن «ثبت‌نشده» و «صفر» رفع شد + تست رگرسیون.
30. ✅ **کلاینت هم‌راستا شد** — شرط‌های پراکندهٔ `role === 'admin'` جای خود را به `can()`/`canScoreDepartment()` دادند. دو دکمهٔ «ثبت ماده جدید» و «ثبت شریک تجاری جدید» **اصلاً گارد نداشتند** (باگ از پیش موجود) و گارد گرفتند.

    تأیید: `tsc` ✓ · `build` ✓ · **۷۱ تست** (۱۷ تست جدید؛ هر سلول ماتریس یک assert) ✓ · تست زندهٔ API با توکن هر ۵ نقش روی ۶ مسیر + ۵ سناریوی امتیازدهی + تست مرورگری هر نقش، با ۰ خطای کنسول.

### تنظیم سطح دسترسی پر‌کاربر با چک‌باکس
31. ✅ **ستون مردهٔ `users.permissions` بالاخره خوانده می‌شود.** نقش = الگو، استثنا = پر‌کاربر. **خالی یعنی «از نقش پیروی کن»** — دقیقاً وضعیت همهٔ کاربران موجود، پس **صفر مهاجرت داده**.
32. ✅ **۱۳ چک‌باکس در ۴ گروه** (سورس‌ها · ارزیابی · مخازن · مدیریتی) در دیالوگ «سطح دسترسی» از لیست کاربران؛ با نشان «افزوده به سمت»/«سلب‌شده از سمت» روی هر تفاوت، دکمهٔ «بازگشت به پیش‌فرض سمت»، و بج «سفارشی» در لیست.
33. ✅ **امتیازدهی چهار مجوز جدا شد** (`score.commercial/qa/planning/finance`) — چیزی که با نقش ممکن نبود: یک کاربر می‌تواند هم‌زمان دو دپارتمان را امتیاز دهد.
34. ✅ **`requirePermission` رکورد کاربر را از DB می‌خواند نه از JWT.** توکن هفت‌روزه فقط `role` دارد؛ خواندن از آن یعنی تغییر دسترسی تا یک هفته بی‌اثر بماند. **تست زنده تأیید کرد که تغییر روی همان توکن و بدون ورود مجدد اعمال می‌شود.**
35. ✅ محافظ‌ها: `checkPermissionSafety` (آخرین دارندهٔ `users.manage` و خودِ کاربر) · تغییر نقش استثناها را پاک می‌کند (با هشدار در فرم) · نام‌های ناشناخته sanitize می‌شوند و فهرستِ کاملاً ناشناخته به‌جای «هیچ دسترسی» به الگوی نقش برمی‌گردد.

    تأیید: `tsc` ✓ · `build` ✓ · **۸۰ تست** (۲۶ تست سیاست) ✓ · تست زندهٔ API: اعطا → ۲۰۰ روی همان مسیری که ۴۰۳ بود، سلب → ۴۰۳، ریست → ۴۰۳، نام ناشناخته ذخیره نشد، هر سه محافظ ۴۰۰ · تست مرورگری: ۱۳ چک‌باکس، بج‌ها، ذخیره، و دیدن دکمهٔ جدید توسط کاربر هدف. ۰ خطای کنسول.

### پروفایل و نشست در منوی کاربر (مورد ۴ گزارش ماژول کاربر)
36. ✅ منوی کاربر از سه دکمه به یک پنل واقعی تبدیل شد: **ورود قبلی** · **اعتبار نشست** (با هشدار نارنجی زیر ۲۴ ساعت) · **تعداد مجوزها** (+ نشان «سفارشی») · **فعالیت اخیر من**.
    - **«ورود قبلی» یعنی دفعهٔ قبل، نه همین حالا** — مقدار `lastLoginAt` **قبل از** بازنویسی خوانده و در پاسخ login برگردانده می‌شود؛ `/api/auth/me` عمداً آن را نمی‌فرستد تا با reload خراب نشود (چون merge به‌صورت `{...prev, ...fresh}` است).
    - **`GET /api/auth/my-activity`** (جدید، `requireAuth`): خواندن فعالیت خودِ کاربر امتیاز ویژه نیست، ولی فیلتر **از توکن** گرفته می‌شود نه از query string — تست شد که `?userId=admin` نادیده گرفته می‌شود.
    - `src/utils/session.ts`: خواندن `exp` از payload توکن **بدون تأیید امضا** — فقط برای برچسب، هرگز برای تصمیم (سرور هر درخواست را خودش تأیید می‌کند).
    - ⚠️ **باگی که تست مرورگری گرفت:** منو برای ادمین «(سفارشی)» نشان می‌داد. علت: کلاینت فقط فهرست **مؤثر** را می‌گیرد، پس `hasCustomPermissions` سمت‌کلاینت هیچ‌وقت درست کار نمی‌کند. با فرستادن `permissionsCustom` از سرور رفع شد.

    تأیید: `tsc` ✓ · `build` ✓ · **۸۱ تست** ✓ · تست زندهٔ API (self-scope و نادیده‌گرفتن `userId`، و درست بودن `previousLoginAt` در ورود دوم) + تست مرورگری کل پنل، با ۰ خطای کنسول.

### ظرفیت ۱۰۰۰+ رکورد — بنچمارک واقعی و اصلاح
37. ✅ **بنچمارک با ۱۲۷۶ سورس واقعی** (۷۲۲۱ لاگ، ۴۸۰۰ رکورد آزمایش) روی Postgres محلی. اعداد **اندازه‌گیری‌شده‌اند، نه تخمین**.
38. ✅ **`getVendorsList(vendorId?)` فیلترپذیر شد.** `getVendorById` قبلاً کل دیتابیس را می‌خواند و بعد `.find()` می‌کرد — **۱۶ endpoint** این مسیر را می‌رفتند. حالا `where` می‌خورد (ایندکس‌هایش از قبل بود).
39. ✅ **رفع O(n²):** `vendorMaterials.find()` داخل حلقهٔ سورس‌ها ⇒ در ۱۲۰۰ رکورد بیش از یک میلیون مقایسه در هر درخواست. با `Map` جایگزین شد.
40. ✅ **`getRankingSnapshot()`** — محاسبهٔ رتبه دو بار کل دیتابیس را می‌خواند تا یک عدد بدهد؛ حالا فقط id/scores/category را می‌گیرد. منطق `rankVendor` دست‌نخورده.
41. ✅ **کش `app_db` سبک و محافظت‌شده.** تاریخچه (لاگ/آزمایش/rawScores) از کش حذف شد (هیچ‌وقت از کش خوانده نمی‌شد) و آرایه‌ها خالی می‌مانند تا شکل رکورد نشکند؛ نوشتن در `try/catch` و در صورت شکست، کش پاک و برنامه ادامه می‌دهد.
42. ✅ **`ErrorBoundary` در ریشه** (`src/main.tsx`) — قبلاً هیچ‌جا نبود؛ هر throw کل درخت را unmount می‌کرد و صفحهٔ سفید می‌داد.

    | سنجه (۱۲۷۶ رکورد) | قبل | بعد |
    |---|---|---|
    | ذخیرهٔ کامل (۶ PATCH) | **۲.۸۳ s** | **۰.۲۴ s** (~۱۲×) |
    | `PATCH /scores` | ۰.۸۰ s | ۰.۰۶ s |
    | `PATCH /contact` | ۰.۴۲ s | ۰.۰۲۵ s |
    | حجم `app_db` | ۶.۳۴ MB | **۲.۱۳ MB** (−۶۶٪) |
    | سقف تخمینی سورس (Chromium ۹.۹MB) | ~۱۹۹۰ | ~۵۹۳۰ |
    | سقف تخمینی سورس (Firefox/Safari ۵MB) | ~۱۰۰۶ | ~۲۹۹۵ |

    - **تصحیح گزارش اولیه:** سهمیهٔ واقعی Chromium اندازه‌گیری شد **۹.۹MB** نه ۵MB؛ پس ادعای «دیوار در ۹۳۰ رکورد» فقط برای Firefox/Safari درست بود. در Chrome دیوار حدود ۲۰۰۰ بود.
    - تأیید: خروجی `GET /api/vendors` **بایت‌به‌بایت یکسان** ماند (۰ رکورد متفاوت از ۱۲۷۶) · پر کردن عمدی سهمیه ⇒ برنامه زنده ماند، ۰ crash · ۸۱ تست ✓

### انتخاب سورس برندهٔ هر ماده (بخش «مقایسهٔ منابع» در صفحهٔ دسته‌بندی)
43. ✅ **تاریخ جعلی حذف شد.** کد قبلی اگر آخرین ارزیابی قدیمی‌تر از ۱۴۰۴ بود، عمداً **تاریخ امروز** را نشان می‌داد («تا کاملاً به‌روز به‌نظر برسد»). در یک سامانهٔ GxP این ادعای نادرستِ تازگی داده است. حالا `getLatestGroupUpdateDate()` می‌تواند `null` برگرداند و در نبود ارزیابی «ارزیابی ثبت نشده» نمایش داده می‌شود.
44. ✅ **نمودار با رتبه‌بندی هم‌خوان شد.** میله‌ها قبلاً `score` پایه را رسم می‌کردند در حالی که ترتیب ردیف‌ها با `engineScore` (خروجی موتور) بود ⇒ گاهی میلهٔ کوتاه‌تر بالای فهرست بود. حالا محور همان `engineScore` است و امتیاز پایه در پرانتز `(پایه {score})` می‌آید.
45. ✅ **حالت «هم‌تراز».** با `DECISIVE_MARGIN = 3`، اگر فاصلهٔ دو سورس اول کمتر از ۳ امتیاز باشد به‌جای اعلام برنده، «هم‌تراز — تصمیم انسانی لازم است» نشان داده می‌شود.
46. ✅ **نشانگر کامل‌بودن داده.** تعداد دپارتمان‌های امتیازدهی‌شده (`n/۴`) و هشدار نبود ارزیابی ریسک، تا پیشنهاد بر پایهٔ دادهٔ ناقص با پیشنهاد کامل اشتباه گرفته نشود.
47. ✅ **ثبت تصمیم (`source_selections`).** جدول جدید + مهاجرت `20260825142747_source_selections` (یکتا روی `materialKey+category`، CASCADE روی vendor) و endpointهای `GET`/`PUT /api/source-selections` با `requirePermission('vendor.write')`. **دلیل الزامی** (حداقل طول، در غیر این صورت ۴۰۰ با پیام فارسی) و ثبت در `audit_log` (`CREATE_SOURCE_SELECTION`). اگر انتخاب انسانی با پیشنهاد موتور فرق داشته باشد، برچسب «متفاوت با پیشنهاد» نمایش داده می‌شود.
    - **عمداً تغییر نکرد:** فرمول امتیازدهی `FmeaService`. تغییر آن یعنی تغییر معیار تصمیم کیفی و باید تصمیم صریح و جداگانهٔ کاربر باشد.
    - تأیید: تست زنده مرورگری (۱۰ سنجه، همه ✓، ۰ خطای کنسول) + تست API (دلیل کوتاه → ۴۰۰، ذخیرهٔ معتبر → ۲۰۰، بازخوانی درست، ردیف audit ثبت شد) + گیت دسترسی (کاربر finance دکمهٔ ثبت را نمی‌بیند ولی انتخاب ثبت‌شده را می‌بیند) · ۸۱ تست ✓ · `tsc` ✓ · `build` ✓

48. ✅ **خلوت‌سازی پنل «موتور تحلیل سیستم».** سه بولت توضیحی، همان سه عددی را که خط فرمول زیرشان می‌گفت دوباره به‌صورت متن تکرار می‌کردند — و این پنل زیر **هر ماده** تکرار می‌شود.
    - نتیجه همیشه دیده می‌شود: سه چیپ فشرده (`پایه ۹۰` · `ریسک Low ×1.05` · `آزمایشگاه ×1.00`) + خط فرمول با اعداد واقعی.
    - متن آموزشی پشت دکمهٔ «چطور محاسبه می‌شود؟» جمع شد — با فعال‌کردن state ‏`showEngineGuide` که از قبل تعریف شده بود ولی به هیچ دکمه‌ای وصل نبود (کد مرده).
    - چیپ تکراری «سورس منتخب» از هدر بخش حذف شد (جعبهٔ پایین همان نام را به‌علاوهٔ دلیل و ثبت‌کننده دارد).
    - **اصلاح یک ادعای نادرست:** متن قبلی نداشتن ارزیابی ریسک را «ضریب خنثی» می‌نامید، در حالی که `FmeaService.riskModifiers.Default = 0.95` یک **جریمهٔ محتاطانه** است نه ۱.۰۰. حالا هم tooltip و هم متن راهنما همین را می‌گویند و چیپ ریسک/آزمایشگاهِ ثبت‌نشده کهربایی نمایش داده می‌شود.
    - **تصحیح برآورد خودم:** پیش‌بینی کرده بودم ارتفاع پنل ~۶۰٪ کم شود؛ اندازه‌گیری زنده نشان داد **ارتفاع تغییر نمی‌کند** (۸۵۵px قبل و بعد). دلیلش این است که ستون راست در `items-stretch` است و ارتفاعش را ستون چپ (نمودار + جدول آزمایشگاه) تعیین می‌کند، نه محتوای خودش. بردِ واقعی **حجم متن** است: ۵۸۷ → **۲۴۶** کاراکتر (−۵۸٪). عارضهٔ جانبی مطلوب: باز کردن راهنما هم چون در فضای خالی موجود باز می‌شود، هیچ ارتفاعی به صفحه اضافه نمی‌کند.

### خوانایی نام موجودیت‌ها (سراسری)
49. ✅ **علت مشترک پیدا شد، نه یک باگ موضعی.** در ۲۰+ نقطه `truncate` روی یک ظرف `flex` بود نه روی خود متن. `text-overflow` به فرزندان flex اعمال نمی‌شود ⇒ متن **بدون «…» سخت بریده می‌شد** و کاربر نمی‌فهمید نام ناقص است.
50. ✅ **زیرساخت:** `src/components/EntityName.tsx` + `src/hooks/useIsOverflowing.ts`. با `line-clamp` (نه `truncate` که `nowrap` تحمیل می‌کند) و tooltip **فقط وقتی متن واقعاً بریده شده**. `src/components/ui/tooltip.tsx` که کد مرده بود (هیچ import نداشت) زنده و اصلاح شد: `TooltipPrimitive.Portal` اضافه شد (تلهٔ `filter` — قاعدهٔ ۸)، `TooltipProvider` در `main.tsx`.
51. ✅ **نمودار مقایسه (نقطهٔ گزارش‌شدهٔ کاربر):** سقف `max-w-[180px]` که با ۳ بجِ `shrink-0` مشترک بود حذف شد؛ نام و بج‌ها در ظرف `flex-wrap` هم‌سطح شدند تا بج‌ها به خط بعد بروند نه اینکه نام را له کنند.

    | | قبل | بعد |
    |---|---|---|
    | عرض جعبهٔ نام | ۱۸۰px | **۲۳۷px** |
    | عرض لازم برای متن | ۳۰۹px | ۲۳۷px |
    | نتیجه | **۴۲٪ نام نامرئی** | کل ۴۹ کاراکتر خوانا |

52. ✅ **۸ فایل دیگر:** `ArchiveView`, `HomeView`, `PartnerSelector`, `App` (breadcrumb)، `SupplierAuditView`, `VendorDetail`, `CommandPalette`. عمداً دست‌نخورده: نام **فایل**‌ها و برچسب‌های ثابت سایدبار.
53. ✅ **باگی که فقط با تست زنده پیدا شد:** هوک درست اندازه می‌گرفت (`303>295`) ولی یک اندازه‌گیری سوم `0 0 0 0` آن را برمی‌گرداند. علت: وصل‌شدن tooltip عنصر را جابه‌جا می‌کند، React گرهٔ قبلی را unmount می‌کند و `ResizeObserver` روی گرهٔ جدا‌شده می‌ماند. با **callback ref** + دور‌ریختن اندازه‌گیریِ گرهٔ جداشده/صفر رفع شد. (در طرح اولیه نوشته بودم این حلقه رخ نمی‌دهد — اشتباه بود.)
    - تأیید: **کنترل منفی اجرا شد** (روی کد قدیم tooltip باز نمی‌شد و آرشیو ۱ برش بی‌صدا داشت) · بعد از اصلاح: ۵ صفحه × ۰ برش بی‌صدا، dark mode ۰، ستون‌های آرشیو هم‌تراز، tooltip بالای هدر می‌نشیند (`elementFromPoint`) و به `body` پورتال می‌شود · ۰ خطای کنسول · ۸۱ تست ✓

### مهاجرت خودکار در بیلد
54. ✅ **`scripts/migrate-if-configured.mjs`** — هیچ‌چیز مهاجرت‌ها را اجرا نمی‌کرد (نه Dockerfile، نه بیلد Vercel)، پس هر استقرار تازه به دیتابیسی بدون جدول وصل می‌شد و همهٔ مسیرها ۵۰۰ می‌دادند. حالا `npm run build` قبل از کامپایل این اسکریپت را صدا می‌زند.
    - **عمداً شرطی است:** اگر `DATABASE_URL` نبود یا مقدارش placeholder بود، رد می‌شود و بیلد **موفق** می‌ماند. بدون این شرط، بیلد لپ‌تاپ و هر preview بدون دیتابیس می‌شکست — از جمله PR #10 که الان سبز است.
    - **ولی خطای واقعی را نمی‌بخشد:** اگر دیتابیس تنظیم شده باشد و مهاجرت شکست بخورد، exit غیرصفر می‌دهد و بیلد را می‌خواباند. انتشار برنامه‌ای که schema‌اش با کدش نمی‌خواند بدتر از انتشار‌نشدن است.
    - اعتبارسنجی URL عمداً از `isValidPostgresUrl` در `server.ts` تکرار شده، چون این اسکریپت قبل از ساخته‌شدن باندل سرور اجرا می‌شود.
    - تأیید: بیلد بدون `DATABASE_URL` ✓ سبز · ۵ مقدار نامعتبر/placeholder ✓ همه skip · دیتابیس خالی ⇒ **۱۴ جدول** ساخته شد (۱۳ + `_prisma_migrations`) · اجرای دوم ⇒ «No pending migrations» (idempotent) · دیتابیس غیرقابل‌دسترس ⇒ **exit 1** · سرور تولیدی روی همان دیتابیس تازه: `/api/health` ✓، ورود `admin/123456` ✓ با `mustChangePassword=true`، `GET /api/vendors` → **۲۰۰** (نه ۵۰۰)، هر ۵ کاربر پیش‌فرض seed شدند · ۸۱ تست ✓
    - `IT_DEPLOYMENT_GUIDE.md` اصلاح شد: ادعای «مهاجرت خودکار اجرا نمی‌شود» دیگر درست نیست، ولی در `docker-compose.yml` فعلی متغیر فقط روی سرویس است نه روی build، پس آنجا هنوز یک بار دستی لازم است — همین تفکیک در سند نوشته شد.

### سطح دسترسی به تفکیک عملیات (ثبت/ویرایش/حذف)
55. ✅ **مجوزهای یکجا تفکیک شدند.** `material.write` یک مجوز برای POST+PATCH+DELETE+status بود؛ حالا `material.create` / `material.edit` / `material.delete`. همین‌طور `partner.*` و `vendor.create`/`vendor.edit`. مجموع ۱۳ → ۱۷ مجوز، و ۱۲ گارد endpoint به‌روز شد.
56. ✅ **دو مجوز نمایشی رفع شد.** `audit.read` در فرم بود ولی هیچ‌جا اعمال نمی‌شد؛ چهار endpoint ممیزی به‌جایش شرط دستی `role !== 'admin'` داشتند — همان چیزی که قاعدهٔ ۱۴ ممنوع کرده. حالا هر چهار endpoint `requirePermission('audit.read')` دارند. `archive.read` **حذف شد** چون هیچ endpointای نداشت که اعمالش کند.
57. ✅ **فرم به جدول ماژول×عملیات تبدیل شد** (`PERMISSION_MODULES`): هر ردیف یک ماژول، هر ستون یک عملیات، به‌علاوهٔ تیک «همه» با حالت indeterminate. **خانه‌ای که سرور اعمالش نمی‌کند چک‌باکس نمی‌گیرد** — قفل می‌شود با دلیل (`LOCKED_REASONS`): `✓` یعنی همه می‌بینند، `—` یعنی چنین عملیاتی وجود ندارد. سه ماژول تفکیک‌ناپذیر (آزمایشگاه/ریسک/کاربران) یک تیک واحد می‌گیرند.
58. ✅ **سازگاری به‌عقب بدون مهاجرت.** `LEGACY_PERMISSIONS` نام‌های بازنشسته را هنگام خواندن باز می‌کند، پس کاربری که `material.write` ذخیره‌شده دارد دقیقاً همان دسترسی قبلی را نگه می‌دارد. دیتابیس دست نخورد.
59. ✅ **دکمهٔ ویرایش ماده که هیچ‌وقت گیت نداشت** گیت شد (`material.edit`) — قبلاً برای همه دیده می‌شد.
    - تأیید زنده (API): کاربر با فقط `material.create` → POST **۲۰۰**، PATCH **۴۰۳**، DELETE **۴۰۳** · همان کاربر با `material.write` ذخیره‌شده → PATCH **۲۰۰**، DELETE **۲۰۰** (سازگاری) · غیرادمین با `audit.read` → **۲۰۰** (قبلاً همیشه ۴۰۳) و بدون آن → **۴۰۳**.
    - تأیید مرورگری: هر ۷ ردیف درست رندر شد، تیک «همه» کار می‌کند (۱→۵)، ۰ خطای کنسول. یک ایراد حین همین تست پیدا و رفع شد: ردیف کاربران دو چک‌باکس برای یک مجوز نشان می‌داد.
    - ۸۳ تست (۲ مورد جدید برای باز‌شدن نام‌های بازنشسته) ✓ · `tsc` ✓ · `build` ✓

### بررسی یکپارچه تأمین‌کنندگان (Supplier 360)
60. ✅ **`ircExpiryDate` نوشته می‌شد ولی هرگز خوانده نمی‌شد.** `PATCH /contact` آن را ذخیره و در audit ثبت می‌کرد، ولی `getVendorsList` اصلاً در شیء vendor نمی‌گذاشتش. یعنی **کارت «IRC نزدیک انقضا» در داشبورد همیشه صفر بود** و تاریخ انقضا در صفحهٔ جزئیات همیشه خالی. یک خط در سریال‌سازی سرور.
61. ✅ **میانگین امتیاز به مجوز وصل شد، نه نقش.** ماژول نیمه‌مهاجرت بود: کارت‌های دپارتمان از `canScoreDepartment` استفاده می‌کردند ولی عدد اصلی در ۵ نقطه `currentUser.role === 'admin'` بود. کاربری با نقش `commercial` که مجوزش به `score.qa` تغییر کرده بود، امتیاز بازرگانی را می‌دید که اجازهٔ ثبتش را نداشت. حالا یک `myDepartments` مشترک تصمیم می‌گیرد.
62. ✅ **تشخیص شریک روی کل گروه، بدون کد مرده.** قبلاً فقط `vendors[0]` را نگاه می‌کرد و به `partner.manufacturerId` تکیه داشت — فیلدی که **در دیتابیس وجود ندارد** (schema: «suppliers no longer reference a manufacturer»). حالا `resolveVendorPartner` روی همهٔ سورس‌های گروه اجرا می‌شود.
63. ✅ **چهار سنجهٔ سطح‌شرکت** که قبلاً فقط per-material وجود داشتند: نرخ قبولی آزمایشگاه · بالاترین ریسک با توزیعش · وضعیت IRC · **تک‌منبع بودن** (موادی که این شرکت تنها تأمین‌کنندهٔ آن‌هاست — ریسک تداوم تأمین که هیچ ماژول دیگری نشان نمی‌داد).
    - **تصحیح یک پیش‌بینی:** گزارش داده بودم گروه‌بندی با نام متنی باگ است. اندازه‌گیری روی داده نشان داد **نیست**: ۶۷ گروه با نام، ۶۷ با نرمال‌سازی کامل فارسی، و ۶۹ با هویت شریک — چون **۷۴ از ۷۶ سورس هیچ لینک شریکی ندارند**، گروه‌بندی با شریک وضع را بدتر می‌کرد. پس بازنویسی انجام نشد؛ فقط `supplierKey()` نرمال‌سازی ی/ي، ک/ك و نیم‌فاصله را به‌عنوان بیمهٔ دادهٔ واقعی اضافه کرد (امروز صفر تغییر).
    - تأیید زنده با دادهٔ ساختگی: آزمایشگاه **۷۵٪** (۸ تست، ۶ قبول، ۲ مردود روی ۴ ماده) · ریسک **بالا** (۱ بالا/۱ متوسط/۱ پایین/۵ بدون ارزیابی) · IRC **۲ منقضی** · تک‌منبع **۱** با نام ماده. هر چهار سنجه هم در حالت خالی و هم در حالت پر تست شدند · ۰ خطای کنسول · ۸۳ تست ✓

64. ✅ **ادغام‌های ۵ تا ۷ (پیشنهادهای همان گزارش):**
    - **مدارک SOP از مخزن شرکا** — قبلاً فقط یک رشتهٔ گرید نشان داده می‌شد. حالا امتیاز کل، تاریخ و ثبت‌کنندهٔ آخرین ارزیابی، و **وضعیت تک‌تک ۵ مدرک** (تأییدشده/تأیید موقت/منقضی/ارائه‌نشده) با لینک مستقیم به رکورد شریک. اگر سورس به هیچ فروشنده‌ای وصل نباشد، همین را صریح می‌گوید به‌جای نمایش خالی.
    - **سورس منتخب** — «n از m» ماده‌ای که این شرکت تصمیم ثبت‌شدهٔ سورس برایشان است (`GET /api/source-selections`).
    - **خروجی پروندهٔ تک‌تأمین‌کننده** (`exportSupplierDossierToExcel`) — سه شیت: خلاصهٔ ممیزی (آزمایشگاه/ریسک/SOP/تداوم تأمین)، اقلام تأمین‌شده با همان فرمت آرشیو (از `buildCategoryWorksheet` بازاستفاده شد)، و سوابق آزمایشگاه. ممیزی همیشه تک‌تأمین‌کننده انجام می‌شود و تا الان یعنی خروجی کل آرشیو و فیلتر دستی.
    - تأیید زنده: کارت SOP با دادهٔ واقعی (Grade C · ۵۵ از ۱۰۰ · ۵ مدرک با ۴ وضعیت متفاوت) · سورس منتخب ۱ از ۸ · لینک به مخزن شرکا ✓ · **فایل اکسل واقعاً دانلود شد** و با باز کردن zip تأیید شد: ۳ شیت با ۴۷/۱۵۳/۱۶ سلول و همهٔ بخش‌های خلاصه پر · ۰ خطای کنسول · ۸۳ تست ✓
      - نکته: اولین اعتبارسنجی من غلط بود — دنبال `<t>` (shared strings) گشتم ولی `xlsx-js-style` مقادیر را inline در `<v>` می‌نویسد. فایل از اول سالم بود.

### کارتابل اقدامات (`#/tasks/<key>`)
65. ✅ **کارت‌های اقدامات معلق حالا وارد فهرست می‌شوند، نه وسط یک رکورد.** رفتار قبلی `onSelectVendor(a.items[0])` بود — یعنی پرش مستقیم به **اولین** رکورد از دوازده‌تا، بدون اینکه کاربر بفهمد کجاست یا بقیه چه‌اند.
66. ✅ **کارت SOP اصلاً کلیک‌پذیر نبود** — `items: []` داشت، پس عددی نشان می‌داد که هرگز قابل باز کردن نبود.
67. ✅ **فهرست IRC از صفحهٔ اصلی برداشته شد** (۶۴ خط). این بخش هرچه بک‌لاگ بزرگ‌تر می‌شد صفحهٔ اصلی را شلوغ‌تر می‌کرد — برعکس کاری که داشبورد باید بکند. نکته: این فهرست **تا یک ساعت پیش همیشه خالی بود** چون `ircExpiryDate` هرگز خوانده نمی‌شد (ردیف ۶۰)؛ به‌محض رفع آن باگ، مشکل شلوغی ظاهر شد.
68. ✅ **یک صفحه با چهار تب، نه چهار صفحه** (`WorklistView`). تعامل هر چهار یکی است و کاربری که بک‌لاگ را خالی می‌کند معمولاً بین آن‌ها جابه‌جا می‌شود. هر تب آدرس خودش را دارد (`#/tasks/risk`) پس قابل ارسال به همکار است.
    - **بازگشت به فهرست:** رکورد روی استک ناوبری push می‌شود، پس «برگشت» به همان فهرست با همان بک‌لاگ برمی‌گردد — نه به صفحهٔ اصلی.
    - **مقصد هر ردیف متفاوت است:** سورس‌ها → پروندهٔ سورس · SOP → مخزن شرکای تجاری.
    - **مرتب‌سازی بر اساس فوریت:** در IRC، منقضی‌شده‌ها بالای نزدیک‌به‌انقضا و بعد بر اساس روز باقی‌مانده.
    - **آگاه به مجوز:** اگر کاربر مجوز اقدام نداشته باشد (`vendor.risk`, `partner.edit`, `vendor.edit`) فهرست را می‌بیند ولی با یادداشت صریح «فقط برای مشاهده».
    - تأیید زنده: هر ۹ سنجه سبز — حذف فهرست از صفحهٔ اصلی، کلیک‌پذیر شدن SOP، کارت→کارتابل، رندر ۹ ردیف، تعویض تب→URL، ترتیب فوریت، ردیف→پرونده، **برگشت→همان فهرست**، و دیپ‌لینک در مرورگر تازه · ۰ خطای کنسول · **۸۸ تست** (۵ تست جدید مسیر) ✓

69. ✅ **پاک‌سازی صفحهٔ اصلی + ساعت هدر:**
    - باکس «در انتظار ارزیابی اولیه کیفی» حذف شد (۳۳ خط). کارکردش با تب «سورس‌های ارزیابی‌نشده» در کارتابل پوشش داده می‌شود و آنجا **کل** فهرست را می‌دهد، نه ۳ مورد اول.
    - **فرمت تاریخ** به ترتیب گفتاری فارسی درآمد: `۴ شهریور ۱۴۰۵ · چهارشنبه`. قبلاً `toLocaleDateString` با هر چهار جزء، خروجی `۱۴۰۵ شهریور ۴, چهارشنبه` می‌داد — سال اول و روز هفته پشت ویرگول. حالا اجزا جدا گرفته و کنار هم چیده می‌شوند (`formatSystemDate`)، که نیاز به `replace` روی رشتهٔ فرمت‌شده را هم از بین می‌برد.
    - ساعت از وسط هدر به **گوشهٔ چپ، کنار باکس حساب کاربری** منتقل شد.
    - تأیید زنده: ۴ سنجه سبز (ترتیب تاریخ، حذف باکس، موقعیت افقی ساعت، نزدیکی به باکس کاربر) · ۰ خطای کنسول · ۸۸ تست ✓

70. ✅ **ساعت هدر: دقیقه‌ای به‌جای ثانیه‌ای + تاریخ میلادی.**
    - ساعت روی `App` زندگی می‌کند، پس هر تیک **کل درخت را re-render می‌کرد** — شصت بار در دقیقه برای رقمی که در یک سامانهٔ ارزیابی تأمین‌کننده کسی نمی‌خواند. ثانیه حذف شد و به‌روزرسانی دقیقه‌ای شد.
    - **تیک روی مرز دقیقه زمان‌بندی می‌شود، نه هر ۶۰ ثانیه ثابت** (`setTimeout` با فاصلهٔ محاسبه‌شده تا دقیقهٔ بعد)، وگرنه ساعت نمایش‌داده‌شده تا ۵۹ ثانیه از دقیقهٔ واقعی عقب می‌افتاد.
    - تاریخ میلادی به شکل ISO (`2026-08-26`) کنار آن اضافه شد — برای مکاتبات با تأمین‌کنندگان خارجی. عمداً ISO و نه فرمت محلی، تا با day-first/month-first اشتباه خوانده نشود.
    - تأیید زنده و **کمّی**: با `MutationObserver` روی خود عنصر ساعت — در ۶ ثانیه **۰ تغییر** (قبلاً ~۶). و تست جداگانه تأیید کرد ساعت **دقیقاً روی مرز دقیقه** جلو می‌رود (۱۲:۲۴ → ۱۲:۲۵)، چون «بهینه‌ای که دیگر به‌روز نمی‌شود» بدتر از مشکل اولیه بود · ۰ خطای کنسول · ۸۸ تست ✓

## کارهای باقی‌مانده (نقشهٔ راه)

### ماژول شرکای تجاری (بقیهٔ پیشنهادها)
- [x] #۲ نمای «سورس‌های متصل به این شریک» در جزئیات (تولیدکننده و فروشنده) ✅
- [x] #۵ خروجی Excel شرکا (فرمت یکسان با آرشیو) ✅
- [x] ستون «نتیجهٔ ارزیابی SOP»: برچسب بر اساس گرید (A→Approved, B→Permit Approval, C→Expired, D→Black List) ✅ (`04d82cf`) — حذف badge گرید تکراری + یکدست‌سازی در نمای جزئیات و خروجی Excel ✅
- [ ] #۴ اعتبارسنجی ایمیل/وبسایت/تلفن

### ماژول‌های دیگر (پیشنهادها از نقشهٔ اولیه)
- [x] **ریسک (FMEA):** ماتریس ریسک بصری زنده (heatmap ۵×۵ اهمیت×احتمال) در فرم ارزیابی ✅ (`cac47bd`) — کنتراست اعداد + نمایش بعد سوم (Detectability) و RPN نهایی زیر ماتریس ✅
- [x] **ریسک (FMEA):** تاریخچهٔ چند ارزیابی + روند SRI/RPN (بازسازی از audit) — audit اختصاصی «Risk Assessment» در POST vendors + endpoint `GET /api/vendors/:id/risk-history` + کارت تاریخچه در نمای سورس ✅ (تست زنده تأیید شد)
- [x] **آزمایشگاه:** نوار خلاصه (Pass/مشروط/Reject/نرخ قبولی) + **نمودار خطی روند کیفی نتایج** (جایگزین timeline قبلی) ✅ (تست زنده تأیید شد)
- [x] **آزمایشگاه:** اتوماسیون Reject→blacklist بازطراحی شد ✅ (تست زنده UI تأیید شد):
  - **نمونه‌ها:** یک نتیجهٔ Reject → خودکار به لیست سیاه (go/no-go).
  - **سورس/تأمین‌کننده:** دیگر خودکار رد نمی‌شوند (یک سورس می‌تواند چند نتیجه داشته باشد). به‌جای آن **باکس تصمیم‌گیری کیفی (فقط ادمین/QA)** با **دلیل الزامی** که در audit (severity Critical، reasonForChange) و در `rejectionReasons` سورس ثبت می‌شود؛ قابلیت بازگردانی از لیست سیاه با دلیل.
  - رفع badge شمارش لیست سیاه + یکدست‌سازی ۴ نقطهٔ فیلتر (سورس رد‌شده فقط در لیست سیاه، خارج از دستهٔ خرید).
- [ ] **آزمایشگاه:** پیوست فایل به رکورد آزمایش
- [x] **Audit:** نمای diff بصری فیلد‌به‌فیلد در جزئیات رویداد (فقط فیلدهای تغییریافته، برچسب فارسی، قبل→بعد رنگی، JSON خام جمع‌شونده) ✅ (تست زنده تأیید شد)
- [x] **Audit:** خلوت‌سازی UI — حذف ردیف ۹ دکمهٔ رنگی، جمع‌کردن ۷ فیلتر در پنل «فیلترها»، کاهش چیپ آمار به ۲، ادغام ستون تاریخ/ساعت و بردن سمت زیر نام کاربر ✅ (تست زنده)
- [x] **Audit:** خروجی Excel لیست audit (فرمت یکسان با شرکای تجاری: هدر 1E3A8A، Segoe UI، border نازک، RTL، رنگ سطح بحرانیت) + ستون «خلاصهٔ تغییرات» (قبل→بعد). خروجی همهٔ رکوردهای فیلترشده (نه فقط صفحه)؛ در حالت لوکال از store محلی. ✅ (تست زنده)
- [x] **داشبورد:** نمودار حلقوی توزیع گرید + مرکز اقدامات معلق (کلیک‌پذیر) + آخرین فعالیت‌های audit + نرخ قبولی آزمایشگاه ✅ (تست زنده)
- [x] **سایدبار:** Command Palette (⌘K) جستجوی سراسری + حالت جمع‌شونده آیکون‌فقط (persist) + badge هشدار قرمز کنار Audit ✅ (تست زنده)
- [ ] **داشبورد/سایدبار (بقیه):** heatmap ریسک پرتفولیو، KPI با دلتا/روند، دسترسی‌محورکردن آیتم‌ها

### فرابخشی (P0 امنیت — Critical از فاز ۱)
> وضعیت هر ردیف با خواندن کد تأیید شده است (نه از حافظه). ارجاع‌ها دقیق‌اند تا سشن بعدی دنبال نگردد.

- [ ] **`JWT_SECRET` پیش‌فرض hardcode** — `server.ts:770` → `process.env.JWT_SECRET || "internal-regulatory-compliance-secret-key-321"`. اگر env ست نشود هر کسی که مخزن را دیده می‌تواند توکن معتبر بسازد. باید بدون مقدار پیش‌فرض باشد و در نبودش fail-fast کند (مثل `requirePrisma`).
- [ ] **secretها داخل `docker-compose.yml`** — خط ۱۴ `JWT_SECRET=...` و خط ۲۴ `POSTGRES_PASSWORD=SecurePassword123`.
- [ ] **بدون `helmet` و `express-rate-limit`** — هیچ‌کدام در `package.json` نیستند ⇒ brute-force روی login هیچ محدودیتی ندارد.
- [ ] **PBKDF2 با ۱۰۰۰ تکرار** (`src/server/security/passwordService.ts:11`) → ~۶۰۰٬۰۰۰ یا argon2. ⚠️ هش‌های موجود **قابل ارتقای درجا نیستند**؛ نیاز به rehash در اولین ورود موفق + نسخهٔ الگوریتم در رکورد. `verifyPassword` هنوز رمز ساده (plaintext) را هم می‌پذیرد و باید حذف شود.
- [ ] **JWT در `localStorage`** → httpOnly cookie (در برابر XSS).
- [ ] **ESLint + CI** — نه `eslint.config`ای هست نه `.github/workflows`. یعنی هیچ گیتی جز اجرای دستی، typecheck/test/build را قبل از merge بررسی نمی‌کند.
- [x] ~~رفع باگ تست `SOP grade boundary`~~ ✅ — با تثبیت رابریک ۸۰/۶۰/۴۰/۳۰ و حذف گرید `D` رفع شد؛ کل سوییت سبز است.
- [x] ~~شکستن `App.tsx`~~ ✅ — ۷۹۵۶ → **۱۸۳۲** خط (ردیف‌های ۱۷ و بعد).
- [ ] **شکستن `server.ts`** — هنوز **۳۴۳۲ خط** است (مونولیت) + لایهٔ data (React Query) پیاده نشده.
- [ ] **code-splitting** — باندل هنوز یک فایل **~۲.۳MB** است (`dist/assets/index-*.js`).

## نکات مهم برای ادامه
- الگوی «تاریخچه از audit» برای هر موجودیتی که audit before/after دارد قابل تکرار است.
- برای هر ماژول: اگر هنوز localStorage-only است (مثل قبلِ مواد/شرکا)، اول به backend وصلش کن.
- قبل از تغییر بزرگ UI، ساختار فعلی را بخوان و درک/پیشنهاد را با کاربر تأیید کن (کاربر این را ترجیح می‌دهد).

## تم (Dark/Light) — یکدست‌سازی سراسری
- دکمهٔ تغییر تم (ماه/خورشید) در هدر + ردیف در منوی کاربر؛ انتخاب در localStorage (`theme`) ذخیره می‌شود و بار اول `prefers-color-scheme` را احترام می‌گذارد.
- **مهاجرت ۱۵۵۹ رنگ ثابت به theme tokens** در ۱۴ فایل: `bg-white→bg-card`, `bg-slate-50/100→bg-muted`, `text-slate-800/700→text-foreground`, `text-slate-600/500/400→text-muted-foreground`, `border-slate-100/200→border-border`, `hover:bg-slate-*→hover:bg-accent`.
- استثناها (دست‌نخورده): پنل‌های عمداً تیره (`bg-slate-800/900`)، رنگ‌های معنایی (emerald/blue/amber/rose)، overlayهای سفید روی دکمه‌های رنگی (`bg-white/20`)، فرم چاپ و صفحهٔ ورود، `print:bg-white`.
